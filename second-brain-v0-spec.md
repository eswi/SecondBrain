# Second Brain v0 — 음성 → 받은함 → 검토

> **[방향 전환 · 2026-07] 웹 PWA 검증을 마치고 네이티브 v1(Mac + iPhone)로 전환한다.** 아키텍처 정본은 **§0-A**이며, 그 근거는 옆의 `native-v1-design-draft.md`. §0·§1·§5~§7은 이 방향에 맞춰 정합화했다. **분류 체계(§2)·분류 프롬프트(§3)·검토 의식(§4)·"데이터=영속 평문" 원칙은 그대로 유지.**

> 목적: "내가 무엇을 잊었는지조차 모른다"는 문제를 막는다.
> 원리: **수집은 여러 입구, 받는 함은 하나. 시스템이 스스로 다시 들이민다(push).**
> v0 입구: **폰 음성 / 컴퓨터 텍스트 복사 / URL.** 코드 없이 시작해 습관과 규칙을 먼저 검증한다.

> **이 앱의 존재 이유·기억 구조의 정본 = `docs/native/memory-philosophy.md`.** SecondBrain은 to-do 앱이 아니라 "느슨해진 기억의 능동적 refresh 도구"이며, 기억은 **새로운 기억 → (기억하기) → 살아있는 기억 → (완료/삭제) → 보관된 기억**으로 흐른다. "이 앱은 무엇인가"의 개념 정본은 그 문서다(이 사양서는 링크만 유지, 내용 중복 안 함).

---

## 0. 두 개의 층 (절대 섞지 않는다)

- **데이터 층 (영속·개방)** — 평문 마크다운. 텍스트 에디터 하나로 20년 뒤에도 읽힌다. 이게 진짜 자산.
- **지능 층 (소모품)** — 분류·요약을 하는 AI. 언제든 갈아끼운다. v0에서는 Claude 챗이 곧 이 층이다.

데이터가 평문이므로 Claude 챗과 Claude Code가 **같은 파일**을 만진다.

**디바이스는 여럿이어도 받은함은 (논리적으로) 하나.** 폰·컴퓨터 무엇으로 잡든 전부 하나의 받은함으로 모인다. 어느 기기에서 적었는지는 `source` 태그 한 칸일 뿐. 디바이스마다 받은함을 따로 만드는 순간, 분산이 만드는 그 고통("여긴 기억나는데 저긴 잊었다")이 그대로 돌아온다. **입구는 여럿, 목적지는 하나.**
(물리적 저장은 파일 하나가 아니라 **기기별 조각 파일 여러 개**다 — 각 기기는 자기 파일에만 쓰고, 앱이 읽을 때 합쳐 "받은함 하나"로 보여준다. iCloud의 "마지막 쓴 게 이긴다"로 인한 충돌을 *원천 차단*하는 방식이며, 상세는 §0-A. v0 시절엔 단일 `inbox.md`를 썼고 실제로 충돌·롤백을 겪었다 — 그 교훈이 이 조각 파일 구조의 근거.)

---

## 0-A. 아키텍처 (확정) — 네이티브 v1

**전환: 웹 PWA(경로 1)는 "네이티브가 왜 필요한지"를 검증하는 게 목적이었고, 며칠 실사용으로 목적을 달성했다. 이제 네이티브로 간다.** 웹의 한계가 구체적 통증 셋으로 드러났다: ①**동기화 취약**(iCloud "마지막이 이긴다"로 실제 충돌·롤백 발생) ②**쓰기 불가**(iOS Safari PWA는 파일 쓰기 안 됨 → 기억하기·수정·삭제·미루기 전혀 불가) ③**기기 고유 기능 부족**(진짜 알림·위젯·매끄러운 입력). 근거 상세: `native-v1-design-draft.md`.

**범위 (확정): v1 = Mac 앱 + iPhone 앱, 둘로 국한.** iPad·Windows는 나중 확장하되, 설계가 그 확장을 막지 않아야 한다(특히 Windows — 아래 데이터 구조가 이를 보장).

**분류의 중심 = iPhone 중심.**
- iPhone이 **수집·분류·검토의 심장**(메인 디바이스이므로). 네이티브라 iPhone이 (a) 파일에 직접 쓰기, (b) Claude API 직접 호출이 가능 → **수집한 것을 그 자리에서 즉시 분류**(웹 시절의 "Mac cron 대기" 제거).
- 백그라운드 자동 분류는 iOS 제약이 있으니 **"앱 열 때 미분류를 모아 분류"**가 현실적.
- **API 키는 iOS 키체인(안전 저장소)에.** (웹보다 안전하나, 키가 있는 곳이 하나 늘어남을 인지.)
- **구현됨(2026-07-20 · 트리거 2026-07-22 pull-to-classify로 전환).** iPhone이 Claude API를 직접 호출해 미분류를 §3 프롬프트로 분류. **"새로운 기억" 화면을 아래로 당기면(pull-to-classify) 미분류를 모아 1회 호출**(진행은 네이티브 새로고침 스피너, 결과·실패·없음은 화면 중앙 토스트) + 설정의 수동 "지금 분류하기". 결과(type/due/resurface)는 **기존 편집 이벤트 경로**로 기기 조각에 append(병합 엔진·직렬화 무변경). 보내는 건 원문+오늘 날짜뿐 — 수집 메타(시각·기기·source)는 전송하지 않음(§7). **AI 삭제 금지(확정)**: 모델이 `discard`로 분류해도 반영하지 않고 미분류로 보존한다 — 사람이 일부러 수집한 기억을 AI가 조용히 없애지 않는다(삭제는 사람만·단방향). 상세 = `docs/native/memory-philosophy.md`. **(2026-07-22 구현됨)** "앱 열 때/재진입 자동 분류"는 제거하고 **"화면 아래로 당기면 분류"(pull-to-classify)로 전환 완료** — 앱을 열거나 백그라운드 복귀만으로는 분류가 걸리지 않고, 사용자가 목록을 아래로 당길 때만 실행한다(과잉 호출·배터리·놀람 방지). 설정의 수동 "지금 분류하기"는 계속 동작.
- **Mac의 역할 = "대량 정리용"이 아니라 "펼쳐놓고 생각·수집하는 곳".** 긴 글 검토, 여러 항목 놓고 사고 정리, 데스크톱 대량 수집(URL·파일). 검토·수집 창구로 살아있음.

**데이터 저장·동기화 = 기기별 조각 파일 + 앱이 읽을 때 합치기 (append 중심).** 이 시스템의 척추.
- 받은함을 파일 하나가 아니라 **기기별 파일 여러 개**로 나눈다: `inbox-iphone.md`, `inbox-mac.md`, (나중)`inbox-windows.md`.
- **각 기기는 자기 파일에만 쓴다** → 두 기기가 같은 파일을 건드릴 일이 없어 iCloud "마지막이 이긴다"가 **애초에 발동 안 함 = 충돌 원천 제거.**
- 앱은 **읽을 때 이 파일들을 다 합쳐** 하나의 받은함처럼 보여준다(사용자 눈엔 여전히 "받은함 하나").
- 다섯 조건 통과: 충돌 없음 / 여러 기기 쓰기 가능(iPhone에서도 기억하기·수정·삭제·미루기) / 평문 마크다운 유지 / iCloud 평문이라 **Windows 나중 참여 가능** / 각 기기가 자기 파일에 분류 기록.
- **합치는 로직(정렬·중복제거·최신 반영)이 앱의 핵심 엔진** — 신중히 구현.

**항목에 하는 행동 = append 이벤트로 기록.** 기억하기(자동 분류/추출값을 사람이 승인해 살려둠) · 수정 · 삭제(discard) · **미루기(snooze — 분류가 아니라 시점 조작: resurface를 미래로 밈, push의 일부)** · 완료(done 표시). 조각 파일이 append엔 완벽하므로, 다른 파일의 항목을 못 건드리는 문제는 **"수정도 새 줄(이벤트)로 자기 파일에 쌓고"** 앱이 합칠 때 최신 이벤트를 반영해 푼다. v1은 iPhone이 쓰기 중심이라 이 난제가 상당히 완화됨(**수정 이벤트 범위 = 완전형으로 확정(2026-07)**: 삭제·완료·미루기에 더해 **분류(종류) 변경 `set type=`까지 포함**해 구현·실기기 검증 완료. 레거시 `legacy:` 항목에도 동일 경로로 안전 동작).

**받은함 ↔ 아카이브 = "흐르는 강, 고이는 호수 아님".** 받은함엔 **"안 본 것 + 곧 닥칠 것"만**. 처리·기억하기 한 항목은 화면에서 빠진다. v1 구현은 **`status: done` 표시로 숨김**(이벤트 한 줄이라 충돌 없고 되돌리기 쉬움). 데이터가 실제로 커지면(수천 개~) done 오래된 것을 별도 아카이브로 일괄 이동 — 구조만 염두, 구현은 나중. (**대량 정리가 필요해지면 그건 설계 실패 신호.**)

**기각한 대안:** ①분류를 여러 기기가 제각각 → 같은 파일에 쓰면 충돌 재발(iPhone 중심으로 회피). ②CloudKit 등 Apple 동기화 DB → 충돌 병합은 깔끔하나 **Windows가 영영 못 낌(Apple 전용) + 데이터가 DB에 갇혀 평문·영속 원칙 훼손.** 충돌은 위 조각 파일 방식으로 더 싸게 풀린다.

**유지되는 원칙:** 데이터=영속 평문(네이티브 앱도 평문 조각 파일에 씀 — 앱이 데이터를 독점하지 않음) · 분류 로직 재사용(§2·§3 — iPhone이 부르든 Mac이 부르든 "어떻게 분류하나"는 동일) · 소비 3방식 pull/push/ambient(§5).

**편집·기억하기·시간 정책의 정본 = `docs/native/edit-policy.md`.** 기억하기/[저장] 구분, 기억하기 단방향, 중요 시간 4가지(최초생성·Due Date·Resurface·수정이력), 완료/취소 시 시간정보 재확인 등 편집 정책의 "왜"는 그 문서가 정본이다(이 사양서는 링크만 유지, 내용 중복 안 함). 삭제·완료 세부는 그 문서에 계속 추가됨. (예전 용어 "확정"은 2026-07-19 "기억하기"로 개명.)

---

## 1. 데이터 형식 (`inbox.md`)

동기화되는 폴더에 평문 마크다운. 모든 입구가 하나의 받은함으로 모인다. (물리적으로는 **기기별 조각 파일 여러 개**이며 앱이 읽을 때 합쳐 보여준다 — §0-A. 아래 줄 형식은 어느 파일에서든 동일.)

미가공 수집 줄(입력 직후, 사람 또는 음성):

```
- 2026-06-26 14:03 | voice | 김대표한테 다음주까지 제안서 보내기로 함
```

분류 후(AI가 필드를 붙인 모습):

```
- 2026-06-26 14:03 | voice | 김대표한테 다음주까지 제안서 보내기로 함
  type: promise
  due: 2026-07-03
  resurface: 2026-07-01
  status: open
```

- 한 줄에 한 항목. `날짜시각 | source | 원문` 형식.
- `source`: voice / web / doc / chat / mail / meeting / image
- 사람이 읽고 grep 할 수 있어야 한다. 화려함 < 단순함.

**URL은 텍스트로.** 받은함이 평문이라 URL도 그냥 한 줄. 단 **URL만 달랑 넣지 말 것** — "왜 잡았나 / 이걸로 뭘 할 건가"를 한 줄 덧붙인다. 그래야 죽은 링크가 아니라 살아있는 info-action이 되고, 링크가 죽어도(link rot) 의도는 남는다.

```
- 2026-06-26 16:40 | web | https://example.com/article
  왜 잡았나: 제안서에 쓸 시장 규모 통계. 다음 제안서 작업 때 인용.
```

### 입구 (on-ramps) — v0는 셋

- **폰 → 음성 받아쓰기** (source: voice). 머릿속 생각·통화 직후·방금 받은 부탁을 그 순간 잡는다.
- **컴퓨터 → 텍스트 복사·붙여넣기** (source: web/doc/chat). 화면의 좋은 텍스트를 복사해 한 줄. **"그래서 뭘?" 한마디 필수.**
- **URL** (source: web). 위 형식대로, "왜 잡았나" 첨부.

> **네이티브 v1 수집(구현됨 2026-07-19).** "폰 → 음성 받아쓰기"를 **앱 안 실시간 STT**로 구현 — "새로운 기억" 화면 우상단 **마이크**를 탭하면 바로 한국어 STT(**온디바이스 강제** — 음성이 기기 밖으로 안 나감, §7 외부 전송 최소화와 정합)로 전사, 교정 후 저장. 텍스트 입력도 같은 시트에서 가능. 수집 결과는 **네이티브 항목(UUID)**으로 생성되고 시각·기기·방식(source)을 성역으로 스탬프, 미분류로 "새 기억들"에 든다(자동 분류는 **§0-A대로 앱 열 때/수동으로 Claude API가 종류·시점을 붙인다 — 구현됨 2026-07-20**). 화면 배치 정본 = `docs/native/memory-philosophy.md` §5. **아이폰 16 Pro 액션 버튼·단축어·Siri로도 이 수집을 앱 밖에서 실행 가능**(App Intent, 메인 타깃 정의 → 익스텐션·특수 entitlement 불필요, 무료 서명 OK — 구현됨 2026-07-19). **원본 음성 보존(구현됨 2026-07-22):** STT로 받아쓰는 동안 원본 음성도 함께 녹음해 **`<uuid>.m4a`로 보존**하고, 항목 create 성역에 **`audio:` 포인터**를 찍는다(이미지의 `assets/` 포인터 선례와 같은 결). **텍스트 층은 가변, 원본 음성은 불변**(write-once) — STT가 불완전해도 나중에 직접 듣고 고치거나 더 나은 STT에 다시 맡길 수 있는 진실 기준. 여러 take(정지→재개)는 한 파일에 시간순으로 이어짐. 상세화면에서 "다시 듣기". **기본은 기기전용**(음성이 기기 밖으로 안 나감 — 온디바이스 STT 원칙 유지, §7). 정본 = `docs/native/audio-capture-design.md`.

**자동 전체 수집은 금물.** 방문한 사이트를 다 받으면 받은함이 방문 기록 복사본·무덤이 된다. 신호가 잡음에 묻혀 원래 고통이 재현된다. *사람의 "이건 잡을 가치가 있다"는 판단 한 번*이 곧 필터다. 무엇을 **안 넣을지**가 핵심.

**스크린캡처는 1층으로.** 이미지는 grep도 안 되고 분류에 OCR이 필요해 평문 원칙과 충돌한다. v0에서는 캡처 대신 복사를 기본으로. 복사 불가한 화면일 때만 예외적으로 캡처해 두고, 옆에 *무엇인지·왜 잡았는지*를 텍스트 한 줄로 남겨 검토 때 이미지를 안 열어도 되게 한다.

### 이미지 수집 — 선결 고려사항 (이번에 입구 포함, OCR은 나중)

이미지(스크린샷·바코드 쿠폰·사진)는 텍스트와 근본이 달라 넣기 전에 정해야 한다.

- **저장 방식 = 포인터.** 이미지 자체는 `inbox.md`에 못 넣는다(평문 원칙). 이미지는 폴더(예: `SecondBrain/assets/`)에 파일로 저장하고, 받은함엔 **경로(포인터) 한 줄 + 사용자가 남기는 "왜" 한 줄**만 남긴다. → "여러 줄 원본 보존"의 포인터 방식과 동일. 예: `- 2026-07-10 09:00 | image | ./assets/coupon_0710.jpg — 왜: 스타벅스 쿠폰, 7/10까지 사용`
- **OCR은 초기 개발 대상에서 제외 (나중에).** 바코드·쿠폰·스크린샷 속 글자를 자동으로 뽑는 OCR은 *편의 기능*일 뿐 — 없어도 수집·검색·활용은 다 된다. OCR이 없으면 이미지 *속 글자*는 검색 안 되지만, **사용자가 단 "왜" 한 줄은 검색된다.** 그래서 OCR은 "왜" 한 줄을 자동으로 채워주는 보조라, 뒤로 미뤄도 무방. → **이번엔 안 만든다. 나중 과제.**
- **이미지 입구는 이번에 넣는다 — 여러 경로 모두.** 이미지가 들어오는 방식은 하나가 아니므로 다 열어둔다:
  - **촬영** — iPhone(메인 디바이스)에서 그 자리에서 사진 찍기. 쿠폰·화이트보드·문서.
  - **복사·붙여넣기** — 이미 있는 이미지를 클립보드로. (iPhone/Mac 스크린샷을 복사한 경우 포함.)
  - **파일 지정** — 파일 이름·경로로 기존 이미지 파일을 가리키기.
  - **앱에서 공유** — iPhone "공유 시트"로 사진·사파리 등 다른 앱에서 받은함으로 보내기. (스크린샷 공유가 여기 해당.)
  - *참고: iPhone 스크린샷은 별도 입구가 아니라 "복사·붙여넣기" 또는 "앱 공유"의 한 경우.* 어느 경로든 → `assets/`에 이미지 저장 + 받은함에 포인터+"왜" 한 줄.
- **분류용 최소 메타.** 이미지만으론 종류·시점을 모른다. 넣을 때 "왜/언제까지" 한 줄은 필수(예: 쿠폰=만료일).

#### 네이티브 v1 확정 (2026-07-24) — 카메라 · 성역·불변 · EXIF 위치

네이티브 v1은 위 여러 입구 중 **촬영 하나만** 먼저 구현하고, 저장·프라이버시 규칙을 아래로 못박는다. 나머지 입구(복사·붙여넣기·파일 지정·앱 공유)와 OCR은 여전히 나중 과제. 설계 근거·전말: `docs/native/photo-capture-design.md`.

- **획득 = 카메라 촬영만.** 앨범 선택 없음. **본문(원문)을 먼저 쓴 뒤에만 촬영 가능** — "원문 없는 기억"을 차단한다. 촬영본은 캡처 시 1회 리사이즈(긴 변 ~2048px)·JPEG 압축한 **그 파일이 곧 원본**이며 이후 **불변(write-once)**. (무손실 아님 — 용량 절약, 주차·쿠폰엔 충분.)
- **저장 = 포인터, 성역·불변.** 사진은 파일로 저장하고 받은함엔 `photo: <파일>` 포인터 한 줄만 남긴다(음성 `audio:`와 같은 자리). 이 포인터는 **create 이벤트에만** 있어 병합(per-field LWW) 때 **절대 안 덮인다 = 사실상 불변(성역).**
- **★ 위치(GPS)는 사진 EXIF에만 — 평문 텍스트 그릇엔 절대 안 박는다 (프라이버시 안전 규칙).** 촬영 순간 좌표를 읽어, 저장하는 JPEG의 **EXIF GPS 태그에만** 1회 기록하고(불변), 볼 때 사진에서 읽어 지도로 표시한다.
  - **왜 그릇에 안 넣나:** 텍스트 그릇(`inbox*.md`)은 **iCloud로 동기화**되지만 사진 파일은 **기기 전용**이다. 좌표만 그릇에 박으면 **사진 없이 위치만 iCloud로 새어나간다 = 민감정보 누출.** 그래서 기계 좌표는 사진 파일 안(EXIF)에만 둔다.
  - **사람이 손으로 쓰는 위치 설명**("지하 2층 B구역")은 좌표가 아니므로 그릇에 넣고 동기화해도 무방. **기계 좌표(GPS)만 EXIF 전용.**
  - 권한 거부·실내 등 신호 없음 → **GPS 없이 저장(graceful).**

---

## 1-A. 텍스트 층 수정 (네이티브 v1)

- **텍스트(원문)는 수정 가능**, **원본 미디어(녹음·사진)와 수집 메타(시각·기기·방식)는 불변.**
- 수정은 **append-only edit 이벤트**로 기록 — 기존 기록을 고치지 않는다. 병합은 **필드별 LWW.**
- 값에 위험 문자가 있으면 **fields.v1 JSON**으로 직렬화한다. 판단은 **왕복 검증**(직렬화→파싱→원본 대조)으로 한다.
- **빈 본문은 저장되지 않는다.**
- 정책·화면 세부 → `docs/native/edit-policy.md` §2-A·§8, `docs/native/memory-philosophy.md` §6.

---

## 2. 분류 체계 (type)

| type | 정의 | due | resurface | 비고 |
|---|---|---|---|---|
| event | 예정된 일 | 있음 | due 며칠 전 | 가장 쉬움 |
| promise | 누가 나에게 부탁 / 내가 한 약속 | 보통 없음 | none | **가장 위험** — 관계·신뢰가 걸림. 완료까지 open |
| info-action | "이걸로 뭔가 해야겠다" | 물어서 정함 | none | "그래서 뭘, 언제?" 답 없으면 → discard |
| info | 행동은 필요 없지만 나중에 참고할 사실·정보 (장소·위치, 특정 인물 메모·평가, 참고 사실) | 보통 없음 | none | 짧아도 버리지 말고 보존. 검색(pull)으로 소비 |
| idea | 머릿속에서 튀어나온 생각 | 보통 없음 | none | 주차되었다가 전환되거나 버려짐 |
| principle | 하루 시작 다짐·생활 원칙 등 반복 인지용 문장 (= ambient) | — | 상시 | 화면 상단 "오늘의 원칙"에 상시 노출 |
| discard | 보관할 가치 없음 | — | — | **명시적으로 버리는 것도 시스템의 기능** · 네이티브 v1은 discard를 **삭제**로 표현(보관함 '삭제' 섹션) · **단 자동 분류(AI)는 discard를 반영하지 않는다** — 삭제는 사람만(§0-A) |

재등장(resurface) 규칙:
- `due`가 있으면 → 1층 앱이 **이번 주(7일 이내이거나 이미 지난)** 항목을 "곧 닥칠 것"에 D-day로 맨 위에 띄운다. (v0: "due 며칠 전" 세밀 조정 대신 7일 창으로 단순화 — 며칠 써보고 필요하면 조인다.)
- `due`가 없으면 → 주간 검토 더미로 간다(promise·info·info-action·idea 등). 조용히 썩지 못하게 강제 노출.
- `principle`은 due·resurface와 무관하게 상단에 상시 노출(ambient).

### 2-A. 분류–세부정보 모델 (네이티브 v1 — 정본 = `docs/native/memory-philosophy.md` §7)

**위 표는 §3 프롬프트와 짝인 "AI가 무엇으로 분류하나"의 보호자산이라 그대로 둔다.** 하지만 **앱의 분류 축과
시점 사용 여부의 정본은 §7**이다. 아래는 그 정정·요약(구현 완료 2026-07-26 ~ 07-29).

- **분류는 코드로 고정한다.** 사용자가 분류를 만들지 않는다(편집 UI·동적 검증·만능 규칙 엔진 없음).
  더 세밀한 세분은 **해시태그**(미결). 새 분류가 진짜 필요하면 개발을 다시 거친다.
- **공통 그릇 + 분류별 정의.** 모든 기억은 하나의 공통 세부정보 그릇을 공유하고, 분류는 새 데이터 구조가
  아니라 그 위의 **정의**다 — 세부정보마다 **(a) 쓸지/안 쓸지 · (b) 제목(=의미) · (c) 동작**을 정한다.
- **모든 분류 평등 — 2층(기본층/유연층) 구조는 없앴다.** 위 표의 6종에 **주차 위치(`parking`)**가
  **나란한 한 항목**으로 들어가 **7개**다(색·심볼도 6종과 같은 격). 초기 구현의 `FlexTypeCatalog`(유연층)는
  흡수돼 사라졌다. `discard`는 분류 축이 아니라 **삭제 취급**이라 이 7개에 없다(§0-A: AI 삭제 금지).
  - **주차 위치는 아직 사람이 지정만** 한다 — §3 프롬프트·자동분류 대상이 아니다(`open-questions.md` **O4**,
    "안 하기로 한 것"이 아니라 "아직 안 한 것").
- **정보·아이디어·원칙은 시간을 안 쓴다.** `due`·`resurface`를 **둘 다** 안 쓴다 → 상세 화면에 "시간 설정"
  섹션 자체가 없고, 값이 남아 있어도 **알림·"지금 챙길 것" 판정에서 제외**된다. **주차 위치는 마감만** 안 쓰고
  **다시 보기는 쓴다**(칸별 판단 — 분류 단위로 통째 막지 않는다).
- **두 날짜 칸의 역할(2026-07-30 분리).**
  - **미리 알림(`resurface`)** = 언제부터 '지금 챙길 것'에 **게시할지**(게시 시작).
  - **마감(`due`)** = 실제 기한. **남은 날짜(D±N) 계산의 유일한 기준.**
  - **미리 알림이 아직 안 왔으면 게시하지 않는다.** 항목이 사라지는 게 아니라 **시점 없는 쪽 목록**에 있다(개수 보존).
  - **규칙 1:** 마감이 **미래**일 때 미리 알림은 마감보다 **최소 하루 빠르다.** 마감이 지났거나 없으면 제약 없음.
  - **미루기는 마감이 아니라 미리 알림을 민다. 마감은 미루기로 안 바뀐다.**
  - 세부 = `docs/native/memory-philosophy.md` §7(c) · `docs/native/edit-policy.md` §4-2/§4-3.
- **정의 없는 분류(미분류·미등록 key)는 전부 씀으로 폴백.** 분류가 안 붙었다는 이유로 사람이 적어둔 날짜가
  조용히 사라지지 않는다(버림 편향 금지).
- **값은 안 지운다.** 안 쓰는 분류의 날짜는 휴면 보존이고 분류를 되돌리면 되살아난다. 위 표·§3 프롬프트는
  그대로라 AI가 아이디어에 `due`를 붙일 수 있는데, **그 값은 저장되지만 시점으로 쓰이지 않는다.**
- **분류 × 세부정보 표의 정본은 코드**(`native/SecondBrainCore/.../ClassSpec.swift`의 `ClassSpecCatalog`)이고,
  문서(§7)는 그 거울이다.
- **되풀이(recurrence) — 8번째 분류(2026-08-02 설계, 양력 반복 구현 진행 중).** 옛 "반복 = 속성"을 뒤집어 **새 분류**로 잡았다.
  정본 = `docs/native/recurrence-design.md`. 아직 위 7개 표에 안 올린다(음력·알림 트리거 등 기반이 남았다).
  **자동분류 대상이 아니다** — 수동 지정만(주차와 동일, §3 프롬프트를 안 건드린다).

  **완료의 뜻이 다르다 (2026-08-05 확정).** 다른 분류의 완료는 **영구히 끝(단방향)** 이고 항목이 보관함으로 간다.
  되풀이의 완료는 **"이번 회차 했다"** 이고 **기억은 살아남아 다음 회차로 이어진다** — `status`를 안 건드린다.
  그래서 **며칠을 반복해도 하나의 기억**이고 놓쳐도 항목이 안 늘어난다(놓친 회차는 "N일 놓침" 표시로만).
  그만두기 = 삭제 / 잠시 멈춤 = 꺼두기 / 이번 것 = 완료 — **셋을 구분한다.**

  **회차(cycle) 개념.** 되풀이 항목은 **마감(due) = 회차 앵커 = 이번 회차의 시각**을 들고 있고,
  완료하면 그 값이 다음 회차로 전진한다(미리 알림도 같은 간격으로 함께 전진 — lead 보존).
  **미리 알림 = 그 회차가 열리는 시각**(없으면 마감 시각부터 열린다).
  완료는 두 값을 남긴다: **`lastDone`**(완료한 시점)과 **`lastDoneDue`**(그 완료가 회차를 옮긴 목적지).
  뒤엣것이 **"이번 회차를 완료로 닫았나"의 증인**이다 — 자동 완성으로 회차가 저절로 넘어간 경우(catch-up)는
  이 값을 안 쓰므로 **"했다"와 "넘어갔다"가 구분된다.** 앱이 안 한 것을 했다고 말하지 않기 위한 장치다.

  → **화면 동작 세부**(표시·취소·재완료 멱등·이른 완료·놓침 표시·예산·알림)는 **전부 정본** `recurrence-design.md`
  (§5 · §5-대가 · §5-조사 · §5-증인 · §9). 이 사양서엔 **완료의 뜻과 회차 개념까지만** 둔다.

### 2-B. 알림 문구 — 세 톤 (용어 자산, 2026-08-06 확정)

> **말은 동작이 아니다.** 화면 동작 세부는 정본(`recurrence-design.md`)에 두는 선을 그대로 유지하되,
> **사용자에게 나가는 말 자체는 §2·§3와 같은 층의 자산**으로 여기에 둔다. 문구를 만질 때 **여기를 먼저 본다.**

| 톤 | 문구 | 언제 |
|---|---|---|
| 재촉 · 예고 | **「곧 챙길 것」** | 완료를 요구하는 항목의 **미리 알림**(lead) |
| 재촉 · 지금 | **「지금 챙길 것」** | 완료를 요구하는 항목의 **마감/회차** |
| 통보 | **「오늘 기억할 것」** | 완료를 **안** 요구하는 것 — 자동 완성이 있는 되풀이(생일·기일) |

- **"챙기다" = 재촉 / "기억하다" = 통보.** `…것` 어법과 핵심어 **"기억"** 을 유지한다.
- **톤은 종류(lead/회차)가 아니라 "완료를 요구하는가"가 먼저 정한다.** 미리 알림 없는 생일은 회차 지점
  한 건을 내는데, 거기에 「지금 챙길 것」이 뜨면 **완료를 안 요구하는 것을 재촉**하게 된다.
- **「지금 챙길 것」은 섹션 이름과 일부러 같다** — 탭했을 때 갈 곳의 이름과 맞춘 것이다.
  ⚠️ **현재 탭해도 그리로 안 간다**(정본 §9 미결 "알림 탭 착지"). **문구가 약속을 앞서 있다** — 말이 아니라 동작을 고친다.
- **본문(body)은 원문 그대로.** 알림이 **기억 자체**를 보여준다(요약·가공 안 함).

**★ 왜 사양서에 두는가 (2026-08-06 결정 근거).**
알림 제목이 **`"받은함 · 곧 닥칠 것"`으로 몇 달 나가고 있었다.** 두 단어 다 **화면에 없는 말**이다
(탭은 "새로운 기억", 섹션은 "지금 챙길 것"). 그런데 **그 말이 어디에도 정본으로 없었다** —
코드 문자열 한 줄이 사실상 유일한 출처였다.
**2026-08-03에 같은 이름 불일치를 발견해 Core 주석 3곳을 정정했는데, 정작 사용자에게 나가는 문자열은 안 훑었다.**
대화에서 드러난 문제라 **대화의 언어(주석·문서)만 손보고 끝낸 것**이다.
→ **말에도 정본을 준다.** 화면 문자열 쪽에는 회귀선을 걸었다(`NotificationToneTests`).

---

## 3. 분류 프롬프트 (그대로 복사해 사용)

> v0: Claude 챗에 붙여넣어 사용. 1층: 스크립트(`classify.py`)가 API 호출에 사용.
> **기준 프롬프트는 `classify.py`의 `SYSTEM_PROMPT`가 정본이다.** 아래는 챗에 붙여넣는 사람용 사본 — 규칙이 바뀌면 둘을 함께 갱신한다.

```
다음은 내 받은함의 미가공 수집 줄들이다. 각 줄을 아래 규칙으로 분류해 마크다운으로 돌려줘.

분류(type): event(예정된 일) / promise(부탁·약속) / info-action(이걸로 뭘 해야겠다) / info(행동은 필요 없지만 나중에 참고할 사실·정보) / idea(생각) / principle(하루 시작 다짐·생활 원칙 등 반복 인지용) / discard(버릴 것)

각 항목에 붙일 것:
- type
- due: 날짜가 명시되거나 맥락에서 추론되면 YYYY-MM-DD, 없으면 none
- resurface: due가 있으면 그 며칠 전 날짜(promise/event는 2~3일 전, 여유 없으면 due 하루 전), 없으면 none. **규칙 1: 미리 알림은 마감보다 최소 하루 빠르다 — 마감 당일이 될 만큼 여유가 없으면 none.**
- status: 항상 open
- question: info-action인데 "구체적으로 뭘, 언제 할지"가 불명확하면 그 한 줄 질문. 아니면 빈 문자열.

규칙:
- 짧다는 이유만으로 버리지 마라. 파편이라도 (1)장소·위치, (2)특정 인물 메모·평가, (3)나중에 참고할 사실이면 info로 보존하라.
- discard는 테스트/시스템 입력('시험 중', '음성 메모 추가' 등)이나, 받아쓰기 실패로 의미를 알 수 없는 조각에만 한정하라.
- 사람과의 약속(promise)은 절대 놓치지 말고 보수적으로 잡아라.
- 시점(due) 추출을 적극적으로: 상대 표현("내일/이번 주/다음 주/이번 달 말")은 오늘 날짜 기준 구체 날짜(YYYY-MM-DD)로 환산. 명시적·추론 가능한 시점이 전혀 없으면 none.
- 하루를 시작할 때의 다짐·생활 원칙 같은 반복 인지용 문장은 principle 로 분류하라.
- 원문은 절대 바꾸지 말고, 필드만 추가하라.

오늘 날짜: {오늘}
미가공 줄:
{여기에 그날의 inbox 줄들 붙여넣기}
```

> **네이티브 v1 자동 분류(구현됨 2026-07-20).** 위 프롬프트(정본 = `classify.py`의 `SYSTEM_PROMPT`)를 iPhone이 Claude API로 직접 호출해 미분류에 적용한다(§0-A). **프롬프트는 그대로** 쓰되, 결과 반영 단계에 두 가지 정책이 붙는다: (1) **AI 삭제 금지** — 모델이 `discard`로 분류해도 반영하지 않고 미분류로 보존(삭제는 사람만·단방향). (2) **question은 아직 파일에 안 씀** — 네이티브 저장 경로가 공백 포함 값을 못 담아 후속 과제로 둠(분류 품질엔 영향 없음 — 모델은 그대로 계산).

---

## 4. 검토 의식 (review loop) — 이게 핵심

- **매일 (1분):** `resurface`가 오늘 이전이거나 `due`가 임박한 항목만 한 묶음으로 본다. → "곧 닥칠 것."
- **매주 (10분):** `due: none`인 **모든** 항목(promise·info-action·idea 전부)을 다시 읽는다. → 잊힌 약속과 정보를 강제로 눈앞에 끌어낸다. 여기서 처리/전환/discard 결정.
  - **주의:** 이 '매주 검토'는 **사람의 검토 리듬(습관)**이지 `resurface` 값이 아니다. 대상 판정은 `resurface` 값이 아니라 **`due: none` 여부**로 한다. `resurface` 값은 `YYYY-MM-DD`(그 날 재등장) 또는 `none`(날짜 없음)뿐이며, **과거 `weekly` 값은 `none`의 동의어였던 레거시**라 읽기만 호환한다(주기적 재등장/반복 기능은 설계·구현된 적이 없다).

검토 의식은 사람이 쥔다. AI는 검토를 *대체*하는 게 아니라 *값싸게* 만든다.

**네이티브 v1 받은함 표현.** 받은함은 세 영역으로 나뉜다 — **원칙**(principle, 상단 상시 ambient) · **곧 닥칠 것**(due/resurface 있는 것, D-day 순) · **기억 목록**(시점 없는 `due: none` 더미 = 주간 검토 대상). 종류 필터(칩)는 **'기억 목록'에만 적용**한다 — 원칙·곧 닥칠 것은 각자 "항상 걸린 분류"(principle 타입 / 시점 있음)라 필터와 무관하게 상시 표시. 항목을 재분류해 원칙이 되거나 시점이 붙으면 기억 목록에서 빠져 해당 영역으로 이동한다.

> **주의(2026-07 갱신):** 위 3영역 서술은 초기 모델이다. **현행 화면 구조(새로운 기억 / 살아있는 기억 / 보관된 기억 + 상단 원칙 영역)와 원칙 개념의 정본 = `docs/native/memory-philosophy.md` §2·§3.** (원칙 = 습관 체화 대상, 지정 무제한·동작 상위 N개, 목록 화면·순서 등은 그 문서가 정본. 전면 재작성은 이 사양서 범위 밖 — 여기선 포인터만.)

**종류 필터는 동적이다(구현됨).** 칩은 고정 목록이 아니라 **필터 전 데이터에 실제 존재하는 분류만** 노출한다
(`전체` + 실재 분류, 미분류는 끝). 순서는 §7 정본 순(6종 → 주차 위치). 데이터에 주차 위치가 생기면 칩이
**자동으로** 나타나고, 사라지면 빠진다 — 단 **현재 선택된 필터는 실재하지 않게 돼도 계속 노출**한다
(보이지 않는 필터에 갇히지 않게). 살아있는 기억과 검색이 같은 UI를 각자 상태에 물려 재사용한다.

> **이름 주의 — 개념명과 화면 헤더가 다르다(2026-07-29에 실제로 혼동을 일으켰다).**
> 코어 코드·이 사양서의 개념명은 **"곧 닥칠 것"**(시점 있음) / **"최근 들어온 것"**(시점 없음)이지만,
> **화면 헤더는 "지금 챙길 것" · "새 기억들" · "살아있는 기억"**이다. 시점 없는 항목은 하나의 목록이 아니라
> `confirmed` 여부로 **"새 기억들"(미확정) / "살아있는 기억"(확정)**으로 갈린다(`InboxModel.Partition`).
> 이 차이 때문에 §7 (c) 검증 안내에서 "받은함의 '최근 들어온 것'에 있어야 한다"고 **잘못 안내한 일**이 있었다
> (실제로는 확정 항목이라 "살아있는 기억" 탭으로 이동). **이름 통일은 별도 과제** — 지금은 사실만 기록한다.

---

## 5. 빌드 3층 (0층부터 시작)

> **[정합화 · 아키텍처는 §0-A 네이티브 v1로 대체됨]** 아래 0층/1층의 **웹 경로**(코드 없는 수동 분류 → 로컬 웹 앱/PWA → 데스크톱 cron 분류)는 v0 검증 단계의 **기록**으로 남긴다 — 최신 아키텍처 정본은 §0-A. **단, 여기 정의된 소비 3방식(pull/push/ambient), "시점 채우는 2+3 방식", 이미지 입구 4경로는 네이티브 v1에서도 그대로 유효**하다(개념은 생존, 구현 수단만 네이티브로 바뀜).

- **0층 — 코드 없음 (지금):**
  폰 음성 받아쓰기 → `inbox.md`(동기화 폴더)에 한 줄. 하루 1회 그날 줄을 Claude 챗에 붙여넣어 §3 프롬프트로 분류받아 다시 파일에 붙인다. 검토는 눈으로. → **습관·규칙 검증 단계. 1~2주 운영.**
  - **현재 셋업 (구현 완료):**
    - 받은함: `C:\Users\me\iCloudDrive\SecondBrain\inbox.md` (iCloud Drive, 3기기 동기화)
    - **iPhone(음성):** 단축어 "받은함에 추가" → 받아쓰기 → `날짜 | voice | 내용`. 시리/홈 화면/잠금 화면 호출.
    - **Mac(텍스트):** 단축어 → 클립보드 + "왜?" 입력 → `날짜 | web | 내용 — 왜: …`. 단축키 `⌃⌥⌘I`.
    - **Windows(텍스트):** PowerShell `add-2-inbox.ps1`(같은 폴더) + 팝업 입력(연속 입력: 빈칸/취소로 종료) → 바탕화면 바로 가기 단축키 `Ctrl+Alt+I`. 긴 클립보드는 한 줄로 압축(아래 미해결 과제 참조).
    - **Edge 북마클릿(URL, Mac·Win 공통):** 즐겨찾기 바에 "받은함 복사" 북마클릿 → 클릭 시 `제목 — URL`을 클립보드에 복사 → 이어서 단축키(⌃⌥⌘I / Ctrl+Alt+I)로 "왜?" 입력 후 받은함에. *URL 긁어 복사하는 수고 제거.* (북마클릿은 브라우저 샌드박스라 `inbox.md`에 직접 못 씀 → 클립보드 경유. 진짜 원클릭 자동저장은 1층 과제.)
    - 줄 형식: `- 날짜 | source | 내용` (대괄호 없음), source = voice/web/doc/chat 등.
    - **묶기(정정·이어말하기)는 0층 수집의 사소한 기능.** 말로 입력하다 생긴 오타 정정이나, 녹음이 빨리 끝나 이어 말한 것을 합치는 것. *분류 기준 아님.* 화면엔 "묶임"을 작은 빨간 점(●)으로만 표시하며, **이 점은 영구 표식이 아니라 일회성 확인 알림 — 사용자가 클릭(확인)하면 사라진다.**
    - **시점 자동 추출:** 내용 맥락에 시점이 있으면("내일까지 연필 주문") 자동으로 뽑아 표시하되, 추출값은 확정이 아니라 **"~까지 ?" (재확인 필요)** 로 표시. 명시적 날짜가 없고 "빨리" 같은 상대 신호만 있으면 그것도 추정 표시.
    - **예정된 입구 (아직 미구현, 자리만 확보):** 이미지 수집(스크린샷·바코드), Outlook 등 외부 앱에서 정보 받아오기. 화면은 source 아이콘으로 확장 가능하게 설계됨(voice/web 외에 image/mail/doc 자리 열어둠).

> **미해결 과제 (1층에서 다룰 것): 여러 줄 원본 보존.**
> 현재 Windows 스크립트는 여러 줄 클립보드의 줄바꿈을 공백으로 눌러 "한 줄 = 한 항목"을 유지한다. 단순함은 얻지만 **원본 구조(코드 들여쓰기, 문단)가 복구 불가하게 손실**된다 — 데이터 영속 원칙과 상충.
> 1층 해법 방향: 받은함은 "한 줄 요약/포인터"만 유지하고, 긴 원문은 별도 파일/아카이브에 보존한 뒤 받은함 줄에서 링크. 즉 *눌러서 버리지 말고, 따로 보관하고 가리키기.* (받은함=능동 알림, 아카이브=원문 보존. 두 그릇 분리.)

- **1층 — 작은 스크립트 (Claude Code):**
  1층의 정체성 = **출구(소비)의 층.** 0층이 수집(입구)이라면 1층은 "놓치지 않고 소비하기". 소비 방식 3가지(사용자 정의):
    - **pull (검색): 나중에 필요할 때 당겨쓸 것.** 평소엔 안 보여도 됨. 저장·검색만 잘 되면 됨. 예: 참고 링크, 시세. → 검토 화면 검색으로 거의 공짜로 해결.
    - **push (시점): 때가 되면 시스템이 먼저 들이밀 것.** 검색으론 안 됨(찾으러 갈 생각을 못 하므로). 마감/시점 근처에 자동 노출·알림. 예: 멤버십 갱신, 일정, "언제까지 만나기". → **1층 1순위** (가장 아픈 문제, 원본에 실제로 묻혀 썩는 종류).
      - **시점 채우는 방식 (확정): 2+3 조합.** (1)수집 때 묻기는 *제외* — 수집 마찰이 너무 커짐. (2)분류가 맥락에서 자동 추출("내일까지 주문"→추출, 애매하면 "~까지 ?" 재확인 표시). (3)검토 화면에서 사용자가 항목에 날짜 직접 지정. → 수집 순간 마찰 0, 시점은 자동+수동으로 채움.
      - **들이미는 방식 (단계적): 우선 "화면 강조"부터.** (1)화면 안 강조 = 앱 열면 때가 된 항목이 맨 위에 D-day로 부각(지금 구조에 바로 얹힘) ← **여기서 시작.** (2)진짜 잠금화면 알림(PWA)은 iOS 제약·서버 필요 문제로 보류 — 화면 강조를 며칠 써보고 "안 열어도 울려야 한다"가 확인되면 그때(네이티브 전환과 함께) 검토. (3)매일 아침 다이제스트는 그다음.
    - **ambient (상시): 늘 곁에 두고 반복 인지시킬 것.** 마감 없음. 매일 반복 노출. 예: 하루 시작 원칙, 생활 리마인드. → push 자리 잡은 뒤 얹음(예: 화면 상단 "오늘의 원칙" 칸).
  - 한국어 STT: 폰 받아쓰기로 충분, 긴 메모는 Whisper(로컬/API).
  - `inbox.md` 새 줄 감지 → §3 프롬프트로 API 분류 → 구조화 저장.
  - **검토 화면(출구 실물) = 1층 앱의 시작.** 실제 `inbox.md`를 읽어 종류별 분류 + 검색 + "다시 볼 것" 표시. **경로 1: 로컬 웹 앱(PWA)로 만들어 iPhone·다기기에서 먼저 검증 → iPhone 네이티브로 전환(§0-A).** (프로토타입 완성: 종류=행동/생각/원칙/정보/정리, 검색, 필터, source 아이콘, 묶음 점(클릭 시 소멸), 시점 "~까지 ?" 재확인 표시.)
  - **기기별 얇은 입구 재정비 (요구사항).** 현재 0층 입구(iPhone 단축어·Windows PowerShell·Edge 북마클릿)는 작동하나 임시방편 — 확장성이 없다(이미지·시점·source를 더하려면 기기마다 따로 손봐야 함). 1층에서 각 기기에 **"제대로 된 최소 수집 도구" 하나씩**을 갖춘다: 지금의 프롬프트/터미널 방식이 아니라, 이미지·시점·출처를 받을 여지를 둔 형태. 단 여전히 *얇게*(마찰 제로) 유지.
  - 한국어 STT: 폰 받아쓰기로 충분, 긴 메모는 Whisper(로컬/API).
  - `inbox.md` 새 줄 감지 → §3 프롬프트로 API 분류 → 구조화 저장.
  - 매일/매주 다이제스트 자동 생성(다른 파일 또는 알림).
  - **이미지 입구(이번 포함) / OCR(나중):** 여러 경로 — iPhone 촬영 / 복사·붙여넣기(스크린샷 포함) / 파일 지정 / 앱 공유 시트 → `assets/`에 저장 → 받은함에 포인터+"왜" 한 줄(§1 이미지 선결사항 따름). **OCR(이미지 속 글자 자동 추출)은 초기 제외 — 나중 과제.**
  - **원클릭 URL 수집:** 북마클릿/브라우저 확장 버튼 → 보는 페이지의 URL+제목을 자동으로 한 줄, "왜 잡았나"만 손으로. *판단은 사람, 타이핑은 자동.* (0층의 Edge 북마클릿→클립보드→단축키 흐름을, 로컬 서버/네이티브 메시징으로 "클릭 시 받은함에 바로 기록"까지 단축. **입구 마찰이 사용량을 결정 — URL은 쓰고 싶은 순간이 많았으나 3단계 마찰로 안 쓰임. 이 마찰 제거가 1층에서 출구와 함께 처리됨.**)
  - 로컬·평문 유지. 위험 권한 없음.

- **2층 — 자율 에이전트 (나중):**
  하트비트 + 메일 자동 감시(흐름을 읽어 promise/event/info 후보 추출 → 검토 더미에 투입).
  **메일을 첫 자동 감시 대상**으로(효용 높고 신호 강함). 카카오톡·SMS는 일단 제외(접근 막힘/약관/취약).
  보안: 읽고 *제안만* 먼저. 행동(메일 발송·삭제·결제)은 사람 승인 게이트 뒤로. 신뢰는 시간을 두고.

---

## 6. Claude Code 인수인계 노트

**첫 목표: 네이티브 v1 (Mac 앱 + iPhone 앱) — §0-A.** v0 웹 PWA(검토 화면·`classify.py`·수집 스크립트)와 실사용 교훈을 근거로, **iPhone 중심 + 기기별 조각 파일** 구조 위에 새로 짓는다. (근거 문서: `native-v1-design-draft.md`.)

**착수 직전 확정할 미결 (초안 §9):**
1. **기술 스택 — ✅ 확정 (2026-07-15): Swift + SwiftUI (Apple 네이티브), iPhone·Mac 코드 공유.** 이유: 파일 쓰기·즉시 분류·로컬 알림·이미지 공유시트·카메라가 Swift에선 1급 기능(크로스플랫폼은 덧대야 함). Windows는 평문 조각 파일로 나중 참여(§0-A)라 앱 프레임워크까지 크로스일 필요 없음. 부속 결정: 프로젝트 생성=**XcodeGen**(`project.yml`→`.xcodeproj`, 머지 충돌 예방) · 배치=repo 내 **`native/`**(웹 v0 별도 보존) · 서명=**무료 Apple ID로 시작**(30일 본격화 때 $99 검토) — **2026-07 유료 Apple Developer Program 등록 완료(서명 ~1년)** · 알림=**로컬 알림**(서버·APNs 불필요) · 타깃=최신 OS(개인용, 구기기 호환 불필요).
2. **수정 이벤트의 v1 범위** — 단순형(조각 = 새 수집 위주) vs 완전형(수정·삭제·미루기까지 append 이벤트). iPhone 중심이라 완화되나 수준 확정 필요. *(다음 확정 대상.)*
3. **합치기 엔진 규칙** — 여러 조각 파일 → 받은함 하나로 합칠 때 정렬·중복제거·최신 이벤트 반영.

**만들 것(우선순위 순):**
1. **수집 입구(기기별)** — "이 부분 실패하면 프로젝트 실패". 텍스트 · URL 원클릭 · **이미지 4경로**(촬영/복붙/파일지정/앱공유, 포인터 저장, OCR 나중) · 파일. 영상·녹음은 뒤로.
2. **조각 파일 합치기 엔진 + 항목 행동**(기억하기·수정·삭제·미루기·done) — §0-A의 append 이벤트 모델.
3. **분류 로직 재사용** — §3 / `classify.py`의 `SYSTEM_PROMPT`를 네이티브에서 호출(iPhone 즉시 분류). "어떻게 분류하나"는 v0 그대로.
4. **소비 3방식** — push(시점 알림, 이제 진짜 네이티브 알림) · pull(검색) · ambient(원칙 상시).

**재활용 금지 (웹 특유 — 초안 §8):** localStorage 임시저장, `<input type=date>`+showPicker 우회책, 서비스워커/PWA 배지, File System Access 쓰기 경로. 네이티브는 깨끗이 새로.

**데이터 원칙:** 평문 유지, 외부 전송 최소화, 모델 교체 가능. 이미지는 `assets/`에 파일, 받은함엔 포인터. **음성 원본**도 파일로 보존(`audio:` 포인터) — **기본 기기전용**(외부 전송 최소화·온디바이스 STT와 정합), 항목별 iCloud 동기화는 향후 옵트인(구조만 열어둠). 상세 = `docs/native/audio-capture-design.md`.

전략·규칙 변경은 Code가 아니라 Claude 챗에서 결정한 뒤 이 사양서를 갱신해 다시 넘긴다.

---

## 7. 성공 기준

기능을 더했는가가 아니라 — **"30일 동안 실제로 매일 썼고, 잊을 뻔한 것을 시스템이 도로 들이밀어준 적이 있는가."**

> **다음 단계 (네이티브 v1): §0-A 방향으로 착수.**
> - 착수 직전 **§6의 미결 3가지**(기술 스택 · 수정 이벤트 범위 · 합치기 엔진) 확정 — 근거 `native-v1-design-draft.md` §9.
> - 그다음 순서: **수집 입구(기기별) → 조각 파일 합치기 엔진·항목 행동 → 분류 로직 연결 → 소비 3방식(push는 이제 진짜 알림)**.
> - (v0 웹 진행 순서 A→D→C는 웹 검증 단계의 것으로, 네이티브 v1에선 위로 대체.)

> 참고: 방문 기록 전체 수집이 정말 필요하면, 그건 받은함이 아니라 *별도의 검색용 아카이브*로 분리한다. 받은함(능동적으로 다시 들이밀 것)과 아카이브(필요할 때 찾는 것)는 다른 그릇. 한참 뒤, 더 큰 설계의 문제로 미룬다.
