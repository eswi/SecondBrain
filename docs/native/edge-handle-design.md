# 「새로운 기억」 우측 가장자리 버튼 — `#` (2026-09-18 · 회사 맥북 · 사용자 지시)

> **성격:** 사용자가 대화에서 지시한 것을 그 자리에서 적은 문서. **하는 일은 아직 정해지지 않았다** — 사용자가 다음 단계에서 정한다.
> 부품 = `native/Sources/App/EdgeHandle.swift` · 얹은 자리 = `InboxView`(`content`의 `.overlay` · 09-21에 루트에서 옮겼다).
> ✅ **사양서 반영 = `second-brain-v0-spec.md` §0-A-4**(2026-09-21 19:4x · 사용자 지시). 사양서는 「무엇인가」만, 전말은 여기.

## 1. 지시 — 사용자 말 그대로 (2026-09-18 16:5x)

> *"그리고 중요한 것 하나, 우선 '새로운 기억' 화면에서 버튼 하나 달아줘. 첨부한 이미지처럼 해주면 되고, 이 버튼을 우측으로 스와잎하면
> 우측으로 숨으면서 '버튼의 곡선 부분 만큼만 보여주고, 그걸 터치하거나 좌측으로 스와잎하면 다시 나타나게 해줘. 나타난 후에 터치하면
> 무슨 일이 생기는지는 다음 단계에서 결정해줄께. 참고로 버튼이 우측 화면으로 들어가면, 지금은 '>>' 으로 그려져 있는 모양만 나타나게
> 해주고 방향도 <<로 해줘. 버튼의 색깔은 첨부 이미지 색깔에 관계없이 네가 SecondBrain 전체 분위기에 맞게 바꿔줘."*

**첨부 이미지:** 잠금화면 오른쪽 가장자리에 물린 **흰 유리 반알약** — 왼쪽은 둥글고 오른쪽은 화면 밖 · 안에 `#`과 `»`.

## 2. 정해진 것 (사용자) · 고른 것 (Claude · 뒤집을 수 있다)

| # | 무엇 | 값 | 누가 |
|---|---|---|---|
| 1 | 자리 | **「새로운 기억」** 화면 · 오른쪽 가장자리 | 사용자 |
| 2 | 펼침 꼴 | `#` + `»` · 왼쪽만 둥근 반알약 | 사용자(이미지) |
| 3 | 오른쪽으로 끌면 | **접힌다 — 곡선 부분만 보인다** | 사용자 |
| 4 | 접힌 꼴 | **`«` 하나만**(방향 뒤집힘) | 사용자 |
| 5 | 접힌 것을 누르거나 왼쪽으로 끌면 | **펼쳐진다** | 사용자 |
| 6 | **펼친 채 누르면** | ⏸ **미정 — 다음 단계** (`onTap`은 비어 있다) | 사용자(대기) |
| 7 | 색 | `surface2` 바탕 · `border` hairline · **`#` = `accent`** · 화살표 `textSecondary` · 그림자 옅게 | Claude(맡김) |
| 8 | 치수 | 높이 52 · 펼침 폭 92 · **접힘 폭 = 반지름 26 + 4** | Claude |
| 9 | 세로 자리 | 화면(머리줄 아래 영역) 세로 한가운데 | Claude · 이미지는 약 48% 지점 |
| 10 | 접힘 상태의 수명 | **기기에 남는다**(`@AppStorage("inbox.edgeHandleTucked")`) | Claude |
| 11 | 폴더 미연결 화면 | 안 보인다(`needsFolder`) — 그 화면은 온보딩이다 | Claude |

⛔ **6번을 짐작해 채우지 말 것.** `#` 기호라 해시태그와 이어질 가능성이 있지만 **사용자가 정한다.**

## 3. 판정 — 폰에서 볼 것

ⓐ 「새로운 기억」 오른쪽 가장자리 세로 가운데에 `#` `»` 반알약이 있다 · ⓑ 오른쪽으로 끌면 곡선 부분만 남고 `«`가 보인다 ·
ⓒ 그것을 누르면 펼쳐진다 · ⓓ 접힌 것을 왼쪽으로 끌어도 펼쳐진다 · ⓔ 펼친 채 누르면 아무 일도 없다 ·
ⓕ 앱을 끝내고 다시 켜도 접힘이 유지된다 · ⓖ 상세로 들어가면 안 보인다 · ⓗ 다른 탭에는 없다.

## 4. 미결

- **누르면 무엇이 나오나** — 사용자가 새로 디자인한다(09-21 §5 · 지금은 아무 일 없음). *(옛 표현 「6번(펼친 채 누르면)」 — 펼침 꼴은 09-21에 거뒀다.)*
- 접근성 이름(VoiceOver가 읽는 말) — 화면의 말이라 사용자가 정한다(항시 규칙 6) · 지금은 없다.

## 5. 2026-09-21 사용자 지시 — 펼침 꼴을 거두고 · 위아래로 옮긴다 · 모양은 스크린샷대로(대기)

원문:
> *"「새로운 기억」에 우측 가장자리 버튼의 모양과 크기는 내가 지금 첨부하는 스크린샷의 모양대로 해줘. 그리고 눌렀을 때 나타나는 버튼은
> 새로 디자인할 것이니 일단 없애자. 가장자리 버튼 자체는 눌러서 이동할 수 있게 해주고, 이동 방향은 위/아래로만 이동 가능하고 이동 범위는
> 최상단 제목 영역으로 침범하지 않도록 그리고 최하단 탭바를 침범하지 않도록 하자."*

| # | 무엇 | 09-18 값 | 09-21 값 |
|---|---|---|---|
| 1 | 펼침 꼴(`#`+`»`) · 좌우 끌어 접기/펼치기 | 있었다(§2-2~5) | **없앴다** — *"새로 디자인할 것이니 일단"*. ⛔ **「안 됐다」가 아니다**(기록 규칙 9 ②) — 되살리지 말 것 |
| 2 | 손잡이 이동 | 없음(세로 가운데 고정) | **눌러서 위아래로만** · **범위 = 머리줄(제목) 아래 ~ 탭바 위**(`InboxView.content`에 얹어 그 높이 안에서만) |
| 3 | 위치의 수명 | — | 기기에 남는다(`@AppStorage("inbox.edgeHandleTop")` · 음수 = 안 옮김 → 가운데) |
| 4 | 모양·크기 | 09-18 접힌 꼴(반지름 26 곡선 + `«` · 폭 30 · 높이 52) | ✅ **스크린샷대로**(17:0x 두 번째 메시지로 왔다 · 홈 화면 오른쪽 가장자리의 손잡이) — **세로로 긴 둥근 네모 · 폭 24 · 높이 92 · 왼쪽 반지름 14 · `‹` 하나(22pt)** · 어두운 반투명 바탕(`surface2` 92%) · hairline 옅게. ⚠️ **치수는 추정**(표시 배율 0.437pt/px로 환산) — 폰에서 사용자가 다듬는다 |
| 5 | 누르면 | 미정(§2-6) | **아무 일도 없다**(사용자가 새로 디자인) |

✅ 화살표는 스크린샷대로 **`‹` 하나**(옛 `«`는 접힘·펼침 기호였다 — 거뒀다).

### 5-1. 「그대로」 — 스크린샷을 픽셀로 재서 맞췄다 (09-21 17:1x~17:3x)

사용자: *"내가 준 스크린샷의 모양 그대로 할 수 없을까? 우선은 바탕색을 같게 해주고, 그 안의 < 모양 도형도 똑같이 맞춰줘."*

17:07 빌드는 **눈대중 추정**(24·92·14 · SF `chevron.left` 22pt · 팔레트 색)이었다. 이번엔 스크린샷 파일(`images/2.png` · 920×2000 =
원본 1206×2622의 축소 · **0.437pt/px**)을 Swift로 RGBA로 뽑아 세고(`scratchpad/dump.swift` · 임시 · 저장소 밖), 시뮬 스크린샷(@3x)을 **같은 방법**으로 재서 대조했다.

| 무엇 | 참고 스크린샷(잰 값) | 시뮬 `c0bdbbe`+ (잰 값) | 어떻게 맞췼나 |
|---|---|---|---|
| 보이는 폭 × 높이 | 24.0 × 95.7 | 24.0 × 96.0 | 96으로 |
| 왼쪽 모서리 | 프로파일 (dy→dx pt) 0→13.1 · 0.9→9.2 · 1.7→7.4 · 4.4→3.9 · 8.7→1.3 · 14.9→0 = **반지름 14 원호** | 0→11.7 · 1.0→8.3 · 1.7→7.0 · 4.3→3.7 · 6.7→2.0 | `.circular` 14. ⚠️ `.continuous` 15는 22pt까지 퍼져 달랐다(첫 실측) |
| 바탕 | 위 `#393E3F` · 가운데(화살표 위) `#313635` · 아래 `#444F55` · 테두리·그림자 없음 | 위 `#373B3C` · 아래 `#3E474B` | 세 색을 세로 그라데이션(0 · 0.5 · 1)으로 |
| 화살표 `<` 상자(획 포함) | 10.0 × 27.5 · 좌우 여백 7.0/7.0 · 세로 중심 = 손잡이 중심 | 10.0 × 27.7 · 7.0/7.0 · 중심 차 −0.2 | 직접 그린 `ChevronMark`(꺾은선 · 둥근 끝) |
| 화살표 획(수평 단면) | 5.7 | 5.8 (5로 그리니 5.3 → 5.5로) | `lineWidth` 5.5 |
| 화살표 색(밝은 심) | `#BDC1C7` | `#BCC0C6` | `Color(hex: 0xBDC1C7)` |

⚠️ **참고의 바탕은 반투명 재질로 보인다**(벽지 위에서 위·아래 밝기가 다르다) — 이 앱 바탕(`#131218`)에서는 비칠 것이 없어 **잰 색을 그대로 불투명으로** 칠했다.
⚠️ **잰 값은 축소 스크린샷 기준**이라 ±0.5pt 오차가 있다. 폰에서 사용자가 보고 다듬는다.

### 5-2. 비치는 재질 · 튕겨서 보내기 (09-21 17:3x · 사용자 물음 둘 → 승인)

사용자: *"내가 보낸 스크린샷은 Youtube 앱의 모습을 찍은거야. … 첫번째는 네가 말한 '바탕이 비치는 반투명 재질' 느낌인데 이걸 우리도 살릴 수 있을까 … 두번째는
… 손가락을 툭 튕겨주면 알아서 그 튕긴 정도에 맞게 이동되는 기능 … 구현 가능할까"* → *"ㅇㅇ 폰에 올려줘. 비율 들은 네가 알아서 해."*

| # | 무엇 | 어떻게 | Claude가 고른 값 |
|---|---|---|---|
| 1 | 비치는 바탕 | `.regularMaterial`(어두운 판 고정) 위에 §5-1의 회색 그라데이션을 덮는다 | **덮는 비율 0.7** — 0.6은 시뮬 실측 밝기 50~57(참고 60~76)로 어두웠다 · 0.7은 52~60. ⚠️ 재질이 이 앱의 검은 바탕을 따라가 **참고보다 여전히 어둡다** — 더 밝히면 덜 비친다(상충) |
| 2 | 튕기기 | `DragGesture` 끝의 `predictedEndTranslation`(놓는 속도로 계산한 예상 도착점)까지 스프링(0.55s · bounce 0.12) · 경계에서 멈춤 | 놓인 자리에 먼저(애니메이션 없이) 놓고 → 목표로 보낸다(`@GestureState` 리셋과 맞물려 튀지 않게) |

⚠️ **비치는 정도는 이 앱에서 약하다** — 뒤에 지나가는 것이 카드(`#1D1B25`)와 바탕(`#131218`)이라 대비가 낮다. 유튜브는 벽지 위였다. 폰에서 사용자가 본다.

### 5-3. 튕기기 두 번째 판 — 지수 감속 모델 (09-21 17:5x)

사용자: *"유튜브는 미끄러지듯 튕겨지는데 너가 한 것은 너무민감하게 튕겨나가고 세게 튕겨나가고 미끄러지는 느낌이 없어. 좀 더 해봐. 동영상이라도 줄까?"*

**첫 판(`5d0f1ac`)이 왜 그랬나 — 둘:** ⓐ `predictedEndTranslation`은 스크롤 뷰의 「보통」 감속률 **0.998**(거리 = 속도 × 499)이라
1500pt/s 튕김이 **749pt** — 손잡이 범위(≈600pt)를 넘어 **어떤 튕김도 끝까지** 갔다. ⓑ 시간이 **고정 0.55초 스프링**이라 거리·속도와 무관 —
멀리 가도 짧게 끝나 「날아가 멈춤」이 됐다.

**둘째 판(`2b53a7a`):** 손가락 속도(`DragGesture.Value.velocity`)를 읽어 **거리 = v·r/(1−r) · 시간 = ln(stop/v)/ln r**(UIScrollView와 같은 꼴 · 값만 다르다).

| 값 | 고른 것 | 뜻 |
|---|---|---|
| 감속률 r | **0.995** | 1500pt/s → 299pt · 0.86초. (0.998 = 749pt · 0.99 = 149pt) |
| 멈춤 속도 | **0.02 pt/ms** | 이 아래로 떨어지면 멈춘 것 — 시간의 끝점 |
| 튕김 문턱 | **150 pt/s** | 이보다 느리게 놓으면 그 자리(「너무 민감」의 처방) |
| 곡선 | `timingCurve(0.1, 0.45, 0.3, 1.0)` | 지수 감속을 닮은 꼴 — 처음 기울기 ≈4.5(놓는 속도가 이어진다) · 끝은 0 |
| 경계 | 잘리면 시간도 그만큼 | 벽에 닿는 순간 멈춘다(튀지 않는다) |

⚠️ **셋 다 Claude가 고른 값 · 유튜브의 실제 곡선은 못 잼.** 사용자가 동영상을 주면 **프레임을 뽑아 손잡이 y를 시간축으로 재서**
r과 곡선을 맞춘다(계측 규칙 4 — 계산은 실측으로 닫는다). 그전까지는 「더 멀리/덜 멀리」 = r · 「더 오래/빨리 멈춤」 = 멈춤 속도, 값 하나씩이다.

### 5-4. 튕기기 셋째 판 — 사용자 동영상을 재서 맞췄다 (09-21 18:2x · 계측 규칙 4)

사용자가 화면 녹화를 줬다(`~/Downloads/ScreenRecording_09-21-2026 18-10-31_1.MP4` · 1206×2622 · 60fps · 5.1초 · **홈 화면 위의 그 손잡이** · 세게 한 번 아래로 · 약하게 한 번 위로).
ffmpeg가 없어 **AVFoundation으로 프레임을 읽는 Swift 스크립트**를 썼다(`native/tools/track-edge-handle.swift` — 화살표 `<`(밝은 회색 + 오른쪽 어두운 바탕)를 찾아 중심 y를 찍는다).
⚠️ 첫 검출기는 「회색 띠 240~340px」로 잡았더니 **하단 독을 손잡이로 읽었다** — 프레임을 눈으로 대 보고 화살표 기준으로 바꿨다(계측 규칙 8: 판정이 조건 덕분인가).
궤적 원본 = `docs/native/measurements/2026-09-21-edge-handle-flick-track.txt`(ms · 중심 px · 화살표 위·아래).

| 튕김 | 놓는 속도 | 간 거리 | **거리/속도** | **멈추기까지** | 지수 감속 최적 r · RMS | **임계감쇠 스프링 ω · RMS** |
|---|---|---|---|---|---|---|
| 세게(아래) | 2229 pt/s | 300 pt | **0.135 s** | **342 ms** | 0.9910 · 13.6 pt | **13.75/s · 8.8 pt** |
| 약하게(위) | 1337 pt/s | 200 pt | **0.150 s** | **358 ms** | 0.9915 · 9.5 pt | **13.50/s · 5.6 pt** |

**갈린 것 셋:** ① 세기와 무관하게 **~350ms에 멈춘다** — 지수 감속이면 빠른 쪽이 더 오래 간다 ② 프레임마다 속도 비율이 **0.97 → 0.8로 작아진다** — 지수 감속이면 일정 ③ 스프링 쪽 오차가 작다.
→ **유튜브 손잡이 = 놓은 자리 + 속도 × 0.14s를 목표로, 초기 속도를 이어받는 임계감쇠 스프링(ω ≈ 13.6/s).** 정규화 초기속도 v/d = 1/0.14 ≈ 7.1/s로 늘 같다 — 그래서 늘 같은 시간에 멈춘다.

| 값 | 셋째 판(`interpolatingSpring`) | 둘째 판 `2b53a7a`(지수 · Claude 추정) | 첫 판 `5d0f1ac` |
|---|---|---|---|
| 도착점 | 놓은 자리 + v × **0.14s** | v × 0.199s(r 0.995) — **1.4배 멀었다** | v × 0.499s(r 0.998) — 끝까지 |
| 시간 | **늘 ~350ms**(ω 13.6 · stiffness 185 · damping 27.2) | ln(stop/v)/ln r — 빠르면 0.86s | 0.55s 고정 |
| 경계 | 초기속도를 ω·0.95까지 — 넘치지 않고 벽에 도착 | 시간만 잘림 | — |
| 문턱 | 60 pt/s(8pt 미만은 안 움직인다) | 150 | 없음 |

★ **둘째 판의 값 셋을 「Claude가 고른 값 · 못 잼」으로 적어 둔 것이 이번에 값을 했다** — 동영상 하나로 셋 다 갈렸다(계측 규칙 7).

### 5-5. 인터넷에서 찾은 것 — 그 손잡이는 유튜브가 아니라 **iOS 시스템 PiP 탭**이다 (09-21 18:3x · 사용자: *"인터넷 어딘가에 검색해볼 수 없을까?"*)

- **정체:** 스크린샷·동영상의 손잡이는 **iOS 시스템 「화면 속 화면(PiP)」 창을 가장자리로 밀어 숨겼을 때 나오는 시스템 탭**이다.
  유튜브는 `AVPictureInPictureController`(시스템 PiP)를 쓰는 것뿐이라 **유튜브 문서에는 물리값이 없고, Apple도 이 값을 공개하지 않는다.**
  (YouTube 도움말: *"drag the window to the edge to hide it and then pull the tab to bring it back"* · iOS 사용 안내 여럿이 같은 동작을 시스템 기능으로 설명.)
- **Apple이 공개한 설계 원칙**(WWDC 2018 「Designing Fluid Interfaces」 §803 + 발표자의 샘플 코드 `fluid-interfaces/Pip.swift`):
  ```swift
  let decelerationRate = UIScrollView.DecelerationRate.normal.rawValue          // 0.998
  let projectedPosition = center + project(initialVelocity: velocity, decelerationRate:)   // (v/1000)·r/(1−r)
  let relativeInitialVelocity = relativeVelocity(forVelocity: velocity, from: center, to: target)   // v / 거리
  UISpringTimingParameters(damping: 1, response: 0.4, initialVelocity: relativeInitialVelocity)
  ```
  발표: *"We recommend starting with 100% damping, or no overshoot"* · 모멘텀이 있는 제스처에만 약간의 overshoot.
- **우리 실측(§5-4)과의 대조:**

  | 항목 | Apple 샘플(공개) | 시스템 PiP 탭(우리 동영상 실측) | 지금 빌드 `1b0fa24` |
  |---|---|---|---|
  | 꼴 | 투사 → **임계감쇠 스프링 + 상대 초기속도** | 같은 꼴(스프링 RMS < 지수 RMS · 시간이 세기 무관) | 같은 꼴(`interpolatingSpring` · mass 1 · stiffness ω² · damping 2ω = `UISpringTimingParameters(damping: 1, response:)`와 같은 식) |
  | response | **0.4 s** | **0.457 · 0.465 s**(2π/ω · ω 13.75 · 13.50) | 0.46 s(ω 13.6) |
  | 투사 배율 | 0.998 → **0.499 s**(샘플은 그 뒤 **가까운 모서리로 스냅**하므로 배율이 커도 된다) | **0.135 · 0.150 s**(≈ 감속률 0.993) | 0.14 s |
  | 초기속도 | v / 거리(상대) | v / 거리 = 1/0.14 ≈ 7.1/s(늘 같다) | 같음 · 벽에서는 ω·0.95까지 |

  ★ **모델은 Apple 공개 원칙과 같고, 값 둘(response · 투사 배율)은 우리 동영상 실측이 유일한 근거다.** 시스템 탭은 스냅점 없이 한 축으로만 움직이므로
  샘플의 0.499s가 아니라 짧은 배율을 쓰는 것이 자연스럽다(샘플은 어디로 던져도 모서리에 붙는다). **바꿀 것 없음** — 폰 판정만 남는다.
- **출처:** Apple WWDC18 803 대본(asciiwwdc.com/2018/sessions/803) · github.com/nathangitter/fluid-interfaces `Pip.swift`(2026-09-21 `gh api`로 읽음) ·
  YouTube 도움말 support.google.com/youtube/answer/7552722 · UIScrollView 감속 = ms마다 r배(Lobanov, 「Deceleration mechanics of UIScrollView」 — 본문은 403이라 검색 요약만).

### 5-6. 「같게」가 아니라 「느리게」 — 사용자가 정하는 숫자 셋 (09-21 18:4x)

사용자: *"그럼 내 말대로 조금만 수정해줘. 느낌이 너무 달라. 튕기는 순간 정지할 위치는 정해지는 것으로 예상돼. 따라서 갈 거리가 정해져다는 의미지.
지금은 이동하는 중간 과정이 너무 짧게 그려져서 마치 순간이동하는 것과 비슷해. 이동이 너무 빠르다는 거야. 그러니 미끄러지듯 가는 느낌을 위하여
속도를 약간 늦춰주고 목적지에 도달할수록 속도라 느려지면서 부드럽게 정리하게 해줘. 일단 한 번 해봐. 필요한 숫자를 알려주면 내가 숫자를 정해줄께."*

⚠️ `1b0fa24`는 **시스템 PiP 탭과 같은 값**이었다(§5-4·§5-5). 사용자가 원한 것은 **같은 것이 아니라 더 느리고 부드러운 것** — 실측은 근거로 남고 값은 사용자가 정한다.
「튕기는 순간 위치가 정해진다 · 거리는 정해진다」는 지금 모델 그대로다(투사). 바꾼 것은 **가는 시간과 시작의 튐**.

| 숫자(코드 상수) | 뜻 | 실측(시스템 탭) | `af4c5ef` | 올리면 / 내리면 |
|---|---|---|---|---|
| `projection` | **얼마나 멀리** — 도착점 = 놓은 자리 + 속도 × 이 값(초) | 0.14 | **0.14** | 멀리 / 가까이 |
| `glideResponse` | **얼마나 오래** — 이 시간(초)쯤에 멈춘다. 임계감쇠라 끝은 늘 스며든다 | 0.46 | **0.80** | 느리게·오래 / 빠르게·짧게 |
| `velocityCarry` | **시작이 얼마나 튀나** — 손가락 속도 중 이어받는 비율(0 = 정지에서 출발 · 1 = 그대로) | 1.0 | **0.5** | 처음에 확 나감 / 처음부터 완만 |

**사용자가 말로 고를 수 있게:** 「더 천천히」 = `glideResponse` 위로 · 「처음이 너무 튄다/너무 굼뜨다」 = `velocityCarry` 아래/위 · 「너무 멀리/가까이」 = `projection`.

### 5-7. 「휙 점프」 — 값이 아니라 배선이었다(추정 · 폰 판정 대기) (09-21 18:5x)

사용자: *"우선은 가는 과정의 Animation이 부드럽게 표현되도록 해줘. 지금은 이동하는 과정이 너무 너무 빠르게 표현되어서 휙 점프해가는 느낌이야."*

★ **가름:** `5d0f1ac`(0.55s 스프링) → `2b53a7a`(0.86s 지수) → `1b0fa24`(0.35s) → `af4c5ef`(0.8s) — **네 값이 다 「너무 빠르다·순간이동」**이었다.
값이 네 배 차이인데 느낌이 같으면 **값이 안 먹은 것**이다(계측 규칙 8: 이 판정은 조건 덕분인가 → 애니메이션이 돌긴 했나).

**짚인 원인 둘(추정):** ① 놓는 순간 **한 프레임에 셋이 겹쳤다** — `@GestureState dragY`가 0으로 리셋 · `topOffset`을 놓인 자리로 즉시 쓰기 ·
`withAnimation`으로 목표 쓰기. SwiftUI가 한 갱신으로 합치면 **어느 트랜잭션이 실리는지 보장이 없다**(애니메이션이 떨어진다).
② `@AppStorage`에 직접 `withAnimation`을 걸었다 — 저장 프로퍼티라 트랜잭션이 안 실린다는 보고가 있다.

**고침(`a83e86f`):** 그리는 위치를 **`@State visualTop`**으로 갈라 놓고(저장값 `topOffset`은 값만), 놓는 프레임에는 놓인 자리로 **고정만**,
목표로의 `withAnimation`은 **`DispatchQueue.main.async`로 다음 턴** — 별도 갱신이라 트랜잭션이 온전히 실린다.
⚠️ **시뮬레이터에서 못 확인했다** — 끌기·튕기기는 화면을 눌러야 한다(항시 규칙 7). 폰 판정으로 닫는다.
⚠️ ✅가 나오면 **§5-3~5-6의 「감」 판정은 전부 애니메이션 없는 상태에서 내려진 것**이다 — 값 셋은 그 뒤 다시 듣는다.

### 5-8. 사용자가 정한 속도 곡선 — 앞 절반 일정 · 뒤 절반 계단 감속 (09-21 19:0x)

✅ `a83e86f` 판정: *"과정이 보인다."* — **§5-7의 추정(배선)이 확정됐다.** 그 말에 이어:

> *"이제 버튼이 이동하는 속도를 30% 정도 올려주고, 이동거리 50% 지금까지는 그대로 유지 후 50% 이후 부터 남은 거리가 5% 줄어들 때 마다
> 속도를 5%씩 줄여줘. 그래서 도착할 때 속도가 0이 되도록."*

**스프링(§5-4~5-7)을 걷고 거리-시각 표를 따라가는 `CustomAnimation`(`StepGlide`)으로** 바꿨다 — 표의 점 사이는 직선(일정 속도)이라 구간마다 속도가 계단으로 바뀐다.

| 숫자(코드 상수) | 뜻 | `aadf8b5` |
|---|---|---|
| `projection` | 얼마나 멀리 — 도착점 = 놓은 자리 + 속도 × 이 값(초) | 0.14 |
| `baseDuration` | 「지금 속도」의 기준 = §5-6의 0.8초(거리를 이 시간에 가는 속도) | 0.80 |
| `speedBoost` | 앞 절반의 일정 속도 = 기준 × 이 값 | ~~1.3~~ → **2.6**(`a8edc6b` · 19:1x 사용자: *"좋아. 일단 지금보다 2배 속도로 맞춰줘. 전체적으로 느려. 이정도면 될 것 같아!"* — 곡선 꼴은 통과 · 속도만 2배) |
| `cruiseFraction` | 일정 속도로 가는 거리 비율 | 0.5 |
| `stepFraction` | 계단 하나의 거리 비율 | 0.05 |
| `decrementPerStep` | 계단마다 줄이는 속도(앞 절반 속도 대비) | **0.10** |

⚠️ **해석 하나 — 「5%씩」을 「10%씩」으로.** 뒤 절반은 5%씩 열 계단이라 5%씩 줄이면 도착 때 **속도 50%가 남는다**(0이 안 된다).
사용자가 함께 말한 「도착할 때 0」을 우선해 계단마다 1/계단수 = 10%씩 줄였다(마지막 계단은 10% 속도로 5%를 가고 멈춘다). **다른 뜻이면 상수 하나.**

**시간표**(거리와 무관 · 비율만): 앞 절반 0.31s → 계단 끝 0.34 · 0.37 · 0.41 · 0.46 · 0.51 · 0.57 · 0.65 · 0.75 · 0.90 · **1.21s**. 마지막 계단(5% 거리 · 10% 속도)이 0.31s.
→ **2.6에서는 모두 절반**: 앞 절반 0.15s · 전체 **0.60s** · 마지막 계단 0.15s.
⚠️ 「속도를 5%씩 줄인다」는 연속으로 하면(속도 ∝ 남은 거리) **영원히 도착하지 않는다**(지수 접근) — 계단으로 두어야 끝이 있다. 그래서 계단 그대로 구현했다.

### 5-9. 두 구간으로 — 앞 90% 빠르게 · 마지막 10% 절반 속도 (09-21 19:2x)

사용자: *"음.. 위치에 따라 속도를 조절하지 말고, 그냥 전체 거리의 90%는 빠르게 가고, 마지막 10% 남은 거리는 2분의 1 속도로 가게 해보자.."*

**§5-8의 계단식(앞 절반 일정 · 5%마다 10%씩 · 도착 0)은 걷었다** — ⛔ 「안 됐다」가 아니라 **「뜻이 아니다」**(기록 규칙 9 ② · 되살리지 말 것 · 코드는 `aadf8b5`·`a8edc6b`에).
`StepGlide`(거리-시각 표)는 그대로 쓰고 **표를 두 점으로** 줄였다.

| 숫자(코드 상수) | 뜻 | `2936e7b` |
|---|---|---|
| `projection` | 얼마나 멀리 — 도착점 = 놓은 자리 + 속도 × 이 값(초) | 0.14 |
| `baseDuration` · `speedBoost` | 빠른 속도 = 거리 ÷ 0.8초 × 배율 | 0.80 · ~~2.6~~ → **3.9**(`51ade41` · 19:3x 사용자: *"지금보다 50% 더 빠르게 하자."*) |
| `fastFraction` | 빠른 속도로 가는 거리 비율 | **0.9** |
| `tailSpeedRatio` | 남은 거리의 속도(빠른 속도 대비) | **0.5** |

시간(거리 무관): 앞 90% **0.28s** + 꼬리 10% **0.06s** = **0.34s**. → **3.9에서는 앞 90% 0.18s + 꼬리 0.04s = 0.23s.** ⚠️ 꼬리가 짧아(60ms · 4프레임쯤) 「느려짐」이 눈에 잘 안 들 수 있다 — 그러면 `tailSpeedRatio`를 내리거나 `fastFraction`을 내린다.

### 5-10. 「드드드득」 · 「90% 구간 속도가 다르다」 — A/B(재질 걷기) + 프레임 계측 (09-21 19:4x)

사용자: *"코드 한 번 체크해줄래? 2개 이상한 느낌이 있어. 1번 이상한 점은 90% 거리가 같은 속도 같지 않다는거야. 50% 정도 구간과 그 후가 속도가 다르게 느껴져.
… 두번째 이상한 점은, 애니메이션이 부드럽지 않아. 드드드득~ 하는 느낌 또는 FPS가 낮은 느낌이야. … 유튜브는 부드럽게 움직인단말이야."* → *"진행해줘."*

**코드 확인:** `StepGlide`는 거리-시각 표의 두 점(0.9@0.18s · 1.0@0.23s)을 **시간에 직선**으로 잇는다 — 0~90% 안에서 속도를 바꾸는 코드는 없다. 애니메이션 중 다른 쓰기도 없다.
→ ①은 코드가 아니라 **프레임이 빠졌다 따라잡는 것**으로 보이고, ②와 뿌리가 같다(**추정**).
**짚인 원인:** 바탕 `.regularMaterial` — 움직이는 재질은 **매 프레임 밑 화면을 다시 블러**한다. 120Hz에서 0.23초 = 27프레임인데 몇 개만 빠져도 「낮은 FPS」로 보인다.

**이 빌드(`3c550d5`)가 하는 것 둘:**
1. **A/B** — `useMaterial = false`로 재질을 걷고 잰 그라데이션만 불투명으로. 직전 빌드(재질)와 같은 튕김을 비교한다.
2. **계측** — `GlideLog`: `StepGlide.animate`가 불릴 때마다 (시각, 진행률)을 메모리에 모아 끝날 때 `Application Support/SecondBrain/edge-handle/glide.log`에 쓴다.
   가져오기 = `CLAUDE.md` 빌드 ⓒ(`devicectl device copy from … --source "Library/Application Support/SecondBrain/edge-handle"`). 읽는 것: **프레임 간격**(120Hz면 8.3ms · 빠진 프레임은 16.7·25ms로 보인다) · **진행률 vs 시각의 직선성**(0~90%에서 기울기가 일정한가).

⚠️ **재질을 걷은 것은 A/B다** — 비침(§5-2)을 포기한 것이 아니다. 부드러워지면 **비침을 포기할지 · 덜 비싼 재질(`.ultraThinMaterial`)을 시험할지 사용자가 고른다.**

### 5-11. A/B·계측 결과 — 재질도, 애니메이션 값도 아니다 (09-21 19:0x)

사용자(`3c550d5` · 불투명 바탕): *"아니, 드드득하는 느낌이 그대로 있어. 유튜브가 보여주는 모습과는 현격히 차이가 나."* → **재질은 원인이 아니다.**

`glide.log`(19:01 가져옴 · 튕김 13번 · 원본 = `measurements/2026-09-21-edge-handle-glide-log.txt`):

| 잰 것 | 값(13번 전부 같다) | 뜻 |
|---|---|---|
| 애니메이션 평가 간격 | **8.3ms**(고유 시각 27개 / 0.226s) · 첫 간격만 16.7ms | **120Hz로 평가됐다** · 시작에 한 프레임 빠짐 |
| 0~90% 진행률 기울기 | 4.89/s · 표준편차 **0.03** | **완전히 일정** — 「50%에서 속도가 바뀐다」는 값에는 없다 |
| 같은 시각의 호출 | 5.2번 · f 전부 같음 · 시각 되돌아감 0 | 애니메이션이 **겹쳐 쌓이지 않았다**(속성마다 불린 것) |

★ **가름:** 값은 120Hz·직선인데 눈에는 드드득 → **원인은 계산이 아니라 「그려서 내보내는 쪽」**이다. 이 로그는 SwiftUI가 프레임을 *평가한* 시각이고,
화면에 *실제로 나온* 프레임은 못 본다(평가는 120Hz로 돌고 표시가 늦거나 빠질 수 있다). **다음 계측 = 우리 앱을 화면 녹화**해 `track-edge-handle.swift`로
프레임마다 위치를 읽는다(유튜브 동영상과 **같은 잣대**). 60fps 녹화라 **두 프레임 이상 멈춘 것**은 보인다.

⚠️ 추정 후보(아직 못 잼): ① 튕김 시작 시 `@AppStorage` 쓰기 둘이 `InboxView` 본문(목록 전체) 재평가를 부른다 — 시작 프레임 하나 빠진 것과 맞는다 ·
② SwiftUI 애니메이션은 **메인 스레드가 프레임마다 값을 밀어 넣는다**(시스템 PiP 탭은 렌더 서버가 돈다) — 메인이 바쁘면 표시가 빠진다.
③ 1330pt/s의 일정 속도(11pt/프레임)와 90%에서의 급한 속도 변화가 홀드형 OLED에서 떨림으로 보일 가능성.

### 5-12. 화면 녹화가 갈랐다 — 「드드득」 = 놓는 순간 두 프레임 정지 + 점프 · 속도 급변 (09-21 19:1x)

사용자가 **우리 앱과 유튜브 탭을 한 녹화에** 담았다(`~/Downloads/ScreenRecording_09-21-2026 19-07-25_1.MP4` · 60fps · 9.3초 · *"처음에 위에서 시작한 것이 유튜브야"*).
추적기를 **후보 전부**를 내보내는 판으로 고쳐(둘이 같은 꼴이라 번갈아 잡혔다) 연속성으로 갈라 읽었다. 원본 = `measurements/2026-09-21-edge-handle-vs-youtube-track.txt`.

**프레임(16.7ms)별 이동량(pt):**

| | 놓은 뒤 프레임별 이동 | 정지 프레임 | 시간 |
|---|---|---|---|
| 유튜브 아래로 | **48 47 44 41 37 34 30 26 23 20 17 14 12 10 8 7 5 4 3 2 1 …** | **0** | 500ms |
| 유튜브 위로 | 47 47 46 43 40 37 33 30 26 23 20 17 14 12 10 8 7 5 4 3 2 1 … | 0 | 500ms |
| 우리 ①(`3c550d5`) | 32 43 25 **0 0** 38 **25 25 25 25 25 25 25 25 25** 20 13 12 | **2** | 300ms |
| 우리 ② | 26 37 44 44 42 23 **0 0** 24 16 16 16 16 16 16 16 16 16 12 8 8 | 2 | 350ms |
| 우리 ③ | 33 17 **0 0** 25 17 17 17 17 17 17 17 17 17 13 8 8 | 2 | 283ms |

★ **가름 둘:**
1. **놓는 순간 두 프레임(33ms) 정지 → 점프.** 배선: 애니메이션을 `DispatchQueue.main.async`로 다음 턴에 미뤘고(§5-7의 처방), 같은 순간 `@AppStorage` 쓰기가 `InboxView` 본문(목록 전체)을 재평가했다. §5-11의 「첫 간격 16.7ms」가 이것이었다.
2. **속도 급변 셋** — 손가락 43 → 글라이드 25(출발) · 90%에서 절반 · 끝에서 12 → 0(급정지). 유튜브는 **손가락 속도로 출발해 매 프레임 조금씩 줄어 0**.

**유튜브 곡선을 모델에 대 봤다**(놓은 뒤 437pt · 2880pt/s · 거리/속도 0.152s):

| 모델 | 프레임별 예측(앞 16) | RMS |
|---|---|---|
| 임계감쇠 스프링 **ω 13.6**(response 0.46s) · 초기속도 = 손가락 속도 | 48 46 44 40 36 32 28 25 21 18 16 13 11 10 8 7 | **1.1 pt/프레임** |
| ω 12.0 | 47 44 41 38 34 30 27 24 21 18 16 14 12 10 9 8 | 2.0 |
| ω 15.7(Apple 샘플 0.4s) | 50 50 47 43 39 34 29 25 21 18 15 12 10 8 7 6 | 1.6 |

→ **§5-4·§5-5의 스프링이 맞았다.** `1b0fa24`가 「느낌이 다르다」로 판정된 것은 **애니메이션이 안 걸리던 배선(§5-7) 아래**였다 — 값이 아니라 배선이 틀렸던 것.

**고침(`750a763`):**
- 배선 — `@GestureState dragY`를 걷고 **끌기도 `visualTop`에 직접** 쓴다(애니메이션 없음). 놓을 때 **`withAnimation` 쓰기 하나**로 지금 값 → 목표(미루지 않는다). 저장 `topOffset`은 **`completion`에서** 한 번.
- 꼴 — 기본 = **스프링 ω 13.6 · 초기속도 = 손가락 속도(거리 대비 정규화 · 경계에서 ω·0.95 캡) · 도착점 = 놓은 자리 + 속도 × 0.14s.** 두 구간 꼴(§5-9)은 `glideStyle = .twoStep`으로 남겼다(사용자가 정한 것 · 되살릴 수 있다).
- 바탕은 **아직 불투명**(§5-10 A/B 그대로) — 재질은 원인이 아니므로 되살릴지는 사용자가 고른다.

★ **계측이 세 겹으로 좁혔다:** 재질 A/B(아니다) → `glide.log`(계산은 맞다) → **화면 녹화 프레임 표**(표시가 멈췄다). 느낌으로는 어느 것도 갈리지 않았다.

### 5-13. 둘째 녹화 — 꼬리는 같아졌고 놓는 순간만 남았다 → 끌기도 스프링으로 (09-21 19:2x)

사용자 녹화(`~/Downloads/ScreenRecording_09-21-2026 19-15-01_1.MP4` · *"윗쪽에서 시작하는 것이 유튜브 … 아래에서 시작하는 것이 우리것"*) · 원본 = `measurements/2026-09-21-edge-handle-vs-youtube-track-2.txt`.

| | 프레임별 이동(pt) | 놓은 뒤 비율 | 정지 |
|---|---|---|---|
| 유튜브 ①~④ | `7 22 20 24 24 19 13 17 26 31 34 36 35 34 32 29 27 24 21 19 16 14 12 10 8 7 6 5 4 3 2` | 0.98 → 0.85 | 0 |
| 우리(`750a763`) ① | `-31 -34 **0 0** -27 -38 -22 -20 -18 -16 -14 -12 -10 -9 -7 -6 -5 -4 -4 -3 -3 -2` | 0.9 → 0.85 | 2 |
| 우리 ② | `39 25 25 24 23 **10 9** 17 16 14 13 12 10 9 8 7 6 5 4 3 3 2 2` | 0.95 → 0.87 | 0(절반 속도 둘) |
| 우리 ③ | `-23 -25 -29 -33 -32 **-15 -12** -24 -23 -20 -18 …` | | 0(절반 속도 둘) |
| 우리 ④ | `42 32 36 41 42 42 **0 0** 26 30 15 12 10 8 6 5 4 3 3 2 1 2` | | 2 |

✅ **미끄러지는 꼬리는 유튜브와 같다**(§5-12의 스프링이 맞았다). ⛔ **놓는 순간**에 한두 프레임이 **비거나 절반**이다 — 손가락이 떨어진 뒤 **터치 종료가 도착하기까지의 틈.**
★ 유튜브 탭의 손가락 구간을 보면 `7 22 20 24 24 19 13 17 26 31 34…`로 **1:1이 아니라 부드럽게 가속한다** — 탭이 손가락을 **스프링으로 따라가** 그 틈을 관성으로 넘는다.

**고침(`a2dd1ec`):** 끌기 `onChanged`를 `withAnimation(.interactiveSpring(response: 0.12, dampingFraction: 0.86))`으로(1:1 → 살짝 뒤따름 · 2000pt/s에서 약 40pt).
놓을 때 `.spring(response: 2π/13.6, dampingFraction: 1)`로 **목표만 바꾼다** — SwiftUI 스프링은 끊기면 속도를 보존해 다시 겨누므로 이음새가 없다.
`interpolatingSpring(initialVelocity:)`는 걷었다(끌기 스프링이 속도를 준다).
⚠️ **끌 때 뒤따르는 것이 보인다** — 유튜브도 그렇다. 거슬리면 `trackingSpring`의 response를 내린다(0.12 → 0.08이면 약 27pt).

### 5-14. ✅ 움직임 완료 · 세게 던지면 끝까지 · 유튜브 탭은 불투명이다 (09-21 19:3x)

사용자(`a2dd1ec`): *"1. 느낌이 좋아. 그러므로 움직임 자체에 대해서는 완료됨!!! 2. 유튜브 버튼은 세게 던지면 갈 수 있는 범위 내에서 끝까지 가지만 우리는 세게 던져도
다 가지 않아. 세게 던지면 범위 내에서 끝까지 가게하자. 3. 유튜브도 바탕 화면이 비치는 재질이 아닌 것 같아. 다시 분석해볼래? 내 느낌이 맞으면 재질 바꾸지마."*

**1. ✅ 움직임 = 완료.** 닫힌 것: 배선(§5-7·§5-12) · 스프링 ω 13.6(§5-4·§5-12) · 끌기 스프링(§5-13). 5-3~5-11의 값 판정들은 전부 「애니메이션이 안 걸리던 배선」 아래였다.

**2. 세게 던지면 끝까지(`f29294a`):**

| 놓는 속도 | 도착점 |
|---|---|
| ≤ `softFlickSpeed` 1200 pt/s | 놓은 자리 + 속도 × 0.14s(그대로) |
| ≥ `hardFlickSpeed` 2000 pt/s | **그 방향의 벽**(제목 아래 / 탭바 위) |
| 사이 | 둘을 비례해 잇는다(문턱에서 갑자기 바뀌지 않게) |

실측 근거: 약한 튕김 1337 · 센 튕김 2229·2880 pt/s(§5-4·§5-12). 시간은 스프링이라 늘 ~0.46s — 벽까지 600pt면 평균 1300pt/s로 간다(유튜브 653pt/500ms와 같은 급).

**3. 유튜브 탭은 불투명이다 — 사용자 느낌이 맞다. 재질 바꾸지 않는다.**
첫 동영상(홈 화면 · 아이콘·위젯·벽지 위를 지난다)에서 탭이 지나간 **63자리**마다 「탭 안 바탕 패치」와 「같은 자리의 탭 없는 배경(첫 프레임)」을 찍었다(`native/tools/probe-edge-handle-fill.swift`):

| | 밝기 범위 | 표준편차 |
|---|---|---|
| 밑 배경(탭이 없을 때 그 자리) | **71 ~ 158** | 21.4 |
| 탭 안 바탕 | **74 ~ 75** | **0.2** |

배경↔탭 밝기 **상관 r = −0.02 · 기울기 0.00**(완전 투명 1 · 불투명 0). 비치는 재질이면 밝은 아이콘 위에서 탭도 밝아져야 하는데 **전혀 변하지 않는다.**
→ **불투명.** §5-2의 「비치는 재질」은 스크린샷 한 장의 위·아래 밝기 차를 재질로 읽은 **오판**이었다(그 차는 탭 자체의 그라데이션). §5-10의 A/B로 걷은 불투명 바탕을 **그대로 둔다** — `useMaterial = false`가 정답이다.
⚠️ 동영상 속 탭 색(`#5B4448`)은 스크린샷(`#393E3F`)과 다르다 — 화면 녹화의 색 공간·톤 매핑 차이(**추정**). 색 값은 스크린샷 실측(§5-1)을 따른다.

### 5-15. ✅ 전부 닫혔다 (09-21 19:3x)

사용자(`f29294a`): *"세게 던지면 끝까지 가는 것 확인했어. 나머지 판정도 다 통과."* — §6의 ⓐ~ⓖ · 세게 던지기 · 해시태그 ㉠㉡ 전부 ✅.
`GlideLog`(프레임 계측)는 걷었다(`276f5d8` · 화면 차이 없어 폰 미설치 · 폰 = `f29294a`).
**남은 것 = §4 미결 둘**(누르면 무엇 · 접근성 이름) — 둘 다 사용자가 정한다.

## 6. 판정 — 09-21 빌드에서 볼 것 *(✅ 19:3x 전부 통과 · §5-15)*

ⓐ 「새로운 기억」 오른쪽 가장자리에 손잡이 하나(펼침 꼴 없음) · ⓑ **눌러 위아래로 끌면 따라오나** · ⓒ **제목 영역 위로 못 올라가나** ·
ⓓ **탭바 아래로 못 내려가나** · ⓔ 좌우로 끌어도 아무 일 없나 · ⓕ 앱을 끝내고 켜도 옮긴 자리에 있나 · ⓖ 누르면 아무 일 없나.
