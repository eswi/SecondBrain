# 실기기 검증 체크리스트 — 되풀이(양력 반복)

> 설계 정본 = `recurrence-design.md`(§10 단계). 헤드리스로 못 보는 화면·동작만.
> 각 항목 `확인:` 칸에 날짜·기기·경로 적고 통과면 `- [ ]`→`- [x]`.
> Stage 4·5는 나중에 몰아서 — 여기 A절은 **Stage 1~3 중간 확인**(2026-08-02).

## A. Stage 1~3 (분류·설정·완료 분기)

- [ ] **A-1. 되풀이로 분류.** 새 항목 또는 기존 항목의 종류를 **되풀이**로 바꿀 수 있다. 앰버 색 + 순환 아이콘, 라벨 "되풀이".
      `확인: ____-__-__ · 기기 ______ · 경로 ______`
- [ ] **A-2. 반복 설정 카드 + 꺼두기 배너.** 되풀이 상세에 **'시간 설정' 아래** '반복 설정' 카드 — 반복 주기(매일/매주/매년)·자동 완성(없음/당일 정오/당일 지나면)·꺼두기. 저장 후 재열람 시 유지.
      **꺼두기 ON → 상세 상단에 "되풀이 꺼둠" 배너**가 바로 보인다.
      `확인: ____-__-__ · 기기 ______ · 경로 ______`
- [ ] **A-3. ★ 이번 것 했어요 → 안 사라짐.** 되풀이 상세에서 **[이번 것 했어요]** → 항목이 **살아있는 목록에서 안 사라지고** "**오늘 완료됨 ✓**"으로 바뀐다. (핵심 — status=done이 아니라 lastDone만.)
      `확인: ____-__-__ · 기기 ______ · 경로 ______`
- [ ] **A-4. 취소 → 되돌아감.** "오늘 완료됨" 옆 **[취소]** → 다시 "[이번 것 했어요]"로. (잘못 눌러도 그 자리에서 되돌림.)
      `확인: ____-__-__ · 기기 ______ · 경로 ______`
- [ ] **A-5. ★ 회귀 — 기존 항목은 여전히 보관함.** 되풀이 **아닌** 항목(할 일 등)을 완료하면 **여전히 살아있는 목록에서 빠지고 보관함(완료된 기억)으로** 간다(Stage 0 그물이 지키는 것). 스와이프 라벨은 "완료"(되풀이는 "했어요").
      `확인: ____-__-__ · 기기 ______ · 경로 ______`

## B. Stage 4 + #3 (회차·놓침·게이트 시각화) — 2026-08-03

> 실사용 "매일 약"으로 보는 게 가장 좋음. 며칠 완료를 건너뛰면 놓침이 실데이터로 뜬다.

- [ ] **B-1. #3 게이트 시각화 — 지난 시각 계속 보임 ★.** 아침 8시 미리 알림 항목을 **오후에** 열어도 '지금 챙길 것'에 **여전히 있다**(시각 지났다고 안 빠짐).
      `확인: ____-__-__ · 기기 ______ · 경로 ______`
- [ ] **B-2. #3 — 미래 시각은 그 시각부터.** 미리 알림이 **오늘 늦은 시각**(예: 20시)인 항목은 낮엔 '지금 챙길 것'에 **없다가 그 시각에 나타난다**. (date-only 항목은 지금처럼 자정부터.)
      `확인: ____-__-__ · 기기 ______ · 경로 ______`
- [ ] **B-3. 완료 → 즉시 다음 회차로 넘어감 ★.** 되풀이 "이번 것 했어요"(또는 스와이프 "했어요") → **'지금 챙길 것'에서 바로 빠진다**(다음 회차는 미래라 게이트가 숨김). 화면이 그대로면 안 됨.
      `확인: ____-__-__ · 기기 ______ · 경로 ______`
- [ ] **B-4. 다음 회차 도래 시 다시 나타남.** 다음 날(또는 다음 주기) 그 시각이 되면 다시 '지금 챙길 것'에 나타난다.
      `확인: ____-__-__ · 기기 ______ · 경로 ______`
- [ ] **B-5. 놓침 = 계속 보이고 "N일 놓침" ★.** 며칠 완료를 건너뛰면 항목이 **계속 '지금 챙길 것'에 남고**(쌓임), 상세 상단 배너·목록 칩에 **"N일 놓침"**이 뜬다. (자동 완성 "없음"인 약에서.)
      `확인: ____-__-__ · 기기 ______ · 경로 ______`
- [ ] **B-6. 자동 완성.** 자동 완성 "당일 지나면"/"당일 정오"로 둔 되풀이는 그 시점이 지나면(앱 열 때) **저절로 다음 회차로 넘어가 안 쌓인다**(습관·생일 케이스).
      `확인: ____-__-__ · 기기 ______ · 경로 ______`
- [ ] **B-7. #4 — 목록에서 오늘 완료가 보인다.** 완료한 되풀이가 '살아있는 기억'으로 가면 **"오늘 완료" 칩**이 목록에 보인다(상세 안 열어도).
      `확인: ____-__-__ · 기기 ______ · 경로 ______`

## C. 앵커 전환 (마감=회차 시각, 미리 알림=게시 시작/lead) — 2026-08-03

> **모델 정정:** 마감 = 회차 앵커(회차 시각), 미리 알림 = 게시 시작(lead). 완료 시 둘 다 같은 간격 전진(lead 보존).
> 마감만 있어도 **마감 시각부터** 보임(게이트 시각 인지). B절의 "앵커=미리 알림" 서술은 이 C절로 대체.

- [ ] **C-1. 두 칸 다 보인다.** 되풀이 상세 "시간 설정"에 **"회차 시각"(마감) + "미리 알림"** 두 칸이 있다(일반 항목과 동일).
      `확인: ____-__-__ · 기기 ______ · 경로 ______`
- [ ] **C-2. ★ 옛 되풀이 3개(마감만·lead 0)가 실제로 돈다.** E036A094·90D927B2·01781308(전부 마감에 시각, 미리 알림 없음):
      **마감 시각부터 '지금 챙길 것'에 보이고** → "이번 것 했어요" → **마감만 다음날로 전진(미리 알림은 계속 비어 있음)** → 즉시 빠짐. (헤드리스로 고정: `testCompletionAdvance_noResurface_onlyDueAdvances`.)
      `확인: ____-__-__ · 기기 ______ · 경로 ______`
- [ ] **C-3. ★ lead 보존 — 8시 마감 + 7시 미리 알림.** 새 되풀이를 마감=오늘 08:00, 미리 알림=오늘 07:00으로 →
      **7시부터 보이고**, 완료하면 **마감 08-04·미리 알림 08-04 둘 다 다음날로**(lead 1시간 유지) → 빠짐.
      `확인: ____-__-__ · 기기 ______ · 경로 ______`
- [ ] **C-4. 규칙 1(시각).** 미리 알림을 마감보다 **늦은 시각**으로 잡으려 하면 막힌다. **미리 알림 = 마감(정각)은 허용**(실질 lead 0). date-only 미리 알림은 여전히 하루 전.
      `확인: ____-__-__ · 기기 ______ · 경로 ______`

> **⚠️ 알림 주의(Stage 5 미구현):** 7시 미리 알림 + 8시 마감 약은 지금 **7시에만 알림, 8시엔 안 옴**(§3-A lead-time 구멍). C절 화면·회차 확인엔 영향 없음 — 알림 시각만 그렇다는 것 인지.
> **01781308(마감 00:10) = 경계 시험용(자정 근처).** 날짜 경계에 걸려 헷갈릴 수 있음 — 확인용으로만 두고, 정리해도 무방(그만두기=삭제, 값은 안 지워짐).

## D. #4 완료 표시 회차 기준 통일 (`doneThisCycle`) + #3 색 — 2026-08-03

> **고친 것:** 칩·배너·완료버튼이 게이트와 **같은 마감 앵커 기준**을 본다. `마감 > now`=이번 회차 닫힘.
> 옛 `doneToday`(날짜 기준)가 게이트(시각 기준)와 갈려 낸 "완료+지남 동시" 모순을 없앴다.
> **폰에 09:13 빌드 설치됨**(`kr.teri.secondbrain`). 헤드리스: Core 167 통과 + iOS 빌드 OK.

- [x] **D-1. ★ 옛 모순 항목(`복약: 놓치는 약 먹기`) 해소.** 지금은 마감 06:00(과거)+lastDone 00:11이라 **"지남"만** 뜨고 **"오늘 완료" 칩은 안 뜬다**(모순 사라짐). → **"이번 것 했어요" 눌러** 완료 → **마감 내일 06:00로 전진** → '지금 챙길 것'에서 빠져 **'살아있는 기억'으로** 가고, 거기서 **"완료" 칩**이 뜬다. (신 빌드 재완료가 실제로 전진하는지도 이걸로 같이 확정.)
      `확인: 2026-08-04 · iPhone 16 Pro · 새로운 기억 탭 '지금 챙길 것' → 복약 상세 "이번 것 했어요" → 살아있는 기억 탭 "완료" 칩` — **통과. "오늘 완료+지남 동시" 모순 사라짐 확인.**
- [x] **D-2. 상세 배너 문구.** 완료 후 상세에서 **"이번 회차 완료됨 ✓" + [취소]**. (옛 "오늘 완료됨"에서 바뀜 — 매주·매년도 맞게.) [취소] 누르면 되돌아가 다시 "이번 것 했어요".
      `확인: 2026-08-04 · iPhone 16 Pro · 복약 상세 → 완료행 배너` — **통과.**
- [x] **D-3. 이른 완료 under-claim** — ⚠️ **"사소"가 아니었다.** 마감 시각 **전에** 완료하면 마감은 다음날로 전진(목록서 빠짐)하되 **"완료" 칩·배너가 안 뜬다**(거짓 완료를 피한 의도적 선택). 항목이 빠지는 것까지는 의도대로지만, **안 뜨는 것의 대가가 화장품이 아니다**(아래 부작용).
      `확인: 2026-08-05 · iPhone 16 Pro(00:37 빌드) · E-4 항목 재사용(미리 알림=과거·마감=미래) → '지금 챙길 것'에서 마감 전 "이번 것 했어요" → 상세 닫고 다시 열기(길 A)` — **통과. 예측 3개 전부 적중.**
      ① 마감·미리 알림 **둘 다 하루 전진**(lead 보존) ② **즉시 '지금 챙길 것'에서 빠져 '살아있는 기억'으로** ③ **완료 표시 안 뜸** — 다시 열었을 때 **`[이번 것 했어요]` 버튼이 돌아와 있음**을 확인(= 판정이 "안 했다"). D-2에서 이 자리에 "이번 회차 완료됨 ✓"이 뜨는 것을 이미 봤으므로 **대조가 성립한다.**
      **★ 이 항목에서 배운 것(방법론):** 첫 예측 3은 *"'살아있는 기억' 목록에 칩이 안 뜬다"* 로 적었는데 **확인 불가능한 예측**이었다 — 항목이 그 목록으로 옮겨간 뒤라 "칩이 뜰 자리인지"가 증명되지 않아, 부재의 원인이 under-claim인지 목록 탓인지 갈리지 않는다. **E-4에서 08-04에 걸린 혼입(대조군 없는 부재)과 같은 잘못을 예측 자체에 심은 것.** 부재를 예측할 때는 **"그 자리에 존재가 뜬 것을 본 적이 있는가"를 먼저 확보**해야 한다.
      **닫는 조건(당시):** 되풀이 항목을 **마감 시각 전에** "이번 것 했어요" → **항목이 '지금 챙길 것'에서 빠지면 통과**("완료" 칩은 안 떠도 정상 — under-claim이 의도). 빠지지 않으면 실패로 기록.
      **⚠️ 준비 조건(2026-08-05 코드 확인 중 드러남 — 이게 없으면 D-3은 아예 관찰 불가):** 항목이 **마감 전인데도 '지금 챙길 것'에 있어야** "빠지는 것"을 볼 수 있다. `ItemSchedule.isPublished`는 되풀이에서 **미리 알림이 있으면 그것만** 본다(`resurface <= now`, 마감은 안 봄). 따라서 **마감만 있는 항목으로는 확인할 수 없다** — 마감 전엔 게시 자체가 안 돼서 '살아있는 기억'에 있고, 완료해도 화면상 달라지는 게 없다.
      → **설정: 미리 알림 = 지금보다 과거(오늘), 마감 = 지금보다 미래(오늘).** 그러면 게시돼 '지금 챙길 것'에 있으면서 마감은 아직이다.
      **⚠️ 예측 3을 처음 적은 형태로는 확인할 수 없었다(사용자 지적, 2026-08-05).** "칩이 없다"는 관찰은 **그 자리가 칩이 뜰 수 있는 자리임이 먼저 증명돼야** 뜻이 있다. 항목이 '살아있는 기억'으로 갔는데 거기서 칩이 안 보이면, **under-claim 때문인지 그 목록이 원래 안 그리는 건지 구분되지 않는다.** (E-4의 혼입과 같은 종류의 잘못 — 대조군 없는 부재.)
      **재확인 두 길(둘 다 같은 자리에서 두 상태를 본다) — 길 A로 닫았다:**
      - **길 A ✅ 사용 — 상세를 닫고 다시 열기(직접·1분).** `DetailView`의 완료행은 D-2에서 **두 상태가 다 그려짐이 증명된 자리**다. 이른 완료 후 **다시 열면 `[이번 것 했어요]` 버튼이 돌아와** 있어야 한다(= 판정이 "안 했다"). ⚠️ **누른 직후엔 안 된다** — `cycleDoneLocal = true`를 낙관적으로 세우므로(`DetailView:537`) 그 화면에선 "이번 회차 완료됨"이 보인다. **닫고 다시 열어야** `doneThisCycle`로 재계산된다.
      - **길 B — 안 씀(예비). ✅2026-08-05 불필요해짐.** 같은 항목의 마감을 **1~2분 전(과거)** 으로 바꿔 '지금 챙길 것'에서 **정시 완료** → '살아있는 기억' 같은 줄에 **"완료" 칩이 뜬다**(`MemoryRow`는 칩을 그린다 — `InboxView:409`). **목록 칩** 자체의 대조가 필요해지면 이 길로. (지금은 D-1이 그 자리에 칩이 뜬 것을 이미 봤고 길 A가 배너로 닫았다.)
        → **E-6 기준선(2026-08-05)에서 '살아있는 기억' 목록에 "3일 놓침" 칩이 실제로 뜬 것을 눈으로 확인했다.** 그 목록이 칩을 그리는 자리임이 증명됐으므로 이 예비 경로는 더 만들 필요가 없다.
      **헤드리스 계측(2026-08-05, D-3 조건 그대로 재현):** 이른 완료 → `doneThisCycle = false`(칩·배너 안 뜸의 출처) / 정시 완료 → `true`(같은 자리에 뜸). **예측 3의 메커니즘은 확인됨** — 실기기는 "그 자리에 뜰 수 있음"만 보이면 된다.
      **⚠️ 부작용 — 화장품이 아니다 (실기기에서 전제 조건 확인됨, 정본 §5에 미결로 올림):** 배너가 안 뜨면 **`[이번 것 했어요]`가 돌아오고 `[취소]`가 사라진다.** **버튼이 돌아온 것은 길 A에서 눈으로 봤다** — 아래 둘의 전제가 실기기에서 성립한다는 뜻이다(재현 자체는 안 해봤다: 하면 실데이터 회차가 어긋난다). 그래서 (a) **다시 누르면 회차가 또 전진한다** — 계측: 마감 08-05 → 1차 완료 08-06 → **2차 완료 08-07**(하루를 건너뛴다. 8시 약을 7시 30분에 먹고 눌렀다가 상세를 다시 열어 또 누르면 **내일 약이 목록·알림에서 사라진다** — 이 설계의 출발점인 바로 그 경우다). (b) **이른 완료를 되돌릴 수 없다** — `취소`가 그 상태에선 안 그려진다.
      **예측(누르기 전에 박아 둔 것):** ① 마감·미리 알림이 **둘 다 하루 전진**(lead 보존) — `completionAdvance`의 `pivot = max(now, 마감)`이 이른 완료에서도 **정확히 1회차** 전진을 보장한다. ② 미리 알림이 미래가 되므로 **즉시 '지금 챙길 것'에서 빠진다.** ③ **"완료" 칩은 안 뜬다** — `doneThisCycle`이 `lastDone ≥ 직전 회차(= 오늘 마감 시각)`를 요구하는데 마감 전에 눌렀으니 `lastDone < 오늘 마감` → false. **이것이 under-claim이고 의도된 것이다**(자동 전진을 "완료"라 부르지 않기 위한 대가).
- [x] **D-4. #3 색 위계.** "지남"(오늘 마감 지남)은 **amber**(주의), "N일 놓침"은 **coral/빨강**(경고). 되풀이가 하루 종일 빨강이 아니라 amber인지 눈으로 확인. (코드 변경 없이 이미 그러함 — 회귀 안 했는지 확인만.)
      `확인: 2026-08-04 · iPhone 16 Pro · 새로운 기억 탭 '지금 챙길 것' 목록 칩 색` — **통과. 두 색(amber/coral) 구분 확인.**

> **미결(안 만듦, 정본 기록):** 자동완성(catch-up)으로 "넘어간" 회차는 지금 화면 어디에도 안 보인다(습관 blind spot). `recurrence-design.md` 미결 참조.

## E. 꺼두기가 실제로 멈추는지 — 2026-08-03

> **고친 것:** 상세 배너가 "되풀이 꺼둠 — 알림·되살아나기 멈춤"이라고 약속하는데 `isPaused`를 읽는 곳이
> DetailView(토글·배너)뿐이라 **셋 다 안 지켜졌다.** 새 기능이 아니라 **이미 있던 거짓 약속**을 고쳤다.
> 네 곳: 알림(`plan`)·게시(`isPublished`)·자동완성 catch-up(`catchUpChanges`)·**놓침 계산**(`missed`).
> 헤드리스: Core **179 통과** + iOS 빌드 OK. **폰에 2026-08-03 18:40 빌드 설치됨**(`kr.teri.secondbrain`, iPhone 16 Pro).
>
> **핵심 실패 양상 = 항목이 사라지는 것.** 꺼두기는 항목을 숨기는 게 아니라 **'지금 챙길 것'에서만 빼는 것**이고,
> 항목 자체는 시점 없는 쪽으로 옮겨가 총 개수가 보존돼야 한다(§7(c)). A-3과 같은 등급으로 볼 것.
>
> **⚠️ 이 절 최초 작성 때 화면 이름을 틀렸다(2026-08-03 실기기 확인에서 드러남).** "최근 들어온 것"이라는 섹션은
> **없다.** Core의 `recent`(시점 없음)를 App이 **확정 여부로 다시 쪼갠다**(`InboxModel.partition`):
> **확정 → '살아있는 기억' 탭 / 미확정 → '새 기억들' 섹션.** 꺼둔 항목이 어디로 갈지는 그 항목의 확정 여부가 정한다.
> Core 주석 3곳의 옛 이름도 같이 정정했다(같은 종류의 "화면에 없는 말" 문제).
>
> **놓침 숫자를 보는 곳 세 군데:** '지금 챙길 것' 카드 칩(`UpcomingCard`) · '새 기억들'/'살아있는 기억' 한 줄 칩
> (`MemoryRow` — 꺼둔 항목은 여기서 보인다) · **상세 상단 배너**(꺼둠 배너 바로 아래, 항상 보임).

- [x] **E-1. ★ 꺼두면 목록에서 빠지되 사라지지 않는다.** 되풀이 항목(예: `복약`) 상세 → "반복 설정" 카드의 **꺼두기 스위치 ON** → 저장. **'지금 챙길 것'에서 빠지는지** 확인 → **확정 항목이면 '살아있는 기억' 탭, 미확정이면 '새 기억들' 섹션에 있는지** 확인. **어느 쪽에도 없으면 즉시 실패로 적을 것.**
      `확인: 2026-08-03 · iPhone 16 Pro · 꺼두기 ON → '살아있는 기억' 탭` — **통과. '지금 챙길 것'에서 빠지고 '살아있는 기억'으로 들어감(확정 항목이라 그쪽이 맞음). 유실 없음.**
- [x] **E-2. 꺼둠 표시가 상단에 바로 뜬다.** 꺼둔 항목 상세를 열면 **스크롤 없이** "되풀이 꺼둠 — 알림·되살아나기 멈춤" 배너(coral)가 보인다. (꺼둔 걸 잊으면 약을 안 챙기게 된다.)
      `확인: 2026-08-03 · iPhone 16 Pro · 꺼둔 항목 상세 상단` — **통과. 붉은 배너 확인.**
- [x] **E-3. 회차가 안 넘어간다.** 자동 완성이 **있는**(정오/당일 지나면) 되풀이를 꺼둔 뒤 **하루 넘겨** 앱을 다시 연다 → 상세의 **회차 시각(마감)이 꺼둘 때 그대로**여야 한다. 날짜가 앞으로 가 있으면 실패.
      `확인: 2026-08-04 · iPhone 16 Pro · 꺼둔 항목(자동완성='당일 지나면') 상세의 회차 시각` — **통과. 어제 날짜·어제 시각 그대로 유지. 꺼두지 않았다면 오늘 열 때 반드시 오늘 날짜로 전진했을 조건이라, catch-up 가드가 실제로 먹은 증거.**
- [x] **E-4. 알림이 안 온다.** 꺼두기 전에 **가까운 미래**(몇 분 뒤)로 회차 시각을 맞춰 알림이 오는 걸 먼저 확인 → 그다음 꺼두고 같은 방식으로 맞춘 뒤 **안 오는지** 확인. (전량 재등록 모델이라 끄면 다음 로드에서 지워진다 — 끈 뒤 앱을 한 번 열 것.)
      `확인: 2026-08-05 · iPhone 16 Pro(00:37 빌드) · 전용 되풀이 항목(매일·자동완성 없음·미리 알림 비움) → 회차 시각 = 지금+5분 → 앱 나감` — **통과.**
      **① 대조군: 알림 옴.** **② 꺼두기 ON + 다시 지금+5분: 안 옴.** 대조군을 먼저 세웠으므로 ②의 침묵은 **"이미 지나서"가 아니라 가드 때문**이다 — 08-04에 못 닫았던 혼입이 이것으로 해소.
      **★ 이 항목의 값:** 꺼두기 작업 전체가 *"화면이 알림 멈춤이라고 약속하는데 거짓말이다"* 에서 시작했는데, **알림이 실제로 안 오는 것을 여기서 처음 봤다.** 동시에 **알림이 실제로 오는 것도 처음 확인**(①) — Stage 5-A 이후 알림 경로가 실기기에서 산다는 증거이기도 하다.
      **⚠️ 혼입 주의(2026-08-04에 실제로 걸린 것):** "알림 안 왔음"만으로는 **닫을 수 없다.** 그날 회차 시각은 오늘 07:00인데 앱을 켠 건 18:00 — 알림 계획은 **미래만** 등록하므로(`plan`의 `fire > now`) 꺼둠 가드가 없었어도 과거 시각엔 등록될 게 없었다. 침묵의 원인이 "꺼둬서"인지 "이미 지나서"인지 **구분이 안 된다.**
      **닫는 조건(반드시 미래 시각으로, 대조군 먼저):** ① 켠 상태에서 회차 시각을 **몇 분 뒤**로 → 앱 한 번 열기(등록) → 알림 **오는지** 확인. ② 꺼두기 ON → 다시 몇 분 뒤로 → 앱 한 번 열기 → **안 오는지** 확인. ①이 없으면 ②의 침묵을 가드 덕이라고 말할 수 없다.
      **⚠️ 절차 상세(2026-08-05 코드 확인으로 못박음 — 여기서 어긋나면 ①이 실패해 ②를 해석할 수 없다):**
      - **전용 항목으로 할 것.** `복약`·E-6 항목을 쓰면 회차가 오염된다. **주기=매일 · 자동완성=없음 · 미리 알림=비움**(→ `cycleBundle`의 lead 0 접힘으로 **딱 1건**만 발행돼 해석이 단순하다). 끝나면 그만두기로 정리.
      - **발화 시각 = 마감 시각 그 자체**(`fireInstant`가 값의 시·분을 그대로 쓴다). 분 단위로 넣을 수 있다.
      - **재등록은 저장만으로 된다** — 모든 로컬 편집이 `append → load() → reload() → scheduleNotifications()`를 탄다(`InboxModel:456`). 별도로 앱을 껐다 켤 필요는 없다(해도 무해).
      - **⚠️ 앱을 포그라운드에 두면 배너가 안 뜬다.** `willPresent` 델리게이트가 없어 iOS가 앱 사용 중 알림을 억제한다. **저장 후 앱을 나가거나 화면을 끌 것.**
      - **미래여야 한다** — `plan`은 `fire > now`만 등록한다(`cycleBundle:184`). 등록 시점에 이미 지난 시각은 조용히 0건. **이게 08-04에 걸린 혼입의 정체다.**
      - **①이 실패하면** 가드 이전 단계(알림 권한·집중 모드·폴더 등록)를 먼저 본다 — ②를 볼 의미가 없다.
      - **②의 가드 위치:** `NotificationPlanner.planned`가 `Recurrence.isDormant(it)`에서 `continue`(`NotificationPlanner:113`) → 묶음 자체를 안 만든다. 전량 재등록이라 **켤 때 등록된 것도 저장 시점에 지워진다.**
      **순서 메모(2026-08-04):** E-4는 **아무 때나 다시 꺼서** 확인할 수 있다(알림 가드는 회차 상태와 무관). 반면 E-5는 꺼둔 상태를 **소모**한다(`recurPausedAt`이 비워짐). 그래서 **E-5를 먼저** 하고 E-4는 나중에 다시 꺼서 하는 편이 낫다 — 지금 밀린 회차(1일)가 쌓여 있어 E-5의 켤 때 보정이 **실제 데이터로** 검증되기 때문.
- [x] **E-5. 다시 켜면 복귀한다.** 꺼두기 스위치 **OFF** → 저장 → 배너 사라짐 + **'지금 챙길 것'으로 복귀**(회차 시각이 지난 상태면).
      `확인: 2026-08-04 18:00 · iPhone 16 Pro · 켜기 전 = 꺼둠·마감 08-03 07:00·lead 없음·자동완성 '당일 지나면'` — **통과. 예측 3개 전부 적중: 회차 시각 → 8/4 오전 7:00(정확히 1회차만 전진) · '지금 챙길 것'으로 복귀 · 배너 사라짐.** 8/5면 이중 전진, 8/3이면 보정 미동작이었다 — 켤 때 보정(k=1)과 재로드 catch-up(임계 8/5 자정 → 더 안 전진)의 **분리가 실데이터에서 작동**함을 확인.
      **칩도 예측대로**(같은 날 추가 확인): 놓침 칩 **없음**(0일) + 오른쪽 시각 칩 **"지남" amber**. 놓침 0이 맞는 이유: 꺼둘 때 놓침이 0이었고(그때 08-03은 "오늘"이라 놓침 아님) 켤 때 그 0을 보존 — 꺼둔 구간의 08-03 회차는 건너뛴 것. **amber "지남"은 놓침이 아니다**(오늘 마감 시각 지남). 색 위계 D-4 그대로.
      **예측(2026-08-04, 켜기 전에 박아 둠 — 맞는지 보는 게 이 항목의 값이다):** 대상 항목은 마감=어제(08-03) 시각 T, 자동완성='당일 지나면', 꺼둠 시점=어제. 켜면 두 단계가 **연달아** 돈다.
      ① `resumeChanges`: k = (지금 놓침 1) − (꺼둘 때 놓침 0) = **1** → 마감 **오늘(08-04) 같은 시각 T**로 전진(미리 알림 있으면 lead 보존).
      ② 재로드의 `catchUpChanges`: 임계 = 오늘 끝(내일 자정) > 지금 → **더 안 전진.**
      **닫는 조건:** 켠 뒤 회차 시각이 **오늘 날짜 + 같은 시각 T**여야 통과. 모레로 가 있으면 **이중 전진**(재로드 분리 실패), 어제 그대로면 보정이 안 돈 것.
      **어디 보일지:** T가 **이미 지났으면** '지금 챙길 것'으로 복귀, **아직이면** '살아있는 기억'에 남는다(되풀이는 마감도 시각 인지 — #3). 둘 다 정상이니 T와 현재 시각을 같이 적을 것.
- [x] **E-6. ★ 놓침이 꺼둔 기간만큼 붙지 않는다. — 통과(2026-08-06)**
      **⚠️ E-3 대상 항목으로는 확인 불가 — 자동 완성 '당일 지나면'이면 놓침이 절대 안 쌓인다**(catch-up이 매일 회차를 넘겨 항상 0). 자동 완성 = **없음**인 **별도 항목**이 필요하다. 이게 자동완성의 뜻 그대로라 버그가 아니다.
      **준비(기다릴 필요 없음):** 되풀이 항목을 하나 새로 만들고 주기=**매일**, 자동 완성=**없음**, **회차 시각(마감)을 며칠 전 날짜**로 넣는다 → 그 즉시 **"N일 놓침"**(N = 지난 회차 수)이 뜬다. 며칠 기다려 쌓을 필요가 없다.
      **절차:** ① 위처럼 만들어 칩 숫자 N을 적어 둔다 → ② 꺼두기 ON, 숫자가 그대로 N인지 확인 → ③ **하루 넘긴 뒤** 다시 보아 **여전히 N**이면 통과(N+1이면 실패 — 고치기 전 동작) → ④ 켜고 나서도 **여전히 N**인지 확인(경계에서 안 튀는지).
      `확인: 2026-08-06 00:0x · iPhone 16 Pro(00:37 빌드, 재설치 전) · 대상 5ACEA309 '약 먹기 시험용 E-6'` — **통과. 여전히 3.**
      두 자리에서 같이 봤다: **'살아있는 기억' 목록 칩 "3일 놓침"(coral)** + **상세의 '되풀이 꺼둠' 배너 바로 아래 느낌표 + "3일 놓침"**.
      **⚠️ 판정이 성립하는 조건을 지켰다 — 자정을 넘긴 뒤, 앱을 완전히 종료했다 다시 열어서 봤다.**
      놓침 숫자는 화면을 그리는 순간 계산되므로, 자정 전에 열어둔 화면이면 08-05의 3(= 고친 코드·안 고친 코드가 같은 값)이라
      판정이 안 된다. 새로 그린 3이라야 증거가 된다. **안 껐다면 08-06엔 반드시 4**(08-02·03·04·05가 오늘 자정 앞)였다.
      → **꺼두기가 놓침을 실제로 멈춘다는 것이 실기기에서 확인됐다. E절 전부 닫힘(E-1~E-6).**
      (기준선: 2026-08-05 10:13에 같은 항목에서 3을 기록해뒀다 — 그때의 3은 판정이 아니라 기준선이었다.)
      **기준선 확보(2026-08-05, 통과 아님).** 대상 실측 상태: 매일 · 자동완성 **없음** · 마감 **2026-08-02T07:00** · 미리 알림 없음 · 꺼둠 ON · `recurPausedAt=2026-08-05T00:47`. 본 것 셋 다 예측대로였다.
      ① **'지금 챙길 것'에 없음** — 꺼둠이라 `isPublished=false`(게시 가드). ② **상세 상단 배너 "3일 놓침"**. ③ **'살아있는 기억' 목록 캡션에도 "3일 놓침" 분홍(coral)** — 색 위계 D-4 그대로(놓침=coral, "지남"=amber).
      **⚠️ 오늘 3은 통과가 아니다 — 구분이 안 되는 숫자다.** 꺼둔 시각이 **오늘 00:47**이고 `missed()`는 날짜 단위(오늘 자정)로 세므로, 고정 컷오프(오늘 00:47)와 `now`(오늘 10:13)가 **같은 날**이라 **고친 코드와 안 고친 코드가 둘 다 3**이다. 헤드리스 계측으로 확인: `놓침=3`, `안껐다면=3`.
      → **이것이 같은 종류의 잘못 세 번째다**(E-4 08-04 혼입 = 대조군 없는 부재 / D-3 예측3 = 부재의 자리 미증명 / 여기 = **구분되지 않는 값**). 앞의 둘은 "부재"였고 이번은 "값"이라 형태가 달라 놓치기 쉬웠다. **판정선을 세울 때 "안 고쳤다면 이 값이 달랐을까"를 먼저 계산할 것.**
      **판정은 2026-08-06:** 꺼둔 채 **3이면 통과 / 4면 실패.** 안 껐다면 내일은 반드시 4(08-02·03·04·05가 오늘 자정 앞)가 됐을 조건이라, 내일의 "여전히 3"이 가드가 먹은 증거다.
      **그대로 두면 안전하다(확인함).** 꺼둠이면 `catchUpChanges`(자동완성 전진)와 `resumeChanges`(켤 때 보정)가 **둘 다 안 돈다** → 앱을 열어도 마감·놓침이 안 변한다. 바꾸는 것은 **수동 편집과 "이번 것 했어요"뿐**이다.
      **★ 곁가지 수확 — 목록 칩의 대조가 여기서 확보됐다.** ③으로 **'살아있는 기억' 목록(`MemoryRow`)이 놓침 칩을 실제로 그린다**는 것이 눈으로 확인됐다. E-5(08-04)에서는 놓침이 0이라 칩이 없었고 "안 봄"으로 남았는데, 그 빈칸이 메워졌다. → **D-3의 "길 B(예비)"는 이제 필요 없다** — 목록 칩이 뜨는 자리임이 증명됐으므로, 앞으로 그 목록에서 "칩이 없다"는 관찰을 **부재의 증거로 쓸 수 있다.**
      **⚠️ N을 예측할 수 있다(2026-08-05 코드 확인).** `missedCount`는 **회차 날짜가 오늘 자정보다 앞선 것만** 센다 — **오늘 회차는 놓침이 아니다.** 따라서 **마감을 D일 전으로 넣으면 N = D**(매일 기준). 예: 오늘 08-05에 마감 08-02 → **N = 3**. 숫자가 이것과 다르면 그 자체가 기록할 사실이다.
      **미리 알림은 비워 둘 것** — 비우면 게이트가 `마감 ≤ now`라 과거 마감이 곧바로 게시돼 '지금 챙길 것'에서 칩이 보인다.
      **핵심 판정선:** 꺼두면 `missed()`가 `now` 대신 **꺼둔 시점**까지만 센다 → **2026-08-06에 봐도 N.** 안 껐다면 08-06엔 반드시 **N+1**이 됐을 조건이라, 08-06의 "여전히 N"이 가드가 먹은 증거다.
      **뜻:** 꺼둔 것은 "세지 않겠다"는 뜻이다. 꺼두기 전 놓침은 **보존**하고(사면이 아니다) 꺼둔 구간만 건너뛴다.
      **주의 1:** "이번 것 했어요"를 누르면 회차가 전진해 놓침이 사라진다 — E-6 동안 **누르지 말 것**.
      **주의 2:** 이 빌드 **이전에 이미 꺼둬 둔** 항목은 꺼둔 시점 기록(`recurPausedAt`)이 없어 옛 동작(계속 셈)으로 폴백한다 — 그건 실패가 아니다. **이 빌드에서 새로 껐다 켠 것**으로 확인할 것.

## F. (b) 이른 완료 — 판정 통합 (`lastDoneDue`) — 2026-08-05 저녁

> **고친 것:** 표시(칩·배너)·완료 버튼·취소 버튼·재완료 가드가 **모두 `doneThisCycle` 하나**를 본다.
> 새 필드 `lastDoneDue`(완료가 회차를 옮긴 **목적지**)가 "완료 vs 넘어감"을 가르고, 게이트(`isPublished`)가
> "회차가 열렸나"를 정한다. 옛 항목(필드 없음)은 **옛 판정 그대로 폴백**(마이그레이션 없음).
> 헤드리스: Core **231 통과** + iOS 빌드 OK. **커밋 `3a70402`·`ade29bb`·`1f31515`.**
>
> **⚠️ 이 절의 모든 줄은 헤드리스에서 맞은 것이지 폰에서 본 것이 아니다.** 2026-08-05 밤에는
> **일부러 설치하지 않았다**(E-6 판정이 08-06이라 빌드를 바꾸면 내일 숫자가 오염된다). 설치는 E-6을 닫은 뒤.
>
> **함께 닫히는 것(전부 같은 뿌리):** under-claim(이른 완료가 "안 했다"로 보임) · (b) 이른 완료를 무를 수 없음 ·
> 눌러도 반응 없는 버튼 · **구멍 1**(회차 시각 뒤의 재압이 하루를 먹음) · **구멍 2**(lead 창의 거짓 완료).
> 구멍 1·2는 **2026-08-05 저녁 계측에서 새로 드러난 것**이다 — 정본 **§5-대가·§5-조사** 참조(옛 이름 §5-A·§5-B — 정본 §5 머리의 이름 규칙 참조).

**준비 항목(전용으로 하나 새로 만들 것 — 실데이터 회차를 오염시키지 말 것):**
주기=**매일** · 자동 완성=**없음** · **미리 알림 = 지금−5분** · **마감 = 지금+30분**.
(미리 알림이 과거여야 '지금 챙길 것'에 있고, 마감이 미래여야 "이른 완료"가 성립 — D-3 준비 조건과 같다.)

- [ ] **F-1. ★ 이른 완료 타임라인.** — **① 통과(2026-08-06 00:5x). ②③④ 대기.** 위 항목을 **지금**(마감 전) "이번 것 했어요" → 아래 표대로 흐르는지.

  | # | 언제 보나 | 화면(상세) | 눌렀을 때 | **안 고쳤다면?** |
  |---|---|---|---|---|
  | ① | **완료 직후**(상세 닫고 다시 열기) | 이번 회차 완료됨 ✓ + **[취소]** | (버튼 없음) | `[이번 것 했어요]` — **다름 ✓** |
  | ② | **마감 시각 지난 뒤**(같은 날, +30분) | **그대로** — 회차 시각에 안 꺼짐 | (버튼 없음) | `[이번 것 했어요]`, 누르면 **마감이 하루 더** — **다름 ✓** |
  | ③ | **다음 날 미리 알림 전** | 그대로 | (버튼 없음) | `[이번 것 했어요]` — **다름 ✓** |
  | ④ | **다음 날 미리 알림 시각 이후** | `[이번 것 했어요]` | 마감 하루 전진 | **같음 — 판정선 아님(그물)** |

  **① ②는 설치한 날 본다. ③ ④는 그 다음 날 아침.**
  **규칙 2 적용("안 고쳤다면 이 값이 달랐을까"):** 옛 판정은 `lastDone ≥ 직전 회차`인데 이른 완료라 항상 미달 →
  ①②③에서 옛 코드는 **언제나 `[이번 것 했어요]`** 다. 값이 갈리므로 판정이 된다.
  **④는 옛·새가 같은 값이라 단독으로는 아무것도 못 가른다** — 회귀 감지선으로만 쓴다(여기서 안 열리면 실패).
  **규칙 1 적용(부재의 대조군):** ①②③의 "버튼 없음"은 부재 예측이다. **그 자리에 두 상태가 다 그려지는 것을
  이미 봤다** — D-2(2026-08-04)에서 "이번 회차 완료됨 ✓", D-3(2026-08-05)에서 `[이번 것 했어요]`가 돌아온 것.
  → **대조 성립.** (이것이 D-3 예측3에서 배운 규칙을 적용한 자리다.)
  **★ ②가 이 절의 핵심.** 구멍 1의 판정선이고, 옛 코드에선 여기서 **내일 회차가 조용히 사라졌다**.

      `확인(①): 2026-08-06 00:5x · iPhone 16 Pro(00:14 설치 빌드) · 항목 가(미리 알림 12:00·마감 13:00)
      → '살아있는 기억'에서 [기억하기] → 상세 [이번 것 했어요] → 상세 닫고 다시 열기` — **통과.**
      **"이번 회차 완료됨 ✓ + [취소]"가 떴다** = under-claim이 사라졌다. 어제 D-3에서 같은 자리에
      `[이번 것 했어요]`가 돌아와 있는 것을 눈으로 봤으므로 **대조가 성립한다**(같은 자리, 두 상태).
      **남은 ②③④는 시각이 와야 볼 수 있다** — 항목 가를 완료 상태 그대로 두었다.

- [x] **F-2. 이른 완료를 무를 수 있다 ((b) 본체). — 통과(2026-08-06)** F-1 ①의 화면에서 **[취소]** 를 누른다 →
      마감·미리 알림·완료 기록이 **되돌아가고** 항목이 **'지금 챙길 것'으로 복귀**, 버튼이 `[이번 것 했어요]`로.
      **한 번 더 누를 [취소]는 없어야 한다**(이력을 거슬러 올라가면 실패 — ③이 지적한 구멍).
      **안 고쳤다면?** `[취소]` 자체가 **안 그려졌다**(그게 (b)였다) — **다름 ✓.** 존재 예측이라 대조군 불필요.
      `확인: 2026-08-06 00:5x · iPhone 16 Pro · 항목 가` — **통과.** [취소] → 마감·미리 알림이 13:00/12:00으로
      되돌아가고 버튼이 `[이번 것 했어요]`로 복귀. **[취소]가 한 번 더 남지 않았다**(③이 지적한 "이력을 거슬러
      올라감"이 안 일어난다). 확인 후 **다시 완료를 눌러 원상복구**했다(②③④가 그 상태를 쓴다).

- [ ] **F-3. lead 창에서 "완료"라고 거짓말하지 않는다 (구멍 2).**
      **다음 날 아침, 미리 알림~마감 사이**(= 항목이 '지금 챙길 것'에 있는데 오늘 것을 아직 안 한 시각)에
      목록 칩과 상세를 본다 → **"완료" 칩이 없고** 상세는 `[이번 것 했어요]`.
      ⚠️ **F-1과 다른 항목으로 할 것** — 이 줄은 **정시(마감 이후) 완료**한 항목이어야 재현된다.
      (F-1 항목은 이른 완료라 옛 코드에서도 칩이 안 떴다 = 구분이 안 된다.)
      **안 고쳤다면?** 정시 완료한 항목은 옛 판정이 참이라 **'지금 챙길 것'에 있으면서 "완료" 칩**이 떴다
      (= 안 먹은 약을 먹었다고 함) — **다름 ✓.**
      **대조군:** "칩 없음"은 부재 예측인데, **그 목록이 칩을 그리는 자리임은 E-6에서 확인됐다**(3일 놓침 coral).

- [x] **F-4. 목록 스와이프 "했어요"가 이미 닫은 회차엔 안 뜬다. — 통과(2026-08-06, 대조군 확보)** 완료 상태인 되풀이 항목을
      '살아있는 기억'에서 **왼쪽 스와이프** → **"했어요"가 없어야** 한다(삭제·미루기는 그대로).
      **⚠️ 대조군이 아직 없다 — 먼저 세울 것.** 이 자리에 **"했어요"가 뜨는 것을 폰에서 본 기록이 없다**
      (지금까지 완료는 전부 상세 버튼으로 눌렀다). 규칙 1대로, **아직 완료 안 한 되풀이 항목을 같은 목록에서
      스와이프해 "했어요"가 뜨는 것을 먼저 본 뒤** 이 줄을 본다. 순서가 뒤바뀌면 부재가 무엇 때문인지 안 갈린다.
      **안 고쳤다면?** 칩은 "완료"인데 스와이프에 "했어요"가 남아 **눌러도 아무 일이 없었다**(§5-대가가 지목한
      dead button 둘 중 목록 쪽) — **다름 ✓.**
      `확인: 2026-08-06 00:5x · iPhone 16 Pro · '살아있는 기억'의 항목 가, 같은 줄에서 완료 전/후` — **통과.**
      **대조군 먼저 세웠다** — 완료 **전** 스와이프에 "했어요"가 **뜨는 것을 보고**(누르지 않고 되돌림),
      완료 **후** 같은 줄 스와이프에 **없는 것**을 봤다. 가는 두 칸이 미래라 완료 후에도 같은 목록에
      남으므로 **목록이 달라져서 생긴 부재가 아니다** — 어제 "대조군 미확보"로 적어둔 구멍이 닫혔다.

- [ ] **F-5. 옛 항목이 안 변한다(회귀).** 이 빌드 **전에** 만든 되풀이 항목(= `lastDoneDue` 없음)의
      칩·배너·버튼이 **어제와 같은지**. 마이그레이션을 안 하므로 옛 판정으로 폴백해야 한다.
      다음 완료를 누르는 순간 필드가 생겨 새 판정으로 옮겨가는 것도 정상이다.
      **주의:** E-6 대상 항목으로는 하지 말 것(누르면 놓침이 사라진다).

**실행 계획 — 확정 (2026-08-06 00:45 착수 · 설치 00:14 `kr.teri.secondbrain` 덮어쓰기, 앱 삭제 안 함).**
새벽에 만들지만 **관찰 시각은 낮·저녁으로 잡는다** — 매일 주기는 다음 회차가 *같은 시계 시각*에 오므로,
새벽 시각으로 만들면 이후 관찰이 전부 새벽이 된다. **만드는 시각과 관찰 시각은 무관하다.**

| 항목 | 미리 알림 | 마감 | 첫 행동 |
|---|---|---|---|
| **가** (F-1·F-2·F-4) | **08-06 12:00** (미래) | **08-06 13:00** (미래) | **이른 완료** |
| **나** (F-3) | **08-05 20:00** (어제) | **08-05 23:55** (어제, 확실히 과거) | **늦은 완료** |

둘 다 되풀이 · 매일 · 자동완성 **없음**.

- **가의 두 칸이 미래여야 한다** — 완료 전에도 후에도 게시가 안 돼 **같은 목록**에 남는다(F-4 대조군의 조건).
- **나는 자정을 걸치게 잡았다** — 늦은 완료 뒤 다음 lead 창이 **2026-08-06 20:00~23:55**로 온다.
  안 그러면 그 창이 새벽에 열려 못 본다.

**⚠️ F-4는 '살아있는 기억'에서 본다 (2026-08-06 코드 확인).** `새 기억들` 섹션의 스와이프는
**기억하기(왼쪽)·삭제(오른쪽)뿐**이고 "했어요"는 **길게 누르기 메뉴**에만 있다(`InboxView:109-111`).
"했어요" 스와이프는 '지금 챙길 것'과 '살아있는 기억'에만 있다. → **가를 만들고 [기억하기]를 눌러
'살아있는 기억'으로 보낸 뒤** 그 목록에서 완료 전/후 스와이프를 비교한다(가는 게시가 안 되므로
완료 후에도 같은 목록에 남는다 = **같은 줄 대조**).

**언제 무엇을 보나** — ⚠️ **절대 날짜로 적는다.** 새벽 세션이 잦아 "오늘/내일"이 실제로 혼란을 냈다.

| 때 | 볼 것 |
|---|---|
| **2026-08-06 00:45~01:00** | 가 만들기 → 기억하기 → **F-4 대조군** → **F-1 ①** → **F-4 본** → **F-2** → 나 만들기·늦은 완료 |
| **2026-08-06 13:00 이후** | **F-1 ②** ← **핵심**(구멍 1 판정선) |
| **2026-08-06 20:00~23:55** | **F-3** (나의 lead 창) |
| **2026-08-07 12:00 전** | **F-1 ③** |
| **2026-08-07 12:00 이후** | **F-1 ④** (판정선 아님·그물) |
| 아무 때나 | **F-5** (옛 항목 회귀, E-6 항목 제외) |

**⚠️ 순서와 시점 (F-2가 F-1의 상태를 되돌리므로 순서가 중요하다):**

| 순서 | 무엇 | 언제 | 항목 |
|---|---|---|---|
| 1 | F-1 준비 항목 만들기 (lead=지금−5분 · 마감=지금+30분) → **이른 완료** → **F-1 ①** | 설치 직후 | **가** |
| 2 | **F-2**(취소 → 복귀 확인) → **다시 이른 완료**로 원상복구 | 바로 이어서 | **가** |
| 3 | F-3 준비 항목 만들기 (lead=**지금−2시간** · 마감=지금−5분, 둘 다 과거) → **정시(늦은) 완료** | 바로 이어서 | **나** |
| 4 | **F-4** — 먼저 **아직 완료 안 한** 되풀이에서 스와이프 "했어요"가 **뜨는 것**을 보고(대조군), 그다음 **가**에서 안 뜨는 것 | 바로 이어서 | 다 → 가 |
| 5 | **F-1 ②** — 마감 시각(지금+30분) 지난 뒤 | 같은 날 +30분 | **가** |
| 6 | **F-1 ③** — 다음 날, 미리 알림 시각 전 | 08-07 이른 아침 | **가** |
| 7 | **F-1 ④** + **F-3** — 미리 알림 시각 이후 / **나**의 lead 창(2시간 폭) 안에서 | 08-07 | 가 · 나 |
| 8 | **F-5** — 옛 항목 회귀(E-6 항목 제외) | 아무 때나 | 기존 |

- **F-3의 lead를 2시간으로 잡는 이유:** 다음 날 관찰 창이 그만큼 넓어진다(5분으로 잡으면 놓친다).
- 끝나면 **가·나·다는 "그만두기"로 정리**한다. **E-6 항목은 건드리지 말 것.**

## 실사용 데이터 만들기 (권장)
- **"매일 아침 약 먹기"** 되풀이 항목을 실제로 하나 만들어 둔다: 종류=되풀이, 주기=**매일**, 미리 알림=오늘+아침 시각, 자동 완성=**없음**(약은 쌓여야 놓침이 생김).
- Stage 4(놓침 계산) 전에 만들어도 **안전** — 필드는 안정 인코딩(recur/recurAuto/recurPaused/lastDone)이고 LWW 자동 병합. Stage 4는 그 위에 계산만 얹는다.
- **놓침 데이터를 만들려면 며칠은 완료를 건너뛴다**(매일 누르면 놓침=0). lastDone(마지막 완료) + 경과가 Stage 4의 입력.

## 문제 생기면 메모할 것
- 본 것을 그대로. 특히 A-3에서 **항목이 사라지면 즉시** 적을 것(핵심 실패 양상).
