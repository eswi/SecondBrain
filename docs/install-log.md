# 설치 이력 — **폰에 무엇이 깔렸나** (정본)

> **이 파일이 「그 커밋이 폰에 깔린 적 있나」에 답하는 유일한 자리다.**
> **append-only** — 최신을 맨 위에 쌓고 **지우지 않는다.**
> `HANDOFF.md`에는 **최신 한 줄 + 이 파일 링크만** 둔다.
>
> ### 왜 여기로 모았나 (2026-08-21 사용자 결정 · A안)
> 이력이 **`HANDOFF.md`와 worklog에 나뉘어** 있었다. **HANDOFF는 갱신되며 잘리는 스냅샷**이라
> **08-20 저녁 여덟 번이 worklog에만** 남았고, 그 구멍 안에 **원칙 목록을 실제로 고친 두 커밋
> (`50a8e48`·`c6ed58b`)**이 있었다. 그래서 §4가 **이미 된 일을 「⛔ 다시 시도하지 말 것」**으로
> 적고 있었다(**일곱 번째 형태**).
> ⛔ **자리가 나뉘면 어느 쪽도 전부를 못 갖는다** — `inbox-state.py`가 임시 자리에만 적혀 있던 것과
> **같은 형태**다(계열 ⑤ · `docs/worklog/2026-08-20-macbook.md` §20).

---

## 쓰는 법 — 설치 직후 그 자리에서 한 줄 (`CLAUDE.md` 기록 규칙 2)

`| 시각 | 커밋 | 어디서 | 확정 근거 | 판정 |` 를 **맨 위 표의 첫 줄에** 넣는다.

- **확정 근거 = 그 빌드에서만 눈에 보이는 것.** ⚠️ **커밋 해시·빌드 시각은 폰 화면에서 볼 수 없다** —
  확정 근거에 그 둘을 적으면 안 된다. 아이콘 신설·문구 변경·색 변경처럼 **직전 빌드와 갈리는 것**을 적는다.
- **판정은 사용자가 한다**(항시 규칙 7). 설치 직후엔 **판정 칸을 비워 두고**, 판정이 나면 그 줄에 채운다.
  ⛔ **「제안했다」와 「됐다」를 같은 칸에 적지 않는다**(기록 규칙 7).
- ⛔ **설치 성공은 `App installed` 줄로만 본다** — `exit code`는 `grep`이 낸 값일 수 있다.
- ⛔ **「빌드 실패인데 설치 성공」이 된다**(빌드 함정 5) — **빌드 `EXIT=0` + `error: 0`**과
  **`App installed`** 를 **함께** 봐야 새 코드가 깔렸다고 말할 수 있다.
- **전말(왜 그 빌드를 만들었나·무엇이 깨졌나)은 그날 worklog에** 쓴다. 여기는 **목록**이다.

---

## 이력 (최신이 맨 위)

| 시각 | 커밋 | 어디서 | 확정 근거 (그 빌드에서만 보이는 것) | 판정 |
|---|---|---|---|---|
| **2026-09-22 20:31** | **`874fd62`** | **맥북** | ★ **아래 띠의 「역선택」이 스위치가 아니라 원을 이루는 화살표 둘 아이콘 + 바탕 없는 글자이고, 누르면 고른 칩들이 그 자리에서 반대로 뒤집힌다**(안 고른 것이 켜지고 고른 것이 꺼진다 · 「태그 없음」도) — 직전 `4097b25`는 시스템 스위치 | ✅ **ⓕ′·ⓖ·ⓗ 전부 통과 — 사용자(20:4x): *"f g h 모두 통과!! 고칠 것 몇개는 집에가서 하자."*** → ⛔ **고칠 것 몇 개 있음 — 내용은 맥미니에서 듣는다**(짐작 금지). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 — 설계 §7-5 ⓕ′·ⓖ·ⓗ · ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 20:31:32 · `App installed` 20:31:34 |
| **2026-09-22 19:53** | **`4097b25`** | **맥북** | ★ **아래 띠에 「역선택」 글자 + 스위치 + 「닫기 >」가 한 줄** — 직전 `bb02f98`는 「닫기」가 닫/기 두 줄로 꺾여 있었다 | ⛔ **스위치 자체가 뒤집혔다 — 사용자(20:2x): *"역선택에 시스템 스위치를 쓴 것은 좋은 결정이 아니었다"*** → 아이콘 + 글자 버튼 · 선택을 즉시 역전 = `874fd62`. ⓖ·ⓗ는 판정 안 받음(다음 빌드에서 함께 본다). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 — 설계 §7-4 ⓕ·ⓖ·ⓗ · ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 19:53:53 · `App installed` 19:53:55 |
| **2026-09-22 19:51** | **`bb02f98`** | **맥북** | ★ **아래 띠의 「역선택」이 칩이 아니라 시스템 스위치(켜면 강조색)** · 제목 글자가 「닫기」와 같은 크기 — 직전 `afde395`는 「역선택」이 회색 칩 · 제목 13pt | ⛔ **결함 있는 빌드 — 「닫기」가 두 줄(닫/기)로 꺾인다**(시뮬 스크린샷에서 발견 · 설치 뒤에 봤다 · 폰에서도 그럴 것 — **추정**) → `4097b25`로 바로 갈았다(2분 뒤). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 — 설계 §7-4 ⓕ·ⓖ · ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 19:50:57 · `App installed` 19:51:02 |
| **2026-09-22 14:24** | **`afde395`** | **맥북** | ★ **해시태그 필터 패널이 세 영역으로 갈린다 — 위 제목 띠에 「전체 N · 선택 N · 그 외 N」, 가운데 칩 영역(안 고른 칩도 회색 바탕), 아래 띠(「역선택」·「닫기 >」)가 서로 다른 바탕색 · 폭이 화면의 45%(181pt)** — 직전 `272be1e`는 제목 없음 · 한 바탕 · 안 고른 칩은 테두리만 · 폭 300 | ✅ **ⓐ~ⓔ 전부 통과 — 사용자(19:4x): *"a~e 모두 확인했어. 특히 e의 경우 #맛 #서을 근교 #업무 이렇게 3개가 모두 한 줄에 들어갔어. 합격이야."*** → 고칠 것 둘 더(「역선택」 = 스위치 · 제목 크기 = 「닫기」) → `bb02f98`. *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 — 설계 `tag-filter-design.md` §7-2 ⓐ~ⓔ · ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 14:24:44 · `App installed` 14:24:50 |
| **2026-09-22 01:27** | **`272be1e`** | **맥미니** | ★ **네 목록 화면(새로운 기억·검색·살아있는 기억·보관된 기억) 오른쪽 가장자리에 손잡이가 있고, 누르면 해시태그 필터 패널(칩 · 맨 끝 「태그 없음」 · 구분선 · 「역선택」 · 「닫기 >」)이 펼쳐진다** — 직전 `f29294a`는 「새로운 기억」에만 있고 누르면 아무 일 없음 | ✅ **여섯 다 통과 — 사용자(01:4x): *"일단 의도한대로 잘 동작해. 1 2 3 4 5 6 모두 잘 되는데, 고칠 부분이 있긴 해."*** → ⛔ **고칠 것 있음 — 내용은 내일(09-22) 맥북에서 듣는다** · 짐작해 먼저 고치지 말 것. *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 — `tag-filter-design.md` §5 ⓐ~ⓘ(패널 펼침·자리 · 중복 선택 OR · 「태그 없음」 · 「역선택」 여집합 · 「닫기 >」 뒤 필터 유지 + `<` 강조색 · 끌기와 누르기 안 섞임 · 상세에 없음 · 자리 공유). ⚠️ **필터로 전부 숨으면 빈 화면**(문구는 사용자 몫 · §4). ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **01:27:11**(빌드 시작 01:27:01) · `App installed` 01:27:44(첫 호출은 exit code를 못 읽어 한 번 더 돌렸다 · 둘 다 `App installed`). |
| **2026-09-21 19:28** | **`f29294a`** | **맥북** | ★ **세게 던지면 범위 끝(제목 아래·탭바 위)까지 간다** — 직전 `a2dd1ec`는 세게 던져도 속도×0.14초만큼 | ✅✅ **전부 통과 — 사용자(19:3x): *"세게 던지면 끝까지 가는 것 확인했어. 나머지 판정도 다 통과."*** → 가장자리 손잡이 = **닫혔다**(누르면 무엇 = 미정 · 사용자가 새로 디자인) · 해시태그 고칠 것 = 닫혔다. *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 하나: **ⓑ‴ 세게 던지면 끝까지 · 살짝은 짧게 · 중간은 그 사이**(§5-14 · 1200/2000pt/s). ✅ 움직임 자체 = 완료(19:3x 사용자). ✅ 바탕 = 불투명 유지(유튜브 탭도 불투명 · §5-14 계측). 나머지(ⓐ 톤 · ⓑ~ⓖ · B 해시태그 ㉠㉡)는 `5d0f1ac` 줄. ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **19:27:57**(빌드 시작 19:27:52) · `App installed` 19:28:00. |
| **2026-09-21 19:20** | **`a2dd1ec`** | **맥북** | ★ **끌 때 손잡이가 손가락을 살짝 뒤따르고(스프링) 놓는 순간 멈춤 없이 이어 나간다** — 직전 `750a763`은 1:1로 따르다 놓는 순간 한두 프레임 비었다 | ✅✅ **ⓑ″ 통과 — 사용자(19:3x): *"느낌이 좋아. 그러므로 움직임 자체에 대해서는 완료됨!!!"*** · 새 요구 = 세게 던지면 끝까지 → `f29294a`(19:28) · 재질 = 유튜브도 불투명(계측) → 그대로. *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 하나: **ⓑ″ 놓는 순간까지 유튜브와 같은가**(§5-13). 다르면 다시 한 녹화(우리+유튜브). ⚠️ 끌 때 손가락보다 약 40pt 뒤따르는 것이 보일 수 있다(유튜브도 그렇다) — 거슬리면 말씀(`trackingSpring` response 0.12). 바탕은 아직 불투명. 나머지(ⓐ 톤 · ⓑ~ⓖ · B ㉠㉡)는 `5d0f1ac` 줄. ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **19:20:27**(빌드 시작 19:20:22) · `App installed` 19:20:29. |
| **2026-09-21 19:13** | **`750a763`** | **맥북** | ★ **튕기면 놓는 순간 멈춤 없이 손가락 속도로 이어 나가 매 프레임 조금씩 느려지며 멈춘다**(유튜브 프레임 곡선 · 약 0.45초) — 직전 `3c550d5`는 두 프레임 멈춘 뒤 점프 · 일정 속도 · 급정지 | ◐ **꼬리는 통과 · 놓는 순간만 남음**(19:15 녹화 · §5-13): 꼬리 비율 0.85~0.9·정지 0 = 유튜브와 같다 · 놓는 순간 `42 0 0 26 30`/`23 10 9 17` → `a2dd1ec`(19:20). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 하나: **ⓑ″ 유튜브와 같은 부드러움인가**(§5-12 · 배선+꼴 둘을 고쳤다). 다르면 **다시 화면 녹화**(우리+유튜브 한 녹화)를 주세요 — 프레임 표로 봅니다. 바탕은 아직 불투명(A/B 그대로 · 재질은 원인 아님 → 되살릴지 사용자가 고른다). 나머지(ⓐ 톤 · ⓑ~ⓖ · B ㉠㉡)는 `5d0f1ac` 줄. ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **19:13:39**(빌드 시작 19:13:35) · `App installed` 19:13:41. |
| **2026-09-21 18:57** | **`3c550d5`** | **맥북** | ★ **손잡이 바탕이 불투명 회색**(직전 `51ade41`까지는 재질 — 목록을 스크롤해도 비치지 않는다) · 튕기면 폰에 `edge-handle/glide.log`가 쌓인다(화면엔 안 보인다) | ⛔ **ⓑ″ 결함 그대로 — 사용자(19:0x): *"드드득하는 느낌이 그대로 있어. 유튜브가 보여주는 모습과는 현격히 차이가 나."*** → 재질 아님 · `glide.log` = 120Hz·직선(§5-11) · **화면 녹화(19:07)로 갈랐다: 놓는 순간 두 프레임 정지+점프 · 속도 급변**(§5-12) → `750a763`(19:13). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 — A/B + 계측(§5-10): **ⓑ″ 이 빌드에서 튕김이 부드러운가**(드드드득이 사라졌나) · **90% 구간 속도가 같아 보이나**. **세게·약하게 몇 번 튕긴 뒤 말씀해 주세요** — 그 뒤 `glide.log`를 가져와 FPS·직선성을 숫자로 봅니다(결과가 어느 쪽이든). 부드러워지면 원인 = 재질 → 비침 포기 / 덜 비싼 재질 중 사용자가 고른다. 나머지(ⓐ 톤 · ⓑ~ⓖ · B 해시태그 ㉠㉡)는 `5d0f1ac` 줄 그대로(ⓐ′ 비침은 이 빌드에선 없다). ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **18:57:36**(빌드 시작 18:57:32) · `App installed` 18:57:38. |
| **2026-09-21 18:49** | **`51ade41`** | **맥북** | ★ **튕기기가 직전(`2936e7b`)보다 50% 빠르다 — 전체 약 0.23초**(꼴은 같다 · 90% 빠르게 · 10% 절반) | ⛔ **ⓑ′ 결함 둘 — 사용자(19:4x): *"90% 거리가 같은 속도 같지 않다 … 50% 정도 구간과 그 후가 속도가 다르게 느껴져"* · *"부드럽지 않아. 드드드득~ 하는 느낌 또는 FPS가 낮은 느낌"*** → 코드상 속도는 일정(두 점 직선) · 짚인 원인 = 움직이는 재질의 재블러 → A/B+계측 `3c550d5`(18:57). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 하나: **ⓑ′ 이 속도면 되나**. ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **18:49:32**(빌드 시작 18:49:28) · `App installed` 18:49:34. |
| **2026-09-21 18:47** | **`2936e7b`** | **맥북** | ★ **튕기면 90%를 빠르게 가고 마지막 10%만 절반 속도로 — 전체 약 0.34초**(직전 `a8edc6b`는 계단식 감속 0.6초) | ✅ **꼴 통과 · 속도만 ⛔ — 사용자(19:3x): *"지금보다 50% 더 빠르게 하자."*** → `51ade41`(18:49). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 하나: **ⓑ′ 이 꼴·속도면 되나**(§5-9). 나머지(ⓐ 톤 · ⓐ′ 비침 · ⓑ~ⓖ · B 해시태그 ㉠㉡)는 `5d0f1ac` 줄 그대로. ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **18:47:46**(빌드 시작 18:47:41) · `App installed` 18:47:48. |
| **2026-09-21 18:44** | **`a8edc6b`** | **맥북** | ★ **튕기기가 직전(`aadf8b5`)의 2배 속도 — 전체 약 0.6초**(곡선 꼴은 같다) | ⛔ **꼴을 바꿨다 — 사용자(19:2x): *"위치에 따라 속도를 조절하지 말고, 그냥 전체 거리의 90%는 빠르게 가고, 마지막 10% 남은 거리는 2분의 1 속도로"*** → `2936e7b`(18:47) · 계단식은 「안 됐다」가 아니라 **「뜻이 아니다」**(기록 규칙 9 ②). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 하나: **ⓑ′ 이 속도면 되나**(사용자: *"이정도면 될 것 같아"* — 확인만). 나머지(ⓐ 톤 · ⓐ′ 비침 · ⓑ~ⓖ · B 해시태그 ㉠㉡)는 `5d0f1ac` 줄 그대로. ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **18:44:23**(빌드 시작 18:44:20) · `App installed` 18:44:25. |
| **2026-09-21 18:40** | **`aadf8b5`** | **맥북** | ★ **튕기면 앞 절반은 일정 속도로 가고 뒤 절반은 계단식으로 느려져 멈춘다**(약 1.2초 · 직전 `a83e86f`는 스프링 0.8초) | ✅ **곡선 꼴 통과 · 속도만 ⛔ — 사용자(19:1x): *"좋아. 일단 지금보다 2배 속도로 맞춰줘. 전체적으로 느려. 이정도면 될 것 같아!"*** → `a8edc6b`(18:44). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 하나: **ⓑ′ 사용자가 정한 곡선대로 느껴지나**(§5-8 · 앞 절반 +30% 일정 · 뒤 절반 5%마다 10%씩 감속 · 도착 0) — ⚠️ 「5%씩」을 「10%씩」으로 해석했다(0에 닿게) · 다르면 말씀. 숫자 여섯은 §5-8 표. 나머지는 `5d0f1ac` 줄 그대로. ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **18:40:39**(빌드 시작 18:40:35) · `App installed` 18:40:40. |
| **2026-09-21 18:34** | **`a83e86f`** | **맥북** | ★ **튕기면 손잡이가 눈에 보이게 미끄러져 간다(약 0.8초)** — 직전 `af4c5ef`까지는 「휙 점프」(애니메이션이 안 걸린 것으로 추정) | ✅ **ⓑ′ 통과 — 사용자(19:0x): *"과정이 보인다."*** → 배선이 원인이었다(§5-7 확정). 이어서 속도 곡선 지시 → `aadf8b5`(18:40). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 하나: **ⓑ′ 이제 이동 과정이 보이나**(부드럽게 미끄러지다 스며들며 멈추나). ✅면 그다음 숫자 셋(§5-6) 조정 · ⛔면 원인이 배선이 아니다(다시 본다). 나머지는 `5d0f1ac` 줄 그대로. ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **18:34:55**(빌드 시작 18:34:51) · `App installed` 18:34:57. |
| **2026-09-21 18:30** | **`af4c5ef`** | **맥북** | ★ **튕기면 같은 거리를 더 천천히(약 0.8초) 간다 · 시작이 덜 튀고 끝이 스며든다**(직전 `1b0fa24`는 약 0.35초 · 손가락 속도 그대로) | ⛔ **ⓑ′ 결함 — 사용자(18:5x): *"지금은 이동하는 과정이 너무 너무 빠르게 표현되어서 휙 점프해가는 느낌"*** → **값이 아니라 배선**(0.35s도 0.8s도 같은 느낌 = 애니메이션이 안 걸림) → `a83e86f`(18:34). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 하나: **ⓑ′ 미끄러지는 감** — 숫자 셋 중 무엇을 어느 쪽으로(`edge-handle-design.md` §5-6: 얼마나 멀리 0.14 · 얼마나 오래 0.80 · 시작이 얼마나 튀나 0.5). 나머지(ⓐ 톤 · ⓐ′ 비침 · ⓑ~ⓖ · B 해시태그 ㉠㉡)는 `5d0f1ac` 줄 그대로. ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **18:30:11**(빌드 시작 18:30:06) · `App installed` 18:30:13. |
| **2026-09-21 18:18** | **`1b0fa24`** | **맥북** | ★ **튕기면 세기와 무관하게 약 0.35초에 멈추고, 세게 튕기면 약 300pt·약하게는 약 200pt 간다**(사용자 동영상의 손잡이와 같은 값 · 직전 `2b53a7a`는 빠를수록 더 오래·1.4배 멀리) | ⛔ **ⓑ′ 결함 — 사용자(18:4x): *"느낌이 너무 달라 … 이동하는 중간 과정이 너무 짧게 그려져서 마치 순간이동 … 속도를 약간 늦춰주고 목적지에 도달할수록 느려지면서 부드럽게"*** → `af4c5ef`(18:30). ⚠️ 값은 동영상과 같았다 — **같게가 아니라 느리게를 원한 것**(뜻이 다르다 · 기록 규칙 9 ②). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 하나(`edge-handle-design.md` §5-4): **ⓑ′ 유튜브 손잡이 동영상과 같은 감인가** — 세게·약하게 튕겨 비교. 나머지(ⓐ 톤 · ⓐ′ 비침 · ⓑ~ⓖ · B 해시태그 ㉠㉡)는 `5d0f1ac` 줄 그대로. ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **18:18:42**(빌드 시작 18:18:38) · `App installed` 18:18:44. |
| **2026-09-21 18:11** | **`2b53a7a`** | **맥북** | ★ **살짝 튕기면 조금 가고 세게 튕기면 멀리·오래 미끄러진다 · 천천히 놓으면 그 자리**(직전 `5d0f1ac`는 어떤 튕김도 거의 끝까지 · 고정 0.55초) | ⛔ **판정 없이 덮였다**(18:1x 사용자가 동영상을 줬다 → 재서 `1b0fa24`(18:18)로). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 하나(`edge-handle-design.md` §5-3): **ⓑ′ 미끄러지는 감이 유튜브에 가까워졌나** — ① 살짝 튕김이 짧게 가나 ② 세게 튕김이 멀리 가되 끝까지는 안 가나(1500pt/s ≈ 300pt) ③ 감속이 자연스럽나(급정거·튐 없나) ④ 천천히 놓으면 안 움직이나. **방향을 말씀해 주세요** — 「더 멀리/덜 멀리」·「더 오래/더 빨리 멈추게」 둘은 각각 값 하나다(감속률 · 멈춤 문턱). **동영상을 주시면 프레임을 뽑아 곡선을 재서 맞춘다.** 나머지(ⓐ 톤 · ⓐ′ 비침 · ⓑ~ⓖ · B 해시태그 ㉠㉡)는 `5d0f1ac` 줄 그대로. ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **18:11:21**(빌드 시작 18:11:17) · `App installed` 18:11:23. |
| **2026-09-21 17:39** | **`5d0f1ac`** | **맥북** | ★ **손잡이를 툭 튕기면 미끄러져 멈춘다**(직전 `b04095e`는 손가락이 놓인 자리에 멈춤) · **목록을 스크롤하면 손잡이 안으로 카드가 흐리게 비친다**(재질 · 직전은 불투명) | ⛔ **ⓑ′ 결함 — 사용자(17:5x): *"너무 민감하게 튕겨나가고 세게 튕겨나가고 미끄러지는 느낌이 없어"*** → `2b53a7a`(18:11)로 고쳤다. 나머지 판정은 아직. ✅ **나머지 전부 통과**(19:3x 사용자 · `f29294a`에서 · *"나머지 판정도 다 통과"*): ⓐ 톤 · ⓑ 끌기 · ⓒⓓ 제목·탭바 경계 · ⓔ 좌우 무반응 · ⓕ 재실행 자리 · ⓖ 누르면 무반응 · **B ㉠ 「추가하기」 · ㉡ 캡션 태그 보라**(ⓐ′ 비침은 §5-14로 「불투명이 맞다」). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 둘(`edge-handle-design.md` §5-2 · §6). **A. 손잡이:** ⓐ 스크린샷과 같은가(바탕 톤 — 재질 덮기 0.7 · 참고보다 조금 어둡다 · 다듬을 방향 말씀) · ⓐ′ 스크롤할 때 뒤가 비치나 · ⓑ 눌러 끌면 따라오나 · **ⓑ′ 튕기면 세기만큼 미끄러지나 · 경계(제목 아래·탭바 위)에서 멈추나 · 튀는 느낌 없나** · ⓒⓓ 제목·탭바를 못 넘나 · ⓔ 좌우 끌기 무반응 · ⓕ 재실행에도 자리 유지 · ⓖ 누르면 아무 일 없나. **B. 해시태그:** ㉠ 「추가하기」 · ㉡ 목록 캡션 `#태그` 보라. ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **17:39:34** · `App installed` 17:39:39. |
| **2026-09-21 17:17** | **`b04095e`** | **맥북** | ★ **손잡이가 보내신 스크린샷 그대로** — 바탕이 **회색 세로 그라데이션**(위 `#393E3F`·아래 `#444F55` · 테두리 없음) · 안의 `<`가 **두껍고 끝이 둥근 밝은 회색**(10×27.5 · 획 5.5) · 모서리가 원호 14 — 직전(`c0bdbbe` 17:07)은 앱 팔레트 색(보라빛 어두운 바탕 · hairline · SF 심볼 화살표) | ⛔ **판정 없이 덮였다**(17:39 `5d0f1ac` — 사용자 물음 둘(재질·튕기기)을 승인해 바로 올렸다). *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 둘(`edge-handle-design.md` §5-1 실측표 · §6 · `hashtag-design.md` §7-1). **A. 손잡이:** ⓐ **스크린샷과 같은가**(바탕색 · `<` 두께·크기·색 · 모서리) — 남은 차이를 말씀 · ⓑ 눌러 위아래로 끌면 따라오나 · ⓒ 제목 영역 위로 못 올라가나 · ⓓ 탭바 아래로 못 내려가나 · ⓔ 좌우로 끌어도 아무 일 없나 · ⓕ 앱을 끝내고 켜도 옮긴 자리인가 · ⓖ 누르면 아무 일 없나. **B. 해시태그:** ㉠ 버튼 글이 「추가하기」인가 · ㉡ 목록에서 태그 있는 줄의 `#태그`가 상세 칩과 같은 보라인가(살아있는 기억·검색·보관함도 같이). ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **17:17:02** · `App installed` 17:17:03. |
| **2026-09-21 17:07** | **`c0bdbbe`** | **맥북** | ★ **「새로운 기억」 오른쪽 가장자리 손잡이가 세로로 긴 둥근 네모(폭 24·높이 92)에 `‹` 하나**다 — 직전(`823beae`·17:06 `c0bdbbe-`)은 반알약·`»`/`«` · 상세 「해시태그」 절의 버튼이 **[추가하기]**(직전은 [붙이기]) · 태그 있는 줄의 캡션 끝 `#태그`가 **보라(강조색)** | ⛔ **판정 없이 덮였다**(17:17 `b04095e`) — 사용자 17:1x: *"내가 준 스크린샷의 모양 그대로 할 수 없을까?"* → 추정 치수·팔레트 색을 걷고 픽셀 실측으로 다시 그렸다. *(옛 볼 것 · 지우지 않는다)* ⏸ 판정 대기 둘(`edge-handle-design.md` §6 · `hashtag-design.md` §7-1). **A. 손잡이:** ⓐ 꼴·크기가 보내신 스크린샷과 맞나(**치수는 추정** · 다듬을 값 말씀) · ⓑ **눌러 위아래로 끌면 따라오나** · ⓒ **제목 영역 위로 못 올라가나** · ⓓ **탭바 아래로 못 내려가나** · ⓔ 좌우로 끌어도 아무 일 없나 · ⓕ 앱을 끝내고 켜도 옮긴 자리인가 · ⓖ 누르면 아무 일 없나. **B. 해시태그:** ㉠ 버튼 글이 「추가하기」인가 · ㉡ 「새로운 기억」 목록에서 태그 있는 줄의 `#태그`가 상세 칩과 같은 보라인가(⚠️ 살아있는 기억·검색·보관함도 같이 바뀌었다 — 뜻이 아니면 말씀). ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **17:07:54** · `App installed` 17:07:55. |
| **2026-09-21 17:06** | **`c0bdbbe-`**(커밋 전 작업 트리 · 손잡이 꼴만 옛것) | **맥북** | [추가하기] · 캡션 태그 보라 · 손잡이 위아래 이동 — **단 손잡이 꼴은 09-18 접힌 꼴 그대로**(스크린샷이 아직 안 와 있었다) | ⛔ **판정 없음 — 1분 뒤 17:07 빌드가 덮었다**(스크린샷이 그 사이에 도착했다). 이 줄은 「그 순간 폰에 무엇이 있었나」의 기록이다. ⚠️ 낡은 `.app` 삭제 · `EXIT=0` · `error: 0` · 실행파일 17:05:59 · `App installed` 17:06:04. |
| **2026-09-18 17:20** | **`823beae`** | **맥북** | ★ **「새로운 기억」 오른쪽 가장자리 세로 가운데에 `#` `»` 반알약 버튼이 있다** — 직전 빌드(`ebee442`)에는 없다 · 상세 「해시태그」 절의 [붙이기]에 **옅은 보라 바탕**이 있다(직전은 글자만) | ⏸ **판정 대기 — 둘** (설계 `edge-handle-design.md` §3 · `hashtag-design.md` §7). **A. 가장자리 버튼(ⓐ~ⓗ):** ⓐ 「새로운 기억」 오른쪽 가장자리 세로 가운데에 `#` `»` 반알약이 있나 · ⓑ **오른쪽으로 끌면** 곡선 부분만 남고 `«`가 보이나 · ⓒ 그것을 **누르면** 펼쳐지나 · ⓓ 접힌 것을 **왼쪽으로 끌어도** 펼쳐지나 · ⓔ 펼친 채 누르면 **아무 일도 없나**(미정 · 다음 단계) · ⓕ 접은 채 앱을 끝내고 다시 켜도 접힘이 유지되나 · ⓖ 상세로 들어가면 안 보이나 · ⓗ 다른 탭에는 없나 · **색·크기·자리가 SecondBrain 분위기에 맞나**(Claude가 골랐다 — 뒤집을 수 있다). **B. 해시태그 절 고침:** ㉠ 「이 분류에서 쓴 해시태그」 칩이 **2pt 커지고 원문 색**인가(14pt · 제목도 13pt) · ㉡ 「해시태그 추가」 자리표시자가 **밝아졌나** · ㉢ [붙이기]가 **밝고 바탕이 있어 버튼으로 보이나**(칸이 비면 옅어진다) · ⚠️ **문구는 하나도 안 바꿨다**(임시 둘 그대로 · 아직 안 고르셨다). ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **17:20:02** · `App installed` 17:20:07. |
| **2026-09-17 02:11** | **`ebee442`** | **맥미니** | ★ **상세에 「해시태그」 절이 새로 있다**(성역·분류 줄 아래 · 자료 카드 위 · 확정·미확정 둘 다) — 직전 빌드(`bdc3d21`)에는 없다 · 태그를 붙이면 **목록 줄 캡션 맨 뒤에 `#태그`**가 붙는다 | ✅ **여섯 다 「말한 대로 나온다」**(2026-09-17 02:2x · 폰 · 사용자: *"네 말대로 그대로 나온 것을 모두 확인했어. 그러나 고칠 것은 있어."*) · ⏸ **고칠 것 = 내일(09-18) 사용자가 말한다 — 아직 모른다**(무엇인지 짐작해 적지 않는다). *(옛 볼 것 · 지우지 않는다)* 볼 것 여섯: ⓐ **정보 기억 하나의 상세 → 「해시태그」 절 → 칸에 `맛집` → 엔터(또는 [붙이기])** → 칩 `#맛집`이 바로 생기나(저장 없이) · ⓑ **앞뒤 공백·앞 `#`·안 공백** — ` #서울 근교 `를 넣으면 `#서울 근교`가 되나 · `Cafe`와 `cafe`가 둘 다 붙나(대소문자 구분) · ⓒ **칩을 누르면** 칸에 그 글이 들어오고 [고치기]로 바뀌나 → 고치면 칩이 바뀌나 · [취소]로 빠지나 · ⓓ **칩의 ×** → 되묻지 않고 바로 지워지나 · ⓔ **다른 정보 기억의 상세**에 「이 분류에서 쓴 해시태그」로 `#맛집`이 나열되고 누르면 붙나 · **다른 분류**(예: 아이디어)의 상세에는 그것이 안 나오나 · ⓕ **「새로운 기억」·「살아있는 기억」 목록 줄 캡션 끝에 `#맛집`**이 보이나 · 태그 없는 줄에는 아무것도 안 붙나 · ⚠️ **미확정(임시) 기억의 상세에도 절이 보인다** — 뜻이 아니면 말씀(Claude 판단 자리 · 설계 §3). ⚠️ **문구 둘 임시**(「해시태그 추가」·「이 분류에서 쓴 해시태그」). ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **02:11:43** · `App installed` 02:11:50. |
| **2026-09-17 01:37** | **`bdc3d21`** | **맥미니** | ★ **사진을 붙인 채 강제 종료하고 다시 열면 「쓰다 만 기억」 줄의 캡션에 「사진 1」이 있고 줄을 누르면 사진이 보인다** — 직전 빌드(`ba4faa2`)는 글만 남고 사진이 사라졌다(01:26 재현 · `draft.log`: `restore … photos=1 found=0`) · 그 밖의 화면 차이는 없다(고친 것이 지우는 길 하나 + `draft.log`에 `discard …` 줄) | ✅ **ⓐⓑ 둘 다 통과**(2026-09-17 01:5x · 폰 · 사용자) · ✅ **`draft.log`(01:47 가져옴)가 추정을 닫았다** — 다섯 번 다 `discard … kept=1 closed=false`(강제 종료 = `onDisappear`) → `restore … found=1`. 설계 `capture-draft-design.md` §5-1. *(옛 볼 것 · 지우지 않는다)* 볼 것 둘: ⓐ ⛔⛔ **사진 재현 다시** — `+` → 사진 한 장 → 글 몇 자 → 앱 전환기에서 밀어 올려 강제 종료 → 다시 열어 줄 캡션에 **「사진 1」** · 줄을 눌러 **사진이 보이나** · ⓑ **[취소하기]로 나가면** 초안이 안 남나(절이 안 생겨야 한다 — 지우는 길을 만졌으므로 짝을 본다). **결과가 어느 쪽이든 말씀해 주세요** — `draft.log`의 `discard` 줄로 「누가 지웠나」의 추정을 닫습니다. ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **01:37:05** · `App installed` 01:37:56(두 번째 설치 · 첫 설치 01:37:29는 종료 코드를 못 읽어 다시 돌렸다). |
| **2026-09-15 17:58** | **`ba4faa2`** | **맥북** | ★ **「쓰다 만 기억」 줄을 누르면 첫 번에 그 글이 채워진 수집 화면이 열린다** — 직전 빌드(`5374b0e`)는 첫 번엔 비었고 두 번째에 채워졌다 · [쓰다 만 기억 불러오기] 글자가 조금 크다(18pt) | ✅ **ⓐⓑ 통과** · ⛔ **ⓒ 결함 — 사진이 안 살아난다**(2026-09-17 01:26 재현 · 폰 · 사용자 · 09-17 01:3x 맥미니에서 받았다) · ⚠️ **별개 보고:** 수집 창에서 사진을 찍으려 하자 **앱이 죽었다**(한 번 · 다시 하니 재현 안 됨 · 크래시 로그를 폰에서 가져와 본다). → **`draft.log`를 가져와 갈랐다**(01:32 · `devicectl copy from`): `01:26:46 move ok` → `persist … photos=["sb-photo-93554F16…jpg"]` → **`01:26:59 restore … photos=1 found=0`** — **적기·옮기기는 됐고 파일만 사라졌다**(JSON엔 사진 1 · 폴더는 남았고 비었다). JSON을 지우는 길(`closeDraft`)은 안 돌았고 **파일만 지우는 길은 `discardTemps` 하나**다(옮겨진 뒤에도 `draftPhotos`의 URL을 「임시 파일」로 알고 지운다 · 부른 자리는 `onDisappear`로 **추정** — 강제 종료의 화면 해체). → 고친다. *(옛 볼 것 · 지우지 않는다)* 볼 것 셋: ⓐ ⛔⛔ **줄을 누르면 첫 번에 글이 채워지나**(강제 종료 뒤 첫 열기에서도) · ⓑ **글자 크기가 원한 만큼인가** · ⓒ ⛔⛔ **사진 재현** — `+` → 사진 한 장 붙이기(카메라 또는 앨범 · 어느 쪽인지 말씀) → 글 몇 자 → 강제 종료 → 다시 열어 줄의 캡션에 **「사진 1」**이 있나 → 줄을 눌러 사진이 보이나. **보이든 안 보이든 그 뒤에 말씀해 주세요** — 폰의 `draft.log`를 맥으로 가져와 어디서 끊겼는지 읽습니다(적기·옮기기·되살리기 중 어느 줄인가). ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **17:58:06** · `App installed` 17:58:13. |
| **2026-09-15 17:18** | **`5374b0e`** | **맥북** | ★ **수집 화면에 글을 쓰다 앱을 강제 종료하고 다시 열면 「새로운 기억」 맨 아래에 「쓰다 만 기억」 절이 생기고 그 글이 줄로 보인다** — 직전 빌드(`ebc641c`)에는 그 절이 없고 글은 사라졌다 · `+`로 수집 화면을 열면 [저장 후 편집하기] 아래에 **[쓰다 만 기억 불러오기]**가 보인다(초안이 있을 때만) | ✅ **ⓐⓓⓕ 통과 · ⓒ 동작(글자 2pt 키우라)** · ⛔ **ⓑ 결함** — 줄을 누르면 글이 비고 `<`로 나와 다시 들어가면 나타난다(강제 종료 뒤에도 반복) · ⛔ **ⓔ 결함** — 사진이 안 살아난다(다시 들어가도) (2026-09-15 17:4x · 폰 · 사용자). → ⓑ 원인 = `sheet(isPresented:)` 첫 표시의 낡은 상태 → `sheet(item:)`으로 · ⓒ 18pt · ⓔ는 **못 가름** → 앱이 `draft.log`를 적게 하고 다음 재현에서 읽는다 → `ba4faa2`(17:58 설치). *(옛 볼 것 · 지우지 않는다)* 볼 것 여섯: ⓐ ⛔⛔ **`+` → 글 몇 자 → 앱 스위처에서 위로 밀어 강제 종료 → 다시 열기** → 「쓰다 만 기억」 절에 그 글이 있나 · ⓑ **그 줄을 누르면** 수집 화면이 그 글을 채워 열리나 · 거기서 **[저장 후 편집하기]** → 기억이 만들어지고 **절에서 그 줄이 사라지나** · ⓒ `+`로 새로 열어 **[쓰다 만 기억 불러오기]**가 보이나 → 글 없이 누르면 바로 채워지나 · **글을 몇 자 쓴 뒤** 누르면 **되묻나**(문구 임시) · ⓓ ⛔ **[취소하기]·`<`로 나가면 초안이 안 남나**(절이 안 생겨야 한다) · **글을 다 지우면** 초안이 사라지나 · ⓔ **사진을 붙인 채 강제 종료** → 되살린 초안에 사진이 있나 · 저장하면 그 사진이 기억에 붙나 · ⓕ **줄을 왼쪽으로 스와이프 → 삭제 → 되묻기**(문구 임시) → 지워지나 · ⚠️ **회귀 자리:** 불러온 글이 있는 채로 녹음 단추를 누르면 **앞글이 남고 뒤에 이어 붙나**(첫 낱말이 덮으면 결함) ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **17:18:39** · `App installed` 17:18:46. |
| **2026-09-14 19:34** | **`ebc641c`** | **맥북** | ★ **미확정 기억의 상세에서 [기억하기]를 누르면 「정말로 기억하시겠습니까?」 없이 바로 기억된다** — 직전 빌드(`7bbcc58`)는 재확인 대화상자가 떴다 · ★ **수집 화면에 글을 쓰다 가로로 돌려도 수집 화면이 그대로이고 글도 남아 있다** — 직전은 전 화면으로 튕기며 내용이 사라졌다 | ✅ **넷 다 통과**(2026-09-17 01:5x · 폰 · 사용자 — ⓒ 탭바는 「09-13에 새로 그린 탭바 그대로」로 확인) *(옛 볼 것 · 지우지 않는다)* 볼 것 넷: ⓐ **미확정 기억 상세 → [기억하기] → 팝업 없이 바로** 살아있는 기억으로 가나 · ⓑ ⛔⛔ **`+` 수집 화면에서 글을 몇 자 쓰고 가로로 돌리기** → 화면이 그대로이고 글이 남아 있나 · **세로로 되돌려도** 그대로인가 · ⓒ ⛔ **회귀 자리: 탭바 모습** — 세로 띠·가로 오른쪽 띠의 자리·크기·가운데 정렬이 09-13 판정 때와 같은가(**배치 방식을 바꿨다** — 값은 그대로 옮겼지만 폰에서 봐야 한다) · ⓓ ⛔ **회귀 자리: 액션 버튼(핫키)으로 연 수집 화면**도 회전에 견디나(그쪽은 `fullScreenCover`라 경로가 다르다 — 원래 안 튕겼을 수도 있다 · 못 잼) ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **19:34:33** · `App installed` 19:34:39. |
| **2026-09-14 17:49** | **`7bbcc58`** | **맥북** | ★ **「정보의 유효 기간 지남」 배너의 글자·아이콘이 직전보다 밝다**(75% · 바탕은 그대로) — 직전 빌드(`6b9b31c`)는 글자도 50%였다 | ✅ **통과**(2026-09-14 18:0x · 폰 · 사용자: *"배너 색 확인했어. 통과!"*) — **분류 재편 v2의 폰 판정이 전부 닫혔다**(ⓐⓑⓒⓓ 14:20 줄 · 색 여기 · ⓔ 대상 없음). *(옛 볼 것)* 볼 것 하나: **배너 글자 밝기가 원한 75%에 맞나**(바탕은 그대로여야 한다) ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **17:49:03** · `App installed` 17:49:19. |
| **2026-09-14 17:41** | **`6b9b31c`** | **맥북** | ★ **유효 기간이 지난 정보의 상세 배너 「정보의 유효 기간 지남」이 빨강(절반 밝기)이다** — 직전 빌드(`b6562e0`)는 **무채색(회색)**이었다 | ⚠️ **ⓐ 색 — 사용자가 다시 정했다**(17:4x · *"배너 색은 글자 명도 75%로 해줘. 바탕색은 그대로 유지"*) → `7bbcc58`(17:49 설치) · ✅ **ⓑ(=14:20 줄의 ⓔ) — 확인할 대상이 없어졌다**(17:5x 실데이터 셈): 13:55의 op으로 삭제된 약속 셋이 **이미 미분류(`type` 빈 값)**이고 데이터에 `promise`·`event` 값이 **하나도 안 남았다**(살아있는 것·삭제된 것 전부 · `inbox-state.py --type promise --all` = 0). 그래서 이 셋은 「미등록 key 폴백」이 아니라 **보통 미분류**로 그려진다 — 폴백 경로는 단위 시험(`ClassGateTests` 4번)이 지킨다. 사용자가 못 찾은 이유: 보관된 기억 탭이 **「완료된 기억」으로 열리고** 삭제된 것은 **세그먼트를 「삭제된 기억」으로 바꿔야** 보이며, 그 목록이 **146개**라 7월 항목이 깊이 있다. *(옛 볼 것 · 지우지 않는다)* 볼 것 둘: ⓐ **배너 색이 원한 「빨강 · 50% 명도」에 맞나**(지금 값 = 「지남」 coral을 반투명 50%로 · 더 진하게/연하게 원하면 말씀) · ⓑ **(14:20 줄의 ⓔ)** 보관된 기억 탭 → 삭제된 것 중 「회의 녹음 파일을 김 연희하고…」·「내일 아침에 출근하면서 꼭 전화해야 돼…」·「김형석 대표 만나야 됩니다」가 **회색 물음표(미분류)**인가 ⚠️ 설치 전 낡은 `.app` 삭제 · 빌드 `EXIT=0` · `error: 0` · 실행파일 **17:41:27** · `App installed` 17:41:41. |
| **2026-09-14 14:20** | **`b6562e0`** | **맥북** | ★ **분류 메뉴(카드 왼쪽 글리프 탭 · 상세의 분류 칸)에 「지식」(주황 책)·「추억」(분홍 하트)이 있고 「약속」·「일정」이 없다** — 직전 빌드(`836e79e`)는 약속·일정이 있고 지식·추억이 없었다 · 정보 항목의 상세에 「시간 설정」 카드가 생기고 그 줄 제목이 **「유효 기간」**이다(직전은 정보에 시간 설정 카드 자체가 없었다) | ✅ **ⓐⓑⓒⓓ 통과**(2026-09-14 17:3x · 폰 · 사용자: *"ⓐ 확인했음! 통과! ⓑ 확인 완료!! ⓒ 확인 완료! … ⓓ 확인했음!"*) · **ⓒ에서 색을 정했다** — *"배너는 빨간색으로 표시해주세요. 대략 50% 정도의 명도로"* → `6b9b31c`(17:41 설치) · ⏸ **ⓔ 대기** — 삭제된 항목은 검색에 안 걸려(`SearchView`는 live+done만) **보관된 기억 탭의 삭제 쪽에서 원문으로** 본다. *(옛 볼 것 · 지우지 않는다)* 볼 것 다섯: ⓐ **분류 메뉴에 지식·추억이 있고 약속·일정이 없나** · ⓑ **정보 항목을 열면 「시간 설정」 카드에 「유효 기간」 줄이 있나**(마감 줄은 없어야 한다) · ⓒ ⛔⛔ **유효 기간을 어제로 넣고 [저장]** → **살아있는 기억 목록에서 그 항목의 아이콘·글자가 회색**인가 · **상세 맨 위에 「정보의 유효 기간 지남」 배너**가 뜨나 · ⛔ **「지금 챙길 것」에는 안 떠야 한다**(뜨면 결함) · ⓓ ⛔ **회귀 자리: 할일의 「다시 보기」·「마감」은 그대로**인가(정보의 변화가 다른 분류로 새지 않았나) · ⓔ **보관함의 삭제된 옛 약속 셋**(「회의 녹음 파일을…」·「내일 아침에 출근하면서…」·「김형석 대표 만나야 됩니다」)이 **회색 물음표(미분류)**로 보이나 ⚠️ **설치 전에 낡은 `.app`을 지웠다** — 빌드 `EXIT=0` · `error: 0` · 실행파일 시각 **14:20:07** = 빌드 시각 · `installationURL` 확인(14:20:36). |
| **2026-09-13 22:38** | **`836e79e`** | **맥미니** | ★ **주차 위치에서 [기억하기]를 누르면 「다시 보기」에 오늘이 「노란색」으로 들어오고 아래에 「저장하지 않은 수정이 있어요」가 뜨며 [저장]이 켜진다** — 직전 빌드(`d8c7cd4`)는 **날짜가 검은색으로 들어오고 [저장]이 꺼져 있었다**(저장한 척) | ✅ **통과 — 다섯 다** (2026-09-13 22:4x · 폰 · 사용자: *"a가 통과되었어. b c d e 모두 확인했어!"*) ★ **손으로 하던 순서를 그대로 자동으로만 한 것이 답이었다.** *(옛 볼 것 · 지우지 않는다)* ⓐ 주차 새 기억 → **[기억하기]** → **노란 날짜 + 「저장하지 않은 수정이 있어요」 + [저장] 켜짐** · ⓑ ⛔⛔ **[저장]을 눌러** 나가면 **「지금 챙길 것」**에 있나 · ⓒ ⛔ **`<`로 그냥 나가면** — 「저장하지 않은 수정」 확인 팝업이 떠야 한다(버리면 날짜도 안 남는다) · ⓓ ⛔ **회귀 자리:** 이미 날짜가 있던 주차 항목은 **안 건드리나** · ⓔ ⛔ **회귀 자리:** **주차가 아닌 분류**는 [기억하기] 뒤에도 「다시 보기」가 **「없음」**인가 ⚠️ **두 번 틀린 뒤의 세 번째다** — ①값이 사라짐 ②저장까지 해버림(뜻이 아님) ③**자동 입력만**. |
| **2026-09-13 22:29** | **`d8c7cd4`** | **맥미니** | ★ **주차 위치를 [기억하기] 한 뒤 `<`로 나갔다 들어와도 「다시 보기」에 오늘 날짜가 남아 있다** — 직전 빌드(`da0f02a`)는 **화면에만 보이고 저장이 안 됐다**(나가면 「살아있는 기억」에 가 있었다) | ⛔ **뜻이 아니었다 — 되돌렸다** (2026-09-13) · ⚠️ **「안 됐다」가 아니다**: 값은 저장됐는데 **사용자가 원한 것은 자동 「저장」이 아니라 자동 「입력」**이었다 — *"기존 순서를 자동으로 해줘. 내가 손으로 [저장]을 눌러서 나갈게."* → `836e79e`에서 그렇게 했다. ⛔ **이 빌드의 방식(기억하기를 먼저 하고 커밋)을 되살리지 말 것.** *(옛 볼 것 · 지우지 않는다)* ⓐ 주차 새 기억 → **[기억하기]** → **`<`로 나갔다 다시 들어와** 「다시 보기」가 **오늘 그대로**인가 · ⓑ ⛔⛔ 그 항목이 **「지금 챙길 것」**에 들어가 있나(직전엔 「살아있는 기억」이었다) · ⓒ ⛔ **회귀 자리:** 이미 날짜가 있던 주차 항목은 **안 덮어쓰나** · ⓓ ⛔ **회귀 자리:** **주차가 아닌 분류**는 [기억하기] 뒤에도 「다시 보기」가 **「없음」**인가 · ⓔ ⛔ **「수정 ≠ 기억하기」** — 분류만 주차로 바꾸고 **[저장]만** 하면 **날짜가 안 생겨야** 한다 ⚠️ **자동 시험으로 못 막는 자리다** — 앱 층에 시험 타깃이 없다(못 잼). 실데이터로도 볼 수 있다: `python3 native/tools/inbox-state.py --id <앞 8자>`. |
| **2026-09-13 22:10** *(같은 것을 다시 깔았다 · 21:17과 같은 코드)* | **`da0f02a`** | **맥미니** | ★ **탭바의 아이콘·이름이 띠 한가운데에 있다** — 직전 빌드(`68e9c71`)는 **위로(가로에서는 한쪽으로) 쏠려** 있었다 (배경만 안전영역까지 뻗고 내용은 안 뻗었다) · ★ **주차 위치를 처음 [기억하기] 하면 「다시 보기」가 오늘 날짜로 채워진다**(직전은 「없음」) | ⚠️ **탭바 셋은 통과 · 주차는 여기서 결함이 드러났다** (2026-09-13) — 사용자: *"탭바는 마음에 들게 잘 정리되었어. 탭 내용, 눌리는 작동, 한가운데 정렬 등등 a b c 모두 정리되었어."* ⛔ **주차(ⓑ)에서 드러난 것:** 날짜가 화면에만 들어오고 **저장이 안 됐다** → `d8c7cd4` → `836e79e`로 갔다. *(옛 볼 것 · 지우지 않는다)* ⓐ **탭바 아이콘·이름이 띠 가운데인가** — **세로·가로 둘 다** (⚠️ **가로는 못 쟀다** — 시뮬레이터가 창을 잃어 회전을 못 시켰다) · ⓑ ⛔⛔ **주차 위치 새 기억을 [기억하기]** → **「다시 보기」가 오늘 날짜인가** · ⓒ ⛔ **회귀 자리: 이미 날짜를 넣어 둔 주차 항목은 안 덮어쓰나** · ⓓ ⛔ **회귀 자리: 주차가 아닌 분류는 「다시 보기」가 그대로 「없음」인가**(할일·약속 등 — **넣으면 안 되는 자리에 넣지 않았는지**) · ⓔ ⛔ **「수정 ≠ 기억하기」가 그대로인가** — 분류만 주차로 바꾸고 **저장**만 하면 **날짜가 안 생겨야** 한다(기억하기를 눌러야 생긴다) |
| **2026-09-13 20:17** | **`68e9c71`** | **맥미니** | ★ **세로 탭바가 떠 있는 캡슐이 아니라 화면 폭을 꽉 채운 띠다**(둥근 모서리·좌우 여백 없음 · 윗단에 가는 선) — 직전 빌드(`214b2fd`)는 **iOS 기본 캡슐 탭바**였다 · 가로에서도 **띠가 화면 오른쪽 끝까지 꽉 차고**(직전은 안쪽에 떠 있는 캡슐) **내용 칸이 크게 넓어졌다** | ⏸ **판정 대기** — 볼 것 다섯: ⓐ ⛔⛔ **가로를 두 방향 다** — **섬이 오른쪽인 회전에서 띠의 아이콘·글자가 섬에 가리지 않나** (띠가 화면 끝까지 가므로 그쪽 회전에서는 위험하다 · 가리면 말씀해 주세요) · ⓑ **띠 모양·크기가 보내주신 스크린샷과 같나** — 실측으로는 **윗단 781.3pt(참고 780.7pt)**로 맞췄다 · ⓒ ⛔⛔ **세로에서 탭이 다 눌리나** — **시스템 탭바를 버리고 직접 그렸다**(가장 위험한 자리) · ⓓ **내용이 띠 밑으로 안 가나**(세로·가로 둘 다) · ⓔ ⛔ **회귀 자리: 왼쪽 여백·노치**는 그대로인가 ⚠️ **19:59에 한 번 헛설치가 있었다** — 폰이 잠겨 빌드가 `EXIT=70`으로 죽었는데 `App installed`가 찍혔다(**빌드 함정 5**). `.app` 시각이 옛 빌드라 걸렀다. **이 줄은 다시 깐 것이다.** |
| **2026-09-13 19:59** | **`214b2fd`** | **맥미니** | ★ **가로에서 스크롤해도 노치(섬) 밑으로 카드가 안 들어간다** — 직전 빌드(`89567a4`)는 **원칙 카드 왼쪽이 섬에 물렸다** · 내용 시작 자리가 **39 → 54pt**로 조금 물러났다 | ⏸ **판정 대기** — 볼 것 셋: ⓐ ⛔⛔ **가로에서 위아래로 스크롤하며 두 방향 다** — 섬 밑으로 **어떤 것도** 안 들어가나 (카드 바탕·글자·아이콘 전부) · ⓑ **왼쪽 여백이 이제 알맞나** — ⚠️ **너무 넓으면 말씀해 주세요**: 섬 안쪽 끝이 **50~52pt**라 그보다 안쪽으로는 못 간다. **지금은 54pt**(끝 +2pt)이고, 카드 왼쪽 끝이 68pt다(직전 78pt → 지금 68pt) · ⓒ ⛔ **회귀 자리: 세로가 그대로인가** ★ **알아낸 것:** 시스템 「‹」 알약이 39pt에 있을 수 있는 이유는 **그것이 섬보다 위**(y 35~57pt)에 있기 때문이다 — **섬은 가장자리의 가운데(y 143~260pt)에만 있다.** 목록은 그 높이를 지나가므로 같은 39pt를 쓸 수 없다. |
| **2026-09-13 13:31** | **`89567a4`** | **맥미니** | ★ **가로 세로 띠에 둥근 캡슐 배경이 생겼고 화면 끝에서 21pt 떨어져 떠 있다** — 직전 빌드(`a2920cf`)는 **배경 없이 바탕에 얹혀 있었다** · **가로에서 제목·카드가 왼쪽으로 39pt 자리까지 왔다**(직전은 78pt) · 세로에서 **`+`가 약간 굵어졌다** | ⏸ **판정 대기** — 볼 것 다섯: ⓐ ⛔⛔⛔ **가로를 두 방향 다 돌려 섬(노치)이 글자를 가리지 않나** — **시뮬에서 섬이 왼쪽인 회전일 때 원칙 카드를 덮었다.** 시스템도 같은 39pt 자리를 쓰지만 우리 내용은 글자라 가리면 못 읽는다. **가리면 말씀해 주세요 — 목표를 39 → 52pt쯤으로 올리면 된다**(섬 안쪽 끝 실측 ≈48pt) · ⓑ **왼쪽 여백이 이제 알맞나** — 시스템 「‹ 새로운 기억」 알약과 같은 자리다 · ⓒ **띠의 캡슐 배경·오른쪽 21pt 여백이 뜻대로인가**(세로 시스템 탭바를 재서 옮긴 값) · ⓓ **`+` 굵기**(`.semibold` — 더/덜 굵게도 됩니다) · ⓔ ⛔ **회귀 자리: 세로가 그대로인가** — 왼쪽 밀기는 가로에서만 돈다(세로는 밀 것이 없다) ⚠️ **띠가 15pt 두꺼워졌다**(76 → 91 = 캡슐 70 + 여백 21) — 내용 칸이 그만큼 줄었습니다. |
| **2026-09-13 12:46** | **`a2920cf`** | **맥미니** | ★ **가로로 여러 번 돌려도 화면이 안 언다** — 직전 빌드(`5ce0c7f`)는 **연달아 여섯 번 돌리면 그림이 멎었다**(프로세스는 살아 있고 화면만 안 바뀐다) · **가로 좌우 여백은 직전 빌드처럼 줄어 있지 않다**(그 기능을 되돌렸다 — 그것이 얼린 원인이었다) | ⏸ **판정 대기** — ⛔ **먼저 앱을 완전히 끝냈다 켜 주세요**(얼어 있던 프로세스가 남아 있습니다). 볼 것 여섯: ⓐ ⛔⛔ **가로·세로를 여러 번 돌려도 안 어나** — **이번에 고친 자리다** · ⓑ **다섯 탭 제목이 같은 자리·같은 크기인가**(세로·가로 둘 다) · ⓒ ⛔⛔ **검색이 그대로 되나** — 걸러지나 · **ID로도 찾아지나** · **x로 지워지나** · ⚠️ **시스템 검색창이 아니다**(당겨서 숨기기·[취소]가 없다 — 괜찮은지 판단해 주세요) · ⓓ ⛔ **회귀 자리: 상세에서 `<` 글자가 온 화면 이름 그대로인가** · ⓔ **`+` 크기가 맞나**(30pt) · ⓕ **설정·보관된 기억 목록이 제목 아래에서 제대로 스크롤되나** ⚠️ **가로 여백은 그대로 큽니다** — 되돌렸습니다. 왜 그런지·다시 하려면 무엇을 조심할지는 `RootView.tabShell` 머리주석에 남겼습니다. |
| **2026-09-13 12:35** | **`5ce0c7f`** | **맥미니** | ★ **검색·보관된 기억·설정의 제목이 「새로운 기억」과 같은 자리·같은 크기로 왼쪽에 붙는다** — 직전 빌드(`569c712`)는 그 셋이 **시스템 큰 제목**이라 더 아래·더 오른쪽이었고 **스크롤하면 접혔다** · **검색창이 내비게이션 바가 아니라 제목 바로 아래 네모**로 바뀌었다 · `+`가 한 번 더 커졌다 | ⏸ **판정 대기** — 볼 것 여섯: ⓐ **다섯 탭 제목이 같은 자리·같은 크기인가**(세로·가로 둘 다) · ⓑ ⛔⛔ **검색이 그대로 되나** — 글자를 넣으면 걸러지나 · **ID로도 찾아지나** · **x로 지워지나** · ⚠️ **시스템 검색창이 아니다**(당겨서 숨기기·[취소]가 없다 — 괜찮은지 판단해 주세요) · ⓒ ⛔ **회귀 자리: 상세에서 `<` 글자가 온 화면 이름 그대로인가**(「새로운 기억」·「살아있는 기억」·「검색」·「원칙」 — `.navigationTitle`을 걷어냈다) · ⓓ **`+` 크기가 이제 맞나**(28 → 30pt) · ⓔ **가로 여백** — ⚠️ **한쪽만 줄었다**: **섬이 없는 쪽**만 되찾는다(양쪽을 없애면 탭바 글자가 섬에 잘린다 · 실제로 봤다). **두 방향 다 돌려** 어느 쪽이 줄었는지 봐 주세요 · ⓕ **설정·보관된 기억 목록이 제목 아래에서 제대로 스크롤되나**(구조를 바꿨다) ⚠️ **못 줄인 것:** 내용 쪽 62pt는 `TabView`·`NavigationStack`이 다시 넣어서 이 방법으로는 안 줄었다. |
| **2026-09-13 12:02** | **`569c712`** | **맥미니** | ★ **가로 세로 띠의 아이콘이 똑바로 서 있고 이름이 그 아래에 좌→우로 놓인다** — 직전 빌드(`613308a`)는 **아이콘까지 누워** 쟁반·보관함이 옆으로 섰고 글자는 위→아래로 읽혔다 · 띠 폭도 **64 → 76pt**(글자가 눕지 않으니 가장 긴 이름 「살아있는 기억」 60.3pt가 폭을 정한다) | ⏸ **판정 대기** — 볼 것 넷: ⓐ **아이콘이 세로 볼 때와 같은 방향인가** · ⓑ **이름이 아이콘 아래에 좌→우로** 읽히나 · ⓒ ⛔ **회귀 자리: 누르는 자리가 그대로 맞나** — **회전을 걷어냈으므로 다시 봐야 한다**(직전 빌드에서 통과한 ⓐ다) · ⓓ **「살아있는 기억」이 안 잘리나**(가장 긴 이름 · 글자 크기를 키우면 줄어들게 해 뒀다) ⚠️ **폭이 12pt 넓어져 내용 칸이 그만큼 줄었다** — 좁아 보이면 말씀해 주세요. |
| **2026-09-13 11:44** | **`613308a`** | **맥미니** | ★ **가로로 돌리면 탭바가 화면 오른쪽에 세로 띠로 선다**(글자·아이콘이 시계 방향 90°로 누워 있다) — 직전 빌드(`0a6c8b9`)는 **아래쪽에 떠 있는 캡슐**이었고 **내용이 그 밑으로 지나갔다** · 세로에서는 **우측 상단 `+`가 눈에 띄게 커졌다**(제목 글자 높이에 맞췄다) | ⏸ **판정 대기** — 볼 것 다섯: ⓐ ⛔⛔ **세로 띠의 탭을 눌러 실제로 화면이 바뀌나** — **가장 위험한 자리다**(띠를 90° 돌려 그려서 **누르는 자리가 어긋날 수 있다**) · ⓑ **내용이 띠 밑으로 안 가나** — 카드 오른쪽 끝이 띠에 안 물리나 · ⓒ **눕힌 방향이 뜻대로인가** — 글자 머리가 **왼쪽**, **위에서 아래로** 읽히나 · ⓓ ⛔ **회귀 자리: 세로로 되돌리면 원래 탭바 그대로인가**(모습·자리·여백) · ⓔ **`+` 크기가 제목과 맞나** — ⚠️ **너무 커졌으면 말씀해 주세요**(잰 값으로 골랐지만 **어울리는지는 판단 사안**입니다) ⚠️ **가로에서 상세 화면·수집 화면까지는 안 밟았다**(못 잼) — 거기서도 띠가 제자리인지 함께 봐 주시면 좋습니다. |
| **2026-09-13 11:11** | **`0a6c8b9`** | **맥미니** | ★ **앱을 띄워 둔 채 폰을 가로로 돌리면 글자가 멀쩡하다** — 직전 빌드(`62041e8`)는 **가로로 늘고 세로로 눌려 두 줄이 겹쳐 보였다**(글자 크기가 변한 것이 아니라 **옛 그림이 새 칸에 늘어난 것**) | ⏸ **판정 대기** — 볼 것 넷: ⓐ **「새로운 기억」을 띄운 채 가로로 돌린다** — 원문 글자가 **안 찌그러지나** (⛔ **여러 줄짜리 원칙·기억이 판별력 있는 표본이다** — 한 줄짜리는 원래도 멀쩡했다) · ⓑ **줄이 새 폭으로 다시 나뉘나** — 가로에서 **「나 / 를」처럼 옛 폭의 자리에서 끊기지 않나** · ⓒ **다시 세로로 돌렸을 때도** 멀쩡하나(**회귀 자리** — 되돌아올 때도 다시 그려야 한다) · ⓓ **상세 화면·검색 결과에서도** 같은가(같은 그리기를 쓰는 다른 자리) ⚠️ **안 고친 것 하나:** 가로에서 **떠 있는 탭바가 목록을 가리는 것**은 그대로다 — **iOS 26이 탭바를 캡슐로 띄우고 내용이 그 아래로 지나가게 하는 기본 동작**이고, 우리 코드에 바닥 여백을 따로 준 자리가 없다(`InboxView`·`LivingView`·`ArchiveView` grep). **바꿀지는 사용자 결정 사안이다.** |
| **2026-09-13 10:41** | **`62041e8`** | **맥미니** | ★ **액션 버튼으로 들어와 [취소하기]를 누르면 앱 전환기에 카드가 남지 않는다**(앱이 정말 끝난다) — 직전 빌드(`fed23a0`)는 **화면만 미끄러져 내려가고 카드가 남아 있었다** · ⚠️ **근거와 판정이 같은 자리다**(`759bd32` 줄과 같은 형태) — **이 빌드가 고친 것이 그것뿐이라 다른 확정 근거를 만들 수 없었다** | ✅ **통과 — 셋 다** (2026-09-13 10:5x · 폰 + 맥 계측) · ⛔⛔ **★ 판정 잣대가 틀려 있었다 — 그것이 오늘의 소득이다.** **앱 전환기 카드는 스냅샷이라 프로세스가 죽어도 남는다** — 09-02부터 「카드가 남는다 = 앱이 살아있다」로 읽어 왔는데 **고쳤든 안 고쳤든 같은 값**이었다(메모리 「판정엔 구분되는 값」의 형태). ✅ **가르는 계측:** `xcrun devicectl device info processes --device <CoreDevice UUID>` 에서 `SecondBrain` 찾기 — **화면을 안 본다.** **대조군이 같은 빌드 안에 있었다:** **ⓒ 직후 10:51:30 = `pid 2961` 살아 있음** · **ⓐ 직후 10:57:45 = 없음**(510줄 중) — **설계대로 갈렸다.** ⓑ 사용자: *"ⓑ는 아주 좋아"*(미끄러져 내려간 뒤 끝난다 — 08-31의 「휙 사라져」로 안 돌아갔다). ⓒ **앱이 떠 있던 경우 앱 밖으로 나가는 것은 맞다**(`suspend` = 홈 단추와 같다) — ⚠️ **내가 볼 것을 적을 때 「끝나면 안 된다」만 적고 「그래도 화면 밖으로는 나간다」를 빠뜨려** 사용자가 결함으로 읽었다(기록 규칙 11의 형태 — 조건이 빠진 참인 문장). ⚠️⚠️ **못 잼 하나:** 이 빌드가 고친 것인지 **`fed23a0`에서 이미 되고 있었는지**는 **안 쟀다** — 옛 판정들이 **가릴 수 없는 잣대**로 내려졌기 때문이다. **값은 지금 맞고, 언제부터 맞았는지는 모른다.** — *(옛 볼 것 · 지우지 않는다)* ⓐ ⛔⛔ **앱을 완전히 끝낸 뒤** 액션 버튼 → [취소하기] → **앱 전환기에 카드가 없나**(이번에 고친 자리) · ⓑ **나가는 모습이 그대로인가** — 수집 화면이 **미끄러져 내려간 뒤** 끝나나 (⛔ **「휙 사라지면」 08-31에 반려된 그 모습이다** — *"너무 허무하게 끝나 … 섭섭해"*) · ⓒ ⛔ **회귀 자리: 앱이 이미 떠 있을 때**는 액션 버튼 → [취소하기]로 **끝나면 안 된다** — **그때 보던 화면으로 되돌아간 뒤 내려놓기만** 한다(다시 열면 그 화면이 있다) |
| **2026-09-03 05:54** | **`fed23a0`** | **맥미니** | ★ **[사진 사용]을 누르면 수집 화면으로 돌아온다 — 카메라가 다시 열리지 않는다**(한 장만) · 직전 빌드(`1a730cc`)는 **닫힌 뒤 곧바로 다시 열렸다** | ✅ **통과** (2026-09-03 06:0x · 폰 · 사용자: *"다 통과했어."*) — 볼 것 넷 전부: ⓐ 사진 찍기가 **한 장으로 끝난다** ⓑ **셔터 두 번에도 안 멈춘다**(문 `done`) ⓒ **앨범 여러 장·순서** 그대로 ⓓ 원문·앞서 붙인 사진이 남는다 |
| **2026-09-03 05:43** | **`1a730cc`** | **맥미니** | ★ **[사진 사용]을 누르면 카메라가 닫힌 뒤 곧바로 다시 열린다**(연달아 찍기) — 직전 빌드(`d7b8006`)는 **한 장 찍으면 수집 화면으로 나가 버렸다** · ⚠️ **사이에 닫힘·열림 애니메이션이 한 번씩 보인다**(겹치지 않게 하는 값) | ⛔ **되돌렸다** (2026-09-03 · 사용자: *"사진찍기는 연속으로 하지 말자. 생소하다."*) — **동작은 했고 뜻이 아니었다.** ⚠️ **「실패」로 읽지 말 것** · 되살리지 말 것(`SystemCamera` 머리주석) · ✅ **ⓑ(셔터 두 번 멈춤)의 문은 남겼다** — 다음 줄에서 통과했다. 옛 볼 것: ⓐ [사진 사용] → 카메라가 다시 열리나 ⓑ ⛔⛔ **촬영 단추를 빠르게 두 번 눌러도 앱이 멈추지 않나** — **직전 빌드가 멈췄던 자리다** ⓒ 카메라 **[취소]**로 나가면 **찍어 둔 사진이 카드에 그대로 남나**(⛔ 사이사이 정리가 돌면 사라진다) ⓓ 앨범 여러 장 고르기(`d7b8006`의 ⓐⓑ)는 **그대로 되나** |
| **2026-09-03 05:29** | **`d7b8006`** | **맥미니** | ★ **[앨범에서 고르기]가 「여러 장 고르고 [추가]」 화면으로 뜬다** — 사진을 눌러도 **바로 닫히지 않고 선택 표시만 붙는다.** 직전 빌드(`5f5b111`)는 **누르는 순간 닫혔다**(한 장 모드) | ✅ **앨범은 통과 · 카메라 연달아는 되돌렸다** (2026-09-03 · 아래 두 줄에서 갈렸다) — ⚠️ **이 줄에서 바로 판정되지 않았다**(그 시험이 **카메라에서 멈췄다**: *"카메라에서 시험이 멈췄어"*) · **앨범 ⓐⓑ는 `fed23a0`에서 통과**로 확인됐다. 볼 것: ⓐ 앨범 **여러 장 → [추가]** · 순서 ⓑ ⛔ **빠른 탭 결함이 사라졌나** — 빨리 눌러도 **의도한 장수만** 들어오나 ⓒ **카메라가 연달아 찍히나** — 한 장 찍고 [사진 사용] 뒤 **촬영 화면으로 돌아오나** (⚠️ **못 잼** — 시뮬에 카메라가 없다 · **닫히면 실패다**) ⓓ ⚠️ **나가는 단추가 시스템 [취소]다** — 눌렀을 때 **찍어 둔 사진이 그대로 남나** (코드는 남기지만 **말이 「취소」라 헷갈리는지는 사용자 판단**이다) ⓔ **상세 화면**에서도 앨범 여러 장·연달아 찍기가 되나 |
| **2026-09-03 05:13** | **`5f5b111`** | **맥미니** | ★ **액션 버튼으로 들어와 [저장 후 편집하기]를 누르면 그 기억의 상세 화면으로 넘어간다** — 직전 빌드(`a2626ac`)는 **화면이 그대로였다**(저장은 됐고 `dismiss()`가 뿌리에서는 아무 일도 안 했다) | ✅ **통과** (2026-09-03 · 폰 · 사용자: *"다 통과되었어."*) — 넷 전부. 볼 것: ⓐ **액션 버튼** → [저장 후 편집하기] → **상세로 넘어간다** ⓑ **`+`로 들어왔을 때도** 그대로 상세로 가나(⛔ **회귀 자리** — 이 길은 원래 됐다) ⓒ ⛔ **뷰어를 열었다 닫은 뒤에도** 붙여 둔 사진들이 **그대로 있나** · 말한 원문도 남아 있나 — ⚠️ **뷰어가 덮는 동안 정리가 돌 수 있었다**(어제 붙인 덮개에 가드가 없었다) ⓓ 사진 셋 중 **하나만 X로 지우면 나머지 둘이 남나**(어제 판정 ⓒ의 판별력 있는 자리) |
| **2026-09-03 05:03** | **`a2626ac`** | **맥미니** | ★ **수집 화면에서 사진 네모를 누르면 뷰어가 뜬다** — 하단에 **썸네일 줄**이 있고 **썸네일 우측 상단에 빨간 X**가 붙어 있다. 직전 빌드(`3187ccb`)는 **첫 장만 크게 보이고 썸네일 줄도 X도 없었다** · 상세 뷰어에도 **X가 새로 생겼다** | ✅ **통과** — ⚠️ **이 줄에서 바로 판정되지 않았다**: 이 빌드로 시험하다 **[저장 후 편집하기]가 아무 일도 안 하는 결함**이 드러나 `5f5b111`로 넘어갔고, 거기서 사용자가 *"다 통과되었어"*라고 판정했다(뷰어·빨간 X·확인 팝업 포함). ⛔ **그래서 이 줄만 읽고 「판정 안 됨」으로 보지 말 것.** 볼 것: ⓐ **수집** 화면 사진 네모 → 뷰어 ⓑ 썸네일 **우측 상단 빨간 X**가 보이나 — ⚠️ **30pt다**(권장 44) · **네모를 고르려다 X가 눌리지 않나** ⓒ X → **「사진을 지울까요? / 되돌릴 수 없어요. / 원본은 그대로 있어요.」** 팝업이 뜨고 [지우기]로 지워지나 ⓓ **상세**(저장된 기억) 뷰어에서도 같은 방식으로 지워지나 — ⚠️ **여기가 사본까지 지우는 쪽이다** ⓔ ⛔ **회귀 자리:** 상세의 **음성 뷰어에는 X가 없어야 한다**(사진만이다) |
| **2026-09-03 04:27** | **`3187ccb`** | **맥미니** | ★ **「보조 자료」 → [앨범에서 고르기] → 사진을 고르면 카드에 네모가 실제로 생긴다** — 직전 빌드들(`759bd32`까지)은 **선택기만 닫히고 아무것도 안 붙었다**(HEIC에 JPEG 표현이 없었다) | ✅ **통과** (2026-09-03 04:3x · 폰 · 사용자: *"앨범에서 고르기 잘 동작해! 통과."*) · ⚠️ **조건을 적는다**(계측 규칙 8): **사용자가 밟은 자리까지만 확인됐다** — 어느 화면(수집/상세)에서 몇 장을 밟았는지는 **안 물었다**. ⛔ **[사진 찍기]는 이번에 안 건드렸고, 눌러 봤는지도 못 잼.** |
| **2026-09-02 02:13** | **`759bd32`** | **맥미니** | ★ **액션 버튼 → [취소하기] 뒤 앱 전환기에 카드가 없다**(앱이 정말 끝난다) — 직전 빌드(`4956097` · 02:02)는 **화면만 내려가고 카드가 남아 있었다**(`exit(0)`이 안 돌았다) · ⚠️ **근거와 판정이 같은 자리다** — 이 빌드가 고친 것이 그것뿐이라 **다른 확정 근거를 만들 수 없었다** | ⛔ **미결** (2026-09-02 02:2x · 사용자: *"아직 해결안 됨. 미결된 것으로 하자."*) — **「깨웠으면 끝낸다」가 아직 안 된다.** ✅ **나가는 모습은 닫혔다**(*"새로운 화면 안거치고 깨끗하게 나와지네. 이건 좋아."* · `4956097`에서 통과) · ⛔ **남은 것은 「앱이 정말 끝나나」 하나다.** **증상(02:3x):** ✅ **목록은 안 스친다** · ⛔ **앱이 안 끝난다** — **앱 전환기에 카드가 남아 있다** (2026-09-02 02:3x · 폰 · 사용자). ★ **`exit(0)`이 아직도 안 돈다** — `beginBackgroundTask`로도 안 열렸다. 밟은 길 셋과 남은 후보 → `docs/worklog/2026-09-02-macmini.md` §3 |
| **2026-09-02 02:02** | **`4956097`** | **맥미니** | ★ **「살아있는 기억」에서 연 상세의 `<`가 「‹ 살아있는 기억」이다** — 직전 빌드(`c3c3b77` · 00:36)는 **거기서도 「‹ 새로운 기억」**이었다(줄 하나를 두 화면이 함께 쓰는데 이름을 그 줄 안에 박았다) | ⚠️ **②는 통과 · ③은 실패** (2026-09-02 · 폰) — ② 사용자: *"2번 통과"* · ③ 사용자: *"깨끗하게 나와지네. 이건 좋아. 그런데.. 앱이 살아있어."* → **`exit(0)`이 안 돌고 있었다**(다음 줄 `759bd32`에서 고치려 했고 그것도 미결). 볼 것: ② 상세의 `<` 네 자리 — 「새로운 기억」 · **「살아있는 기억」** · 「검색」 결과 · **원칙 띠 → 원칙 목록 → 그 원칙** ③ 앱을 완전히 끝낸 뒤 **액션 버튼 → [취소하기]** → **목록이 안 스치고** 수집 화면 그대로 미끄러져 내려가며 앱이 끝나나 — ⚠️ **오래 머문 뒤 눌러도 끝나야 한다**(3초 문턱을 걷어냈다) |
| **2026-09-02 00:36** | **`c3c3b77`** | **맥미니** | ★ **가운데 제목이 「기억 수집」이고 좌측 상단이 「‹ 새로운 기억」** — 직전 빌드(`eceb868` · 08-31 05:21)는 **둘 다 「새 기억」**이었다(그래서 두 번 읽혔다) · 상세의 `<`도 **온 화면의 이름**이 된다(「새로운 기억」·「살아있는 기억」·「검색」·「원칙」) | ⚠️ **①은 통과 · ②는 깨졌다 · ③은 실패** (2026-09-02 · 폰 · 사용자: *"1번 통과. 2번은 「살아있는 기억」에서 상세 화면 갔을 때 `<` 버튼 제목이 「살아있는 기억」이 아닌 것을 확인함. 3번은 전혀 안 바뀌었음."*) → ②는 **줄 하나를 두 화면이 함께 쓰는데 이름을 박아** 둔 탓이었다(`4956097`에서 고쳐 통과) · ③은 **붙잡는 방식이 늦었다**. 볼 것: ① 「‹ 새로운 기억」 + 「기억 수집」 ② **상세**의 `<` 글자가 **온 화면의 이름**인가 — ⚠️ **네 자리를 다 밟아야 갈린다**: 「새로운 기억」 목록 · 「살아있는 기억」 · 「검색」 결과 · **원칙 띠 → 원칙 목록**(같은 스택인데 이름이 달라야 한다) ③ 앱을 완전히 끝낸 뒤 **액션 버튼 → [취소하기]** 때 **「새로운 기억」이 안 보이고** 수집 화면 그대로 미끄러져 내려가나 |
| **2026-08-31 05:21** | **`eceb868`** | **맥미니** | ★ **좌측 상단이 아이콘만이 아니라 「‹ 새 기억」(글자가 보인다)** — 직전 빌드(`f102879` · 05:15)는 **동그라미 안 `‹` 아이콘뿐**이었다(`Label`이 글자를 안 그렸다) · 상세도 「‹ 기억」이 된다 | ⚠️ **①은 통과 · ②는 거슬렸다 · ③은 실패** (2026-09-02 · 폰 · 사용자: *"②는 거슬려!!"*) → ②에서 **규칙이 뒤집혔다**: `<`의 글자는 **갈 곳의 이름**이고 제목은 **「기억 수집」**이 됐다(`c3c3b77`) · ③은 그 뒤로도 이어져 **미결**이다. 볼 것: ① 글자가 보이나 ② ⚠️ **왼쪽 「‹ 새 기억」과 가운데 제목 「새 기억」이 두 번 읽히는 것이 괜찮나** ③ 액션 버튼 → [취소하기] 때 **미끄러져 내려간 뒤 끝나나**(휙 사라지지 않나) |
| **2026-08-31 05:15** | **`f102879`** | **맥미니** | ★ **액션 버튼으로 들어올 때 「새로운 기억」이 한 순간도 안 보인다** — 직전 빌드(`1391ea5` · 04:58)는 **짧게 스쳤다**(커버가 한 프레임 뒤에 올라와서) | ✅ **셋 다 통과** (2026-08-31 05:1x · 폰) — 사용자: *"오! 바로 뜬다. 앱을 완전히 죽이고 액션버튼 누르고 들어오니 새로운 기억 창이 **전혀 안 보임**."* ① ✅ ② ✅ *"< 버튼 누르면 새로운 기억 화면으로 간다"* ③ ✅ 앱이 끝난다<br>★★ **이것으로 「바로 수집 화면」이 닫혔다** — 왕복 넷 끝(애니메이션 끄기 → 120ms 문 → 계측 장치 → **URL 스킴 + 뿌리로 그리기**).<br>⛔ **함께 온 요구 둘:** ㉠ *"너무 허무하게 끝나 … 섭섭해"* ㉡ *"< 글자는 붙이자"* → 아래 줄에서 했다 |
| **2026-08-31 04:58** | **`1391ea5`** | **맥미니** | ★ **사파리 주소창에 `secondbrain://capture` 를 넣으면 앱이 열리고 「바로 수집 화면」이 뜬다** — 직전 빌드(`8493560` · 04:52)는 **그 주소를 아무도 받지 않았다**(앱이 안 열린다) | ⚠️ **하나 통과(시뮬) · 셋 남았다** — ① ✅ **된다** (2026-08-31 05:0x · 사용자: *"시뮬레이터에서 SAFARI로 해보니까 그 URL이 바로 수집화면으로 가는 것 같다"*). ★ **내가 앞서 잰 것과 맞는다** — 맥미니에서 앱을 **죽인 뒤** `simctl openurl`로 콜드 런치해 **목록도 탭바도 없이 수집 화면**이 뜬 것을 스크린샷으로 봤다.<br>⚠️⚠️ **조건을 적는다**(계측 규칙 8): **시뮬레이터 · 사파리**다. ⛔ **실기기에서도 같은지는 못 잼** — 랩·시뮬은 실제보다 유리하다. ⛔ **단축어의 「URL 열기」로 액션 버튼을 묶었을 때는 아직 안 밟았다** — **그 길이 진짜 목표**이고 **확인 팝업이 뜨는지도 그때 갈린다**.<br>② ⏸ **단축어·액션 버튼** ③ ⏸ 보통 실행이 굼뜨지 않나 ④ ⏸ 옛 App Intent 경로<br>⚠️ **사용자 관찰(05:1x): 「SecondBrain에서 이 페이지를 열겠습니까?」 팝업이 뜬다.**<br>　⛔ **이것만으로 이 길을 접지 않는다 — 조건이 「사파리」다:**<br>　**ⓐ 사파리는 커스텀 스킴을 열 때 늘 묻는다**(사파리 정책 · 우리 코드가 아니다) ·<br>　**ⓑ 단축어의 「URL 열기」는 대개 안 묻는다 — ⛔ 못 잼** ·<br>　**ⓒ 시뮬 실측에서 그 팝업은 첫 번째만 떴다**(앱을 한 번 실행한 뒤엔 안 떴다).<br>　▶ **가르는 시험 = 단축어로 액션 버튼을 묶어 「앱이 죽은 상태에서」 누르는 것.**<br>　✅ **밟았다 — 통과** (2026-08-31 05:1x · 폰 · 사용자: *"단축어로 액션 버튼 묶어서 해봤는데 팝업 안 뜨고 바로 수집화면 나와"*). ★ **사파리 결과로 접었으면 틀렸을 것이다** — 조건이 「사파리」였다.<br>　⛔ **남은 것 하나:** *"짧은 시간동안 새로운 기억 화면이 띄긴 하네. 짧게."* → 아래 줄에서 고쳤다 |
| **2026-08-31 04:47** | **`8723e23`** | **맥미니** | ★ **핫키(액션 버튼·단축어)로 들어오면 수집 화면이 「전체 화면」이다 — 아래에 목록도 탭바도 안 보인다** — 직전 빌드(`549755b` · 02:58)는 **카드처럼 얹혀 아래 목록이 보였다** | ⏸ **판정 대기** — 볼 것 넷: ① **핫키로 들어올 때 「새로운 기억」이 스치나**(안 스치는 것이 목표) ② 수집 화면이 **전체 화면인가**(탭바가 안 보이나) ③ **앱 안의 `+`는 그대로 카드처럼** 뜨나 ④ ⚠️ **보통 실행(아이콘으로 열기)이 굼떠 보이지 않나**(문 120ms) |
| **2026-08-31 02:58** | **`549755b`** | **맥미니** | ★ **수집 화면 좌측 상단이 `<`만이 아니라 「‹ 새 기억」(글자가 붙어 있다)** — 직전 빌드(`1de57bc` · 02:50)는 **아이콘만**이었다 | ⏸ **판정 대기** — 볼 것 넷: ① 「‹ 새 기억」으로 보이나(상세의 「‹ 기억」과 같은 꼴인가) ② **앱을 쓰던 중** 핫키 → 수집 → [취소하기] → **그때 보던 탭으로 돌아간 뒤 앱이 내려가나**(다시 열면 그 화면인가) ③ **앱을 끝낸 상태**에서 핫키 → 수집 → [취소하기] → **앱이 끝나나** ④ 어느 경우든 `<`는 **「새로운 기억」에 남나**(앱이 안 나가나) |
| **2026-08-31 02:50** | **`1de57bc`** | **맥미니** | ★ **핫키(액션 버튼·단축어)로 수집 화면에 들어와도 좌측 상단에 `<`가 있다** — 직전 빌드(`f44c4d6` · 02:40)는 **그 경우에만 `<`가 없었다** | ⏸ **판정 대기** — 볼 것 넷: ① 핫키로 들어와도 **`<`가 있나** ② 핫키로 들어와 `<` → **「새로운 기억」 화면으로 가나**(앱은 그대로 켜져 있나) ③ 핫키로 들어와 **[취소하기] → 앱 밖으로** 가나 ④ **둘 다 적은 것이 있으면 팝업이 먼저 뜨고**, [나가기]를 누르면 **각각 제 갈 곳으로** 가나 (⚠️ `<`로 눌렀는데 앱 밖으로 나가면 **뜻이 섞인 것**이다) |
| **2026-08-31 02:40** | **`f44c4d6`** | **맥미니** | ★ **적은 것이 있는 상태에서 [취소하기]나 `<`를 누르면 「나가면 지금 적은 내용이 사라져요」 팝업이 뜬다** — 직전 빌드(`50105a8` · 02:30)는 **바로 닫혔다** · 곁: **핫키(액션 버튼·단축어)로 들어오면 `<`가 없다** | ⏸ **판정 대기** — 볼 것 다섯: ① 앱 안 `+`로 들어오면 **`<`가 있다** ② 핫키로 들어오면 **`<`가 없다** ③ 적은 것이 있을 때 [취소하기]·`<` → **팝업이 뜨고** [나가기]/[계속 쓰기]가 각각 맞게 동작하나 ④ **빈 상태에서는 팝업 없이 바로 나가나** ⑤ ⚠️ **핫키로 들어와 [취소하기] → 앱 밖으로 나가나** (⛔ **끝나는 것이 아니라 배경으로 내려간다** — 다시 열면 목록이다. 이 동작이 의도와 맞는지 판정해 주세요) |
| **2026-08-31 02:30** | **`50105a8`** | **맥미니** | ★ **수집 화면 좌측 상단에 `<`(뒤로) 아이콘이 있다** — 직전 빌드(`789e0fa` · 02:23)는 **위쪽 툴바가 완전히 비어 있었다** · 곁: **열 때 흔들림이 눈에 띄게 느려졌다**(≈0.72 → ≈1.44초) | ⏸ **판정 대기** — 볼 것 셋: ① 좌측 상단 `<`가 있고 **누르면 수집이 닫히나**([취소하기]와 같은 일) ② 흔들림 속도가 **적당한가** ③ `<`에 **글자가 없는 것이 맞나**(상세는 「‹ 기억」이다) |
| **2026-08-31 02:23** | **`789e0fa`** | **맥미니** | ★ **수집 화면을 열면 녹음 동그라미가 스스로 짧게 흔들린다** — 위로 두 번, 왼쪽으로 두 번(≈0.72초). 직전 빌드(`37aa418` · 02:01)는 **가만히 있었다** · 곁: **꾹 눌러 잡히는 순간 약한 진동**(직전 빌드엔 없다) | ✅ **통과** (2026-08-31 02:2x · 폰) — 사용자: *"진동 햅틱 좋아. 처음 시작할 때 좌우로 흔들리는 것도 좋아."* ① ✅ ② ✅ ③ ⏸ **못 잼**(안 물었다)<br>⛔ **함께 온 요구 둘:** ㉠ *"흔들리는 속도는 2배로 느리게"* ㉡ *"일관성을 위하여 좌측 상단에 < 아이콘을 넣어보자"* → 아래 줄에서 했다 |
| **2026-08-31 02:01** | **`37aa418`** | **맥미니** | ★ **오른쪽 아래 단추가 [삭제하기](빨강·휴지통)가 아니라 [취소하기](회색·X)이고, 오른쪽 큰 단추의 바탕이 앰버가 아니라 보라(accent)다** · ★ **녹음 동그라미가 눈에 띄게 커졌다**(글꼴 52 → 65) — 직전 빌드(0f5eab1 · 01:41)는 **[삭제하기]·앰버·작은 동그라미**였다 | ✅ **다섯 통과 · 하나 미검증** (2026-08-31 02:1x · 폰) — ① ✅ ② ✅ *"딱 좋아"* ③ ✅ ⑤ ✅ **⑥ ✅ 「줄바꿈도 의도대로 동작해」** ← ★ **저장 형식 변경(`RawLine`)이 여기서 닫혔다** · ④ ⏸ **미검증** — 사용자: *"검증해봐야 해. 내가 나중에 더 해보고 말해줄께. 일단 검증 안 됨. 이걸 지금 시험하지는 않는다."* ⛔ **「통과」로 적지 않는다**<br>⛔ **함께 온 요구 둘(③의 추가):** ㉠ **잡히면 haptic 진동** ㉡ **처음 진입할 때 위로 두 번·왼쪽으로 두 번 짧게 흔들기**(움직일 수 있다는 신호 · 녹음 중에) → 아래 줄에서 했다 |
| **2026-08-31 01:41** | **`0f5eab1`** | **맥미니** | ★ **녹음 단추(52pt 마이크 동그라미)가 텍스트 칸 아래 가운데가 아니라 「텍스트 칸 안 우측 하단」에 얹혀 있다** · ★ **왼쪽 위 [취소]가 없다** — 직전 빌드(`75747b4` · 01:23)는 **마이크가 칸 아래 가운데**에 있고 **[취소]가 있었다** | ⚠️ **둘 통과 · 셋 이어짐** (2026-08-31 01:5x) — ① ✅ *"마이크 위치 정말 좋아"* ② ✅ ③ ⚠️ *"텍스트를 좀 가릴 수 있지만 위치를 옮기는 방식으로 해결할 수 있을 것 같아"* → **끌어 옮기기로 풀었다**(아래 줄) · ④⑤ ⏸ **여전히 못 잼**<br>⛔ **함께 온 요구 넷:** 크기 25%↑ · 꾹 눌러 끌기(테두리 제한) · [삭제하기]→[취소하기](X) · 저장 색 → 상세의 활성 저장색 → 아래 줄에서 다 했다<br>*(옛 서술: "⏸ 판정 대기 — 볼 것 다섯: ① 마이크가 칸 안 우측 하단 ② [취소]가 없다 ③ 단추가 글자를 가리나 — 쟀다: 칸 시작 높이 80 · 안쪽 여백 10×2 → 글자 ≈60pt(17pt에서 세 줄) · 단추가 **62pt**를 오른쪽 아래에서 먹는다 → **둘째·셋째 줄 오른쪽이 가려질 수 있다** ④ **줄바꿈이 저장돼도 남는다**(앞 빌드에서 못 잼) ⑤ **옛 기억 원문이 그대로 보인다**(형식 회귀 · 앞 빌드에서 못 잼) |
| **2026-08-31 01:23** | **`75747b4`** | **맥미니** | ★ **「새로운 기억」 머리줄 오른쪽 아이콘이 마이크가 아니라 `+`다** · ★ **수집 화면 오른쪽 위에 [저장]이 없고, 텍스트·마이크 아래에 [삭제하기]·[저장 후 편집하기] 두 단추가 있다** — 직전 빌드(`8251d53` · 00:52)는 **마이크 아이콘 + 오른쪽 위 [저장]**이었다 | ⚠️ **둘 통과 · 셋 못 잼** (2026-08-31 01:3x) — ① ✅ 머리줄 아이콘이 **`+`** ② ✅ 순서 ③④⑤ ⏸ **못 잼**(안 물었다 — ⛔ **④⑤가 남은 것이 무겁다:** ④는 이번 변경의 본체이고 **⑤는 저장 형식 회귀 확인**이다. 다음 판정에서 함께 본다)<br>⛔ **함께 온 요구 둘:** ㉠ *"3번은 취소 버튼을 지우는 방식으로 결론내자"* ㉡ *"텍스트 아래에 마이크 아이콘은 정말 안 어울린다 … Floating Button 방식으로 바꿔서 텍스트 박스 우측 하단에"* → 아래 줄에서 바꿨다 |
| **2026-08-31 00:52** | **`8251d53`** | **맥미니** | ★ **수집 화면에서 [저장]을 누르면 「그 기억의 상세 화면」이 열린다** — 직전 빌드(`78a4362` · 00:48)는 **새로운 기억 화면에서 멈췄다**(밀 항목을 못 찾아 조용히 지나갔다) | ✅ **통과** (2026-08-31 **00:57** · 폰) — 사용자: *"저장하니까 상세화면으로 잘 넘어가. < 아이콘도 하나야."* ① ✅ 상세로 넘어간다 ② ✅ **`‹`가 하나다** ③ ⏸ **못 잼**(뒤로 나온 뒤 다시 들어가는 것은 안 물었다 — ⚠️ 그 경로는 **혼자서는 깨진 적이 없다.** 둘로 보이던 것은 **반쪽 밀기 자국이 남았을 때**였고 그 밀기가 없어졌다) |
| **2026-08-31 00:48** | **`78a4362`** | **맥미니** | ★ **수집 화면에서 [저장]을 누르면 목록이 아니라 「그 기억의 상세 화면」이 열린다** — 직전 빌드(`821ea01` · 00:34)는 **목록으로 돌아왔다**(자동 이동을 걷어낸 판이었다) | ⛔ **실패** (2026-08-31 00:5x) — 사용자: *"저장 하면 상세화면으로 자동으로 넘어가지 않고 새로운 기억 화면에서 멈춤."* **원인은 타이밍이 아니었다** — `append`가 부르는 **`load()`가 비동기**라(`Task { await reload() }`) 저장 직후 `current(newId)`가 **nil**이고 밀기 코드가 **조용히 지나갔다.** → `8251d53`에서 「찾을 때까지 기다리기」(`resolveAfterWrite`)로 고쳤다 |
| **2026-08-31 00:34** | **`821ea01`** | **맥미니** | ★ **상세 화면 좌측 상단의 `‹`가 하나다** — 직전 빌드(`22d62ad` · 08-30 23:48)는 **둘이었다.** 곁: **[저장]하면 목록으로 돌아온다**(직전 빌드는 상세로 가려다 반쪽만 갔다) | ⚠️ **판정 못 했다** — 이 판은 **자동 이동을 걷어낸 것**이고, 사용자가 *"자동으로 가게 해줘"*로 **기능을 되살리라고** 했다(내가 앞 판정의 *"그건 괜찮아. 유지하자"*를 **「기능을 버려라」로 잘못 읽었다**). 그래서 이 판의 확인 목록은 **의미를 잃었다** — ⛔ 「통과」로도 「실패」로도 적지 않는다 |
| **2026-08-30 23:48** | **`22d62ad`** | **맥미니** | ★ **수집 화면에 「보조 자료」 카드가 있고 [사진 찍기] 버튼이 없다** — 직전 빌드(`06aff97` · 21:42)는 **[사진 찍기] 버튼 + 40pt 썸네일 + 「사진 1장 첨부됨」** 줄이었고 **카드가 없었다.** 곁: **글 쓰는 칸이 낮아졌다**(160 → 80pt) | ⚠️ **넷 통과 · 하나 못 잼 · 결함 하나** (2026-08-31 00:1x · 폰) — ① ✅ *"카드가 나왔고 [사진 찍기]가 있어"*(카드의 `+` 시트 줄 · 옛 버튼은 **주석에만 남아 있다**) ② ✅ *"글쓰는 칸은 적당해 줄었어. 일단 이 크기 좋아."* ⚠️ **사용자: *"그러나 다시 검토는 해볼거야"*** → 열려 있다 ③ ✅ *"사진찍기, URL 등록하기 둘 다 여러개 등록 잘되고"* ④ ✅ **길게 눌러 지우기** — *"선택 메뉴 뜨는 것 확인했고, URL은 … 삭제까지 해봤어. 잘 돼."* / ⏸ **짧게 눌러 크게 보기는 못 잼**(언급이 없었다 — 값을 만들지 않는다) ⑤ ✅ *"저장뒤에서 똑같이 보였어"*<br>⛔ **결함: 상세 화면 좌측 상단의 `‹`가 둘이었다** — *"< 아이콘이 좌측 상단에 2개가 나와!!!"* → 아래 `821ea01`에서 고쳤다. **원인은 [저장]의 자동 이동**이고 사용자가 **그 기능 자체를 「안 가는 쪽으로 유지」**로 정했다 |
| **2026-08-30 21:42** | **`06aff97`** | **맥미니** | ★ **수집 화면에서 [저장]을 누르면 목록으로 나오지 않고 「그 기억의 상세 화면」이 열린다** — 직전 빌드(`12add9e` · 08-29 00:19)는 **목록으로 나왔다.** ⚠️ **뚜렷한 확정 근거는 이것 하나다**(나머지 셋은 「없어지던 것이 안 없어진다」라서 **직전 빌드와 나란히 볼 수 없다**). 곁: **아래로 쓸어도 수집 화면이 안 닫힌다**(직전 빌드는 닫혔다) | ✅ **넷 다 통과** (2026-08-30 **22:49** · 폰) — 사용자: *"잘 됨!!! 1 2 3 4 모두통과!!"* ① 녹음 뒤 사진을 찍고 돌아와도 **글이 그대로** ② 다시 [녹음]하면 **빈 줄 둘 뒤에** 이어 붙는다 ③ [저장] → **상세 화면** ④ 아래로 쓸어도 **안 닫힌다**([취소]로만)<br>*(옛 서술: "⏸ **판정 대기** — 사용자 확인 필요")* |
| **2026-08-29 00:19** | **`12add9e`** | **맥미니** | ★ **검색 결과 한 줄 안에서 밝기가 세 층으로 갈린다 — 원문(가장 밝다) > 화면 이름 > 날짜(가장 어둡다)**. 직전 빌드 `a83e95d`(08-28 23:26)는 **화면 이름이 원문과 똑같이 밝았다**(두 층). ⚠️ **밝기 차이라 뚜렷하지 않다** — 판정은 **「이름이 원문보다 어둡나」** 하나로 본다 | ✅ **통과** (2026-08-29 **00:19~00:51 사이** · 폰) — 사용자: *"폰에서 적당한 색깔로 설정된 것 확인했음! 완료!"* **색 문제가 여기서 닫혔다**(왕복 넷 끝 · 시작은 08-28 01:3x *"색이 회색이라 좀 흐리지만 나중에 고치겠음"*). 시뮬 픽셀 실측: 원문 #ECEBF1 15.72:1 · 이름 #A7A4B3 7.63:1 · 날짜 #746F82 3.85:1 |
| **2026-08-28 23:26** | **`a83e95d`** | **맥미니** | ★ **검색 결과 각 줄 오른쪽 끝의 화면 이름**(「살아있는 기억 / 원칙 / 새 기억들 / 완료된 기억」)**이 같은 줄의 날짜와 다른 색이다 — 원문과 같은 흰색**(직전 빌드 `52557a1`는 **날짜와 완전히 같은 흐린 회색**이었다). ⚠️ **이것은 밝기 차이라 아이콘 신설처럼 뚜렷하지 않다** — 다만 **한 줄 안에서 날짜와 갈리는지**를 보면 갈린다 | ✅ **통과** (2026-08-29 **00:0x** · 폰) — 사용자: *"의도대로 색깔이 밝아졌어."* ⚠️ **함께 온 요구 하나: 「근데 너무 밝아. 날짜 색깔과 원문 색깔의 중간 정도로 조정해보자.」** → **위 줄(`12add9e`)에서 바꿨다** |
| **2026-08-28 01:31** | **`52557a1`** | **맥미니** | ★ **상세 화면 원문 칸의 제목이 「원문」이 아니라 「원문: E036A094」**(8자 ID가 붙어 있다) · ★ **검색 탭 빈 화면 안내가 「원문으로 찾기」가 아니라 「기억 찾기」**(검색창 자리표시자도 「기억 검색」) — 직전 빌드(`820e20e` · 08-26 20:25)에는 **셋 다 없다** | ✅ **넷 다 통과** (2026-08-28 **01:34**) — 사용자: *"1 2 3 4 모두 제대로 동작해! 소문자로, 앞네글자만 해도 잘 나오며 답답하지 않음."* ⚠️ **함께 온 요구 하나: 「색이 회색이라 좀 흐리지만 나중에 고치겠음」** → **안 고쳤다**(사용자가 미룬 것) |
| **2026-08-26 20:25** | **`d5ad925`+** = **`820e20e`** | **맥북** | ★ **URL 네모를 길게 누르면 「미리보기 다시 받기」 메뉴가 뜬다** — 직전 빌드(`a8e99bc`)에는 **그 메뉴가 없다**(URL 네모에 길게 누르기가 아예 없었다) | ✅ **통과 셋** (2026-08-26 20:3x) — 사용자: *"1 2 3 다 확인했어. 너의 안내대로 동작하고 있어. 모두 성공이야."* ① 메뉴가 뜬다 ② **낡은 캐시가 페이지 첫 화면으로 갱신된다**(`wowanalytica` 171×171 → 캡쳐) + 「내려받는 중」 ③ `wow.inven.com`은 여전히 ①꼴(**TLS 거부 · 그 사이트 문제**) |
| **2026-08-25 16:55** | **`78fd33f`+**(=`a8e99bc`) | **맥북** | ★ **URL 네모의 그림 아래에 도메인 이름 띠가 있다**(흰 글자 + 검은 반투명) — 직전 빌드(16:43)에는 **없었다** | ✅ **통과** (2026-08-26 19:5x · **맥북**) — 사용자: *"네모 아랫쪽에 도메인 이름 띠가 보임. inven이라 써있음."* **표본 = `85346E05`「사냥감 추적자…」**(첫째 URL `wow.inven.co.kr` · 캡쳐 **558×558** · 08-25 16:51 = **그 빌드가 뽑은 것**). ⚠️ **판정이 나흘 뒤에 왔다 — 세 세션 연속 밀렸다**<br>*(옛 서술: "⏸ **판정 대기** — 사용자 확인 필요")* |
| **2026-08-25 16:43** | **`78fd33f`+** | **맥북** | ★ **새 URL을 붙이면 네모에 페이지 첫 화면이 실제로 나온다** — 직전 빌드(16:30)는 **한 번도 안 찍혔다**(대표 그림으로 내려갔다) | ✅ **통과** — 사용자: *"이제 제대로 가져오네"*. ⚠️ **다만 62pt에서 너무 작아 알아보기 어렵다** → 다음 줄에서 이름 띠를 얹었다<br>⚠️ **이 줄은 뒤늦게 적었다** — 설치 직후에 안 적고 판정을 먼저 물었다(기록 규칙 2를 어겼다) |
| **2026-08-25 16:30** | **`a644ad6`+**(=`b18ade9`) | **맥북** | ★ **웹페이지 본문에서 글자로 복사한 주소도 「URL 담기」 칸에 자동으로 채워진다** — 직전 빌드(16:24)는 **사파리 주소창에서 복사한 것만** 채워졌다(평문은 놓쳤다) | ✅ **통과** — 사용자: *"자동 채움 확인됨"* |
| **2026-08-25 16:24** | **`2aa2c4a`** | **맥북** | ★ **URL을 새로 붙이면 네모에 「그 페이지의 첫 화면」이 나온다**(글자·배치가 보이는 화면) — 직전 빌드(01:33)는 **대표 그림이나 아이콘**이 나왔다 | ⛔ **실패** — 사용자: *"화면을 캡쳐해오지는 않아"*(대표 그림으로 내려갔다). **원인 둘을 16:43 빌드에서 고쳤다** |<br>⚠️ **설치가 두 번 실패했다**(14:31·14:34 — `Connection interrupted`). 폰이 잠겨 있으면 이렇게 끊긴다. **빌드는 14:31 것 그대로**이고(실행파일 시각으로 확인) 설치만 늦었다 |
| **2026-08-25 01:33** | **`31b45b2`+**(=`a5d0c3d`) | **맥북** | ★ **목록의 X가 「빨간 동그라미 안의 검은 X」가 아니라 그냥 빨간 X다** · ★ **확인 화면이 「…기억에서 지웁니다」이고 버튼이 [지우기]다** — 직전 빌드(01:25)는 **「…뗍니다」 / [떼기]**였다 | ✅ **둘 다 통과** — 사용자: *"1 2 확인됨"*(2026-08-25 · 이 세션의 마지막 판정) |
| **2026-08-25 01:25** | **`e625345`+**(=`836ae9c`) | **맥북** | ★ **목록의 줄마다 오른쪽 끝에 빨간 X가 있다** — 직전 빌드(01:12)에는 **없었다** · 누르면 **「이 URL을 기억에서 뗍니다. 되돌릴 수 없어요.」**가 뜨고 [떼기]를 누르면 그 URL이 사라진다 | ✅ **넷 다 통과** — 사용자: *"1 2 3 4 모두 확인됨"*. ⚠️ **함께 온 요구 둘: 「떼기」→「지우기」 · 빨간 원+검은 X → 그냥 빨간 X** → 다음 줄에서 바꿨다 |<br>⚠️ 첫 설치 시도가 **통로 시간초과로 실패**했다(`App installed` 줄이 없어서 바로 알았다) — 두 번째에 됐다 |
| **2026-08-25 01:12** | **`cc49d60`+**(=`a70e3b5`) | **맥북** | ★ **URL이 둘인 기억에서 네모를 누르면 목록이 「그 네모 바로 아래에 붙어」 내려온다** — 직전 빌드(00:50)는 **같은 목록이 화면 맨 아래에서 올라왔다** | ✅ **통과** — 사용자: *"기대한대로 목록이 잘 뜬다"*. ⚠️ **함께 온 물음 하나(목록이 길어지면?)와 요청 하나(빨간 X로 떼기)** → 다음 줄 |
| **2026-08-25 00:50** | **`83aae12`+**(=`59cc3d6`) | **맥북** | ★ **URL이 둘 있는 기억에서 네모를 누르면 브라우저가 아니라 「고르는 목록」이 먼저 뜬다** — 직전 빌드(00:13)까지는 **늘 첫 URL이 곧바로 열렸다**(둘째로 갈 길이 없었다) · URL이 **하나**인 기억은 여전히 바로 열린다 | ✅ **다섯 다 통과** — 사용자: *"1 2 3 4 5 모두 말해준대로 동작했어"* (00:13 빌드의 판정 둘도 이 빌드에서 함께 받았다 — 문턱 3:1과 캐시 판). ⚠️ **함께 온 요구 하나: 목록이 화면 아래에서 올라온다 → 누른 네모에 붙어 내려오게** → 다음 줄에서 바꿨다 |
| **2026-08-25 00:13** | **`2983133`+**(=`2883524`) | **맥북** | ★ **`wowanalytica` URL을 새로 하나 더 붙이면 그 네모가 「금색 마크」로 나온다**(정사각 아이콘) — 직전 빌드(00:10)는 **같은 URL을 다시 붙여도 링크 아이콘 + 글자에 머물렀다**(캐시가 「이미 해봤다」로 걸렸다) | ✅ **통과** — ⚠️ **판정은 01:12 빌드 확인에서 함께 받았다**(그 빌드가 이것을 담고 있다). 확인 목록 **4번**(*"URL이 없는 다른 기억에 wowanalytica.com을 붙이면 금색 마크로 바뀐다"*)이 정확히 이 줄의 확정 근거였고, 사용자: *"1 2 3 4 5 모두 말해준대로 동작했어"*. ⛔ **추정으로 채운 것이 아니다** — 그 목록의 어느 번호가 이 줄인지 위에 적었다 |
| **2026-08-25 00:10** | **`8ac5a60`+**(=`555370e`) | **맥북** | ★ **어제 붙인 `wowanalytica` 네모가 금색 글자판에서 「링크 아이콘 + wowanalytica 글자」로 바뀐다** — 직전 빌드(08-24 19:10)는 **그 글자판을 아주 작게 그렸다**(높이 11.1pt)<br>⛔ **옛 서술(틀렸다 · 지우지 않고 남긴다):** *"그리고 같은 URL을 새로 붙이면 금색 마크(정사각 아이콘)로 나온다"* — **이 빌드에서는 안 된다.** 캐시 이름이 URL 해시라서 **같은 URL은 「이미 해봤다」로 걸려 다시 안 뽑는다.** **적은 뒤 스스로 확인하다 찾았고**(사용자에게 판정을 부탁하기 전이었다) 다음 줄에서 고쳤다 | ✅ **통과** — ⚠️ **판정은 01:12 빌드 확인에서 함께 받았다**(그 빌드가 이것을 담고 있다). 확인 목록 **1번**(*"어제 붙인 wowanalytica 네모가 「링크 아이콘 + wowanalytica」로 바뀌어 있다"*)이 이 줄의 확정 근거였고, 사용자: *"1 2 3 4 5 모두 말해준대로 동작했어"* |
| **2026-08-24 19:10** | **`740df60`+**(=`8ac5a60`) | **맥북** | ★ **보조 자료 카드의 `+` → 종류 시트에서 「URL」 줄이 눌린다** — 직전 빌드(01:49)까지 그 줄은 **흐렸고 누르면 「아직 못 담아요」**가 떴다 · 누르면 **「URL 담기」** 화면이 뜬다(그 화면은 이 빌드에서 처음 생겼다) | ✅ **여덟 다 통과**(2026-08-24 밤) — 사용자: *"5 6 7 모두 확인됨!!!"* · *"8번 확인됨"* · 클립보드는 **iOS 팝업을 허용한 뒤** 채워졌고 **한 번 허용하니 다른 기억에서도** 됐다. ⛔ **함께 나온 결함 하나: 가로로 긴 대표 그림이 너무 작아 글자를 못 읽는다** — *"가로로 긴 글자 보드가 아이콘 형태라서 확대해야 글자가 보임"*(367×75 → **높이 11.1pt**). ★ **8번 표본이 그것을 잡으려고 고른 것이었다** |
| **2026-08-24 01:49** | **`f35c08c`+**(=`1a7be79`) | **맥미니** | ★ **손을 대고 있으면 썸네일 줄이 안 사라진다**(직전 빌드는 **손이 움직이는 동안 1초도 안 되어 사라졌다**) · 사라지는 시간이 **5초**로 늘었다 | ✅ **통과** — 사용자: *"다 잘 돼"*(2026-08-24 · 이 세션의 마지막 판정) |
| **2026-08-24 01:45** | **`830bd14`+**(=`531dd13`) | **맥미니** | ★ **썸네일 줄이 손을 뗀 뒤 3초쯤 지나면 서서히 사라진다** — 직전 빌드는 **늘 떠 있었다** · 넘치지 않으면 **가운데로 모인다**(직전 빌드는 왼쪽에 붙었다) | ⚠️ **반만 통과** — 가운데 정렬·사라짐은 됐지만 ⛔ **자꾸 1초도 안 되어 사라졌다**(취소를 삼킨 결함) · **스크롤이 뻑뻑하다** → 다음 줄에서 고쳤다 |
| **2026-08-24 01:39** | **`3d8a821`+**(=`661270e`) | **맥미니** | ★ **사진을 넘기면 아래 썸네일 줄도 함께 움직여 초록 테두리가 가운데로 온다** — 직전 빌드는 **줄이 가만히 있어 초록 테두리가 화면 밖으로 나갔다** | ✅ **통과** — 사용자: *"ㅇㅇ 다 잘 돼!!"* · *"가로 모드에서도 썸네일이 동작하네. **매우 좋아**"* |
| **2026-08-24 01:32** | **`f433079`+**(=`ddce584`) | **맥미니** | ★ **사진을 넘기면 미끄러진다**(직전 빌드는 즉시 갈렸다) · **세로에서도 큰 화살표가 있다**(직전 빌드에서 사라져 있었다) · **가로에서도 썸네일 줄이 있다** | ✅ **통과**(⚠️ **뒤늦게 적었다** — 종료 점검에서 빠진 것을 찾았다) — 사용자: *"썸네일 테두리가 초록색으로 바뀌어서 **좋은데**"* + 요청 하나(*"초록 테두리는 화면 밖으로 나가버려"* → 다음 줄에서 따라 스크롤을 넣었다) |
| **2026-08-24 01:21** | **`105ad47`+**(=`41e7e35`) | **맥미니** | ★ **세로에서 뷰어 아래에 썸네일 줄이 있고 고른 것에 초록 테두리**가 있다 — 직전 빌드에 **없던 것** · 화살표가 **2배로 커졌다** | ⚠️ **반만 통과** — 썸네일 줄은 *"맘에 들어"*(사용자), ⛔ **세로에서 화살표가 사라졌다**(내 오독) · **전환이 너무 기계적** · **뷰어가 낡은 항목을 받는 결함**이 함께 나왔다 → 다음 줄에서 셋 다 고쳤다 |
| **2026-08-24 00:58** | **`e667476`+**(=`a1b9339`) | **맥미니** | ★ **뷰어 상단에 「1 / 3」 숫자**와 **좌우 가장자리에 `‹` `›`**가 있다 — 직전 빌드까지 뷰어는 **첫 자료만** 보여줬고 넘길 길이 없었다 | ✅ **통과**(2026-08-24) — 사용자: *"1/5 숫자 잘 나와. `‹` `›` 눌러서 사진 바뀌어도 숫자에 반영돼"* · 3·4번도 *"잘 돼"*. ⛔ **함께 나온 것 둘:** 회전하면 사진이 작게 남는 결함 · **「하나뿐이면 안 보인다」는 옳지 않다**(다음 줄에서 고쳤다) |
| **2026-08-24 00:43** | **`1d82ee5`+**(=`c501e59`) | **맥미니** | **카메라를 열면 미리보기가 화면 전체로 나온다** — 앞 빌드 둘은 **커버에 임베드**돼 있어서 **가로에서 띠로 눌렸다**. ★ 세로에서도 다르다: **시스템 카메라가 모달로 올라온다**(아래에서 밀려 올라오는 전환) | ✅ **통과** — 사용자: *"**가로모드에서 잘 된다**"*(2026-08-24) |
| **2026-08-24 00:31** | **`e24329d`+**(=`439f708`) | **맥미니** | **카메라를 열고 폰을 가로로 돌렸을 때 화면이 안 돈다**(세로 유지) — 직전 빌드는 **가로로 돌면서 배치가 깨졌다**<br>⚠️ **이 근거는 판정 대상과 같다** — 오버라이드가 안 먹으면 **직전 빌드와 화면이 구분되지 않는다** | ⛔ **되돌렸다 — 방향이 틀렸다**(사용자 지시 2026-08-24: *"카메라로 보이는 부분이 너무 축소되어서 촬영 자체가 불가능해. **가로모드에서도 잘 되도록 고치자**"*). ⚠️ **이 빌드의 화면은 판정되지 않았다** — 사용자 문장이 이 빌드를 본 것인지 앞 빌드를 본 것인지 **확인하지 않았다**(기록 규칙 7) |
| **2026-08-23 22:49** | **`54bca5f`+**(=`f91a9bb`) | **맥미니** | ★ **보조 자료 카드의 점선 `+`를 누르면 종류 시트가 뜬다**(사진 찍기 · 앨범에서 고르기 · 동영상 · 음성 · URL · PDF · 기타) — 직전 빌드까지 `+`는 **안 눌렸다** | ✅ **통과**(2026-08-23 밤 · 판정 여섯) — 사용자: *"시트가 뜬다. 의도대로 나온다"* · *"사진 하나 있는 기억에 사진 찍기로 하나, 앨범에서 하나 추가해서 **총 3이 나오는 거 확인**했음"* · *"이상 증상은 발견하지 못했음"*(C 회귀 없음). ⛔ **함께 나온 결함 하나: 가로로 돌려 찍으면 카메라 화면이 깨진다**(사진 자체는 정상 · 다음 줄에서 고쳤다) |
| **2026-08-23 17:01** | **`99243e2`+**(=`1f1d2f8`) | **맥미니** | ★ **자료가 있는 항목에도 맨 뒤에 점선 `+` 네모가 있다** — 직전 빌드까지는 **자료가 0개일 때만** 점선 네모가 떴다 | ✅ **통과** — 사용자: *"자료 있는 항목에도 맨 뒤에 점선 +가 보이고, 없는 항목에는 그것만 있다. **카드가 닫혔다.**"* |
| **2026-08-23 15:21** | **`06eacb4`+**(=`8126c7e`) | **맥미니** | **사진을 두 번 두드리면 「그 자리」가 커지고 확대·축소에 애니메이션이 있다** — 직전 빌드는 **화면 한가운데 기준**으로 **퍽 하고** 바뀌었고 **축소하면 사진이 화면 밖으로 나갔다** | ✅ **통과** — 사용자: *"이미지 보기 문제는 다 해결되었음."* |
| **2026-08-23 15:04** | **`c6db952`+** | **맥미니** | **뷰어에서 사진을 두 번 두드리면 꽉 찬다**(직전 빌드는 두 번 두드리면 전체 보기로 되돌아가기만 했다) · **막대를 끄는 동안 소리가 멈춘다** | ⏸ 판정 대기 — ⚠️ **이 표에 판정이 안 적혔다** — 그 세션의 판정은 설계 `media-expansion-design.md` **§3-R**에 있다(2026-08-24 종료 점검에서 발견 · ⛔ **추정으로 채우지 않았다**) |
| **2026-08-23 14:45** | **`f0c7090`+** | **맥미니** | **뷰어의 음성 재생기가 「경과 / 길이」를 보이고 막대를 끌 수 있다** — 12:58 빌드는 **길이만** 보였고 막대가 안 끌렸다 · 그리고 **「보조 자료」 제목이 다른 카드 제목과 같은 크기**다 | ⏸ 판정 대기 (커밋은 이 설치 직후 — 아래 worklog) — ⚠️ **이 표에 판정이 안 적혔다** — 그 세션의 판정은 설계 `media-expansion-design.md` **§3-Q·§3-R**에 있다(2026-08-24 종료 점검에서 발견 · ⛔ **추정으로 채우지 않았다**) |
| **2026-08-23 12:58** | **`f0c7090`** | **맥미니** | ★ **상세에 「보조 자료」 카드가 있다** — 성역+분류 **아래**에 폭 전체 카드가 서고 **62pt 정사각형 네모**가 있다. **그리고 성역 안에 「원본 음성 다시 듣기」 줄이 없다**(직전 `7cd8356`까지는 자료가 성역 안에 있었다 · 사진 260pt·지도 150pt도 거기 있었다) · 접힘 머리 줄 아이콘이 **하나**(방식)로 줄었다 | ⏸ **판정 대기** ⚠️ **이 표에 판정이 안 적혔다** — 그 세션의 판정은 설계 `media-expansion-design.md` **§3-P**(실기기 판정 첫 회)에 있다(2026-08-24 종료 점검에서 발견 · ⛔ **추정으로 채우지 않았다**) · 볼 것 12개는 `media-expansion-design.md` **§3-M**. ⛔ **점 표시·끄는 감각은 이번에 판정 못 한다**(종류가 최대 둘이라 안 넘친다) |
| **2026-08-21 17:33** | **`7cd8356`** | **맥북** | **원문 편집 칸이 6줄에서 멈추고 그 안에서 스크롤된다** — 직전 `7f8cf83`은 내용만큼 계속 늘어나 성역·분류를 밀어냈다 · 그리고 **편집 중에도 부호가 줄 앞에 안 온다**(직전엔 「… 쓰자 / . 면도도」) | ✅ **넷 다 통과** (2026-08-21 · 사용자) — *"마침표가 줄 앞에 위치할 수 있었던 문제는 해결되었음. 화면 크기 최대 6줄로 되었음. **크기 적절함.** 스크롤 자연스러움. 짧은 원문에서는 바깥 화면이 스크롤됨. 영어, 숫자 편집 중에도 적절하게 표시됨."* |
| **2026-08-21 16:42** | **`7f8cf83`** | **맥북** | **상세 원문 칸이 화면 안에 있다** — 직전 `9cb1866`은 **긴 문장에서 칸이 가로로 늘어나 화면 밖으로 나갔다**(사용자가 잡은 회귀) · 그리고 **원칙 번호 왼쪽 여백이 좁아졌다**(14→9) | ◐ **일부 통과** — ✅ 칸이 화면 안 · 탭하면 커서·키보드 · **밖을 누르면 내려감** · [저장] 살아남 · 번호 왼쪽 간격 ✅. ⛔ **부호가 여전히 줄 앞에 왔고** 세로로 계속 늘어났다 → `7cd8356`에서 고쳤다 |
| **2026-08-21 16:10** | **`9cb1866`** | **맥북** | **목록·검색·보관함의 원문도 좌우 맞춤이다**(직전까지는 원칙 화면만 그랬다) · **원칙 번호가 두 자리 폭으로 붙어 10번 이후에도 원문이 안 밀린다** | ◐ **일부 통과** — ✅ **목록 다섯 · 원칙 번호** *"적절하게 잘 나오고 있어"*. ⛔ **편집 칸이 가로로 늘어나 화면 밖으로 나갔다**(회귀) → 확인 1·4를 **못 봤다** → `7f8cf83`에서 고쳤다 |
| **2026-08-21 15:48** | **`726a7de`** | **맥북** | **부호가 줄 앞에 안 온다** — 「…줄이지 않는 / **다.** 못 하면」처럼 **한글이 줄 시작**이다. 직전 `9b0253a`는 「**.** 못 하면」으로 부호가 앞에 왔다. 그리고 **줄 앞 빈칸이 없다** | ✅ **전부 통과** (2026-08-21 · 사용자) — *"4개 모두 확인했는데 이상없다!!"* (부호 · 줄 앞 빈칸 · 영어·숫자 · 접근성) |
| **2026-08-21 15:33** | **`9b0253a`** | **맥북** | **원칙 문장의 오른쪽 끝이 가지런하다(좌우 맞춤)** — 직전 빌드는 왼쪽 맞춤이라 들쭉날쭉했다. 「새로운 기억」 탭 맨 위 **원칙 영역**에서 바로 보인다 | ◐ **좌우 맞춤은 됐고 대가 둘이 드러났다** — 그것이 **줄바꿈 원칙 셋**(부호·빈칸·비한글) 요구로 이어졌다 → `726a7de` |
| **2026-08-21 00:23** | **`c756655`** | 맥북 | **끌어서 놓아도 앱이 안 죽는다** (직전 `069f2e9`은 놓는 순간 죽었다) | ✅ **전부 통과** — *"이제 안 죽어. 세 경우 다 통과. 여백도 지금 이 상태가 좋아. 더 볼 것 없어."* (08-21) |
| 2026-08-21 00:10 | `069f2e9` | 맥북 | **끌고 있는 도중에 번호가 바뀐다**(손 떼기 전에) | ⛔ **놓으면 앱이 죽었다** → `c756655` |
| 2026-08-20 23:53 | **`c6ed58b`** | 맥북 | **줄 앞이 별표가 아니라 숫자(1 2 3 4 5)** · 카드에 테두리가 있다 | ✅ **테두리가 됐다** — 원칙 영역 짜임 차용(바탕 `0.14` · 테두리 `0.28`) |
| 2026-08-20 23:45 | `6ec812f` | 맥북 | **항목마다 바탕색이 깔린다** · 「대기」가 없다 | ⚠️ 켜진 카드의 별 대비가 약했다 → `c6ed58b`에서 **원인이 사라졌다**(숫자로 바뀜) |
| 2026-08-20 23:23 | `2ff782d` | 맥북 | **줄 사이가 눈에 띄게 벌어졌다**(직전엔 붙어 있었다) | ⚠️ 바탕색이 생기며 전제가 바뀌어 6pt로 다시 좁혔다 |
| 2026-08-20 23:05 | `fa0f689` | 맥북 | **목록 글자가 원칙 영역보다 크다**(직전엔 같았다) | ✅ *"일단 글자 커졌고"* · ✅ **접근성에서 올리면 따라 커진다** |
| 2026-08-20 18:20 | **`50a8e48`** | 맥북 | **끌면 원래 자리가 빈다**(흐린 사본이 없다) · 목록 배경/여백이 살짝 달라졌을 수 있다(UIKit 셀) | ✅ **잔상이 없어졌다** — `UICollectionView` 대화식 이동 |
| 2026-08-20 17:59 | `6973775` | 맥북 | ⚠️ **없다 — 16:36과 코드가 한 글자도 다르지 않다**(사용자 요청 재설치) | 잔상이 여전했다 → **「낡은 빌드였다」가 배제됐다.** 그것이 이 재설치의 값이다 |
| 2026-08-20 16:36 | `6973775` | 맥북 | **끌어도 테두리가 아예 안 뜬다** (직전 `5a9c934`는 떴다) | 테두리를 없앤 빌드. **그 판단은 뒤집혔다** → `c6ed58b` |
| 2026-08-20 11:31 | `5a9c934` | 맥미니 | **제자리에서 손을 떼도 테두리가 바로 사라진다**(직전엔 남았다) | ㉠ 끌기 ✅ · ㉡ **끄는 시각이 흔들렸다** ⛔ |
| 2026-08-20 11:11 | `344e28a` | 맥미니 | **끌면 사본이 보이고 그 줄에만 테두리가 뜬다** (직전 둘은 끌면 항목이 아예 안 보였다). 코드는 `11fd8cb`와 같다(주석만 늘었다) | ✅ *"오! 지금 잘 돼!!!! 바로 이거야"* |
| 2026-08-20 11:04 | `4dc8b9c` | 맥미니 | **끌 때 원래 자리가 비어 보인다** | ⛔ *"선택된 것 자체가 사라져"* |
| 2026-08-20 10:59 | `11fd8cb` | 맥미니 | **끌고 있는 그 줄에만 테두리가 뜬다** — 손 떼면 사라진다 | ✅ 드래그 살아 있고 그 줄만 테두리(`.onDrag`) |
| 2026-08-20 10:53 | `ea97b0d` | 맥미니 | **원칙 목록의 줄에 테두리가 「없다」** + 머리글 첫 줄이 「아래 3개가 원칙 영역에 노출됩니다」 | 잘못 만든 것(모든 줄 상시 테두리)을 되돌린 착지점 |
| 2026-08-20 10:39 | `2b05d96` | 맥미니 | 머리글 첫 줄이 **「아래 3개가 원칙 영역에 노출됩니다」** + 각 줄에 테두리 | ⛔ **요구와 정반대**(모든 줄에 상시) |
| 2026-08-20 10:31 | `4b20926` | 맥미니 | **각 줄 오른쪽에 ≡ 손잡이가 있다** | ✅ *"지금은 드래그 된다"* — 제스처를 통째로 뺐다 |
| 2026-08-20 10:24 | `7e643f4` | 맥미니 | **Siri에 「SecondBrain」만 말해도 수집이 열린다** (화면으로는 안 갈린다 — **Siri로 갈린다**) | ⛔ 드래그 여전히 안 됨 · ✅ ㉡ 집힌 느낌 · ✅ ㉢ Siri |
| 2026-08-20 10:16 | `d1749e2` | 맥미니 | **원칙 목록 머리글이 두 줄이다** — 「눌러 끌어서 순서를 바꾸세요」가 붙었다 | ⛔ **이 빌드가 드래그를 깼다** |
| 2026-08-20 09:39 | **`b49f85f`** | 맥미니 | **삭제 스와이프 버튼이 빨강(coral)이다** — 직전 `f7018d6`에서는 앱 강조색(청록)이었다 | ✅ 확인 목록 7개 **전부 통과** · 덤으로 **지도 핀**·삭제 스와이프 빨강 |
| 2026-08-19 18:52 | **`f7018d6`** | 맥북 | 맥의 `audio/`·`photo/` 디렉터리 · `.sb-media.log` · **「받는 중」·한도 배너** | ✅ 이관 판정 **여섯 전부 통과** · 135/135 |
| 2026-08-18 15:55 | **`dbbafc8`** | 맥북 | 「새 기억들」을 길게 눌렀을 때 **완료가 없다**(기억하기·삭제 둘뿐) | ✅ 확인 다섯 통과 — **당겨서 분류는 그때부터 막혀 있다** |
| 2026-08-18 01:51:59 | `f43a84d` | 맥미니 | 임시 항목에서 `<`로 나갈 때 **「기억하기를 하지 않으면 …」**이 뜬다 | ✅ ③ 통과 |
| 2026-08-18 01:29:03 | `c045f31` | 맥미니 | 「새 기억들」 줄이 **두 줄이고 수집 날짜가 보인다** | ✅ |
| 2026-08-12 20:41 | `15aa486` | 맥북 | **성역·분류 두 카드가 좌우로 나란히 선 것** | ✅ (1단계 압축) |
| 2026-08-07 21:20 | *(커밋 미기재)* | 맥북 | *(적혀 있지 않다)* | — |

---

## 이 목록의 한계 (지금 아는 것만 적는다)

- **여기 있는 것은 문서에 적혀 있던 설치 전부다**(2026-08-21에 `HANDOFF.md` §4-A와 worklog에서 옮겼다).
  **2026-08-07 이전은 기록이 없다** — 없는 것을 만들지 않는다.
- **`2026-08-07 21:20` 줄은 커밋이 안 적혀 있다**(`docs/worklog/2026-08-07-macbook.md`의 *"폰 설치: 2026-08-07 21:20 빌드."* 줄이 전부다).
  ⛔ **추정으로 채우지 않는다.**
- ⚠️ **`0727499`는 이 목록에 없다** — **폰에 깔린 기록이 없다.** 그 커밋의 지도 핀은 **맥 앱에서** 판정됐고
  (`docs/worklog/2026-08-20-macmini.md`), **폰에는 `b49f85f`로 처음 들어갔다**(*"지도 핀 `q=`(폰에서 처음)"*).
- **어느 맥에서 설치했나는 그 세션의 worklog 파일 이름으로 갈랐다.** ⚠️ 폰은 **두 맥 어디서나 붙는다**
  (2026-08-20 실측) — 「기기를 옮겼으니 설치는 다음에」라고 미룰 이유가 없다.
- **판정 칸이 비어 있으면 「아직 안 봤다」는 뜻이다.** ⛔ 비어 있는 것을 통과로 읽지 말 것.
