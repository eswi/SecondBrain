# 2026-08-23 저녁 (집 맥미니) — **A·B 확정 + C 구현: 조회의 축이 「파일명」이 됐다** · 폰엔 안 깔았다

> ## 재개 한 줄 — **2026-08-23 21:4x (이 줄이 최신이다)**
>
> ### ✅ **A·B·C가 닫혔다** — A·B는 사용자 결정, **C는 구현**(§3-X)
> **C:** 조회의 축을 **항목 id → 파일명**으로 바꿨다. **제약 9-b 닫힘 · 9-a 절반 닫힘**(수집은 옛 꼴 유지).
> **시험 399 통과 · 빌드 `EXIT=0`·`error: 0`(시뮬 26.5) · 검산 실측 140개 셋 다 0.**
> ⛔ **폰엔 안 깔았다** — 1·2단계는 확정 근거를 못 만든다. **「사진·음성이 여전히 보이나」는 3단계 설치에서 판정한다.**
> ### ✅ **3단계도 했다 — 실기기 판정 여섯 통과** (§9)
> **카드의 `+` → 종류 시트 → 사진 찍기/앨범에서 고르기** · 추가는 **op**(성역 아니다).
> ★★ **실데이터가 A·B·C를 한꺼번에 증명했다** — 새 파일명 꼴 · `set photo.<자료id>` · **한 항목에 둘·셋·다섯** ·
> 업로더가 새 이름을 올렸다 · **검산 photo 31 → 43 · 셋 다 0.**
> ⛔ **결함 하나:** 가로로 돌려 찍으면 카메라 화면이 깨진다(**사진은 정상** — 4032×3024로 쟀다)
> → **카메라만 세로 고정** 설치 `439f708`(00:31) · ⏸ **먹는지 판정 대기.**
> # ▶ **다음은 뷰어 `<` `>`** — 여럿이 실제로 생긴 지금 필요해졌다 (그 밖 → §3-Y-9)
>
> ### (앞 단계) ✅ **A와 B가 닫혔다 — 둘 다 사용자 결정**
> **A 포인터 꼴 = 자료마다 별도 필드** `<종류>.<자료id>` (*"오케이! 가자"* · 19:4x) ·
> **B 파일명 = `<항목id>-<자료id>.<확장자>`** (20:1x).
> **정본 = `docs/native/media-expansion-design.md` §3-W**(A) · **§3-W-6**(B) · 값 표는 **§0-5의 36·37번.**
>
> ### ⚠️ **폰은 안 건드렸다 — 화면 변화 0**
> **폰 = `1f1d2f8`**(2026-08-23 17:01 설치 · 그대로). 이 세션은 **`SecondBrainCore`에만** 더했고
> **앱은 새 코드를 아직 안 쓴다.** ⛔ **1·2단계는 확정 근거를 못 만든다**(§3-V-5) — **폰은 3단계에서 연다.**
>
> # ~~▶ 다음은 C~~ → ✅ **했다**(아래 §7)
> ### (옛) ▶ 다음은 C — 조회의 축 (`forId:` → `forPointer:` · 2단계)
> 닿는 자리: `PhotoStore` 8 · `AudioStore` 7 · `MediaFetch` 6 · `MediaCard` 3 · `MediaViewer` 2 ·
> `InboxModel` 2 · ⛔ **`MediaCloud`는 통째로 id 기반**(§3-V-3).
> ⚠️ **함께 걸리는 것:** **2026-08-20 이관 검산이 「포인터 고유 수 = 항목 id와 1:1」 위에 서 있다** —
> 여럿이 되면 **그 전제가 깨진다. 검산식을 다시 세워야 한다**(§3-W-4의 4번).
> ⚠️ **표본은 아직 없다** — 같은 종류가 둘 이상인 항목이 **0개**다. **3단계(추가 기능)에서 처음 생긴다.**

---

## 1. A — 포인터 꼴 (§3-W)

**정한 것:** 자료마다 **별도 필드**. `photo: a.jpg, b.jpg` 같은 **쉼표 목록을 안 쓴다.**

**왜:** 병합이 **항목별·필드별 LWW**라 **다른 필드는 둘 다 살고 같은 필드는 하나만 산다**
(`merge-design.md` §3). 쉼표 목록은 **같은 필드**라서 두 기기가 각각 붙이면 **한쪽이 통째로 진다**
— **자료가 사라지고**, 파일은 남으므로 **조용하다**(고아가 된다).

★ **병합 규칙을 한 줄도 안 건드렸다** — 규칙이 아니라 **꼴**을 골랐다(항시 규칙 5의 살아있는 자산).

### 1-1. ⛔ 재 보니 제약이 나왔다 — **자료 id는 소문자 16진 32자(하이픈 없음)**

**필드 이름이 되니까 파서가 읽을 수 있는 글자만 쓸 수 있다.** 19:2x에 `EventLog.parse`에 넣어 **쟀다:**

| 키 꼴 | create·편집 블록 **필드 줄** | `set k=v` | `fields.v1` |
|---|---|---|---|
| 하이픈 있음 | ⛔ **줄이 통째로 스킵된다 — 아무 신호도 없다** | ✅ | ✅ |
| 대문자 있음 | ⛔ 소문자로 접혀 **다른 키**가 된다 | ✅ | ✅ |
| 소문자 16진 | ✅ | ✅ | ✅ |

**원인 두 줄:** `fieldRe`가 키를 `[A-Za-z0-9_.]+`로만 읽고, 같은 자리가 키를 `lowercased()`한다.
⚠️ **`UUID().uuidString`은 대문자+하이픈이다** — **항목 id가 그 꼴이라 그대로 쓰기 쉬운데 키에 쓰면 안 된다.**
(항목 id는 필드의 **값**이라 이 제약에 안 걸린다 — `id: 495BA146-…`는 지금도 잘 읽힌다.)

⚠️ **안 고른 길:** 파서 charset을 넓혀 하이픈을 허용하는 것 — **왕복 계약과 레거시 파일을 건드린다.**
✅ **키 꼴을 좁혀 위험을 없앴다.**

## 2. B — 파일명 (§3-W-6)

**`<항목id>-<자료id>.<확장자>`** — 예: `495BA146-…-4c0882a0….jpg`

★ **§3-V-2의 두 안 어디에도 없던 셋째 꼴이다** — ㉮의 「어느 기억 것인지 안다」를 ㉯의 「충돌 없다」와 함께 갖는다.
**내가 처음에 기울여 말한 것은 `<자료id>.jpg`였다.** **파일명을 코드에서 훑어 보니** 파일명을 항목과
맞춰 보는 자리가 **앱에 없어서**(두 Store의 한 줄이 전부) — **갈리는 축이 코드 비용이 아니라
「폴더를 사람이 읽을 수 있나」**였다. 그 축에서는 **항목 id 접두어가 값을 한다**(grep · 정렬 · 고아 판정).

**옛 파일은 이름을 안 바꾼다**(§6). **가르는 법:** 항목 id에는 하이픈이 있고 자료 id에는 없다 →
**마지막 하이픈 뒤가 소문자 16진 32자면 새 꼴.**

## 3. 못 박은 자리 — **결정 파수꾼 11개**

- **`MediaPointer.swift`**(Core 신설) — 꼴의 **한 자리**. 자료 id 생성 · 필드 이름 조립·해석 ·
  **옛 단일 필드와 함께 읽기** · 파일명 조립·해석.
  ⛔ **화면 순서 결정이 아니다** — `pointers`의 정렬은 **결정적이려고** 정한 것뿐이다.
- **`MediaPointerTests`(11개)** — 머리에 **①무슨 결정 ②사실 ③깨지면 무엇을 의심하나**를 적었다.
  ★ **대조군이 둘이다** — **2번**(㉮ 쉼표 목록이 자료를 잃는다) · **10번**(`-n`이 두 기기에서 **같은 이름**을 만든다).
  **그 둘이 통과해야 「별도 필드/자료 id가 낫다」가 증거가 된다**(계측 규칙 7의 판별력).
- **8번은 「지금 사실」을 못 박는다** — create 블록(성역)은 **아직 자료 필드를 안 쓴다**(화이트리스트 고정 목록).
  ⛔ **깨지면 = 화이트리스트를 연 것**이고, 그때 **하이픈 규칙을 반드시 함께** 본다.

**계측 조건**(계측 규칙 8): **표본** = 손으로 만든 이벤트·create 블록 텍스트(실데이터에는 이 꼴이 **하나도 없다**) ·
**배치** = 조각 파일 왕복 포함(직렬화 → 파싱 → 병합) · **범위** = 세 왕복 경로 전부.

**시험:** `swift test --package-path SecondBrainCore`
— **19:49에 394 통과**(A 8개 포함 · 그 전 386) → **20:13에 397 통과**(B 3개 추가) · **0 실패 · `error: 0`**.

## 4. 문서

**§3-W 신설**(A: 결정·실측표·계측 조건·남은 것 넷) · **§3-W-6**(B) · §0-5에 **36·37번** ·
§3-V-1·§3-V-2에 **「정해졌다」** · **`photo-capture-design.md` §4-②의 예고를 「㉯로 정해졌다 · 쓰지 않는다」로**
갱신(예고·경고는 **안 지웠다**) · HANDOFF 재개 지점.

## 5. 정본 훑기 — **지금 고칠 것은 없다. 2단계에서 낡을 자리 둘을 찾아 뒀다**

> ⚠️ **이 절은 A·B를 끝낸 20:2x의 값이다 — 같은 세션 안에서 낡았다.**
> **그날 저녁 C를 하면서 그 자리들을 실제로 고쳤다 → §7-5.**

축이 실제로 바뀌는 것은 **2단계(조회)**이고 **코드는 아직 1:1이다.** 그래서 **아직 참인 문장**들이다:
- `audio-capture-design.md` §1 — *"항목 ↔ 음성 1:1 결정적 연결"*
- **2026-08-20 이관 검산식** — *"포인터 고유 수와 1:1"*(§3-W-4의 4번에 적었다)
- **사양서 §(사진 저장) — *"이 포인터는 **create 이벤트에만** 있어 병합 때 절대 안 덮인다 = 사실상 불변(성역)"***
  ⚠️ **나중에 붙는 자료는 op으로 간다** → 그 문장이 **새 자료에는 안 걸린다.** **⏸ 사양서 반영은 사용자에게 물었다.**

## 6. 다음 할 일

1. **C — 조회 축**(2단계 · §3-V-3) + **이관 검산식 재수립.**
2. **create 블록 화이트리스트**(제약 10 · 성역 경계) — **기억하기 전에 붙는 자료가 여럿일 때만** 필요하다.
3. **3단계 — 추가 기능**(카드의 `+` → 카메라·앨범). ★ **표본이 여기서 처음 생기고, 폰 판정도 여기서 한다.**

## 7. C — **조회의 축을 파일명으로** (§3-X · 20:3x~21:4x)

**옛 축:** 항목 id를 받아 **이름을 계산**했다(`"\(id).jpg"`) → 그것이 **자료 하나의 상한**이었다.
**새 축:** **포인터 값(파일명)을 받는다.** 바뀐 것은 **매개변수 하나**이고, 자리는 **일곱 파일**이다
(Core 셋 · 저장소 둘 · `MediaCloud`·`MediaFetch`·`MediaUploader` · 화면 둘).

### 7-1. ★ 안전 근거가 **셋 겹친다** — 하나만으로는 부족했다
1. **경로 문자열이 옛 이름에서 글자 그대로 같다**(파수꾼 시험 둘 신설).
2. **★ 포인터 값은 애초에 `finalize`가 돌려준 파일명 그 자체다**(`extra["photo"] = name`) —
   **그래서 「이름으로 찾기」와 「id로 찾기」는 정의상 같다.** 1번은 *계산*이 같다는 것이고 이것은 *값*이 같다는 것이다.
3. **실데이터 검산 통과**(아래 7-3).

### 7-2. ⛔ 축을 바꾸니 **잠자던 결함 하나가 튀어나왔다**
`MediaCloud.adopt`가 **iCloud는 `candidates()`로 찾는데 로컬에 만들 이름은 id에서 계산**하고 있었다.
**옛 꼴에서는 두 값이 같아 아무 일도 안 났다.** 새 꼴에서는 어긋나서 **받아온 파일을 못 찾게** 된다.
★ **컴파일 오류로 드러났다** — `id`가 사라져 그 줄이 안 넘어갔다. **축 변경이 그 자리를 들춘 것이다.**

### 7-3. ✅ 검산식 재수립 — **1:1을 버리고 셋으로**
**누락**(포인터가 가리키는데 파일 없음) · **고아**(파일은 있는데 포인터 없음) · **겹침**(한 이름을 둘이 가리킴).
도구 **`native/tools/media-audit.py`**(⚠️ 이름만 읽는다 = dataless를 안 받아온다 · `CLAUDE.md`에 올렸다).
**실측 2026-08-23 21:35 맥미니:** audio **109/109** · photo **31/31** · **셋 다 0**
(⚠️ 08-20의 135와 다른 수다 — 그 뒤 수집이 늘어 **140**이다).
★ **「셋 다 0」을 그냥 안 믿었다** — **대조군 표본을 지어 넣어** 누락·고아·겹침을 **각각 잡는지 확인**했다.
⛔ **그 확인이 없으면 「0」은 「도구가 아무것도 못 본다」와 구분되지 않는다.**

### 7-4. 지킨 것 / 안 한 것
**지킨 것:** 시험 **399 통과**(파수꾼 둘 추가) · 빌드 **`EXIT=0`·`error: 0`**(시뮬 26.5 UDID) · 검산 실측 · 코드 대조.
⛔ **안 한 것 — 화면 확인.** 폰에 안 깔았고(확정 근거를 못 만든다) **시뮬로도 못 한다** —
샘플은 **포인터만 있고 파일이 없어** `.here` 갈래를 안 밟는다(§3-L · 계측 규칙 8).
→ **「사진·음성이 여전히 보이나」를 3단계 설치 판정에 얹었다**(§3-X-6).

### 7-5. 정본 훑기 — **축이 실제로 바뀌었으니 이번엔 고쳤다**(다섯 자리)
`audio-capture-design.md` §1(「1:1」의 범위를 **유지/뒤집힘으로 갈라** 적었다) ·
`media-icloud-design.md`(함수 이름이 **「당시」 것임을 명시** · **1:1 검산 줄에 새 검산식 링크**) ·
설계 §6의 **9-a·9-b 닫힘 표시** · `SampleData` 주석(**「필드 값은 조회에 안 쓰인다」가 뒤집혔다**).

## 8. 다음 할 일 (갱신)
1. **3단계 — 추가 기능**(카드의 `+` → 카메라·앨범 · 제약 10). ★ **표본·폰 판정이 여기서 처음 생긴다.**
2. **자료마다 상태**(`MediaFetch`가 종류마다 하나다) · **뷰어 `<` `>`** — 둘 다 3단계에서 걸린다.
3. **create 블록 화이트리스트** — 기억하기 **전에** 붙는 자료가 여럿일 때.

## 9. 3단계 — **카드의 `+`로 사진을 붙인다** (§3-Y · 23:0x~00:3x)

**사용자 결정 여섯:** 범위는 **사진만** · 추가는 **성역이 아니다(op)** · `+`는 **종류를 먼저 고른다** ·
**여섯을 다 보이고** 안 되는 것은 **「아직 못 담아요」** · 문구는 **「사진 찍기」·「앨범에서 고르기」** ·
**카메라만 세로 고정** · 대표는 **수집 당시의 원본 우선.**

### 9-1. ★★ 실데이터가 **A·B·C를 한꺼번에** 증명했다
`4C0882A0-…-18a8587849f4….jpg`(새 파일명 꼴) · `set photo.18a85878…=…`(op) ·
**한 항목에 둘·셋·다섯** · **업로더가 새 이름을 그대로 올렸다**(C의 `Item.name`이 실제로 돈 것) ·
**검산 photo 31 → 43 · 누락·고아·겹침 0.**
★ **셋 중 하나라도 틀렸으면 이 줄들이 안 나온다** — 그래서 이것이 A·B·C의 공통 판정이다.

### 9-2. ⛔ 결함 하나 — 가로 촬영
앱 창은 도는데 카메라 화면은 **세로 배치 그대로**(스샷 둘). **원인: `project.yml`에 방향 키가 없다**(iOS 기본값).
✅ **찍힌 사진은 정상 — 쟀다:** 가로 **4032×3024** / 세로 3024×4032.
⚠️ **그 계측이 상태를 바꿨다** — 그 파일들이 맥미니에 받아졌다(dataless가 아니게 됐다).
→ **카메라만 세로 고정.** ⚠️ **먹는지는 판정 대기** · ⚠️ **수집 화면도 같은 래퍼라 원래 있던 결함일 수 있다**(미확인).

### 9-3. ⏸ 대표 사진 — 왜 「무작위로 보였나」
**수집 원본이 있으면 그것이 첫째다**(성역을 먼저 읽는다) — `A3D2A7AF`가 그 경우다.
**없으면**(`4C0882A0`·`BFE53B0B` = 음성만 있던 항목) 붙인 것 중 **자료 id가 가장 작은 것**이 보인다.
⛔ **「붙인 순서」로 못 한다** — `MergeEngine`이 **필드별 HLC를 결과에 안 남긴다.** 내보내야 풀린다.

### 9-4. 뒤집힌 것 둘 (지우지 않고 갈라 적었다)
**①** 「없는 기능을 알리는 문구를 짓지 않는다」 → **알린다**(앞문장 「누르면 아무 일 없는 단추를 만들지 않는다」는 유지).
**②** 「추가·삭제는 뷰어에」 → **추가는 카드의 `+`**(삭제는 그대로 뷰어) · §0 25번·§3-F-6에 적었다.

## 10. 다음 할 일 (갱신)
1. **뷰어 `<` `>`** — 여럿이 생긴 지금 필요해졌다.
2. **대표 사진**(필드별 HLC를 내보내는 것이 선결) · **삭제**(뜻 ③) · **자료마다 상태.**
3. **가로 판정** · **수집 화면도 같은 결함인지 확인.**
