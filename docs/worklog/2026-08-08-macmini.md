# 2026-08-08 (맥미니) — B: 고르는 단계의 상한을 뺐다

> **재개 한 줄:** 폰에 올리고 **`date-roles-verify-checklist.md` B-5-B(①②③) + B-5-C**를 실기기에서 확인한다.
> ①②가 판정선, ③이 대조군. 확인 전까지 "고쳐졌다"고 말하지 않는다.

---

## 0. 오늘 한 일 한 줄

사용자가 **저장 전 상태에서 값이 조용히 바뀌는 것**을 실기기에서 잡았다. 조사 → 대조군으로 원인을 가름 →
**날짜 피커의 범위 제한(`in: ...ub`)을 제거**하고 규칙 1을 **[저장] 한 곳에서만** 막게 바꿨다.
미결 「DatePicker가 선택값을 조용히 당기나」(G-3)가 **닫혔다 — 답은 「당긴다」.**

---

## 1. 버그 — 사람이 안 건드린 값이 바뀐다

**재현 (실기기, 2026-08-07 밤):**
1. 회차 시각(마감) = `08-08 19:00`
2. **날짜만** `08-09`로 바꿈 — **저장 안 함**
3. 미리 알림 = `08-08`, 시각 토글 꺼짐
4. 미리 알림의 **시각 토글을 켬**
→ 미리 알림 날짜가 **저절로 `08-09`** 가 된다.

**어긋난 원칙:** *"앱은 사람이 안 건드린 값을 조용히 바꾸지 않는다"* — 2026-08-06 (b)를 만들 때 세운 것이다.

---

## 2. 조사 — 무엇을 배제했나

**토글 코드는 날짜를 못 바꾼다**(증명됨). `timeEnabledBinding`은 원래 값의 날짜부를 그대로 다시 찍는다:

```swift
let base = Self.fmt.string(from: ItemSchedule.parseDay(b.wrappedValue ?? "") ?? Date())
b.wrappedValue = on ? "\(base)T09:00" : base      // "2026-08-08" → "2026-08-08T09:00"
```

`timeBinding`(시·분)도 날짜부를 `wrappedValue`에서 가져오므로 마찬가지다.
**미리 알림 날짜를 바꿀 수 있는 코드는 `dateBinding`의 setter 하나뿐이고, 그것을 부르는 것은 DatePicker밖에 없다.**
(완료·취소·미루기는 이 절차에서 안 눌렸다.)

**상한이 토글에 반응한다.** `DetailView:383`이 body 안에 있어 매 렌더 다시 계산되고, 입력 둘이 다 화면 값이다:

| 시점 | resurface | `resurfaceHasTime` | 상한 |
|---|---|---|---|
| 토글 OFF | `2026-08-08` | false | `startOfDay(08-09) − 1일` = **08-08** |
| 토글 ON | `2026-08-08T09:00` | true | `startOfDay(08-09)` = **08-09** |

토글 하나로 범위가 `...08-08` → `...08-09`로 넓어지고, **새 값이 새 상한과 정확히 같다.**
`due`가 draft(@State)라 **저장 안 한 마감 편집이 이미 상한을 움직인다**는 것도 여기서 드러났다.

---

## 3. ★ 대조군이 갈랐다 (2026-08-08)

코드만으로는 "왜 넓어지는데 끌려가나"를 못 정했다. 실기기에서 셋을 쟀다:

| | 결과 | 뜻 |
|---|---|---|
| ① 토글 끄기 | 08-09 → **08-08** | 상한이 좁아지자 당김 = **G-3이 묻던 바로 그것** |
| ② 켜기/끄기 왕복 | 08-08 ↔ 08-09 | **양방향.** 값이 상한을 따라다닌다 |
| ③ **대조군** — 08-07에서 켜기 | **안 움직임** | **상한에 붙어 있을 때만** 끌린다 |

**③이 결정적이다.** 토글이나 시·분 피커가 원인이라면 ③도 움직였어야 한다. 움직인 것과 안 움직인 것의
차이는 `in: ...ub` 하나뿐 → **범위를 가진 피커가 값의 원천이 되고 있다.**

**왜 두 번(08-06 저녁·08-07 저녁) 못 닫혔는지도 설명된다** — 그때는 값이 상한 안쪽이거나 상한이 nil이라
**끌릴 조건 자체가 없었다.** ①의 경로도 예상하던 것(마감을 당김)이 아니라 **시각 토글**로 왔다.

> **배운 것:** 부재를 예측할 때만 대조군이 필요한 게 아니다. **"움직였다"를 원인에 붙일 때도** 필요하다 —
> 안 움직이는 자리를 같이 재지 않으면 "원래 그런 것"과 못 가른다.

---

## 4. 뿌리와 결정

**뿌리:** `dateBinding`의 setter(`:815`)는 피커가 뱉은 값이 **사람이 고른 것인지 피커가 스스로 재조정한
것인지 구분할 수 없다.** 범위를 준 순간 피커는 값을 뱉을 이유가 생긴다.

**후보 A**(setter에 가드) — 못 쓴다. 피커가 미는 값도 "실제로 다른 날짜"라 원리적으로 구분이 안 된다.
**후보 C**(상한을 그 날 끝으로) — 오늘 증상만 덮고 뿌리를 남긴다.
**→ 후보 B 채택 (사용자, 2026-08-08).**

**사용자 근거:**
- 날짜 칸이 **두 가지를 섞어** 하고 있다 — 못 고르게 막는 것과 값을 조용히 바꾸는 것. 뒤엣것은 아무도
  원하지 않았는데 앞엣것에 **딸려 온다.**
- **실제로 막지도 못한다.** 시각 칸엔 상한이 없어 같은 날 늦은 시각은 통과한다 — **반쪽 방어**다.
- 어제 만든 문구(`rule1Block`)가 정확히 이 흐름을 위한 것인데, 날짜와 시각의 경로가 갈려 **나올 길이 드물다.**
  B가 한 경로로 합친다.

---

## 5. 한 일

### 코드
| 자리 | 무엇 |
|---|---|
| `DetailView.swift` `timeSection`/`timeRow` | `in: ...ub` 분기 제거, `upperBound` 인자 제거, `bound` 계산 제거. **두 칸이 같은 `timeRow`를 같은 인자로 쓴다** = 날짜·시각이 같은 방식 |
| `DetailView.swift` `commit()` | 「최종 방어선」 → **「막는 유일한 자리」.** 사람이 위반을 고르는 것은 **정상 경로**임을 주석에 못박음 |
| `PickerBoundRemovedTests.swift` (새 파일, 7개) | 경계 못박기 |

**`resurfaceUpperBound`는 안 죽는다** — `deferSevenDays`·`rule1Block`(cap 계산)이 계속 쓴다.
**미루기(+7일)는 안 바뀐다** — 피커의 `in:`과 무관하고, 당겼으면 팝업으로 **말한다**(조용하지 않다).

### B는 방어를 넓힌다 (숫자로 확인)

| 경우 | 옛 피커 상한 | 저장 검사 |
|---|---|---|
| 미리 알림 시각 **없음** | 마감−1일 | **같은 경계** ✓ |
| 미리 알림 시각 **있음** | 마감 당일(날짜만) | **더 넓게** — 같은 날 **늦은 시각**도 잡는다 |
| 마감 없음/지남 | 제약 없음 | 같음 ✓ |

달라지는 것은 **언제 아느냐** 하나: 고를 때 → [저장] 누를 때.

### ⚠️ 단계 0은 만들 수 없다 (사용자 승인)

①②③은 UIKit `UIDatePicker`의 동작이고 **UI 테스트 타깃이 없다**(`project.yml` 확인).
그리고 B는 **판정 로직을 안 바꾼다** → 어떤 Core 시험도 **고치기 전에 이미 통과한다.**
그건 **거짓 안심이지 단계 0이 아니다.** 새 시험 7개는 **회귀 방지용**으로 명시했다(파일 헤더에 적음).

특히 `testSilentlyChangedValuePasses` — **오늘 조용히 만들어진 값 `2026-08-09T09:00`은 규칙 1 위반이 아니다**
(09:00 ≤ 19:00). 저장 검사를 **그냥 통과한다.** → **막는 것으로는 이 종류를 못 막는다. 안 바꾸는 것으로만 막힌다.**

**시험: 305개 통과 (0 실패).** 기존 298개 그대로 — Core를 안 건드렸다는 증거. iOS 빌드 성공.

### 문서
| 자리 | 무엇 |
|---|---|
| `memory-philosophy.md` | 「세 곳이 그걸 쓴다」 → **「막는 곳은 하나, 안 만드는 곳은 둘」**(B안). 끝에 한 줄: *"자리를 세는 방식은 2026-08-06에 이미 뚫렸다 — 자리를 더 넣어도 안 닫힌다."* |
| `worklog/2026-08-07-macbook.md` | 15:32 관찰 **안 지움.** 아래 「B로 대체됨」 블록 — *관찰이 틀린 게 아니라, 막음과 조용한 변경이 한 몸이라 함께 뺐다* |
| `recurrence-verify-checklist.md` **G-3** | **닫음.** `재현 2026-08-07 밤 · 대조군 포함 확인 2026-08-08` — **둘 다 적었다**(재현과 확인이 다른 날인 것이 이 관찰의 성격) |
| `date-roles-verify-checklist.md` **B-5** | 취소선 + 「B로 대체」. **막는 일은 B-6이 이어받는다 — 자리를 옮겼지 사라진 게 아니다** |
| 〃 **B-5-B**(새) | **B의 판정선** — ①②③ 표. ③이 대조군 |
| 〃 **B-5-C**(새) | B가 **새로 연** 경로 — 위반을 고를 수 있고 저장에서 막히는지. 어제 만든 문구가 실제로 뜨는지 |

---

## 6. 다음 (미결)

- [ ] **실기기 확인 — B-5-B(①②③) + B-5-C.** 사용자가 직접. **이것 전에는 "됐다"가 아니다.**
- [ ] **사양서 §0-A-2 되풀이 유형 표에 넣을지 — 물어볼 것.** 이 건은 표의 「화면의 말 ↔ 사실」과
      결이 조금 다르다(**말이 아니라 값**이 어긋났다). 2026-08-06 (b)와 같은 계열(★새 거짓 = draft를
      조용히 건드림)로 보이는데, **표를 늘릴지 열을 넓힐지는 사용자 판단**이다.
