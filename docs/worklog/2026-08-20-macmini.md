# 2026-08-20 (집 맥미니) — 단계 5 준비: **iCloud 다운로드 시간 실측**

> ## 재개 한 줄
>
> ### ▶ 다음 — **보안 스코프 수명 결정 → 단계 5(Mac 사진 보기)**
>
> `waitSeconds` 실측은 끝났다(아래 §2). **사용자가 스코프 수명을 고르면 단계 5에 들어간다.**
> ⚠️ **문서의 전제 하나가 흔들렸다** — 「Mac에서 걸린다」는 **확인 안 된 추정**이다(§4).

---

## 1. 세션 시작 — ⚠️ **9커밋 뒤처진 채로 시작했다**

| | |
|---|---|
| 기기 | 집 맥미니(`teri.kr`) |
| HEAD (시작 시) | `287c39b` — **원격보다 9커밋 뒤** |
| HEAD (pull 후) | `4455f0f` · clean · 원격과 `0 0` |
| 폰에 깔린 코드 | `f7018d6` = 저장소 마지막 코드 커밋 (`git diff f7018d6..HEAD -- native/`가 **빈 출력**) |

**사용자가 읽으라고 지정한 파일 둘이 그 HEAD에 없었다** — `docs/worklog/2026-08-19-macbook.md` ·
`media-icloud-design.md` **§12**. 어제 맥북 세션(단계 1~4 + 이관)이 안 내려와 있었다.

→ **`CLAUDE.md`에 「세션 시작 절차」를 신설했다** — `git pull`은 「작업 시작 전」이 아니라 **「문서를 읽기 전」**이다.
문장으로는 이미 있었는데(11행) **「읽기는 작업이 아니다」로 읽어서 안 걸렸다.**

**시작 확인 셋:**

| | 결과 |
|---|---|
| iCloud `audio/`·`photo/` | ✅ **135개 그대로** (음성 106 · 사진 29) · 루트 폴백 0 · 찌꺼기 0 · `.sb-media.log` 2줄(수정 08-19 18:56) |
| `xcodegen generate` | ✅ EXIT=0 |
| 시뮬 UDID | `iPhone 16 Pro (26.5)` = **`34A5033B-BCFC-4FF9-AD1B-7756397C09DE`** (Booted) |

### ⚠️ 시뮬 이름 겹침이 **런타임 축으로** 실제로 있었다

| 런타임 | 표시 이름 | UDID |
|---|---|---|
| iOS 26.5 | **`iPhone 16 Pro (26.5)`** | `34A5033B-…` |
| iOS 26.2 | `iPhone 16 Pro` | `734ABE20-…` |

**26.5 쪽만 이름 끝에 `(26.5)`가 붙어서** `name=iPhone 16 Pro`는 **26.2로 간다.**
⛔ **이 오작동은 `EXIT=0`으로 끝난다** — UDID 오타(`EXIT=64`)와 반대 성질이다.
→ `CLAUDE.md` 빌드·테스트 ③에 이 대조표를 넣었다.

---

## 2. ⛔ 조작 기록 — **표본 16개를 받았다. 그만큼 dataless가 사라졌다**

**측정: 2026-08-20 00:53:37 ~ 00:54:19 KST · 맥미니 · 커밋 `4455f0f`(코드 변경 0줄)**

| | |
|---|---|
| 무엇을 | `startDownloadingUbiquitousItem` 뒤 상태가 `Current`가 될 때까지의 시간 |
| 표본 | **16개** (음성 8 · 사진 8) — 크기 분위로 골랐다 |
| 상태 변화 | 16개가 `NotDownloaded` → **`Current`.** **되돌릴 수 없다**(다시 만들려면 `brctl evict`) |
| 남은 dataless | **119개** — 단계 5 검증 재료로 **일부러 남겼다**(사용자 지시) |
| 도구 | `측정 스크립트는 스크래치패드에만 있다` — 저장소에 안 넣었다 |

**검산:** 총 135 불변 · 바이트 있음 16 · 바이트 없음 119 · 할당 크기 `audio 1.5M` + `photo 4.5M`(받은 것만).

### 회선 (00:52 · 같은 세션)

| | |
|---|---|
| 경로 | **유선 `en0`** · `2500Base-T full-duplex` · active (Wi-Fi 있으나 기본 경로 아님) |
| 용량 (`networkQuality -s`) | **다운 850.7 Mbps** · 업 748.3 Mbps |
| 지연 | idle **6.2ms** · 다운링크 응답성 19.4ms |

**⚠️ 다른 회선에서는 다를 수 있다 — 아래 값은 이 회선의 값이다.**

### 잰 값 (ms · 전부 실측)

| 종류 | n | 최소 | 중앙 | 최대 | 평균 |
|---|---|---|---|---|---|
| **전체** | **16** | **526** | **604** | **924** | **654** |
| 음성 | 8 | 526 | — | 924 | 699 |
| 사진 | 8 | 537 | — | 689 | 609 |

**크기 vs 시간 (bytes / ms):**

```
   78647  918  audio      235966  604  photo
   85616  826  audio      353875  689  photo
  102045  545  audio      462625  585  photo
  118218  526  audio      537836  657  photo
  118227  924  audio      572898  537  photo
  160345  628  audio      660492  549  photo
  229936  635  audio      777973  585  photo
  662797  597  audio     1083603  673  photo
```

### ★ 결론 셋 — **크기가 아니라 왕복이 지배한다**

1. **크기와 무관하다.** 피어슨 **r = −0.364**(인과로는 말이 안 되는 방향 = 잡음이 지배).
   **결정적 대조:** `118218B = 526ms` vs `118227B = 924ms` — **9바이트 차이에 398ms 차이.**
   가장 큰 1,083KB가 673ms, 가장 작은 78KB가 918ms다.
2. **웜업 비용이 아니다.** 1차 배치의 최대값(918ms)이 첫 항목이어서 데몬 콜드스타트를 의심했는데,
   **2차 배치(웜 상태)의 중간 항목도 924ms**였다. 첫 항목만 느린 것이 아니다.
3. **파일당 고정 왕복 ~0.5~0.9초**로 읽는 것이 맞다. 대역폭은 남는다(850Mbps에 1MB).

### ⚠️ 못 잰 것 — **값을 만들지 않았다**

| 무엇 | 왜 |
|---|---|
| **부분 진행률** | **관측 불가.** `totalFileAllocatedSize`가 0 → 완주값으로 **한 번에** 바뀐다(원자적 교체). 최초바이트 시각이 완료 시각과 **같은 값으로 나왔다** |
| **iOS** | 안 쟀다. 폰은 로컬 사본이 있어(§5) 이 경로를 안 탄다 |
| **셀룰러·느린 Wi-Fi** | 안 쟀다. 유선 850Mbps 하나뿐 |
| **1MB보다 큰 파일** | 안 쟀다. 표본 최대가 **1,083,603B** — **자료 확장 ②(PDF·동영상)에서는 이 값을 다시 재야 한다** |

> ### ★ 「부분 진행률 관측 불가」가 설계를 지지한다
> §8의 「원본 음성 받는 중…」에 **퍼센트가 없는 것이 맞다.** 붙일 수 있는 값이 애초에 없다
> (`NSMetadataQuery`의 `percentDownloaded`를 새로 쓰지 않는 한). **문구를 안 바꿔도 된다.**

---

## 3. ★ 실측이 드러낸 것 — **문제는 15초가 아니라 1초 폴링이다**

`MediaFetch.swift:39-45`가 **1초 간격으로** 상태를 다시 본다:

```swift
private static let waitSeconds = 15
...
for _ in 0..<Self.waitSeconds {
    try? await Task.sleep(for: .seconds(1))   // ← 여기
```

| | |
|---|---|
| 실제 다운로드 | **0.53~0.92초** |
| 화면이 아는 시점 | **1.0초**(첫 폴링) — 빠르면 **0.5초를 그냥 기다린다** |
| 15초가 걸리는 경우 | 이 회선에서는 **없었다**(최대 0.92초 = 한도의 **6%**) |

**즉 `waitSeconds`는 넉넉해서 문제가 아니고, 사용자가 느끼는 지연은 폴링 간격이 만든다.**

---

## 4. ⚠️ 문서의 전제 하나가 흔들렸다 — 「Mac에서 걸린다」

설계 §2-A·§4·HANDOFF가 **「폰은 로컬 사본이 있어 안 걸리고 Mac에서 걸린다」**로 적고 있다.
그런데 **잰 것:** `native/project.yml`에 **엔타이틀먼트 파일이 아예 없다**(`*.entitlements` 0개 ·
`sandbox` 키 0개). 설계 §2도 같은 사실을 *"맥 앱은 App Sandbox entitlement가 없어 보안 스코프가
애초에 안 걸린다"* 로 이미 적어 뒀다.

| | 잰 것 / 추정 |
|---|---|
| 엔타이틀먼트 파일 없음 | ✅ **잰 값** |
| 그래서 **macOS에서 스코프가 no-op**이라 스코프 밖 읽기가 **된다** | ⚠️ **추정.** 문서에 근거는 있지만 **이 앱으로 확인 안 했다** |
| **iOS는 무조건 샌드박스**라 스코프가 **실제로 걸린다** | ⚠️ **추정.** 폰은 로컬 사본이 가려서 **현상이 안 난다** |

> ### ⛔ 그러면 방향이 뒤집힌다
> 「Mac에서 터지고 폰은 안전」이 아니라 **「Mac에서는 안 터지고, 폰에서는 로컬 사본이 가리고 있을 뿐」**일 수 있다.
> 후자면 이건 **잠복 결함**이다 — 폰의 로컬 사본이 없어지는 날(재설치 · 두 번째 기기) **조용히 나타난다.**
> **어느 쪽이든 스코프 수명은 정해야 한다.** 다만 **「Mac에서 재현해서 고친다」는 계획은 성립하지 않을 수 있다.**

---

## 5. 사용자 결정 둘 (2026-08-20 · 실측을 보고)

| | 답 | 사용자 근거 |
|---|---|---|
| **보안 스코프 수명** | **C — 받으면 로컬에도 둔다** | **★ 「추정 위에 설계를 안 얹는다」가 결정적.** 전제가 흔들린 지금 A·B는 어느 쪽이 맞는지에 따라 설계가 달라지는데 **C만 그 답과 무관하다** — 스코프 밖에서 읽는 코드가 아예 없어진다. 그리고 **「받는 중」이 파일당 한 번으로 끝나는 것은 정직함의 문제** — A·B는 evict될 때마다 다시 받고 사용자는 「아까 받았는데 또?」가 된다 |
| **`waitSeconds` 15초** | **유지 + 폴링 1초 → 0.2초** | 15초는 **「안 온 것으로 본다」 문턱**이라 넉넉함이 비용이 아니다. **줄일 근거가 없다 — 안 잰 조건이 셋**(셀룰러·느린 Wi-Fi·1MB 넘는 파일). 사용자가 느끼는 지연은 **폴링이 만든다** |

**정본에 올린 것 넷:**

| 자리 | 무엇 |
|---|---|
| **§2-A** | 미결 → **C 채택** · 옛 서술(「Mac에서 걸린다」)을 **「흔들린 전제」로 남겼다**(지우지 않았다) |
| **§5** | 로컬 사본의 뜻이 **「확정 때 쓴 것」 → 「받은 것도」**로 넓어졌다 · **무엇이 유지되나**를 같은 자리에 |
| **§6** | 실측 전문 + **「15초를 재려다 1초 폴링이 진짜 문제인 것을 알았다」** + 회선 조건 + 못 잰 것 넷 + 자료 확장 ② 예고 |
| **§8** | **퍼센트가 없는 것이 맞다** — 문구를 정할 때는 몰랐던 근거가 **뒤늦게** 붙었다 |

### ★ 전제 확인은 단계 5에서 함께 한다 (사용자 지시)

**「C가 그 답에 안 의존하는 것과, 그 답을 몰라도 되는 것은 다르다」**:

1. **§2는 「맥 앱은 엔타이틀먼트가 없어 스코프가 안 걸린다」고 적었는데 §2-A가 안 이어받았다** —
   **문서 안에서 어긋나 있다.**
2. **「폰에서 잠복 결함」이 사실이면 C와 별개로 알아야 할 사실이다** —
   **다른 코드에도 같은 패턴이 있을 수 있다.**

---

## 6. 단계 5 구현 — C안 + 폴링 0.2초 + Mac 사진 보기

### 코드

| 자리 | 무엇 |
|---|---|
| **Core** `MediaAdopt.swift` (신규) | 들여올 것인가(`shouldAdopt`) + **임시 이름 규칙**(`MediaAdoptNaming`) |
| **Core** `MediaAdoptTests.swift` (신규) | 시험 **9개** — 322→356→**365** |
| `MediaCloud.swift` | `readableURL` **삭제** → **`adopt(_:id:intoDir:)`** · `sweepAdoptLeftovers` |
| `AudioStore.swift` · `PhotoStore.swift` | `url(forId:)`가 **로컬만** 본다 · `adoptFromCloudIfNeeded` 신설 · `localFiles()`에 `.part` 방어 |
| `MediaFetch.swift` | **폴링 1초 → 0.2초** · 판정 **전에** 들여오기(메인 밖) |
| `FragmentFolder.swift` | `withFolder` 주석에 **규칙**을 올렸다 — 「`body`는 URL이 아니라 값을 돌려준다」 |
| **App** `PlatformMedia.swift` (신규) | 이미지 로딩 · 지도 앱 열기의 플랫폼 갈래 **둘만** |
| `DetailView.swift` | `photoRow`·`photoMap`에서 **`#if os(iOS)`가 사라졌다** · 안 쓰는 `UIKit` import 제거 |

### ⛔ 잡은 함정 하나 — 임시 이름이 업로더에 잡히면 **고아를 만든다**

`localFiles()`는 로컬 디렉터리를 **확장자로 걸러** 업로더의 차집합 왼쪽을 만든다. 들여오기 임시 이름을
`sb-adopting-<id>.m4a`로 두면 **그 필터를 통과해** 업로더가 id를 `sb-adopting-<id>`로 계산하고
**iCloud에 엉뚱한 이름의 파일을 올린다.** 그 파일은 어느 포인터와도 안 맞아 **고아**가 되고,
차집합이 매번 재계산되므로 **지워도 다음 실행이 또 올린다**(§5의 「지우면 끝이 아니다」와 같은 함정).

→ 임시 확장자를 **`.part`**로 두어 그 필터에 **애초에 안 걸리게** 했다.
**시험이 이것을 못박는다** — `testPartName_확정파일_필터에_안_걸린다` + **대조군**
(`testPartName_대조군_확정파일은_걸린다` — 필터가 망가져도 앞 시험이 초록이면 알 수 없으므로).

### 검증 (전부 잰 값 · 2026-08-20 01:0x~01:1x)

| | 결과 |
|---|---|
| `swift test --package-path SecondBrainCore` | ✅ **365개 · 실패 0** (356 → **+9**) |
| `xcodegen generate` | ✅ EXIT=0 (**새 파일 둘 때문에 반드시 필요**) |
| iOS 빌드 (`id=34A5033B-…` = **`iPhone 16 Pro (26.5)`**) | ✅ EXIT=0 · `error:` **0** · `warning:` **0** |
| macOS 빌드 (`SecondBrainApp-macOS`) | ✅ EXIT=0 · `error:` **0** · 코드 경고 **0** (AppIntents 알림은 코드가 아니다) |

⚠️ **처음엔 경고 2건이 났다** — `UIApplication.shared.open`이 메인 액터에 묶여 있었다.
`PlatformMedia.openInMaps`에 `@MainActor`를 붙여 닫았다(부르는 자리가 버튼 동작이라 **이미 메인**이다).

## 7. ✅ 전제 확인 — **「Mac에서 걸린다」는 틀렸다** (헤드리스로 쟀다)

**대조군이 성립한다:** 맥 앱과 측정 CLI가 **둘 다 샌드박스가 없다.**
`FragmentFolder`의 macOS 갈래와 **같은 호출**을 재현했다:

| 무엇 | 잰 값 |
|---|---|
| `bookmarkData(options: [.withSecurityScope])` | ✅ 성공(808 bytes) |
| `URL(resolvingBookmarkData:, options: [.withSecurityScope])` | ✅ 성공 · `stale=false` |
| `startAccessingSecurityScopedResource()` | **`true`** |
| **스코프 닫은 뒤 iCloud 파일 읽기** | ✅ **읽혔다**(160,345 bytes) |

> ⛔ **옛 서술이 거꾸로였다.** 「Mac에서 걸리고 폰은 안전」이 아니라
> **「Mac에서는 안 걸리고 폰에서는 로컬 사본이 가리고 있을 뿐」**이다.
> **옛 계획(「Mac에서 재현해서 고친다」)은 성립하지 않았다.**
>
> ⚠️ **내가 추정한 메커니즘도 틀렸다** — 「샌드박스가 없으니 `startAccessing…`이 **false**일 것」이라고
> 적었는데 실제로는 **`true`**였다. **결론은 맞았지만 이유가 달랐다.**
> 그래서 잰 값으로 적고 추정한 메커니즘은 정정으로 남겼다(설계 §2-A).

**iOS 쪽은 못 쟀다** — 폰은 로컬 사본이 가려 이 경로를 안 탄다. 탐침 빌드가 필요하고,
**C가 그 경로를 없애므로 재야 할 이유가 사라졌다.** **「못 쟀다」로 남긴다 — 값을 만들지 않는다.**

### ✅ 같은 패턴이 다른 데 있나 — **훑었다: 0개**

`withFolder` 호출 **일곱** 개의 반환형이 전부 `Plan?`·`Bool`·`Void`·`[MediaKind: MediaPlace]?`·`(Bool, Bool)`다.
**URL을 내보내는 것은 0개.** `read()`도 내용(`String`)을 돌려준다.
**`readableURL`이 유일한 위반이었고 사라졌다.** → 규칙을 `withFolder` 주석에 올렸다.

## 8. 맥 앱 실행 전 기준선 (2026-08-20 01:14:54 · 실측)

| | |
|---|---|
| 맥 로컬 `~/Library/Application Support/SecondBrain/audio` · `photo` | **둘 다 없음** ← 들여오기의 확정 근거가 될 자리 |
| `.sb-media.log` | **2줄** · 수정 **08-19 18:56:08** |
| iCloud 자료 | **135개** · **dataless 119** · 바이트있음 16 |
| 맥 앱 폴더 연결 | ✅ **북마크 있음**(808 bytes) · `sb_device_id=mac-1883` · 설정 파일 수정 **07-17 11:18** → **맥 앱은 07-17에 돌아간 적이 있다** |

### ★ 예측 — 무엇이 보여야 정상인가 (실행 **전에** 적는다)

| 무엇 | 예측 | 근거 |
|---|---|---|
| `.sb-media.log` | **2줄 그대로** | `MediaPlaceRecord`의 비교에 **기기가 안 들어간다**(kind·place·err만). 맥이 계산할 `place=subdir`가 앞 줄과 같으므로 `appendIfChanged`가 nil |
| 이관 배너 | **안 뜬다** | 맥 로컬이 비어 있어 차집합(로컬−iCloud)이 **0** → `plan.pending` 없음 |
| iCloud 파일 수 | **135 그대로** | 올릴 것이 없다 |
| 맥 로컬 디렉터리 | 사용자가 본 항목의 자료가 **생긴다** ← **들여오기의 확정 근거** |
| dataless 수 | 본 만큼 **줄어든다**(119에서) | 상세를 열면 그 항목 것 하나만 받는다(§6) |
