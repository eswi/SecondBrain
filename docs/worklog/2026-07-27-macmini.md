# 작업 로그 — 2026-07-27 (mac mini · 07-25 저녁~심야 이어서)

> **⚠️ 재개 한 줄(내일 회사 맥북):** §7 분류–세부정보 모델 **Stage A~C 완료·실기기 통과**.
> 다음 = **Stage D**(며칠 실사용 뒤) — 제목(=의미)·동작 규칙 확장 · `FlexTypeCatalog`를 §7 모델로 흡수 ·
> 6종 정리 · 자동분류 연결(O4). **당장 급하지 않음** — 며칠 써보고 §7 감각 얹은 뒤 착수.
> `git pull` → `cd native && xcodegen generate` → 스킴 **`SecondBrainApp-iOS`** 로 빌드.
>
> **인수인계 노트(신규):** `docs/HANDOFF.md` — 철학·용어·구현·이슈·열린질문·사양 미반영을 근거 경로와 함께 정리.
> 맥북에서 맥락 빠르게 잡으려면 이거부터. (단 §3 구현표·§5 열린항목은 Stage A~C 반영 전 스냅샷 — 아래 "다음 할 일" 참조.)

## 오늘 한 일 (07-25 worklog `e87fbb5` 이후, 커밋 순)

### 1. §7 분류–세부정보 모델 개념 정본화 (`a945a27`)
- `memory-philosophy.md` **§7 신설** — 핵심 전제(분류는 코드 고정·사용자가 안 만듦·세분은 해시태그·구조의 ART 회피),
  결정된 개념 그림(공통 그릇 + 분류=그릇 위 정의 (a)쓸지 (b)제목=의미 (c)동작 (d)누가값 / 개수 / 미디어 확장 /
  분류 평등 / resurface / 해시태그), 미결(구현·FlexTypeCatalog 정리·해시태그·동작·자동분류 O4).
- `open-questions.md` D1에 "→ §7에서 모든 분류 평등으로 재구성됨" 포인터(내용은 역사 보존).

### 2. 인수인계 노트 `docs/HANDOFF.md` 신설 + 3회 정제 (`78e132b`·`3d76806`·`f3c1b2f`)
- 저장소 전체 근거로 현재 상태 지도. 추측 배제·각 항목 파일경로/커밋 표기.
- 네이티브 v1 대상 = **Mac + iPhone 둘 다** 명시. spec은 **저장소 루트** 위치 명시.
- §2 용어표에 question·분류 2층 구조·유연층 추가. 정본 목록 6종(경로·역할) + open-questions는 정본 아님(논의 문서).
- **코어 테스트 = SwiftPM `swift test` → macOS 호스트 실행**("iOS 코어 테스트" 오라벨 수정). 근거 Package.swift·실행타깃 `arm64-apple-macos`·CI 부재.
- 3장 "검증 플랫폼" 열 + 4장 "검증 부채" 소절(미확인만: 자동분류·수집·EXIF·원칙 부분) + §5 "question 답변 경로 없음" 미결.
- 탭 점프 버그 메모리 `[[tab-jump-bug-pending]]` → `[[tab-jump-bug-resolved]]` 리네임(해결 완료 반영).

### 3. §7 구현 Stage A~C (`e118a69`·`c04b999`·`e6cfe96`)
**§2·§3·`MergeEngine`·`TypeMeta` 전부 무변경 — §7은 위에 얹은 표시 정책 층. 자동분류는 O4 그대로(§7이 안 앞당김).**

- **Stage A — 분류 관리 화면 제거** (`e118a69`) — §7 전제(분류 코드 고정)와 어긋나는 설정의 '분류 관리 → 재설계 후보'
  화면 삭제(`ClassificationManagerView.swift` 208줄 + 진입점). 폰 UserDefaults 후보 = **"루틴" 하나**(devicectl로 추출 확인,
  살마인드 아님) → open-questions 부록에 이관·보존(D3/O3 연결). `reclass.candidates.v1` 키만 일회성 정리
  (`SecondBrainApp.init`, 핵심 키 무변경). **실기기 통과**(설정에서 사라짐 + 키 제거를 devicectl 재추출로 확인).
- **Stage B — ClassDef 틀 도입, 동작 0** (`c04b999`) — `ClassDef.swift` 신설: `Detail`(due·resurface·photo·location)
  + `ClassDef`(key + 쓰는 Detail의 **Set**, 멤버십=씀) + `ClassCatalog.all` + `ClassRegistry.def(key)`. 최소 틀 —
  §7 (a)쓸지만(제목·동작·개수는 Stage D, 과잉추상화 회피). 7개 분류 전부 네 Detail 다 씀 = **현재 동작의 거울**.
  **동작 0 증명**: 참조 0(grep) + iOS·macOS 빌드 + 코어 71 불변.
- **Stage C — 주차위치만 마감 숨김, 첫 실동작** (`e6cfe96`) — `ClassCatalog`에서 주차만 `.due` 제거(6종 mirror 유지).
  `DetailView.timeSection`: `usesDue = ClassRegistry.def(normalizedType)?.uses(.due) ?? true` → false면 마감 행 숨김.
  **보장**: `.due` 제거는 parking key에만 → 6종 `usesDue=true` 회귀 없음, 미분류·미등록은 `?? true`로 마감 유지.
  **표시 전용**(마감 행만 숨기고 저장된 due 값 안 지움 — 비파괴적, due 무효화는 Stage D). **실기기 통과**
  (ⓐ 주차 마감 숨김·다시보기 유지 / ⓑ 6종 마감 유지 / ⓒ 미분류 마감 유지 / ⓓ 분류 전환 시 실시간 반응).

## 상태
- main = `e6cfe96`. **push 완료**(local=origin). working tree clean. 브랜치/worktree = main 단일(정리 불필요).
- 폰(iPhone 16 Pro): Stage C까지 최신 빌드 설치·실기기 검증 통과.
- 이어받을 기기 = **회사 맥북**(→ `git pull` 먼저).

## 사양서 반영 확인 (규칙 #2)
- **여전히 보류가 맞음.** §7 모델·주차·필터 동적화의 사양서(§2) 반영은 **Stage D까지 굳은 뒤**. 지금 반영하면 곧 또 고침.
  근거는 `memory-philosophy.md` §7 · `photo-capture-design.md` · `open-questions.md`(D1~D5, O1~O4)에 유지.
- 이미 반영된 것(변경 없음): 사진·EXIF 프라이버시(`db83c9a`, 지난 세션).

## 다음 할 일 (우선순위)
1. **(며칠 실사용 뒤) §7 Stage D** — 제목(=의미)·동작 규칙 확장 · `FlexTypeCatalog`를 §7 모델로 흡수 ·
   6종 정리 · 자동분류 연결(O4 유연층 자동분류). 착수 전 계획 먼저 보여주기(그간 방식).
2. **HANDOFF.md 갱신** — §3 구현표에 §7 Stage A~C(주차 마감 숨김·ClassDef) 반영, §5 열린항목의 "§7 구현 계획"을
   "Stage A~C 완료, D 남음"으로 갱신. (지금은 Stage A~C 반영 전 스냅샷.)
3. Stage D 확정 후 §7 사양서(§2) 반영 판단(규칙 #2).
4. (남은 것) question 답변 경로(HANDOFF §5) · 항목별 iCloud 옵트인 · 여러 장 사진 · 반복 규칙 O3.

## 참고 — 이 기기(맥미니) 환경 특이사항 (맥북엔 무관할 수 있음)
- 맥미니는 이번에 Xcode 라이선스 미동의 + iOS 26.5 시뮬 런타임 부재 이슈가 있었음(해결됨). 맥북은 07-24에 정상 빌드했으니
  대체로 무관하나, 안 되면 `docs/worklog/2026-07-25-macmini.md`의 "환경 주의" 참조.
