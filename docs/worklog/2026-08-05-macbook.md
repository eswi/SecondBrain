# 작업 로그 — 2026-08-05 (수) 낮·저녁 · 회사 맥북 — E-6 기준선 + D-3 (a) 재완료 멱등

> **재개 한 줄:** 같은 날 새벽 맥미니 세션([`2026-08-05-macmini.md`](2026-08-05-macmini.md))을 **아침 10:09 pull로 이어받았다.**
> 낮에 **E-6 기준선**을 세우다 *"오늘의 3은 통과가 아니다"* 를 발견했고(같은 종류의 잘못 세 번째), 저녁에 **D-3 (a)
> 회차 데이터 유실을 끊었다**(그물 → 가드, 후보 ② = 재완료만 멱등).
> **남은 것 셋:** ① **E-6 판정 — 2026-08-06에 폰에서 놓침이 여전히 3인지**(4면 실패) ② **정본 §5 (b) 방향 결정**
> — 이른 완료를 무를 수 없음 + 같은 뿌리인 "눌러도 반응 없는 버튼"(후보 ①③ — Claude 챗에서 사양서로) ③ **Stage 5-C 체인**.
> ⚠️ **폰엔 멱등 가드가 없는 00:37 맥미니 빌드**가 깔려 있다 — (a)는 아직 실기기에서 본 적이 없다.

## 이 세션의 성격
**같은 날 안에서 기기가 바뀐 세션이다** — 새벽 집 맥미니 → 아침 회사 맥북. 두-Mac 절차를 하루에 한 번 더 탄 셈이라
worklog도 날짜가 아니라 **기기로** 갈린다(08-03·08-04와 같은 짝 구조).

## 1. 준비 (이어받기)
| | 결과 |
|---|---|
| `git pull` (10:09) | fast-forward → `323837f` (새벽 맥미니분 4커밋) |
| `xcodegen generate` (10:09) | 프로젝트 재생성 — `project.pbxproj` 10:09:31 |
| Core `swift test` | 200 통과 / 0 실패 (이어받은 상태 확인) |
| 기기 설치 | **안 함** — 폰엔 새벽 맥미니의 **00:37 빌드**가 그대로다(E-6의 `recurPausedAt`이 그 빌드에서 기록돼 재설치 불필요) |

## 2. E-6 기준선 확보 — 통과가 **아니다** (`d821c11`)
폰(iPhone 16 Pro, 10:13)에서 대상 `5ACEA309 '약 먹기 시험용 E-6'` 실측: 매일 · 자동완성 **없음** ·
마감 **08-02T07:00** · 미리 알림 없음 · 꺼둠 ON · `recurPausedAt=2026-08-05T00:47`. 예측 셋 다 적중 —
① '지금 챙길 것'에 없음(게시 가드) ② 상세 배너 **"3일 놓침"** ③ 목록 캡션에도 **"3일 놓침" coral**.

**★ 그런데 이 3으로는 판정할 수 없다.** 꺼둔 시각이 **당일 00:47**이고 `missed()`는 날짜 단위(자정)로 세므로,
고정 컷오프와 `now`(10:13)가 **같은 날**이라 **고친 코드와 안 고친 코드가 둘 다 3**이다. 헤드리스로 확인:
`놓침=3`, `안껐다면=3`. → **판정은 2026-08-06**(안 껐다면 반드시 4가 될 조건이라, 내일의 "여전히 3"이 증거다).

**이것이 같은 종류의 잘못 세 번째다** — E-4 08-04 혼입(대조군 없는 부재) / D-3 예측3(부재의 자리 미증명) /
여기(**구분되지 않는 값**). 앞의 둘은 "부재"였고 이번은 "값"이라 형태가 달라 놓치기 쉬웠다.
→ 규칙: **판정선을 세울 때 "안 고쳤다면 이 값이 달랐을까"를 먼저 계산한다.**

**★ 곁가지 수확:** ③으로 **'살아있는 기억' 목록이 놓침 칩을 실제로 그린다**는 것이 눈으로 확인됐다 →
D-3의 **길 B(예비)가 불필요**해졌다(앞으로 그 목록의 "칩 없음"을 부재의 증거로 쓸 수 있다).

## 3. D-3 (a) 고침 — 재완료 멱등 (`4174552` 그물 → `a474881` 가드)
미결 셋 중 **(a) 회차 데이터 유실**만 끊었다. 방향은 **후보 ②**(표시는 그대로, 재완료만 멱등) —
데이터 유실을 먼저 막되 **"완료"의 정의는 안 건드리는** 길이라 사양서 판단을 안 기다려도 된다.

**Stage 0 그물 먼저(`4174552`, 코드 무변경).** 버그를 `_현재동작_두번전진` 두 개로 **사실로 기록**하고,
바뀌면 안 되는 것들(비되풀이·다음날 이른·다음날 늦은·놓친 항목·취소 후 재완료·꺼둔 항목·앵커 없음)을 쳤다.
**뒤집히는 diff 자체가 "고치기 전에 무엇이었나"의 기록**이 된다(시각 Stage 0과 같은 방식).

**가드(`a474881`).** `completionChanges`가 이미 닫은 회차엔 **빈 변경**을 돌려준다.
- **새 필드를 만들지 않았다** — 전진이 정확히 1회차이므로 `stepBack(due)`가 방금 닫은 회차의 앵커다.
  저장할 게 없어 **병합 어긋남·마이그레이션·취소 로직 수정이 전부 없다**(새 필드였다면 셋 다 딸려왔다).
- **두 절이 필요하다.** `doneThisCycle`은 **늦은·정시 완료만** 잡는다 — 이른 완료는 under-claim이라 false다
  (D-3에서 확인한 그 대가). 그래서 이른 완료는 **"방금 닫은 회차 시각에 아직 도달하지 않았다"** 로 잡는다.
- `InboxModel.markDone`: `append`는 `appendBatch`와 달리 빈 것을 안 걸러내므로 **빈 변경이면 이벤트를 안 쓴다**.

**⚠️ (a)만 막으면 "눌러도 반응 없는 버튼"이 남는다 — 정본 §5-A에 적어뒀다.** 뿌리가 (b)와 같아서
가드는 **해를 없애지만 이상함을 없애지 못한다**: 상세는 `cycleDoneLocal`을 낙관적으로 세워 **거짓 반응**을 보이고
(닫고 다시 열면 또 `[이번 것 했어요]`), 목록 스와이프는 **무반응**이다. **피드백은 지금 안 만든다** — (b)와 함께 본다.
(표시를 맞추려고 `lastDone`만 갱신하는 변형은 under-claim을 부분적으로 가려 (b)의 판단을 흐리므로 **일부러 안 했다.**)

## 4. 세션이 끊겼다 — 복구하며 훑어서 채운 빈칸 둘
가드를 다 쓰고 **검증·커밋 전에 서버 문제로 세션이 끊겼다.** 새 세션에서 저장소 상태로 중단 지점을 재구성해 이어받았고,
"중단 전에 하려던 것 중 빠진 게 없는지" 훑어 **둘을 찾았다.**
- **테스트 — "첫 완료"가 가드 밑에서 시험된 적이 없었다.** 다른 테스트의 첫 압은 전부 `now > 방금 닫은 회차`라
  3번째 조건에서 이미 탈락한다 → **"완료 이력 없으면 첫 압" 절이 단독으로 시험되는 경우가 0개**였다.
  `testFirstCompletion_noHistory_neverBlocked` 추가(새 항목을 **한 주기보다 더 이르게** 완료).
- **문서 — 정본 §5 미결이 아직 "아직 안 고쳤다"였다.** 같은 문서의 새 §5-A와 어긋났다 → (a) 해소 표시,
  남은 후보 ①③을 **(b)용으로** 정리.

**Stage 0 그물 쪽은 이상 없었다** — 버그를 기록하던 둘만 뒤집혔고 그물은 한 줄도 안 바뀌었다.

## 5. ⚠️ 날짜·기기 표기 정정 (사용자 지적, 저녁)
복구 세션이 **오늘 낮·저녁 맥북 작업을 새벽 맥미니 문서에 §7로 이어 썼다.** 실제와 안 맞아 갈랐다 —
이 문서가 그 분리본이고, 맥미니 문서에는 이어짐 표시만 남겼다.
- **`1e76e5d`의 커밋 메시지가 "08-05 맥미니"로 잘못 적혔다** — 내용은 맥북 작업이다.
  **이미 푸시돼 다른 Mac이 받아갔으므로 이력은 안 고친다**(두-Mac 클론이 깨진다). 여기 적는 것으로 대신한다.
- 복구 세션이 맥미니 문서의 *"다음 할 일 (내일 · 회사 맥북)"* 을 그대로 옮겨 적어 **오늘 아침 10:09에 이미 끝낸
  `git pull`·`xcodegen generate`를 "내일 할 일"로 보고**했다. 원인은 같다 — **한 문서 안에서 기기·날짜가 바뀌었는데
  상대 날짜("내일")를 그대로 믿었다.** → 규칙: **worklog의 "내일/오늘"은 절대 날짜로 적는다.**

## 6. (b) 방향 후보 조사 — 결정은 안 했다 (정본 §5-B)
E-6·D-3 실기기가 다 08-06이라, 남은 시간을 **코드가 아니라 결정 준비**에 썼다(사용자 판단). 임시 probe로 계측하고 지웠다(213 유지).

- **게시 게이트는 `doneThisCycle`을 안 부른다** — 호출처는 `DetailView:74`·`Components:126` 둘뿐이고 `isPublished`는 `resurface`/`due`를 직접 본다. #4의 "통일"은 함수 공유가 아니라 **앵커 공유**(`due > now`가 게이트의 숨김 조건과 같은 사실) → **①로 #4는 안 되살아난다. 단 `due > now` 절을 건드리면 그 순간 부활한다.**
- **흔들리는 곳은 catch-up과의 구분이다.** 이른 완료(A)와 넘어감(B)이 **필드 모양이 같다**(둘 다 `due=08-06T08:00`, `lastDone`이 한 회차 넓힌 창 안). 소박한 ①은 **B도 "완료"라 부른다**(계측). 가드(`now ≤ 방금 닫은 회차`)를 붙이면 B는 걸러지지만 **A의 완료 표시가 08:00에 스스로 꺼진다.**
- **③은 셋을 못 막는다** — dead button 그대로 / "[이번 것 했어요]"와 "[취소]" 동시 표시 / **`undoRecurComplete`가 완료 전용이 아니라** 자동 전진(catch-up·resume)까지 되돌린다.
- **원인:** `completionAdvance`와 `catchUpChanges`가 같은 `advanceBy`를 쓰고 catch-up은 `lastDone`을 안 건드려 **"어느 회차를 닫았나"가 어디에도 안 남는다.**
- **→ 갈림길이 하나로 좁혀졌다: 닫은 회차를 기록할 것인가.** 기록하면 표시·취소·dead button·(a) 가드가 한 번에 풀리고, 값은 (a)에서 피했던 것들(LWW 병합·마이그레이션·취소 수정)이다. 안 치르면 **(b)를 연 채 under-claim 유지** — 그게 지금 상태이고 **(a)가 막혀 데이터 유실은 이미 없다.**

## 헤드리스
Core **213 통과 0 실패**(200 → 그물·멱등 13개 증가) · iOS 시뮬 빌드 SUCCEEDED(iPhone 16 Pro).
커밋 — `d821c11`(E-6 기준선·문서) / **`4174552` 그물 · `a474881` 가드**(코드) / `1e76e5d`(worklog).
**기기 빌드·설치 없음** → 폰은 여전히 00:37 맥미니 빌드다.

## 다음 할 일 (집에서 이어서 · 실기기는 2026-08-06)

**1. (b) 방향 결정 — 조사 결과를 놓고 판단.** 정본 **§5-B**에 계측까지 다 적혀 있다(위 §6은 요약).
   갈림길은 하나로 좁혀졌다: **닫은 회차를 기록할 것인가.**
   - 기록한다 → 표시·취소·dead button·(a) 가드가 **한 번에** 풀린다. 값 = 새 필드의 LWW 병합·마이그레이션·취소 수정.
   - 안 한다 → **(b)를 연 채 under-claim 유지**(= 지금 상태). **(a)가 막혀 데이터 유실은 이미 없다.**
   - ①·③ 단독은 **둘 다 부족하다**는 것까지 계측으로 확인됨 — 그 근거를 사양서 대화에 그대로 쓸 것.
   - **정해지면 정본 §5·§5-A·§5-B와 `second-brain-v0-spec.md`를 같은 변경에서 함께 갱신**(사용자 승인 2026-08-05).

**2. E-6 판정 (2026-08-06).** 폰에서 놓침 칩이 **3이면 통과, 4면 실패.** 통과면 체크리스트 E절 전부 닫힘(E-1~E-6).
   ⚠️ 그 항목에 **"이번 것 했어요" 금지**(누르면 회차가 전진해 놓침이 사라진다).
   재설치 **불필요** — 오히려 안 건드리는 쪽이 깔끔하다(`recurPausedAt`이 00:37 빌드에서 기록됐다).

**3. D-3 (a) 실기기 확인 — 재설치가 먼저다.** 폰엔 **가드 없는 00:37 빌드**가 깔려 있다.
   재설치 뒤: 이르게 완료 → 상세 닫고 다시 열어 **또 눌러도 마감이 안 밀리는지.**
   헤드리스 213개가 지키지만 폰에서 본 적은 없다. (2번을 먼저 끝내고 재설치할 것 — 순서가 섞이면 E-6이 오염된다.)

**4. Stage 5-C — 체인**(호라이즌, 다음 K회차) + 5-B 실측에서 드러난 두 구멍:
   **놓친 되풀이의 영구 침묵** / **재등록 시점이 좁다**. 그 뒤 5-D 문구 두 톤 → 5-E.

곁가지: 시험용 항목 정리 — E-4 항목은 삭제 가능, **E-6 항목은 판정 뒤에.**
