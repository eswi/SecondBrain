# 2026-07-30 (집 맥미니) — 날짜 두 칸 역할 분리 · 게시 게이트 · 규칙 1

> **재개 한 줄:** 날짜 역할 작업은 코드·문서·실기기(3건)까지 끝(9커밋 푸시, `96b6fc2..9598162`).
> 다음: **다음 실제 자동분류 때 미리 알림이 마감 하루 전으로 붙는지 확인(실기기 확인 목록 11번)** +
> **실기기 미확인 9건** 훑기. 그다음 후보: 미리 알림 칸 이름 통일(§7-2), 일정 세분화(O1).

## 이번 발견의 핵심 ★
- **미루기가 오늘까지 실제로는 아무것도 미루지 못하고 있었다.** 미리 알림(resurface)에 **날짜만** 쓰고,
  그 날짜가 **게시 게이트로 쓰이지 않았다** — 미리 알림이 미래여도 항목은 이미 '지금 챙길 것'에 떠 있었다.
  즉 "미루기"가 이름만 있고 동작이 없던 상태. 이번 작업(역할 분리 + 게시 게이트 + 규칙 1)이 이걸 실제 동작으로 만들었다.

## 배경
회사 맥북에서 낮에 시작했다 끊긴 작업(커밋·푸시 없음)을 집 맥미니에서 처음부터 다시 함.
출발점 `96b6fc2`, 워킹트리 깨끗, swift test 89 통과 확인 후 착수.

## 한 일 — 기능 커밋 4 + 문서 5 (전부 main 푸시)
1. `f116103` **역할 분리** — `effectiveDay` → `publishDay`(게시 시작=미리 알림 우선, 없으면 마감) / `deadlineDay`(due만).
   배지·색조=deadlineDay(마감 없으면 배지 없음, `UpcomingEntry.dday` 옵셔널). 정렬=deadline?→publish. 알림=publishDay(동작 0).
2. `460d631` **게시 게이트** — `isPublished`(① 미리 알림 유효+오늘/과거만 게시 ② 아니면 마감 유효면 게시 ③ 그 외 안 함).
   미도래는 recent로 이동·개수 보존. 대시보드 '챙길 것'도 자동 감소. **← 미루기를 실제로 작동하게 만든 핵심.**
3. `2b145b7` **규칙 1**(미리 알림 ≤ 마감−1일, 마감 미래일 때만) — Core 한 곳(`resurfaceUpperBound`/`violatesRule1`/
   `deferSevenDays`) + 3곳(DatePicker·저장검증 / 자동분류 코드방어 / 미루기). 미리 알림 안 쓰는 분류는 미루기 액션 숨김.
   **프롬프트 문구 정정**: classify.py·ClassifyPrompt.swift "여유 없으면 due 당일" → "due 하루 전; 하루도 없으면 none".
4. `50d4710` **캡션 게이트** — `itemCaption`이 게이트 안 봐 안 쓰는 칸 날짜 노출되던 것 수정(마감=deadlineDay / 미리 알림=신규 `gatedResurface`).
5. `50ed4d5` **edit-policy §4-2 본문 수정** — "미루기 = Due Date 수정"(옛 뜻)을 본문에서 정정(각주 금지). 미루기는 미리 알림을 민다.
6. `98e591c` **사양서 반영** — spec §2 역할 정의(짧게, UX 서술 없음) + 프롬프트 규칙 1 정정.
7. `5f5c4e2` worklog(초안).
8. `047cdde` **O1 기록** — 일정 세분화(시작·끝 시각) 방향·제약 기록(결정·구현 아님) + §7-2 O1 포인터.
9. `9598162` **캡션 색** — 날짜 캡션이 너무 어두워(textTertiary) 원문과 같은 색으로: 지금 챙길 것 textPrimary /
   보관된 기억 textSecondary(그 화면 원문 톤). 크기 유지, 색만. 검색은 별개라 미변경.

## 검증 — 정직하게 구분
- swift test **89 → 111**(0 실패). iOS 빌드 각 커밋 SUCCEEDED.
- **실기기(ter.iPhone 16p) 설치**(devicectl, 디버거 없이, 기존 서명 재사용·갱신/삭제 없음).
- **실기기 확인은 3건만**(사용자 직접): ① 「T 우주 가입도 다시 검토」 미도래 → 지금 챙길 것에서 빠지고 살아있는 기억 쪽(개수 보존)
  ② 「우리 샌드박스 협력 병원」 배지 D+1→D-1(마감 기준) ③ 「이사 계획 논의」(마감 08-01) 미루기 → 07-31로 당겨지고 안내.
  캡션 색도 실기기 확인.
- **나머지 9건은 미확인**(3건이 맞으면 같이 맞을 것으로 봄 — 확정 아님).
- **스크린샷의 엄마 생신/아이 학원/팀 회의/치과는 시뮬레이터 SampleData** — 실데이터 아님, 스모크용. 샘플로 본 것은 샘플로 구분.

## 다음 할 일 (미확인·미결)
- **[확인] 자동분류 규칙 1 효과** — 프롬프트 문구를 바꿨으니, **다음에 실제로 분류를 돌릴 때 미리 알림이 마감 하루 전으로
  붙는지**(같은 날/이후로 안 붙는지) 확인. = 실기기 확인 목록 **11번**. 코드 방어(classifyFields·classify.py)는 들어가 있음.
- **[확인] 실기기 미확인 9건** — 배지·미루기 세 경로·날짜 선택 범위·캡션 게이트·알림 불변 등.
- **[미결] 미리 알림 칸 이름 통일** — 약속·일정 "미리 알림" / 할일·주차 "다시 보기"(memory-philosophy §7-2). 이번에 안 함.
- **[미결] 일정 세분화(O1)** — 시작·끝 시각. 시각 도입은 날짜 칸 표현 변경 문제 + 오늘 정한 역할과의 재정의 필요. 지금 결정 안 함.
- **[미결] 마감일 별도 알림** — 현재 알림은 게시 시작(미리 알림)에만. 필요하면 별도.
