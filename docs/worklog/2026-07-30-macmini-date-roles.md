# 2026-07-30 (집 맥미니) — 날짜 두 칸 역할 분리 · 게시 게이트 · 규칙 1

> **재개 한 줄:** 날짜 역할 작업은 실기기 검증까지 끝났다(커밋 6개 푸시). 다음: 미리 알림 칸
> 이름 통일(약속·일정 "미리 알림" / 할일·주차 "다시 보기") 여부 결정 — memory-philosophy §7-2 미결.

## 배경
회사 맥북에서 낮에 시작했다가 끊긴 작업(커밋·푸시 없음)을 집 맥미니에서 처음부터 다시 함.
출발점: `96b6fc2`, 워킹트리 깨끗, swift test 89 통과 확인 후 착수.

## 한 일 — 논리 단위 커밋 4개 + 문서 후속 2개 (전부 main 푸시)
1. `f116103` **역할 분리** — `ItemSchedule.effectiveDay` → `publishDay`(게시 시작=미리 알림 우선, 없으면 마감)
   / `deadlineDay`(due만). 배지·카드 색조 = deadlineDay(마감 없으면 배지 없음, `UpcomingEntry.dday` 옵셔널화).
   정렬 = deadlineDay?→publishDay. 알림(NotificationPlanner) = publishDay(동작 0).
2. `460d631` **게시 게이트** — `ItemSchedule.isPublished`(① 미리 알림 유효+오늘/과거만 게시 ② 아니면 마감 유효면
   게시(먼 미래도) ③ 그 외 안 함). 미도래는 recent로 이동·개수 보존. 대시보드 '챙길 것'도 자동 감소.
3. `2b145b7` **규칙 1**(미리 알림 ≤ 마감−1일, 마감 미래일 때만) — Core 한 곳(`resurfaceUpperBound`/`violatesRule1`/
   `deferSevenDays`)에 두고 3곳이 씀: (a) DatePicker 범위 제한 + 저장 시점 검증(최종 방어선) (b) 자동 분류 코드 방어
   (앱 classifyFields + classify.py 양쪽) (c) 미루기(+7일, 당김/막힘/그대로). 미리 알림 안 쓰는 분류는 미루기 액션 숨김.
   **프롬프트 문구 정정**: classify.py·ClassifyPrompt.swift "여유 없으면 due 당일" → "due 하루 전; 하루도 없으면 none".
4. `50d4710` **캡션 게이트** — `Components.itemCaption`이 게이트를 안 봐 안 쓰는 칸 날짜 노출되던 것 수정
   (마감=deadlineDay / 미리 알림=신규 `gatedResurface`).
5. `50ed4d5` **edit-policy §4-2 본문 수정** — "미루기 = Due Date 수정"(옛 뜻)을 본문에서 정정(각주 금지):
   미루기는 미리 알림을 민다·마감은 안 바뀐다·섹션 위치는 publishDay+게시 게이트. §4-3 임시 각주 제거.
6. `98e591c` **사양서 반영** — second-brain-v0-spec.md §2에 역할 정의(짧게, UX 서술 없음) + 프롬프트 규칙 1 정정.

## 검증
- swift test **89 → 111** (0 실패). iOS 빌드 4커밋 각각 BUILD SUCCEEDED.
- **실기기(ter.iPhone 16p) 설치** — devicectl(디버거 없이), 기존 서명 재사용(갱신·삭제 없음).
- **실기기 확인 통과(사용자 직접)**: ① 「T 우주 가입도 다시 검토」 미도래 → 지금 챙길 것에서 빠지고 살아있는 기억 쪽
  (개수 보존) ② 「우리 샌드박스 협력 병원」 배지 D+1→D-1(마감 기준) ③ 「이사 계획 논의」(마감 08-01) 미루기 → 07-31로
  당겨지고 안내. 나머지 항목은 이 셋과 함께 맞는 것으로 봄.
- (스크린샷의 엄마 생신/아이 학원/팀 회의/치과는 **시뮬레이터 SampleData** — 실데이터 아님, 스모크용.)

## 다음 할 일
- **미결(이번에 통일 안 함)**: 미리 알림 칸 이름이 분류마다 다름 — 통일 여부 결정(§7-2).
- 마감일 별도 알림(현재 알림은 게시 시작=미리 알림에만) — 필요하면 별도 작업.
- classify.py/앱 규칙 1 코드 방어의 실제 효과(미리 알림이 마감 하루 전으로 붙는지)는 다음 실제 자동분류 때 눈으로 확인.
