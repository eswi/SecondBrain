# 작업 로그 — 2026-07-25 (mac mini · 회사 맥북 Stage4 재설계 이어받기)

> **⚠️ 재개 한 줄(다음 세션):** ①·② 완료. 다음은 **며칠 실사용 뒤** 분류 2층 모델·주차위치·필터 동적화의
> **사양서 반영 방향 확정**(지금은 보류, 근거는 open-questions/photo-capture-design에 유지). 그 전엔 남은 것
> (항목별 iCloud 옵트인·여러 장 사진·반복 규칙 O3) 중 착수.
> `git pull` → `cd native && xcodegen generate` → 스킴 **`SecondBrainApp-iOS`** 로 빌드.
>
> **환경 주의(이 맥미니 고유):** ① 최초 `git`·`xcodebuild`가 **Xcode 라이선스 미동의**로 막혔음 → 터미널에서
> `sudo xcodebuild -license` 직접 동의(완료됨). ② Xcode 26.6은 iOS **SDK 26.5**뿐인데 시뮬 런타임이 26.2라
> iOS 빌드 destination 0개였음 → `xcodebuild -downloadPlatform iOS`로 **26.5 런타임 설치 완료** +
> **iPhone 16 Pro (26.5) 시뮬 생성**(id `34A5033B-…`). 이제 정상.

## 오늘 한 일

### 0. 환경 세팅 (맥북 → 맥미니 이어받기)
- Xcode 라이선스 동의 · `git pull`(`9c74226→344d00f`, clean) · `xcodegen generate` · **코어 `swift test` 71/71 통과**.
- iOS 빌드: 26.5 런타임 설치 + 26.5 시뮬 생성 후 **BUILD SUCCEEDED**(맥북 삽질 교훈대로 로그파일+`EXIT=$?`+`.app` mtime 검증).

### 1. ① 필터 칩 ClassRegistry 통일 — 실재 분류만 동적 노출 (`f79e69a`, push됨)
- **문제:** '살아있는 기억'·검색 필터 칩이 `TypeCatalog`(기본층 전용) 고정 목록만 써서 유연층(주차)이 필터 후보에서 빠짐.
  표시·메뉴 통일(`84f3152`)과 같은 뿌리.
- **수정:** `FilterChipsBar`에 `presentTypes` 파라미터 → 각 화면이 **필터 전 데이터의 distinct 타입** 공급.
  고정 primary/overflow(⋯메뉴) 제거 → **실재 분류만** 칩(가로 스크롤). 순서 = `ClassRegistry` 정본 순(기본층 6 → 주차), 미분류 끝.
  **선택된 필터 타입은 present에서 사라져도 계속 노출**(보이지 않는 필터 갇힘 방지 — union). 데이터 공급:
  `InboxModel.livingPresentTypes` / `SearchView` hits 기준.
- `TypeCatalog.primaryFilters·overflowFilters`는 미사용으로 남김(보호 자산, 삭제 X). §2·§3·MergeEngine 무변경.
- **실기기 검증 통과(위대표):** A 주차 칩 노출·필터 ✅ / B 실재 분류만 ✅ / C 갇힘 방지(칩 유지 + "이 종류가 없어요") ✅ /
  D ⋯제거·기본층 회귀 없음 ✅ / E 검색 재사용 ✅. (E: 칩 7개 시 가로 넘침→스크롤로 접근, "A로 두고 넘어감"으로 확정.)

### 2. ② 사진·EXIF 프라이버시 사양서 반영 (`db83c9a`, push됨) — 규칙 #2
- 위대표 결정: **프라이버시(EXIF-only 위치) + 사진(카메라·성역·불변)만 지금 반영**, 분류 2층·주차·필터는 며칠 뒤로 보류.
- 사양서 이미지 섹션에 "네이티브 v1 확정" 소절 추가: 카메라 촬영만·본문 먼저·압축본=원본·불변, `photo:` 성역·불변,
  **★ GPS는 사진 EXIF에만·평문 그릇엔 절대 안 박음**(사진 없이 좌표만 iCloud 누출 방지 = 안전 규칙). §2·§3 무변경.

### 3. O4 + 주석 정정 — 유연층 자동분류 = "아직 안 한 것" (`abfe562`, push됨)
- 위대표 지적: "유연층은 자동분류 안 함"이 **결정처럼 굳는 함정** 방지. 실제론 임시(§3 보류 + 유연층 하나뿐).
  **원래 의도 = 유연층도 자동분류 대상**(본문 보고 AI가 잡게).
- `open-questions.md` **O4** 신설 + `FlexType.swift` 주석 "절대 참조 안 함" → "지금은 참조 안 함(임시)·O4 편입 예정"으로 정정.

## 상태
- main = `abfe562`. **push 완료**(local=origin). working tree clean. 브랜치/worktree = main 단일(정리 불필요).
- 폰(iPhone 16 Pro): `f79e69a` 기준 최신 빌드 설치·실기기 검증 통과. (`db83c9a`·`abfe562`는 문서/주석이라 앱 동작 무관.)
- 이어받을 기기 = 미정(다음 세션 시작 시 pull 먼저).

## 사양서 반영 확인 (규칙 #2)
- **완료(부분):** 사진·EXIF 프라이버시 → 반영(`db83c9a`).
- **보류:** 분류 2층 모델·주차위치·필터 동적화 → **며칠 실사용 뒤 방향 확정 후** 반영. 근거는
  `photo-capture-design.md`·`classification-redesign-open-questions.md`(D1~D5, O1~O4)에 유지. Stage 4를 하루 전 통째로
  뒤엎은 만큼 곧 또 바뀔 수 있어 지금 사양서에 박지 않음.

## 기억(메모리) 갱신
- 신규 [[flex-autoclassify-deferred]] — 유연층 자동분류는 "안 함"이 아니라 "아직 안 함"(근거 O4). MEMORY.md 색인 추가.

## 다음 할 일 (우선순위)
1. **(며칠 실사용 뒤)** 분류 2층·주차위치·필터 동적화 사양서 반영 방향 확정(규칙 #1·#2).
2. 남은 것: 항목별 iCloud 옵트인(사진 동기화) · 여러 장 사진 · 반복 규칙 설계(O3).
3. (열린 설계) 유연층 자동분류 편입(O4) — 유연층이 여럿 쌓이고 "분류마다 자동분류 규칙" 설계가 설 때.
