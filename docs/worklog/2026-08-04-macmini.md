# 작업 로그 — 2026-08-04 (집 맥미니) — 되풀이 #4 완료표시 회차 기준 통일 + #3 색 경위

> **재개 한 줄:** 되풀이 완료 표시를 **회차 기준(`doneThisCycle`)** 으로 통일해 실기기 모순("오늘 완료 + 지남 동시")을
> 없앴다. 실기기 **D-1·D-2·D-4 통과**(2026-08-04, iPhone 16 Pro). **D-3(이른 완료 under-claim)만 저녁에 확인 예정 —
> `recurrence-verify-checklist.md` D-3 닫는 조건 참조.** D-3 닫히면 **Stage 5(알림 전환) + lead-time 구멍**으로.
> Core 167 테스트 0 실패. 정본 = `docs/native/recurrence-design.md`.

## 발단 — 실기기 모순 (사용자 지적)
`복약: 놓치는 약 먹기`(01781308)가 **"오늘 완료" 칩 + '지금 챙길 것' 지남**을 동시에 표시. 조사 결과:
- **원인:** 칩 `doneToday`(lastDone **날짜** == 오늘, 날짜만) vs 게이트 `isPublished`(마감 **시각**까지) → **날짜 vs 시각 기준 분열.**
- **경위:** Stage 3의 날짜기준 최소판 `doneToday`가 **앵커 전환(마감=앵커) 때 갱신되지 않아** 생긴 구멍.
- **실물 모순은 구 빌드(01:44 이전) 완료 잔재**로 확정(00:11 완료가 마감 전진 안 함). 신 빌드는 재완료 시 전진.

## 한 일 (커밋 이번 1건)
- **#2 `doneThisCycle` 신설**(`Recurrence.swift`, + `stepBack`). **칩·배너·완료버튼·게이트가 같은 마감 앵커 기준:**
  `마감 > now` = 이번 회차 닫힘(= `!isPublished`) → 두 기준이 **구조적으로 못 갈림.**
  **세 상태 = 아직(마감≤now) / 했다(마감>now & lastDone≥직전회차) / 넘어갔다(그 외).**
  `lastDone`은 **버리지 않고 "했다 vs 넘어감(자동전진)" 증인**(회차 경계 비교, 날짜 아님). 자동 넘어간 걸 "완료"라 하면 거짓말이라.
  **매주·매년도 같은 기준으로 성립** → "정확한 회차 창은 Stage 4" 옛 구멍 함께 닫힘.
- 호출부: `recurStatusChip`("오늘 완료"→**"완료"**), `DetailView`(`cycleDoneLocal`, 배너 **"이번 회차 완료됨"**).
- **테스트** `testDoneToday` → `testDoneThisCycle`(했다/아직/**넘어감**/매주/앵커없음).
- **#4 정본 반영** — `recurrence-design.md` 완료표시 절 신설(경위·3상태), **§200 "앵커=미리 알림" 정정**(마감 앵커), §220 `doneToday` 폐기 표시, 미결 갱신.
- **#3 색 — 코드 변경 없음.** 계획의 전제("지남도 빨강")가 **틀림**: `DDayCalc`는 날짜 기준이라 "오늘 마감 지남"=`.today`=**이미 amber**, coral(빨강)은 **과거 날짜=진짜 놓침**에만. 사용자가 원한 위계가 이미 구현돼 있었음 → **회귀 방지 주석만**(`InboxView` + 문서).

## 실기기 확인 (2026-08-04 · iPhone 16 Pro) — D절
- **D-1 통과 ★** — "이번 것 했어요" 정상, **"오늘 완료+지남 동시" 모순 사라짐 확인.** (지금 챙길 것 → 상세 완료 → 살아있는 기억 "완료" 칩)
- **D-2 통과** — 상세 배너 "이번 회차 완료됨 ✓".
- **D-4 통과** — 두 색(amber 지남 / coral 놓침) 구분 확인.
- **D-3 미확인(열어둠)** — 이른 완료 under-claim. **오늘 저녁 확인 예정.** 닫는 조건: 마감 시각 전에 완료 → 항목이 목록서 빠지면 통과("완료" 칩 안 떠도 정상).

## 헤드리스
Core **167 테스트 0 실패** · iOS 빌드 OK · 폰 설치(09:13 빌드, `kr.teri.secondbrain`).

## 미결 (정본 기록)
- **"넘어감" 흔적 없음(습관 blind spot)** — 자동완성 catch-up 전진은 `missedCount`에 안 잡히고 목록·칩에도 안 보임. 생일·기일은 맞지만 습관은 흔적 필요. `lastDone` witness로 나중에 산출 가능. **안 만듦, 미결.**
- **"지남" 상시 amber 피로** — 며칠 써보고 daily 지남을 neutral까지 낮출지 재검토.
- **Stage 5 lead-time 알림 구멍** — 7시 미리 알림+8시 마감이 7시만 울림. + 음력(§7).

## 다음 할 일
1. **저녁: D-3 확인**(위 닫는 조건). 통과분 체크리스트에 닫기.
2. D-3 닫히면 **Stage 5 — 알림 전환**(iOS 반복 트리거, 예산 분리, **+ lead-time 구멍**).
