# 2026-08-11 (맥북) — G-7: F절 회귀를 다시 밟았다

> **재개 한 줄:** **F-1 ②만 남았다(오늘 못 봤다 — 아래 §4).** 그 다음은 **내일 20:15 F-3**이고,
> 둘이 닫히면 **G-7이 끝난다(기한 8/13)**. F-1 ② 되살리기는 **폰만 있으면 된다** — 맥이 필요 없다.

---

## 0. 재개 점검 (78c3ae4에서 이어받음)

| | 결과 |
|---|---|
| `git pull` | ✅ `78c3ae4`까지 · clean · `main...origin/main` |
| `xcodegen generate` | ✅ `native/SecondBrain.xcodeproj` |
| `swift test` | ✅ **322개 통과**(0 failures) — 08-10과 무변경 |
| iOS 빌드 | ✅ **BUILD SUCCEEDED** · error 0 · 산출물 17:23:08 |

**⚠️ 빌드에서 한 번 헛짚었다 — 프로젝트 이름 ≠ 스킴 이름.**
`SecondBrainApp.xcodeproj`로 잡아 `does not exist`. 실제는 **프로젝트 `SecondBrain.xcodeproj` · 스킴 `SecondBrainApp-iOS`**.
스킴에 `App`이 붙었다고 프로젝트도 그럴 것이라 넘겨짚은 것. → 메모리에 못박음.

## 1. 폰 빌드 확정은 건너뛰었다 (사용자 판단)

설치가 08-10 새벽이고 **그 뒤 `native/` 변경이 0줄**이며(마지막 코드 커밋 `f889697` 08-10 01:55, 이후 `9a7aefd`·`db432bd`·`78c3ae4`는 전부 문서),
08-10 실기기에서 통과한 **G-3·G-6·G-8·G-11·G-12가 전부 그 빌드의 것**이다. 의심할 근거가 없다.

**★ 다만 관행이 하나 빠졌다 — 08-10 worklog에 설치 시각 기록이 없다.**
08-07에는 *"앱 아이콘 + 설정의 「연결된 폴더·파일 5개」"* 로 **어느 빌드인지 확정한 근거**를 남겼는데 08-10엔 없었다.
그래서 오늘 "폰에 깔린 게 최종인가"를 커밋 날짜와 G-12로 **역추적**해야 했다. 없어도 될 일이었다.
→ **다음부터 설치 직후 한 줄:** `설치: YYYY-MM-DD HH:MM · <커밋> · 확정 근거: <그 빌드에서만 보이는 것>`

## 2. F절 절차 — 배치 변경으로 달라진 것

**F절이 보는 것은 판정 하나**(`doneThisCycle`: ①마감>now ②lastDoneDue==마감 ③!isPublished)**와 그것을 보는 네 자리**
(목록 칩·상세 버튼·취소 버튼·스와이프)다. 08-09~10 작업은 *"배치만 바꿨다"* 고 주장하고 **G-7이 그 주장을 검사한다.**
→ **판정과 대조 논리는 그대로. 바뀐 것은 값을 넣는 손길과 읽는 화면.**

| | 달라진 것 | 절차에 미친 영향 |
|---|---|---|
| **①** | **시각 넣는 법이 통째로 바뀜** — 접이식·[날짜 설정] → **값 누름 → 대화상자 → [취소]│[확인]** | **★ [확인]은 커밋이 아니다.** 하단 **[저장]**까지 눌러야 한다. 안 그러면 판정이 옛 값을 본다 → **F절이 조용히 엉뚱한 것을 잰다.** 확인법: **amber가 사라진 것을 보고 다음으로** |
| **②** | 피커 범위(`in:`) 제거(`1bb9a8a`) | 과거 시각을 고르는 단계에서 안 막힌다. **막는 곳은 [저장] 하나** — 상한 안내가 거기서 뜬다(오늘은 안 떴다) |
| **③** | 미루기 1·3·7일 신설 | 기준이 **오늘+N일**이라 준비에 쓰면 의도한 값이 안 나온다 → **달력으로 직접 찍었다** |
| **④** | amber 세 자리(값 글자·미루기 버튼·달력 tint) | **F절의 색(칩)과 G절의 색(amber)은 다른 것.** 칩은 저장값을, amber는 화면값과 저장값의 차이를 본다 |
| **⑤** | 대화상자가 안전 영역 밖(`f889697`) | 열려 있으면 하단 배너를 가린다 → **F-2에서 값은 대화상자를 닫고 읽는다** |
| **⑥** | 반복 설정 5줄→4줄 | 「반복 주기」·「자동 완성」이 **한 줄에 둘** — 표적이 좁다 |

**그대로인 것:** 판정 세 절 · `lastDoneDue` 의미 · 폴백 규칙 · **F-4(목록 스와이프)는 상세 배치와 무관** ·
각 줄의 "안 고쳤다면?" 논리와 대조군 근거(D-2·D-3·E-6).

**★ 확인표의 준비 조건을 한 군데 바꿨다.** 확인표는 *"미리 알림 = 지금−5분"*(과거)인데 그대로 하면
**완료 전 '지금 챙길 것' → 완료 후 '살아있는 기억'으로 목록이 갈려 F-4의 대조군이 깨진다.**
08-06에 「가」로 F-4를 통과시킨 근거가 *"가는 두 칸이 미래라 완료 후에도 같은 목록에 남는다"* 였다.
→ **두 칸 모두 미래**로 잡았다(「가」의 모양). (확인표의 그 준비 조건은 **접은 F-1 ③**을 되살리려던 자리의 것이다.)

## 3. 결과

| 줄 | 상태 | 무엇을 봤나 |
|---|---|---|
| **F-1 ①** | ✅ **통과** | 이른 완료 직후 상세를 닫았다 열면 **「이번 회차 완료됨 ✓ + [취소]」** |
| **F-1 ②** | ⛔ **못 봤다** | **아래 §4** — 회귀가 아니라 **절차 미실행** |
| **F-2** | ✅ **통과** | [취소] → 값이 되돌아가고 버튼 복귀. **[취소]가 한 번 더 남지 않음**. amber 없음 |
| **F-3** | ⏳ **내일 20:15** | 「나」(`131B923C`)로 본다 |
| **F-4** | ✅ **통과** | **대조군까지** — 완료 전 스와이프에 "했어요" **뜸** → 완료 후 **같은 줄**에서 **없음**(삭제·미루기는 남음) |
| **F-5** | ✅ **통과** | 아래 |

### F-5 — ★ 숫자가 맞았다 (부재가 아니라 대체)

| 항목 | 어디 | 본 것 |
|---|---|---|
| `약 먹기 시험 용  E-4` (`EAB3BB92`) | **지금 챙길 것** | 상세에 **「5일 놓침」** |
| `감기약 먹기 시험용 감기약 먹기` (`90D927B2`) | **살아있는 기억** | 상세에 **「되풀이 꺼둠 — 알림·되살아나기 멈춤」** |

**★ 「5일 놓침」이 계산과 정확히 일치한다.** E-4는 마감 **2026-08-06 02:00** · 매일 · 꺼둠 아님 · `lastDoneDue` 없음(폴백).
08-06 예측표의 셈법(놓침 = 오늘 − 마감 날짜)을 적용하면 08-06 자정 전 **0**(그때 실제로 「칩 없음」을 봤다) →
08-07…08-10 **1·2·3·4** → **08-11 = 5**. 같은 셈법이 08-06 `복약`(마감 08-04 → **2일 놓침**)에서도 맞았다.
→ **폴백 경로가 살아 있을 뿐 아니라 값이 정확하다. 죽은 렌더가 아니라 마감을 보고 계산되고 있다.**
(08-06 F-3의 「1시간 남음」이 해준 것과 같은 종류의 증거다.)

**★ 둘이 서로의 대조군이 됐다 — 상세의 같은 배너 자리.** ①엔 「5일 놓침」이 그려지고 ②엔 「되풀이 꺼둠」이 **대신** 그려진다.
**부재가 아니라 대체**라 *"그 자리가 원래 아무것도 안 그리는 것 아니냐"* 가 성립하지 않는다.
②는 마감 08-09 19:00·미리 알림 08-08 21:00이 **둘 다 지났는데도** 게시되지 않았다 → **꺼두기가 실제로 멈추고 있다.**

⚠️ E-6 대상 `5ACEA309`「약 먹기 시험용」은 제외했다. ①과 이름이 거의 같다 — **끝에 `E-4`가 붙었는지**로 가른다.

## 4. ⛔ F-1 ②를 못 봤다 — 회귀가 아니라 **12번(원상복구) 미실행**

19:36에 상세가 `[이번 것 했어요]`를 보여줬다. **회귀처럼 보였지만 아니었다.**
전용 항목 **`G-7 회기 08-11`(`9F950B73`)** 의 실데이터:

| 시각 | 기록 | 무엇 |
|---|---|---|
| 18:08 | 만듦 | |
| 18:21 | `due=18:49 · resurface=18:39 · recur=daily` | 준비(첫 값) |
| 18:41 | `due=19:25 · resurface=19:10` | 시각 밀기 |
| **18:45** | `due=08-12 19:25 · lastDone=18:45 · lastDoneDue=08-12 19:25` | **9번 완료** |
| **18:46** | `due=08-11 19:25 · lastDone=(빔) · lastDoneDue=(빔)` | **11번 [취소]** |
| — | **없음** | **⛔ 12번 원상복구가 없다** |

그래서 19:36의 상태는 **「완료 안 된 항목 + 마감 19:25이 이미 지남」**이고, 판정 1절(`마감 > now`)에서 끊겨
`doneThisCycle = false` → **`[이번 것 했어요]`가 맞는 화면**이다. **옛 코드도 같은 값이라 아무것도 안 갈린다.**

> **판별 근거 둘이 같은 말을 했다:** ⓐ 동기화된 로그에 완료 op가 없다 ⓑ **폰이 스스로 `[이번 것 했어요]`를 그리고 있다**
> (= 폰의 로컬 상태에도 완료가 없다). ⓑ가 **iCloud 지연 가능성까지 닫는다** — 지연이었다면 폰은 완료로 그렸을 것이다.

**★ 절차의 구멍이었다.** 8·9·10·11엔 「무엇이 보여야 하나」가 다 있었는데 **12번(원상복구)만 통과 조건이 비어 있었다.**
**눌렀는지 화면으로 확인하는 관문이 없어서** 안 눌린 채로 넘어갔고, 그대로 마감이 지나 창이 닫혔다.
→ **교훈: 「원상복구」·「되돌리기」 같은 중간 단계에도 통과 조건을 적는다.** 상태를 만드는 단계는 전부 확인 대상이다.

## 5. 남은 일

### 집에서 / 폰만 있으면 된다 — **F-1 ② 되살리기**
1. `G-7 회기 08-11` 상세 → **미리 알림·회차 시각을 미래로** (예 20:00 / 20:10)
   🔶 대화상자 **[확인]** → 하단 **[저장]** → **amber 사라짐 확인**
   ⚠️ 미리 알림이 지금보다 미래여야 '살아있는 기억'에 머문다(F-4 때와 같은 이유)
2. **[이번 것 했어요]** (= 마감 전이므로 **이른 완료**) → 상세 닫았다 열어 **「이번 회차 완료됨 ✓ + [취소]」 확인하고 넘어간다** 🔶 **이번엔 여기서 반드시 확인**
3. **마감 지난 뒤 +5분** → 앱 **완전 종료 → 다시 열기** → **「이번 회차 완료됨 ✓」 그대로면 통과**
   ❌ `[이번 것 했어요]`면 **그때가 진짜 구멍 1의 회귀** — 누르지 말 것

### 내일 — **F-3 (⏰ 20:00~23:55, 놓치면 다음은 8/19)**
알람 **19:50**(뒤집히기 전 상태를 같은 줄에서 먼저 봄 — 누르지 말 것) · **20:15**(판정) · 백업 21:30.
> **20:15 — 앱 완전 종료 후 다시 열고, 「나 - 정시 완료 시험」이 ①'지금 챙길 것'에 있고 ②그 줄에 「완료」 칩이 **없고**(대신 「N시간 남음」) ③상세 버튼이 `[이번 것 했어요]`인지. 누르지 말 것.**

저장값 확인함: 마감 **2026-08-12 23:55** · 미리 알림 **2026-08-12 20:00** · `lastDone 08-06 01:02` · `lastDoneDue 08-12 23:55`.
옛 판정은 `lastDone ≥ 직전 회차(08-05 23:55)` = **참** → 「완료」 칩을 달았을 자리다. **값이 갈리므로 판정선이 선다.**
⚠️ **「나」에 손편집 금지** — 만지면 「가」에서 난 lead 뒤집힘이 그대로 난다.

### 정리
- **`G-7 회기 08-11`(`9F950B73`)은 F-1 ②가 끝나면 「그만두기」로 정리.** 지우지 말 것
- **「가」(`AF9BAB30`)를 풀어줄지는 F절이 끝난 뒤 결정** — (c)는 `8ae9ee2`(08-08)로 이미 닫혔다
- 그다음 큰 것: **A(지난 마감 상한 N=7)** — 착수 전 `2026-08-07-macbook.md` §4-C의 「2026-08-08 추가」부터
