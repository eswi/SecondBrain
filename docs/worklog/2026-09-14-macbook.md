# 2026-09-14 (회사 맥북) — **분류 재편 v2 — 약속·일정을 빼고 지식·추억을 더했다 · 정보에 「유효 기간」**

> ## 재개 한 줄 — **2026-09-14 14:1x (이 줄이 최신이다)**
>
> ### ✅ 코드 = **`e23776a`** — 시험 436 통과 · iOS·맥 빌드 EXIT=0. **폰에는 아직 안 깔았다.**
> ### ▶ 사용자가 정할 것 넷 — 아래 §5
> ① 지식·추억의 **색·심볼**(임시 후보로 들어가 있다) ② 상세 배너 **문구**(임시 「유효 기간이 지났어요 — ◯까지」)
> ③ **정본 훑기 목록**(§4)을 사양서·정본에 반영할까 ④ **폰 설치** 진행 여부.
> ### ▶ 그 다음 주제 = **해시태그(tag 속성)** — 사용자: *"분류 이후에 바로 논의해야해."*
> 정보 안의 세분(맛집·장소 …)이 첫 요구처다. `classification-v2-design.md` §4.

---

## 0. 세션 시작 — 맥미니 09-13 작업을 받았다

`git pull`로 커밋 31개. 맥북에서 시험 434 · iOS·맥 빌드 EXIT=0 확인(재편 전 상태). 시뮬 `ED17E1A9-…`(iPhone 16 Pro · 이름 하나뿐) ·
폰 CoreDevice `95CF732E-…`(맥미니와 같음). 이 맥북의 `ls`는 `/bin/ls` 그대로(별칭 함정 없음).

## 1. 무엇을 했나 — 한 줄씩

| 시각 | 무엇 | 커밋 |
|---|---|---|
| 13:0x~13:4x | 사용자와 **분류 재편 논의** — 결정 셋 + 정보의 유효 기간 | — |
| 13:5x | **설계 문서** `docs/native/classification-v2-design.md` 신설(결정 원문 인용 그대로) | `729fdd9` |
| **13:55:40** | ★ **실데이터 조작** — 삭제된 약속 셋을 미분류로(새 파일 `inbox-unclassify-promise-20260914.md` · op 세 줄) | (파일 · git 밖) |
| 14:0x | **Core·App 구현** + 시험 둘 + 기존 시험 하나 정정 | `e23776a` |

## 2. 결정 — 원본은 `classification-v2-design.md`(여기는 요약 · 기록 규칙 10)

- **약속·일정을 뺀다** — 다른 앱이 맡는다. 새로 못 고르고, 데이터는 안 지운다(옛 값 = 미등록 key → 미분류 폴백).
- **지식을 더한다** — 정보와의 차이 = **시간에 따라 가치가 변하는가**(사용자: *"철학적으로 너무나 중요한 차잇점"*).
  ⚠️ **정보를 태그로 세분한다는 §7-1 입장은 그대로다** — 지식은 정보를 쪼갠 것이 아니라 **옆에 더한 것**.
  (처음에 「뒤집힌다」로 적었다가 사용자 정정으로 바로잡았다 — 설계 문서 §1-2에 그 경위가 있다.)
- **추억을 더한다** — 내 인생의 사건. **날짜는 아직 안 쓴다.** 옛 계획의 「기록」은 **이번에 안 넣는다**(성격이 다르다 · 나중에).
- **정보의 유효 기간** — 다시 보기 칸에 제목 「유효 기간」 · 지나면 아이콘·텍스트 **회색** · 자리는 살아있는 기억 그대로 ·
  상세에 표시 · 검색은 나중.

## 3. 구현 — 핵심은 「보이는 칸」과 「시점인 칸」을 가른 것

`ClassSpec.uses`(보임) 옆에 **`schedules`**(시점)·**`validUntil`**(유효 기간 칸)을 뒀다. 기본은 같고, **정보만 갈린다**
(`uses`에 다시 보기 · `schedules` 비움). `ItemSchedule`의 판정 넷(게시 시작·마감일·게시 게이트·늦음)은 `schedules`를,
캡션 표시(`gatedResurface`)와 상세 「시간 설정」만 `uses`를 본다. **그래서 유효 기간이 지난 정보가 「지금 챙길 것」에 안 뜨고
알림도 없다** — 그 결정을 `ClassGateTests.testInfo_validUntil_isVisibleButNotASchedule`이 지킨다(결정 파수꾼 · 깨지면 결정을 의심).

- **유효 기간 판정**(`isExpired`): 날짜만이면 **다음 날 자정부터** 지남(「9/20까지」의 뜻 · 게시 게이트의 date-only와 방향이 반대) ·
  시각이 있으면 그 시각부터.
- **회색**: 살아있는 기억(`MemoryRow`) · 검색 · 보관함 셋 다 같은 판정(`isExpiredNow`) · 글리프(`TypeMenuButton`·`TypeGlyph`)도 회색.
- **상세 배너** `expiredBanner` — 무채색(재촉이 아니라 통보) · 저장값을 본다 · **문구는 임시**.
- **미루기**는 `schedules`를 보게 바꿨다 → 정보에서 사라진다(유효 기간은 미룰 것이 아니다).
- **시각 메타**: 지식 = 주황 `0xFB923C` · `book.fill` / 추억 = 분홍 `0xF472B6`(약속이 쓰던 색) · `heart.fill` — **임시 후보.**
- ⛔ **안 건드린 것**(항시 규칙 8): §3 프롬프트 · `ClassifyPrompt` · `InboxModel.validTypes`(여전히 event·promise를 품고 있다 —
  자동 분류 재설계 때) · `classify.py`.

### 3-1. 시험 하나가 걸렸다 — 구현이 아니라 결정이 바뀐 것
`RecurrenceClassTests.testExistingClasses_unchanged`가 *「info resurface 불변(안 씀)」*을 단정하고 있었다. 오늘 결정으로 그 사실이
바뀌었으므로 **시험을 결정에 맞춰 고쳤다**(주석에 「옛 단정은 그날 결정으로 바뀐 것 — 회귀가 아니다」를 남겼다).
★ 정확히 `CLAUDE.md` 「시험을 쓰는 법」의 ③ — *깨지면 무엇을 의심하나*가 이 시험에는 안 적혀 있었고, 그래서 처음 봤을 땐
「내가 뭘 깨뜨렸나」부터 찾았다.

## 4. ⏸ 정본 훑기 — 축 변경이다 (찾았고 **아직 안 고쳤다** · 정본 훑기 규칙 6: 목록으로 먼저 보고)

| 자리 | 인용 | 갈래 |
|---|---|---|
| `memory-philosophy.md` §7 (c) 표 | 「약속 (`promise`) · 일정 (`event`)」 두 행 · 「정보 (`info`) ✕ ✕」 | **사실이 틀림** — 행 둘 빼고 지식·추억 넣고, 정보의 다시 보기 = 「○ "유효 기간"(보임 · 시점 ✕)」 |
| 같은 절 | *"화면과 판정은 **같은 함수 하나**를 본다(`ClassSpecCatalog.uses`)"* | **사실이 틀림** — 이제 둘(`uses`/`schedules`)이고 정보만 갈린다. **왜 갈랐나**를 함께 |
| 같은 절 | *"약속·일정은 「미리 알림」, 할 일·주차 위치는 「다시 보기」"* | **시제** — 「당시 약속·일정이 …였다」 · 예를 되풀이·정보(「유효 기간」)로 |
| 같은 절 | *"정보·아이디어·원칙은 마감·다시 보기를 둘 다 안 쓴다 → 「시간 설정」 섹션 자체가 없다"* | **사실이 틀림** — 정보는 이제 「유효 기간」 줄이 있다 |
| §2-2 | *"현실의 사건(할 일·일정·약속)"* | **시제** — 나열이 옛 분류. 뜻(시점 있는 것은 살아있는 기억에 안 들어간다)은 그대로 |
| §7-1 해시태그 항목 | *"「정보」를 여러 분류로 쪼개지 않고 태그로 나눈다"* | **유지** — 지식은 쪼갠 것이 아니다. 그 옆에 한 줄만 |
| 사양서 §2 표 | `event`·`promise` 행 · `info` 행 | **⚠️ 보호자산**(§3 프롬프트와 짝) — 사용자가 정한다. 지우기보다 「2026-09-14 뒤집혔다」 |
| 사양서 §2-A | *"6종에 주차 위치가 나란히 들어가 7개"* · *"정보·아이디어·원칙은 시간을 안 쓴다"* | **사실이 틀림** |

## 5. 사용자가 정할 것 넷

1. **지식·추억의 색·심볼** — 지금 값(임시): 지식 주황·책 / 추억 분홍·하트. 다른 후보: 지식 라임 `0xA3E635`·`graduationcap.fill` / 추억 청록 `0x2DD4BF`·`photo.on.rectangle`.
2. **상세 배너 문구** — 후보: ⓐ 「유효 기간이 지났어요 — 9월 20일까지」(지금 임시) ⓑ 「유효 기간 지남 · 9월 20일」 ⓒ 「이제 유효하지 않은 정보예요 — 9월 20일까지」.
3. **§4 목록을 반영할까** — 사양서 §2·§2-A · `memory-philosophy.md` §7.
4. **폰 설치** — 하면 `docs/install-log.md`에 한 줄. 확정 근거 후보: **분류 메뉴에 「지식」·「추억」이 있고 「약속」·「일정」이 없다.**

## 6. 실데이터 조작 기록 (기록 규칙 1·2)

- **13:55:40 KST** · 백업 `SecondBrain/backups/inbox-all-20260914-135540/`(7파일) → 새 파일 `inbox-unclassify-promise-20260914.md`
  (`@ 1789361200551.{0,1,2}.macbook-unclassify | <UUID> | set type=`) → 실데이터 확인: 약속 **0개** · `59856396`·`E230C805`·`0D2C7208` 셋 다
  「삭제된 기억」 자리 그대로 · `type` 빈 값. **되돌리기 = 그 파일 하나 삭제.**
- dry-run은 사본 폴더에서 먼저 돌렸다(약속 3 → 0 · 자리 불변).
- `376686D1` 「김대표한테 그거 하나 보내기」는 **사용자가 폰에서 손으로** 할일로 바꿨다(실데이터 `type=info-action` 확인 · 09-14).
