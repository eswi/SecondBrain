# 작업 로그 — 2026-07-13 (SecondBrain)

> 이 세션에서 한 일 전체 기록. 재검토용.

## 0. 세션 시작 — 전체 검토
- 로컬 repo가 origin보다 **3커밋 뒤처져** 있어(새벽 작업분) `git pull`로 동기화 후 시작.
- **사양서(`second-brain-v0-spec.md`) 기준으로 전 파일 검토.** 결론:
  - 진행상황: §7 로드맵 **A(시점 알림) 완료 / D(분류 cron) 수동까지 / C(입구 개선) 미착수** — 계획대로.
  - 코드 정합성: 파서·분류기 정규식/필드형식 호환, API 사용 최신 규격, 데이터 분리 원칙(inbox·키·백업 미커밋) 준수 — **양호**.
  - 발견한 모순: ①사양서 §2 표·§3 프롬프트에 `info`·`principle` 누락(코드·실데이터엔 존재), ②resurface "3일 전"(사양서) vs 7일 창(앱), ③README 낡은 문구.

## 1. 내려진 결정 (사용자)
- **GitHub 사양서 = 단일 정본**, 수정은 항상 Claude Code를 통해서.
- 위 세 가지 문서 수정 승인.
- **D단계(cron) 착수** — 이 Mac에 매시간.
- **iPhone 실험 개시.**
- 시점 **날짜 사용자 조정 기능은 보류**(나중에).
- iPhone 날짜 피커는 **적용/취소 2단계 방식** 채택.
- 파일 위치 정리: 사양서 iCloud·Downloads 사본 **삭제**, ps1 iCloud본 **유지**(Windows 실행본), `inbox 복사본.txt`는 본인이 정리.

## 2. 문서 변화 (커밋 `191e210`)
- **사양서 §2 분류표:** `info`(정보·참고)·`principle`(원칙=ambient) 행 추가, resurface 규칙을 실제 앱 동작(**7일 창** + principle 상시)으로 갱신.
- **사양서 §3 프롬프트:** `classify.py`의 실제 프롬프트와 싱크(짧다고 버리지 말 것·question 필드·적극적 due 추출), "정본은 classify.py"임을 명시.
- 이후 D단계에서 **§0-A·§6-2·§7(D)** 도 cron 구현 반영.
- **README:** info/principle, cron(launchd) 섹션, `automation/` 구조, 낡은 문구 수정.

## 3. 코딩

### ① D단계 — macOS 자동 분류 (커밋 `ec5baab`)
- `automation/setup-mac.sh` (launchd, 매시간 :00 설치) · `run-classify.sh` (타임스탬프 로깅 래퍼) · `uninstall-mac.sh`.
- 키는 `~/.config/secondbrain/anthropic_key` 파일 사용(launchd는 셸 env 못 읽음), 미분류 없으면 API 호출 0(멱등), **단일 기기에만**(§0-A 동시 실행 금지). 이 Mac에 실제 설치·등록됨.

### ② classify.py 무인 실행 하드닝 (커밋 `51618be`)
- 401/403/429/네트워크/기타 HTTP 오류를 **traceback 대신 한 줄 메시지 + exit 1**로. 로그: `~/Library/Logs/secondbrain-classify.log`.

### ③ iPhone ＋시점 버그 — 단계적 수정 (커밋 `23cdbb7`→`6791b8d`, 배지 v3~v7)
- `23cdbb7` 안 열림 → 투명 오버레이 `<input type=date>`
- `f8d51f9`(v4) 안 골라도 닫힘 → 터치에선 `showPicker()` 중복 호출 제거
- `6897efb`(v6) 고르는 즉시 닫힘 → 값 반영을 `change`→**`blur`(달력 닫힘)** 로 이동(재렌더가 달력 파괴하던 문제)
- `6791b8d`(v7) 오늘 오입력 → **적용/취소 단계**: 달력 닫히면 `📅 날짜 [적용][취소]` 표시, [적용]눌러야 반영 — **최종 동작 확인됨**
- 부수: 헤더에 **빌드 버전 배지**(`6de2cbb` v3, `e9fb5fe` v5 컬러) — PWA 캐시 반영 확인용. **셸 변경 시 배지 숫자 + `sw.js` CACHE 함께 올리는 규칙** 확립.

## 4. 실행·검증에서 실제로 나온 것
- classify 첫 실행이 **401(옛 org의 폐기된 키)** 로 실패 → 키 교체 후 성공, 미분류 2줄 분류·백업 생성.
- **iPhone 실험 중 iCloud 동기화 충돌 실물 발생**: 데스크톱 분류와 아이폰 수집이 겹쳐 last-writer-wins로 **분류 2건 롤백**(원문 손실 없음) → 재분류로 복구. 설계서 §0 경고 위험의 실제 사례.
- **시점 자동추출·discard·info/principle** 실데이터에서 정상 확인("내일까지"→+1, "이번 주 안에"→그 주 일요일, 빈 음성→버림).
- 참고: 07-19는 **일요일**(금요일 아님) — "이번 주 안에"의 일요일 추론.

## 5. 파일 위치 정리
- 인벤토리 확인 → **`~/Downloads` 구버전 사양서 삭제**, **iCloud 사양서 사본 삭제**, **iCloud ps1 유지**(Windows 실행본).
- 최종: **사양서 = repo/GitHub 한 곳** · **ps1 = repo(소스)/iCloud(Windows 실행본)** · **iCloud 폴더 = 데이터 + ps1만**.

## 6. 커밋·배포 현황
- 이 세션 커밋 **9개, 전부 push 완료** (`main == origin/main`):
  `191e210 → ec5baab → 51618be → 23cdbb7 → 6de2cbb → f8d51f9 → e9fb5fe → 6897efb → 6791b8d`
- 웹앱 **v7 배포** (https://eswi.github.io/SecondBrain/), launchd 분류기 **active**.

## 7. 메모리 기록
- `second-brain-two-location-dev.md` 신규+갱신, `second-brain-layout.md` 갱신.
- **보류 항목**: 시점 폰→데스크톱 전달 / iCloud 충돌 완화 / iPhone 날짜피커 더 나은 해법(커스텀 or 네이티브).

## 8. 한계·주의 (재검토 시 참고)
- **iPhone 수정은 직접 테스트 불가** → 사용자 피드백으로 4회 반복해 수렴. v7은 "iOS 확인/바깥탭 구분 불가"를 우회한 것이라 **"바깥 탭=즉시 취소"는 아님**(스테이징 칩 뜬 뒤 취소 필요).
- 초기에 **push 누락**으로 "증상 그대로" 혼선 1회(이후 배지로 배포 확인 체계 마련).
- 남은 동기화 의무 1개: **repo에서 ps1 고치면 iCloud로도 복사.**
- **Windows 자동 분류(작업 스케줄러)는 미구현** — 필요 시 추가.
- iCloud `inbox 복사본.txt`는 사용자가 정리 예정(그대로 둠).
