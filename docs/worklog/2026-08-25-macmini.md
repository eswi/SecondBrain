# 2026-08-25 밤~08-26 새벽 (집 맥미니) — **맥에서 보니 카드가 「첫 장」만 말한다**

> ## 재개 한 줄 — **2026-08-26 00:3x (이 줄이 최신이다)**
>
> ### ✅ 맥 앱을 이 기기에서 빌드해 띄웠다
> **커밋 `18e0955`** · Core 시험 **410 · 실패 0** · 빌드 `EXIT=0` · `error: 0` ·
> 실행파일 시각 **22:41:39**(실행 22:41:47 · 디버거 없이 `open` — 성능 측정 규칙 1).
> **폴더는 이미 기억돼 있었다** — 바로 목록이 떴다(맥은 08-25 저녁에 이미 기기로 들어왔다).
>
> ### ⏸ 여전히 대기 중인 판정 하나 — **폰의 이름 띠** (`a8e99bc` · 08-25 16:55)
> **이 세션에서도 안 받았다.** 맥을 보는 쪽으로 갔다. **다음 세션이 먼저 확인받는다.**
>
> # ▶ 사용자 결정 대기 — **여럿일 때 카드가 무엇을 말하나** (아래 §3)
> **카드 테두리·받아오기 둘 다 「첫 장」만 본다.** 색 규칙은 사용자가 정한 것이라(08-23) 내가 못 넓힌다.

---

## 1. 한 일

| # | 무엇 |
|---|---|
| 1 | `git pull`(22커밋) · `xcodegen generate` · Core 시험 410 · 시뮬 UDID 재확인 |
| 2 | **맥 앱 빌드·실행** (스킴 `SecondBrainApp-macOS` · `-destination 'platform=macOS'`) |
| 3 | ⛔ **맥북이 남긴 표본 ②가 못 쓰는 것이었다** — 정정하고 대체 표본을 데이터에서 계산 |
| 4 | **표본 ④를 화면에서 판정** → **결함 하나 · 설계대로 하나 · 미정 하나**가 갈렸다 |

---

## 2. ⛔ 표본 ②가 못 쓰는 것이었다 — **「보관된 기억」에는 상세가 없다**

맥북이 남긴 표본 ②는 *"「우유 사오기」 · 보관된 기억 → 완료된 기억 · 배지 「5」 · 테두리"*였다.
**그 화면에는 상세로 가는 길이 없다** — `ArchiveView`에 이동도 자료 네모도 **하나도 없고**,
줄에는 원문·구분·「완료/삭제」만 있다. 유일한 조작은 **왼쪽 스와이프 「되돌리기」**.
⚠️ **맥 문제가 아니다 — 폰에서도 같다.** 사용자가 *"상세로 들어갈 수가 없는걸"*로 발견했다.

**정정한 자리:** `docs/worklog/2026-08-25-macbook-2.md` 표본 표(②줄 아래 · 옛 줄은 남겼다).

### 대체 표본은 데이터에서 계산했다 (계측 규칙 7)
**사진 2장 이상 + 상태가 섞인 항목은 데이터 전체에 셋뿐이다:**

| 표본 | 원문 | 어디 | 사진 상태 (**맥미니** 2026-08-25 22:4x) |
|---|---|---|---|
| ② `5F82A031` | 우유 사오기 | 완료된 기억 | 5장 중 4 못 받음 · 1 받음 — ⛔ **못 쓴다**(상세 없음) |
| ③ `A3D2A7AF` | 주차 위치 | 지금 챙길 것 | 9장 중 7 못 받음 · 2 받음 · 음성 1 못 받음 |
| **④ `BFE53B0B`** | **권기오한테 연락 한번 해야함!** | 지금 챙길 것 | **2장 중 1 못 받음 · 1 받음** ← 가장 단순한 대조 |

**이 기기의 dataless:** photo **37/52** · audio **105/115** (⚠️ **기기별 값**).
★ **섞여 있는 것이 중요했다** — 전부 못 받음이면 첫 장만 보든 전체를 보든 **똑같이 앰버**라 판정이 안 된다.

---

## 3. ★★ 표본 ④의 판정 — **셋이 갈렸다**

**사용자가 본 것:** *"테두리는 밝은 회색임. 한장은 사진이 있고 한 장은 없음.
url도 하나 있는데 이 네모에서는 링크 아이콘과 domain name만 나옴."*

### ㉮ ⛔ **결함 — 테두리가 「첫 장」만 말한다** (판정: 그렇다)
**밝은 회색 = `.ready`(정상)**이다. 그런데 그 항목의 사진 둘 중 **하나는 못 받은 상태**다.
`MediaCard`의 사진 갈래가 **`names.first`로만** 상태를 계산하기 때문이다.
⛔ **뒤엣것이 무슨 상태든 카드에 안 나타난다.** 맥북이 물으려던 것이 이것이고, **답은 「첫 장만 본다」**다.

### ㉯ ⛔ **그리고 더 나쁜 짝 — 받아오기도 첫 장만 한다**
`photoFetch.start(...)`가 **`.first`에만** 걸려 있고, **뷰어에는 `start`가 아예 없다.**
그래서 **둘째 사진은 아무도 받아주지 않는다** → 뷰어에서 사용자가 본 *"한 장은 없음"*이 그것이다
(`missing` = 구름 내리기 아이콘 · **글자 없음**).
★ **앰버의 약속이 안 지켜진 자리다** — 색 규칙(08-23)은 앰버를 **「기다리거나 눌러서 볼 수 있다」**로
정의했는데, **둘째부터는 기다려도 안 오고 눌러도 안 받는다.** 게다가 카드가 **정상**이라 앰버조차 안 뜬다.
⚠️ **뷰어에 글자가 없는 것은 미룬 것이다**(그 자리 주석: *"새 문구를 안 짓는다 — 뷰어가 할 말은
뷰어 상세 문서에서 정한다"*) — 결함과 갈라 적는다.

### ㉰ ✅ **URL 네모에 캡쳐가 없는 것은 설계대로다** (결함이 아니다)
`MediaCard`의 URL 갈래가 **`#if os(iOS)`**로 갈려 있고, **맥은 늘 그림 없음**이다.
그래서 맥에서는 **아이콘 + 도메인 이름**이 정상 얼굴이다. ⚠️ **미리보기 캐시는 폰 안에만 있다**(iCloud에 없다).

### ⏸ 못 본 것 하나 — **배지**
그 항목은 사진이 **2장**이라 배지 「2」가 떠야 한다. **사용자 보고에 배지 언급이 없어 못 잼으로 남긴다.**
⛔ 그 자리 주석은 *"지금 데이터에서는 늘 1이라 배지가 안 뜬다"*로 **낡아 있다**(여럿이 생겼다).

---

## 4. ⏸ 사용자 결정 대기 — **여럿일 때 카드가 무엇을 말하나**

⛔ **색 규칙은 사용자가 정한 것이다**(2026-08-23) — 「첫 장만」을 「전체를 접어서」로 넓히는 것은
**내가 결정할 수 없다**(항시 규칙 6). 갈래는 아래 §5.

---

## 5. 다음에 할 일

1. **⏸ 폰의 이름 띠 판정** — 두 세션 연속 밀렸다.
2. **⏸ ㉮㉯를 어떻게 고칠지 사용자 결정** — 테두리가 여럿을 어떻게 접나 · 뒤엣것을 언제 받나.
3. **③ `A3D2A7AF`(주차 위치 · 배지 9)** — 아직 안 열었다. **종류 셋이 다 있는 유일한 기억.**
4. **⏸ 맥에서 URL 네모를 어떻게 열 것인가** — ㉮ 기본 브라우저 ㉯ 맥 전용 창 ㉰ 안 한다.
