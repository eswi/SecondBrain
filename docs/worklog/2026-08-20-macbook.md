# 2026-08-20 (회사 맥북 · 세 번째 세션) — **`5a9c934` 판정 · 테두리를 없앴다**

> ## 재개 한 줄
>
> **폰 = `6973775` = 저장소 코드. 미확인 빌드 없음.**
> 다음은 `HANDOFF.md` §4 — **「있는 목록이 소진되게 한다」**.

---

## 1. 세션 시작 — **36커밋 뒤처져 있었다**

| | |
|---|---|
| 기기 | **회사 맥북**(`MacBook-Pro-2.local`) — 집 맥미니에서 넘어왔다 |
| 시작 HEAD | `4455f0f` → `git pull` → **`4ac2b18`** (**36커밋**) |
| `git status` | clean · 원격과 동기 |
| `xcodegen generate` | ✅ 돌렸다 (기기가 바뀌었다) |
| 이 기기 시뮬 UDID | **`ED17E1A9-8F2A-4994-AAD2-C9AC46177EB5`** (맥미니는 `34A5033B-…` — 다르다) |

**세션 시작 절차가 값을 했다.** 문서를 열기 전에 당겼으므로 사용자가 지정한
`docs/worklog/2026-08-20-macmini-2.md`가 **그 자리에 있었다** — 안 당겼으면 **없는 파일**이었다
(08-20 아침 맥미니 세션이 밟은 그 형태).

**폰에 깔린 것과 저장소 코드 대조:** `git diff 5a9c934..4ac2b18 -- native/`가 **비어 있었다.**
그 뒤 셋은 전부 문서 — **폰 = 저장소 코드**였다.

---

## 2. ✅ `5a9c934` 판정 — **㉠ 통과 · ㉡ 못 했다**

| | 무엇 | 판정 |
|---|---|---|
| **㉠** | 끌기가 여전히 되나 | ✅ *"끌기는 되는 상태. 순서도 바뀌고, 동작 자체가 진행되기는 해."* |
| **㉡** | 제자리에서 놓으면 테두리가 바로 사라지나 | ⛔ **아니다** |

**㉠이 통과한 값:** `List`에 `.onDrop`을 얹으면 순서 바꾸기를 뺏길 수 있다는 것이 **미리 적어 둔 위험**이었는데
**안 깨졌다.** (`d1749e2`·`7e643f4`에서 두 번 깬 자리라 같은 무게로 적어 뒀던 것이다.)

**㉡ 사용자 판정 그대로:**
> *"테두리가 사라지는 시간은 그때 그때 달라. 어떤 때는 바로 사라지고 어떤 때는 1초 좀 더 지나서
> 사라지고, 누르고만 있으면 아직 손을 떼지 않았는데도 사라지고 그래.
> 테두리는 없애자. 너가 못 하는 것으로 생각해."*

---

## 3. ⛔ 왜 못 했나 — **끄는 신호가 없다**

**켜는 쪽은 3차(`11fd8cb`)에서 풀렸다** — `.onDrag`는 끌기가 **실제로 시작될 때** 시스템이 부른다.
**막힌 것은 끄는 쪽이었고, `List`는 「지금 끌린다」를 안 알려준다.** 그래서 세 가지를 겹쳐 썼는데
**셋이 서로 다른 시점에 걸렸다:**

| 끄려던 것 | 언제 걸리나 | 왜 안 맞나 |
|---|---|---|
| `.onMove` | 순서가 **실제로 바뀔 때만** | 제자리에 놓으면 **안 불린다** |
| `.onDrop(isTargeted:)` | 드래그가 영역에 **드나들 때** | 손을 뗀 순간이 아니다 |
| **1.5초 안전망** | 끌기 시작 **1.5초 뒤** | **손을 떼기도 전에 걸린다** ← *"누르고만 있으면 … 사라지고"* |

**★ 1·2차는 켜는 쪽에서 깨졌고, 3차는 끄는 쪽에서 깨졌다.** 같은 요구의 반대편 끝이다.

**안전망은 원래 다른 증상을 막으려던 것이었다** — *"가만히 그 자리에서 손을 떼면 테두리가 안 없어져"*
(5초 → 1.5초). ⚠️ **그 땜질이 새 증상을 만들었다:** 5초일 땐 안 보이던
「누르고 있는데 꺼진다」가 1.5초로 줄이자 **정상 조작 시간 안으로 들어왔다.**

---

## 4. 코드 — 뺀 것과 **남긴 것**

`native/Sources/App/PrincipleListView.swift` (`6973775`)

| | 무엇 |
|---|---|
| **뺐다** | `.onDrag`(테두리 전용이었다) · `.onDrop`/`dropInside`/`onChange` · `draggingID` · `overlay` 테두리 · `animation` · `import UniformTypeIdentifiers` · `row(dragging:)` 인자 |
| **남겼다** | `.onMove`(순서 바꾸기는 `List` 기본으로) · **여백 고정 `.padding(.vertical, 5)`** · **머리글 둘째 줄** |

**★ 여백 고정을 남긴 이유:** 테두리가 없어졌으니 그 코드는 이제 아무 일도 안 하는 것처럼 보이지만,
**`26c6560`의 교훈이 거기 붙어 있다** — *"보이는 것(그림)을 바꾸려다 재는 것(레이아웃)까지 바꾸지 않는다."*
주석으로 함께 남겼다.

> ### ⚠️ 「끌 수 있다」를 알리는 것이 **머리글 둘째 줄 하나만 남았다**
> ≡ 손잡이는 `30e2d91`에서 뺐고(사용자: *"이거 뭐야?"*), 테두리는 지금 뺐다.
> 남은 것은 **「눌러 끌어서 순서를 바꾸세요」 + iOS 기본 들어올림**뿐이다.
> ⛔ **그 줄을 지우면 아무 신호도 안 남는다** — 주석에 적었다.

**MARK: 주석에 「못 했다」를 남겼다** — 시도 다섯을 표로, 그리고 **다시 시도하지 말 것**.
⛔ 탈출구는 **`List`를 버리고 `ScrollView`+직접 만든 순서 바꾸기**뿐이고,
**그것은 잔상 과제와 정확히 같은 탈출구다.** 그래서 HANDOFF §4에서 **둘을 한 줄로 합쳤다** —
값을 치를지는 **사용자가 한 번에 정한다.**

---

## 5. 검증

| | 결과 |
|---|---|
| `swift test --package-path SecondBrainCore` | ✅ **374개 · 실패 0** (변동 없음 — 화면 코드다) |
| iOS 시뮬 빌드 (`ED17E1A9-…` = 이 맥북) | ✅ EXIT=0 · `error:` 0 · `warning:` 0 |
| macOS 빌드 | ✅ EXIT=0 · `error:` 0 · **코드 경고 0** (AppIntents 툴체인 알림 1건은 코드가 아니다) |
| 실기기 빌드 + 설치 | ✅ **`App installed`** — `6973775` (**16:36**) |

### ★ 알게 된 것 — **폰이 회사 맥북에도 붙는다**

`devicectl list devices`에 `ter.iPhone 16p`가 **`available (paired)`**로 잡혔고 설치가 됐다.
⚠️ **지금까지 설치는 전부 맥미니에서 했다.** 「기기를 옮겼으니 설치는 다음에」라고 미룰 이유가
**없다는 것을 실측으로 알았다.** → HANDOFF §2에 적었다.

---

## 6. 설치 이력 (그 자리에서 적었다)

| 시각 | 커밋 | 확정 근거 |
|---|---|---|
| **16:36** | **`6973775`** | **끌어도 테두리가 아예 안 뜬다** (직전 `5a9c934`는 떴다) |

---

## 7. 다음

1. **「있는 목록이 소진되게 한다」** — `HANDOFF.md` §4
2. ⏸ 원칙 목록 **잔상 + 테두리**(합쳤다) — `List`를 버리는 값을 사용자가 정한다
3. ⏸ 알림 탭 — 승인된 미결(사용자: 지금 열지 않는다)
4. 자료 순서표 **②자료 확장** → ③새 분류 셋 → ④해시태그 (`HANDOFF.md` §3-A)

---

## 8. ★ 잔상 — **재현 장치를 만들어 쟀다** (사용자 요구: *"방법을 찾아봐, 제발"*)

**「두 번 시도하고 안 된다」로 닫아 뒀던 자리인데, 그 결론의 전제가 `.onDrag`였다.
오늘 `.onDrag`를 뺐으므로 전제가 사라졌다.** 그래서 다시 쟀다.

### 8-1. 무엇을 만들었나 — **처음으로 「눌러야 나오는 것」을 눌러 봤다**

| 조각 | 무엇 |
|---|---|
| `GhostLab` (scratchpad) | 실험용 iOS 앱 — 같은 목록을 **다섯 꼴**로 그린다(A~E). **진짜 앱은 안 건드렸다** |
| XCUITest | `press(forDuration:thenDragTo:withVelocity:thenHoldForDuration:)` — **끌다가 12초 멈춘다** |
| 촬영 루프 | 시험이 도는 동안 `simctl io screenshot`을 **시험이 끝날 때까지** 반복 |
| 판정기 | 스크린샷에서 **빨강 사각형이 몇 개의 y-띠에 있나**를 센다 — **2개면 잔상** |

⚠️ **시뮬 창을 마우스로 조작하는 길은 이 맥북에서 막혔다** — 창이 39×135로 줄어 있고
`System Events`가 Simulator 창을 못 본다. **XCUITest는 창이 필요 없다.**

### 8-2. 잰 값

| 꼴 | 무엇 | 끌 때 빨강 띠 | 판정 |
|---|---|---|---|
| **A 기본** | `List` + `.onMove` | **시종 1개** (y387→y782로 **이동**) | ✅ 잔상 없음 |
| **C onDrag** | + `.onDrag` (오늘 뺀 꼴) | 원본이 **목록에서 빠지고** 작은 칩이 뜬다 | ✅ 잔상 없음 |
| **D 실제꼴** | `NavigationStack`+`NavigationLink`+`listRowBackground` = **지금 앱과 같은 꼴** | **시종 1개** | ✅ **잔상 없음** |

**D의 화면(`shots/D_150.png`):** 목록이 `BBBB · CCCC · [들린 AAAA] · DDDD · EEEE` —
**원래 자리에 아무것도 없다.** 사용자가 원한 *"있던 것을 들어서 옮기는 것"* 그대로다.

### 8-3. ⛔ 첫 판정 셋은 **버렸다 — 내 촬영이 먼저 끝났다**

C·D·E의 첫 실행에서 *"끌기가 아예 안 됐다"*고 읽었는데 **틀렸다.**
모드 전환 탭 때문에 끌기가 2~3초 늦어졌고 **고정 개수(70~85장) 루프가 그 전에 끝나** 있었다.
→ **「시험이 끝날 때까지」로 바꾸니 235프레임이 찍혔고 끌기가 다 들어 있었다.**
⚠️ **없는 것을 「안 된다」로 읽을 뻔했다** — 계측 규칙 #7의 「못 잼을 값으로 만들지 않는다」와 같은 형태다.

### 8-4. 그래서 무엇이 남았나

**시뮬레이터에서는 세 꼴 다 잔상이 없다.** 즉 **`.onDrag`가 범인이라는 내 가설도 확증되지 않았다** —
C에도 잔상이 없었다. **못 잰 것:** 실기기 · **진짜 손가락**(합성 끌기가 아니다) ·
**여러 줄로 접히는 실제 원칙 문장**(실험용 줄은 한 줄이다).

▶ **다음 판정은 폰에서 난다** — 지금 깔린 `6973775`는 `.onDrag`가 없는 **D와 같은 꼴**이다.
