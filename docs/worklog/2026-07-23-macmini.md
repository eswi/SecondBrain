# 작업 로그 — 2026-07-23 (mac mini · 07-22 저녁 이어서)

> **재개 한 줄:** `git pull` → `cd native && xcodegen generate` → 빌드 확인.
> **다음 후보:** ① 분류 관리 "재설계 후보" 실사용하며 아이디어 쌓기(며칠 곱씹기) → 층위1 개념 논의 준비
> ② STT `question` 필드 후속(공백 직렬화) ③ ⑥ 원문 편집(음성 들으며 텍스트 고치기) ④ 음성 항목별 iCloud 옵트인.
> **주의:** 분류 재설계는 §2·§3 근본 재설계라 **실사용 데이터 더 쌓인 뒤 신중히** — 즉흥 금지(규칙 #1).

## 오늘 한 일 (커밋 순)
1. **pull-to-classify + 자동 스윕 제거** (`e92fa87`) — "새로운 기억" 목록에 `.refreshable`. 당기면 미분류를 Claude로 분류(§0-A).
   진행은 네이티브 새로고침 스피너(`runningToast:false` — 중복 스피너 방지), 결과·실패·없음은 중앙 토스트.
   RootView에서 `autoClassifyOnOpen`·`autoClassify()`·.task/scenePhase 자동 호출 제거(**reload·탭 복원·토스트 오버레이는 유지**).
   실기기 확인: 스피너만으로 충분(사용자 확인). SettingsView 각주도 갱신.
2. **'새 기억들' 역순 보기 토글** (`14a5963`) — 제목 줄 우측 화살표(오래된 순=↑ / 최신 순=↓, 역순 시 accent).
   **표시 순서만** 뒤집음 — 데이터·수집시각(성역) 불변. **세션 한정**(@State, 앱 재실행 시 기본 복귀), 항목 2개↑일 때만 노출.
   문서: `memory-philosophy.md` §5에 한 줄 반영(기본 "오래된 순" 유지).
3. **§0-A 갱신** (`727ca6b`) — pull-to-classify "대체 예정 → 구현됨(2026-07-22)". 앱 열 때 자동 제거·당길 때만 실행 반영.
4. **분류 체계 재설계 미결 문서** (`0bd9e6a`) — `docs/native/classification-redesign-open-questions.md`.
   결정 아님·질문 붙잡기. 3층위(개념/구조/통합) + 실사용 확인 항목 + 현재 §2 스냅샷. **§2·§3·merge-design 무변경 배너.**
5. **분류 관리 화면 껍데기** (`cf575ff`) — 설정→"분류 관리". §2(`TypeCatalog.assignable` 6종)는 **읽기 전용**(자물쇠).
   "재설계 후보"는 **별도 저장소** `CandidateStore`(UserDefaults `reclass.candidates.v1`, JSON) — 추가/삭제/이름·메모 편집 실제 저장, 앱 재실행에도 유지.
   화면에 "실제 분류 아님 · 자동 분류는 안 씀" 강조. 후보 편집: 이름·메모만 실제, 특성·필드·규칙은 "준비 중" 껍데기.
   **분리 검증:** 자동분류 경로(InboxModel·ClaudeClassifier·§3·TypeCatalog)는 후보를 전혀 참조 안 함(grep 확인 · 이 파일이 그쪽을 import/호출 안 함).
6. **원문 2줄 표시** (`4c2d22f`) — 공유 셀 `MemoryRow` `lineLimit(1)→(2)`. 새 기억들·살아있는 기억 통일(보관/검색은 이미 2줄).
   배지·메타 배치는 그대로, 줄 수만. 보관 vs 검색 형식 차이(색·상태배지·구분선)는 **의도된 차이로 유지**.

## 상태
- main = `4c2d22f`, 로컬=원격 동기(push 완료), working tree clean. 브랜치/worktree = main 단일(정리 불필요).
- 폰: 최신 빌드(위 6개 변경 전부 포함) 설치·실기기 확인됨.

## 보호 자산 (이번 세션 무변경 확인)
- §2 분류 체계 / §3 프롬프트(`classify.py`) / `TypeCatalog`(Theme.swift) / `merge-design.md` — **쓰기 0**.
- 분류 관리 화면은 §2를 **읽기만**, 후보는 완전 분리된 UserDefaults 저장소.

## 사양서 반영 확인 (규칙 #2)
- pull-to-classify → §0-A "구현됨"으로 갱신 완료(`727ca6b`), memory-philosophy §5 역순 토글 반영 완료.
- 분류 재설계 → **의도적으로 사양서 미반영**(근본 재설계·미결). 미결 문서로만 붙잡아 둠. §2 손대지 않음.

## 작업 방식 메모 (이번에 굳힘)
- **착수 전 계획 먼저 보여주고 확인** 받기 · **재개 절차는 말 말고 실제로 실행**(pull→xcodegen→빌드·테스트). (기억에도 저장.)
