# 작업 로그 — 2026-07-24 (회사 맥북 · 어제 맥미니 Stage 4 이어받기)

> **재개 한 줄(다음 세션):** Stage 4 주차 UX 재설계 **완료**(추가 필드 제거 + 목록 유연층 표시/선택 통일, 실기기 검증 통과).
> 다음: **① '살아있는 기억' 분류 필터에 유연층(주차) 포함**(위대표 명시 보류 — 곧) → **② photo/parking 사양서 반영 여부 판단**(규칙 #2).
> `git pull` → `cd native && xcodegen generate` → 스킴 **`SecondBrainApp-iOS`**(주의: `SecondBrain-iOS` 아님)로 빌드.
>
> **① 착수 지침(집=맥미니 시작 시, 위대표):** '살아있는 기억' 분류 필터에 주차위치가 빠진 것부터.
> 원인(필터가 기본층만 아는지 = `ClassRegistry`로 통일 안 됐는지) + 필터 방식(**모든 분류 나열** vs **실제 있는 것만**)
> 확인하고, 표시·메뉴와 **같은 뿌리면 `ClassRegistry`로 맞춤**. 그리고 이 필터 수정을 '유연층 통일' 커밋에 표시·메뉴와
> 함께 넣기(같은 뿌리라 한 커밋).
> └ (사실 주석: 표시·메뉴 통일은 이미 `84f3152`로 **push됨** → 문자 그대로 한 커밋 합체는 히스토리 재작성이라 안 함.
>    같은 뿌리를 완성하는 **후속 커밋**으로 붙이고 메시지에 `84f3152`를 참조해 "같은 뿌리"임을 명시.)

## 오늘 한 일

### 1. Stage 4 주차 UX 재설계 — 이중 입력 제거 (개념 반영)
- **개념(위대표):** "분류가 무엇을 보여주고/중요하고/편집 가능한지 결정한다." 공통 구조 = 각 분류가 필요로 하는
  정보의 합집합(억지 공통 설계 X). 개념의 올바른 형태 = **이미 있는 정보의 표시·중요도 선택**이지 새 입력 필드 추가가 아님.
- **어제 문제:** 주차위치로 분류되면 위치·메모 텍스트 필드 2개를 또 입력 → 사진·GPS·본문으로 이미 충분한데 이중 입력.
- **수정:** 주차위치 추가 필드(위치·메모) 제거. `FieldSpec`/`ClassSchema`/`ClassRegistry.schema(_:)`도 제거
  (parking이 필요 필드 0 → 빈 껍데기 = 과잉추상화, D4 위배). `DetailView`의 flexDrafts/flexFieldsSection/flexBinding 제거.
  주차위치 = **사진 1장 + GPS(EXIF) + 본문**을 성역 카드가 그대로 보여줌. GPS 지도 유지.
- **유지:** question이 쓰는 `fields.v1` 파서(Stage 1 그릇)는 무관하게 그대로. 보호 자산(§2 `TypeCatalog`·§3 프롬프트·`MergeEngine`) 무변경.
- 문서: `classification-redesign-open-questions.md`에 **D5**(개념) 신설 + 층위2 예시 정정.

### 2. 유연층 표시/선택 통일 — "반쪽 수정" 제거 (B안)
- **버그:** 상세화면에서 주차위치로 분류했는데 목록('새 기억들')엔 미분류로 표시. **저장은 정상**(commitEdits/changeType가
  validTypes 필터 없이 append — validTypes는 자동분류 전용). 원인 = 목록 행이 `TypeCatalog.meta`(기본층 6 전용)를
  써서 `"parking"`이 미분류로 폴백(`TypeCatalog.meta = table[key] ?? table[nil]`).
- **수정(`Components.swift` 3곳):** `TypeMenuButton` 메뉴/아이콘 + `TypeGlyph`를 **`ClassRegistry`**(기본층+유연층)로.
  → 목록·상세 어디서든 같은 분류 집합. 목록 아이콘 탭으로도 주차위치 지정 가능. 위대표 원칙 "모든 분류는 어떤
  기억에서든 후보로 나타나야 한다"와 정합.
- **범위 밖(투명):** 필터 바(`InboxView` primary/overflowFilters)는 기본층 전용으로 남김 — "분류로 필터링"은 별도 축.
  → **보류 과제로 명시(아래 §다음 할 일).**

## 상태
- 커밋: (아래 참조) 3개 — ① Stage4 필드 제거 ② 유연층 표시/선택 통일 ③ worklog. **push 완료(로컬=원격).**
- 폰(iPhone 16 Pro): 최신 빌드(`SecondBrainApp-iOS`, 오늘) 설치. **실기기 검증 통과** — 주차위치가 초록 car로 표시·목록에서 변경 가능, 기본층 6종 정상(회귀 없음).
- 다음 이어받을 기기 = **집 맥미니**(→ `git pull` 먼저).

## 실기기 검증 결과(오늘, 위대표 확인)
- 주차위치 상세: 추가 필드 안 뜨고 사진·지도·본문만 ✅
- 목록: 주차위치 초록 car 아이콘 표시 ✅ / 목록 아이콘 탭으로 주차위치 지정 ✅
- 기본층 6종 정상(회귀 없음) ✅
- (발견) '살아있는 기억' 필터에 주차 미포함 → 보류 과제로 등록.

## 삽질 기록 (교훈)
- 처음에 iOS 빌드가 **exit 0으로 보고됐지만 실제 실패**했음. 원인 둘: **① 스킴 이름 틀림**(`SecondBrain-iOS` →
  실제는 `SecondBrainApp-iOS`), **② `xcodebuild ... | tail`이 exit 코드를 가림**(파이프 exit=tail=0). 그 결과
  DerivedData의 **7/21 옛 .app을 실기기에 설치** → "어제 작업이 다 사라졌다"는 오해(실제 git·코드는 정상이었음).
- **처방(앞으로):** 빌드는 로그를 파일로 보내고 `; echo EXIT=$?`로 진짜 exit 확인 + **산출물 .app mtime이 방금인지** 반드시 검증.

## 다음 할 일 (우선순위)
1. **'살아있는 기억' 분류 필터에 유연층(주차) 포함** — 위대표 명시 보류, 곧. `ClassRegistry`로 필터 후보 확장.
   착수 전 필터 UX(칩 개수·overflow) 확인.
2. **photo/parking 사양서·`memory-philosophy` 반영 여부 판단**(규칙 #2) — Stage 4 재설계가 확정됐으니 이제 반영 시점.
3. (남은 것) 항목별 iCloud 옵트인(사진 동기화) · 여러 장 사진 · 반복 규칙 설계(O3) 등.
