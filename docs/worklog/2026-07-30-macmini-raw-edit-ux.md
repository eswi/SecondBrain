# 2026-07-30 (맥미니) — 본문(원문) 수정 UX 다듬기 (실기기 반복 검증)

> **재개 한 줄:** 본문 수정 기능 **완성·실기기 통과**(원문 바로 편집·저장·이탈 경고·키보드 정리 전부 확인).
> 다음 후보 = ① `second-brain-v0-spec.md`에 본문 수정 반영할지 결정(규칙 #2, 아래 "사양서 질의") ②
> 이탈 경고의 남은 두 과제(왼쪽 스와이프·강제종료 초안 — `edit-policy.md §2-A`에 별도 과제로 기록됨)
> ③ 그간 쌓인 미분류·미기억 항목을 실제로 정리(이 기능을 만든 원래 목적).

전날(`2026-07-29-macmini-raw-edit.md`)의 A(저장·왕복)·A'(왕복 검증)·B(화면) 위에, 오늘은 **실기기 피드백을 받아 UX를 반복**해 다듬었다. 전부 홈 화면 탭 실행(디버거 미부착)으로 위대표가 확인.

## 오늘 한 일 (커밋 순)
- **c993367 / fbd77d2** — 원문 편집 명시적 진입(읽기전용 → [편집] → 체크 [완료], 완료≠저장) + 문서 §2-A 신설·§6 갱신.
- **42066a1 / b4856bd** — 버튼 문구([수정하기]/[수정 완료]) + "저장하지 않은 수정이 있어요" 표시 + 이탈 팝업 문구·선택지([나가기]/[계속 수정하기], [취소]는 무팝업). §2-A에 이탈 한계·별도 과제, §8 "원문 수정 보류" 해소 정정.
- **836176a / 17e7c9b** — ⏪ **되돌림**: 명시적 진입 방식을 버리고 **상세 진입 시 원문 바로 편집**으로. (위대표 판단 — 바로 편집이 낫다.) 문서도 되돌리되 "시도→되돌림" 이력 남김.
- **74d2f28** — 스크롤: `TextEditor` → `TextField(axis:.vertical)`. 자체 스크롤 없어 긴 원문 위에서도 페이지가 넘어감(드래그 가로채기 해소).
- **5db26c3** — 회귀 가드: "열고 안 만지고 나가기"가 팝업 없이 통과(로드만으로 dirty 아님). nil·공백·줄바꿈·`|`·이모지. Core 88→89.
- **f1c7bb7 / e25ff18 / 76209a2** — 키보드 정리: 원문 밖(빈 곳·버튼·메뉴·날짜·하단 바) 탭 시 키보드 내림·포커스 해제, 원문 다시 탭 시 커서·키보드 복귀. Menu는 탭을 삼켜서 `DragGesture(0)`로 별도 처리.

## 실기기 통과 확인 (위대표)
- 원문 바로 편집 / 열 때 키보드 안 뜸 / 긴 원문 아래로 스크롤 됨 / 원문 밖(분류 메뉴 포함) 탭하면 키보드 내려감·컨트롤 정상.
- (사용자 육안 확인 항목: 저장 안 됨 표시·이탈 팝업·빈본문 차단은 코드상 확정 + 눈으로 재확인.)

## 검증
- Core **89 통과, 0 실패**(전날 88 + 로드-dirty 가드 1). iOS(iPhone 16 Pro)·macOS 빌드 통과.
- 왕복 안전 층(직렬화 판단=왕복 검증, `|`·유니코드 줄구분자 차단)은 전날 확정분 그대로.

## 정본 문서 상태
- `edit-policy.md` §2-A(원문 편집 정책·바로 편집·이탈 경고·한계/별도 과제), §8(원문 수정 해소) 반영 완료.
- `memory-philosophy.md` §6(화면 개념) 반영 완료.
- **미결(사양서 질의):** 본문 수정은 네이티브 v1 편집 정책이라 `docs/native/`에 정본을 뒀다. 최상위 `second-brain-v0-spec.md`에도 반영할지는 규칙 #2로 사용자에게 질의 예정.

## 안 한 것 / 다음
- **이탈 경고 별도 과제 2건**(§2-A): 왼쪽 가장자리 스와이프 뒤로(팝업 못 가로챔), 강제종료 대비 초안 자동 보관. 지금 구현 안 함.
- **두 기기 동시 같은 본문 수정 = LWW**(전날 기록한 미결) 그대로.
