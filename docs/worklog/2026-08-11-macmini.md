# 2026-08-11 (맥미니) — F-1 ② 통과. G-7은 F-3 하나만 남았다

> **재개 한 줄:** **F-3 하나만 남았다 — 2026-08-12 20:15, 「나 - 정시 완료 시험」(`131B923C`).**
> 닫히면 **G-7이 끝난다**(기한 8/13보다 하루 빠르다). **폰만 있으면 된다 — 맥이 필요 없다.**

### ⏳ 지금 열려 있는 것 하나
`G-7 회기 08-11`(`9F950B73`) **「꺼두기」** — 결정은 났고(§5) **사용자가 폰에서 실행 중.**
**누른 직후 §5의 빈 한 줄을 채울 것.** 안 하면 내일 20:00에 F-3 옆에 똑같이 생긴 줄이 하나 더 뜬다.

### 🖥 맥북에서 시작하면 **이것부터** (기기 넘어온 정리)
`~/.claude`는 git 밖이라 맥북의 메모리는 이 기기에서 손댈 수 없었다. **맥북 세션 시작 직후:**
1. **설치 시각 관행 메모리를 지운다** — 이제 `CLAUDE.md` 「기록 규칙」에 있다(사본이 갈리면 그게 다음 사고다).
2. **「프로젝트 이름 ≠ 스킴 이름」을 `build-sim-target.md`에 한 줄로 합친다** —
   프로젝트 `SecondBrain.xcodeproj` · 스킴 **`SecondBrainApp-iOS`**. 합친 뒤 원래 메모리는 지운다.
- 먼저 `ls ~/.claude/projects/-Users-eswi-Projects-SecondBrain/memory/`로 **실제 이름·개수를 확인**하고 손댈 것
  (이 기기에서 못 봐서 확정하지 못했다).

### 📱 내일 (2026-08-12)
**1. F-3 — 20:15** · 「나 - 정시 완료 시험」(`131B923C`) · **손대지 말 것** · 알람은 사용자가 건다.
   **⏰ 20:00 정각은 피하고 20:15에 본다** — 20:00이 미리 알림 시각이라 게시가 그 순간 일어난다.
   정각에 보면 **게시 전/후 어느 쪽을 본 것인지 모른다.**
   **앱 완전 종료 → 다시 열기** 후 셋:
   | | 봐야 하는 것 |
   |---|---|
   | ① | **'지금 챙길 것'에 있나** (미리 알림 20:00이 지났으므로 게시됨) |
   | ② | 그 줄에 **「완료」 칩이 없나** — 대신 **「N시간 남음」**(마감 23:55이니 **3~4시간**대) |
   | ③ | 상세 버튼이 **`[이번 것 했어요]`** 인가 |

   **⚠️ 이름이 같은 항목이 하나 더 있다** — `70942905`(매일·`status=done`·보관함). **대상은 `131B923C` = 매주인 쪽.**
   **왜 판정선인가:** 옛 판정 `lastDone(08-06 01:02) ≥ 직전 회차(**매주**라 08-05 23:55)` = **참** →
   옛 코드는 **「완료」 칩을 달았다.** 새 판정은 3절(미게시)에서 끊겨 false → **값이 갈린다.**

**2. F-3 통과하면 G-7 닫힘** — `date-roles-verify-checklist.md`의 G-7을 `[x]`로, 현황 표에 F-3 통과 기입.

**3. 그다음 상세 화면 2차 압축** — **미결 셋을 먼저 정하고, 계측부터.**
   미결 = **새 자리 이름 · 위치 처리 · 나눌지.** ★ **새 자리 이름은 사용자가 정한다** — 후보 2~3개 + 뜻 한 줄로 물을 것.
   ⚠️ **계측이 먼저다** — 1차 압축 때 G-8의 근거가 「~590pt / 가용 694pt」 계측이었다. 정하기 전에 지금 높이를 잰다.

---

## 0. 재개 점검 (맥북 `0564cb6`에서 이어받음)

| | 결과 |
|---|---|
| `git pull` | ✅ `78c3ae4` → **`0564cb6`** (맥북 19:47:40 커밋) · clean |
| `xcodegen generate` | ✅ `native/SecondBrain.xcodeproj` |
| `swift test` | ✅ **322개 통과**(0 failures) — 08-10·08-11 맥북과 무변경 |
| iOS 빌드 | ✅ **BUILD SUCCEEDED** · 스킴 `SecondBrainApp-iOS` |

**코드는 한 줄도 안 건드렸다.** 오늘 맥미니 몫은 **실기기 관찰 하나 + 기록 정정**이다.

## 1. ★ 손대기 전에 실데이터를 읽었고, 그게 하루를 구했다

맥북 worklog는 *"F-1 ②를 못 봤다 — 마감 19:25이 지나 창이 닫혔다"* 고 적혀 있었다.
그 전제로는 **새 항목을 만들어 하루를 더 써야** 했다. 그런데 `9F950B73`의 op 로그를 읽으니 **19:41에 op 둘이 더 있었다:**

| 시각 | 기록 |
|---|---|
| **19:41:33** | `due=08-11 **20:10** · resurface=**20:00**` — 되살리기 **1번**(시각 밀기, lead 10분) |
| **19:41:44** | `due=08-12 20:10 · lastDone=08-11 19:41 · lastDoneDue=08-12 20:10 · resurface=08-12 20:00` — **2번**(이른 완료) |

즉 **회사에서 1·2번을 이미 밟았고 창은 열려 있었다.** 마감은 19:25이 아니라 **20:10**이었다.

**창이 열려 있음을 확정한 계산 —** `Recurrence.doneThisCycle`(`Recurrence.swift:103`) 세 절을 **전부** 대입:
①`due 08-12 20:10 > 지금(22:0x)` ✓ ②`lastDoneDue == due` ✓ ③`resurface 08-12 20:00`이 미래라 미게시 ✓ → **true.**
관찰 시각 22:0x는 **원래 회차 마감(08-11 20:10)이 1시간 50분 전에 지났고 아직 같은 날** = 확인표 ②의 조건 그대로.
**창의 바깥 한계는 08-12 20:00**(3절이 1절보다 10분 먼저 끊는다) — 08-07에 한 절만 보고 창을 넓게 잡아 ③을 놓친 교훈을 적용했다.

> **교차 확인이 문서를 이겼다.** 다음에도 실기기 상태를 전제로 판단할 때는 **문서만 믿지 말고 `inbox-*.md`의 op 로그를 읽는다.**
> 다른 기기 파일 넷에는 이 항목의 op가 0개, 파일 동기는 20:54(20:45 항목 포함)로 최신이었다 — 지연 가능성까지 닫고 시작했다.

## 2. ✅ F-1 ② 통과 — 2026-08-11 22:0x · iPhone 16 Pro

`항목 「G-7 회기 08-11」(9F950B73) · 앱 완전 종료 후 재실행` — **여섯 개를 전부 확인.**

| # | 본 것 |
|---|---|
| 1 | 「이번 회차 완료됨 ✓」 **그대로** |
| 2 | **[취소]** 그대로 |
| 3 | **`[이번 것 했어요]` 없음** (부재) |
| 4 | 회차 시각 **2026-08-12 20:10** — **하루만 전진** (08-13이면 이중 전진 = 실패였다) |
| 5 | 미리 알림 **2026-08-12 20:00** — **lead 10분 보존** |
| 6 | '살아있는 기억'에 **「완료」 칩** |

**왜 판정인가:** 옛 판정 `lastDone ≥ stepBack(due)` 에 넣으면 `08-11 19:41 < 08-11 20:10` → **미달 → 옛 코드는 `[이번 것 했어요]`.** 값이 갈린다.

### ★ 이 관찰이 08-06보다 값지다 — 08-06이 못 본 것을 여기서 다 채웠다

1. **lead 보존을 처음으로 깨끗하게 봤다.** 08-06엔 대상 항목 `가`의 lead가 **손편집으로 −23시간**이라
   미리 알림이 회차와 함께 제대로 따라오는지를 **볼 수 없었다.** 이번엔 손편집이 없어 **lead 10분이 그대로 한 회차 밀렸다.**
2. **`[이번 것 했어요]`의 부재를 처음 봤다** — 08-06 ②는 존재(배너·[취소])만 보고 부재를 남겼다.
3. **목록의 「완료」 칩도 처음 봤다** — 같은 반쪽.

**★ 2·3은 08-07에 「잃은 것」으로 확인표에 적어둔 바로 그 둘이다.** F-1 ③을 접을 때 같은 창에서 함께 닫혔던 것들 —
이번 재통과로 **회수됐다.** 확인표의 「잃은 것」 문단에 회수 표시를 달았고, 상세 기록은 **「✅ F-1 ② 재통과」 절**에 넣었다.

## 3. ★ 맥북 worklog의 빠진 것을 보탰다 — 오늘만 두 번째다

`2026-08-11-macbook.md`에 **19:41의 op 둘을 표에 보태고**, 왜 빠졌는지와 그 대가를 「보탬」 절로 남겼다.
맨 위 **재개 한 줄도 정정**했다(틀린 전제를 다음 세션이 또 읽지 않게).

**왜 빠졌나:** worklog를 **19:47:40에 커밋**했는데 **6분 전(19:41)의 조작이 반영되지 않았다.**
문서는 19:36에 본 화면까지만 담고 멈춰 있었다. **조작과 기록 사이에 시차가 있으면 다음 세션이 틀린 전제로 출발한다.**

**★ 같은 뿌리의 실수가 하루에 두 번 났다:**

| | 안 적힌 것 | 대가 |
|---|---|---|
| **아침** | 08-10 **설치 시각** | 폰에 깔린 게 최종 빌드인지 **커밋 날짜와 G-12로 역추적**해야 했다 |
| **저녁** | 19:41 **조작 두 개** | **창을 놓쳤다고 잘못 판단**하고 새 항목을 만들 뻔했다 |

### ★ 세 번째가 있었다 — 「그만두기」를 「정리」 뜻으로 쓴 것

맥북 worklog: *"`G-7 회기 08-11`은 F-1 ②가 끝나면 「**그만두기**」로 정리. **지우지 말 것**"* — **한 줄 안에서 모순**이다.
**「그만두기」는 삭제다.** 앱에서 그 셋은 이미 갈라져 있다 — 사양서 §5(`second-brain-v0-spec.md:378`)와
`docs/native/recurrence-design.md:83`이 **「그만두기 = 삭제 / 잠시 멈춤 = 꺼두기 / 이번 것 = 완료」** 를 명시하고,
구현도 그렇다(`DetailView.swift:974` — 「되풀이를 그만둘까요? **기록도 함께 삭제돼요**」).

**→ 문서의 구멍이 아니다. 작업 기록에서 말이 미끄러진 것이다.** 그래서 더 위험하다 —
정본은 맞는데 다음 세션이 읽는 문서가 틀렸고, **이 미끄러짐은 파괴적인 행동을 지시한다.**
①②는 시간을 잃는 실수였는데 **③은 항목을 지울 실수였다.**

→ **규칙으로 올렸다:** 「사용자 문구를 작업 기록에서 뜻대로 쓰지 말 것. 느슨하게 쓰려면 앱 문구가 아닌 말을 쓴다.」

### 관행을 `docs/` + `CLAUDE.md`로 옮겼다 (사용자 승인)

처음 메모리(`~/.claude/…/memory/`)에 적었는데 **`~/.claude`는 git 밖이라 기기를 넘어 안 따라온다** —
맥북에서 적은 것이 이 기기에 없었다. **집 맥미니 ↔ 회사 맥북을 오가는 프로젝트**라 관행은 저장소에 있어야 한다.

**위치는 「성능 측정 규칙」의 선례를 그대로 따랐다** — 그것도 `CLAUDE.md`에 규칙 블록 + `docs/lessons/`에 전말 링크다:

| 어디 | 무엇 |
|---|---|
| **`CLAUDE.md` 「기록 규칙」** (신설) | 여섯 줄 규칙. **매 세션 자동 로드**되므로 다음 세션이 반드시 읽는다 |
| **`docs/lessons/2026-08-11-record-at-the-moment.md`** (신설) | 전말 — 세 사례·실데이터·왜 그랬나·곁가지 |
| **`CLAUDE.md` 항시 규칙 #4** | worklog 규칙에 *"실기기 조작·설치는 세션 끝까지 미루지 말 것 → 「기록 규칙」"* 포인터 |
| **메모리 `install-time-record-practice.md`** | **지웠다** — CLAUDE.md가 매 세션 로드되니 사본은 중복이고, 두 곳이 갈리면 그게 다음 사고다 |
| **메모리 `MEMORY.md`·`recurrence-work-state.md`** | 「메모리에 두지 않는다」는 한 줄과 실데이터 교차 확인 경고로 대체 |

> **★ 남은 정리 대상 — 회사 맥북에서 할 것.** 맥북의 `~/.claude/…/memory/`에 08-11 세션이 만든 메모리가 있다:
> - **`install-time-record-practice.md`** (또는 비슷한 이름) — 설치 시각 관행. **지울 것.** 이제 `CLAUDE.md`에 있다.
> - **프로젝트 이름 ≠ 스킴 이름**을 못박은 메모리(§0의 *"→ 메모리에 못박음"*). 내용은 **살릴 값이 있다**
>   (`SecondBrain.xcodeproj` · 스킴 `SecondBrainApp-iOS`). 이 기기엔 없다 —
>   **`build-sim-target.md`에 한 줄로 합치거나, 빌드 명령이 문서에 있으면 거기로 옮길 것.**
>
> ⚠️ 맥북 메모리는 이 기기에서 볼 수 없어 **이름·개수를 확정하지 못했다.** 맥북 세션에서 `ls`로 확인하고 정리한다.

## 4. G-7 현황 — F-3 하나만 남았다

`date-roles-verify-checklist.md`의 G-7에 현황 표를 넣었다.

| 줄 | 상태 |
|---|---|
| **F-1 ①** | ✅ 통과 (08-11 18:45) |
| **F-1 ②** | ✅ **통과 (08-11 22:0x)** — 여섯 개 전부 |
| **F-2** | ✅ 통과 |
| **F-3** | ⏳ **남은 하나** — **08-12 20:15** · `131B923C` · **손대지 말 것** |
| **F-4** | ✅ 통과 (대조군까지) |
| **F-5** | ✅ 통과 (「5일 놓침」이 셈과 일치) |

→ **F-3이 닫히면 G-7이 끝난다. 기한 08-13보다 하루 빠르다.**

## 5. ✅ 결정 — `G-7 회기 08-11`은 **「꺼두기」로 멈춘다** (사용자 결정, 근거 승인)

**역할은 끝났다.** F-1 ③은 접혔고 ④는 08-07에 관찰됐으니 이 항목이 더 볼 것은 없다.

**★ 그런데 「그만두기」는 삭제다** (`DetailView.swift:974`): 대화상자가 **「되풀이를 그만둘까요? 기록도 함께 삭제돼요」**
/ [그만두기]이고, 삭제 로직은 tombstone → **'보관된 기억'**(복구 가능). 맥북 worklog는 *"「그만두기」로 정리. **지우지 말 것**"*
이라고 적었는데 **그 둘이 서로 어긋난다.** 「잠시 멈춤」은 **「꺼두기」**다(F-5에서 배너 **「되풀이 꺼둠 — 알림·되살아나기 멈춤」**으로 확인).

**★ 지금 정리해야 하는 실제 이유 — 내일 F-3과 시각이 겹친다:**

| 항목 | 미리 알림 | 마감 | 08-12 20:15의 모양 |
|---|---|---|---|
| `9F950B73` (G-7 회기) | **08-12 20:00** | 08-12 20:10 | 1절(`마감>now`) 실패 → **칩 없음 + `[이번 것 했어요]`** |
| `131B923C` (F-3, 「나」) | **08-12 20:00** | 08-12 23:55 | 3절(미게시) 실패 → **칩 없음 + `[이번 것 했어요]`** = **F-3의 통과 화면** |

**미리 알림이 똑같이 08-12 20:00이다.** 20:00에 **알림이 둘** 오고, 20:15엔 '지금 챙길 것'에
**모양이 구분 안 되는 두 줄이 나란히** 뜬다. F-5에서 이름이 비슷해 항목을 가려야 했던 것과 같은 종류의 위험이고,
하필 **G-7에 남은 마지막 판정선** 위다.

**→ 제안: 지금 `9F950B73`을 「꺼두기」로 멈춘다.** 게시가 멈춰 **내일 20:00에 뜨지도, 알림이 오지도 않는다.**
**F-3에는 영향이 없다** — 별개 항목이고 꺼두기는 그 항목만 건드린다.
**포기하는 것:** 이 항목으로 F-1 ④의 나머지 반쪽(회차가 열린 뒤 눌러 마감이 하루 전진하는 것)을 볼 기회.
**그러나 ④는 판정선이 아니라 그물이고 08-07에 이미 관찰됐다 — 잃는 것이 없다.**
(F-3 뒤로 미뤄 20:20에 눌러보는 길도 있지만, **20:00 알림과 나란한 줄은 그때 이미 F-3 위에 있다.** 권하지 않는다.)

**✅ 결정: 「꺼두기」.** 사용자가 근거를 확인하고 골랐다 — 삭제/멈춤 구분과 **내일 F-3과의 시각 충돌** 둘 다.

**⏳ 실기기 조작은 사용자가 한다 — 아래 한 줄을 누른 직후 채운다** (§3의 관행 = 조작 하나 → 기록 한 줄):

```
꺼두기: 2026-08-11 __:__ · `9F950B73` · 상세 상단에 「되풀이 꺼둠 — 알림·되살아나기 멈춤」 배너 확인: ____
```

**누른 뒤 확인할 것 하나:** 상세 상단에 **「되풀이 꺼둠 — 알림·되살아나기 멈춤」** 배너가 뜨는지.
(F-5에서 `90D927B2`가 같은 배너를 보여줬고, 마감·미리 알림이 **둘 다 지났는데도 게시되지 않았다** —
꺼두기가 실제로 멈추고 있다는 증거다. 여기서 배너를 보면 **내일 20:00에 안 뜬다**는 근거가 된다.)
⚠️ **[꺼두기] 스위치를 켠 뒤 하단 [저장]까지 눌러야 한다** — amber가 사라진 것을 보고 넘어간다.

## 6. 다음 할 일

> **할 일 목록과 볼 것 셋은 맨 위 「재개 한 줄」에 있다** — 여기 옮겨 적지 않는다(사본이 갈리는 것이 오늘 배운 것이다).
> 이 절은 **재개 한 줄이 안 담는 근거**만 남긴다: 저장값 · 판정 셈 · 걸림돌 둘.

**F-3의 근거 (2026-08-12 20:15 · `131B923C`)**
   저장값(08-06 이후 무변경 확인): 마감 **08-12 23:55** · 미리 알림 **08-12 20:00** · `lastDone 08-06 01:02` ·
   `lastDoneDue 08-12 23:55` · **`recur=weekly`(매주)**.
   옛 판정은 `lastDone ≥ 직전 회차` 인데 **매주라 직전 회차 = 08-05 23:55** → `08-06 01:02 ≥ 08-05 23:55` = **참**
   → 「완료」 칩을 달았을 자리. **값이 갈리므로 판정선이 선다.**
   알람은 **사용자가 건다.** ⚠️ **「나」에 손편집 금지.**

   > ⚠️ **여기서 한 번 틀렸다 — 이 줄에 처음 「직전 회차 08-11 23:55」로 적었다.** 「나」를 **매일**로 넘겨짚어
   > 하루를 뺀 값이다. 실데이터를 보니 **`recur=weekly`** 라 직전 회차는 **일주일 전(08-05 23:55)** 이다.
   > 결론(옛 판정 = 참 → 판정선이 선다)은 같지만 **셈이 틀렸다.** 「기록 규칙」 6의 자리다 —
   > **판정 근거를 적을 때 주기·필드를 실데이터에서 확인하고 적는다.**

   > ⚠️ **이름이 같은 항목이 하나 더 있다** — `70942905`(**매일** · `status=done`). 화면에 뜨는 이름이
   > 「나 - 정시 완료 시험」으로 **같다.** F-3 대상은 **`131B923C` = 매주 · 보관함에 없는 쪽**이다.
   > (`70942905`은 08-06 01:0x에 `status=done`이 찍혀 **보관된 기억**으로 갔고 그 뒤 op가 없다 —
   > '지금 챙길 것'에는 안 나온다. 그래도 상세를 열었을 때 가리는 기준을 미리 정해 둔다: **주기가 매주인 쪽.**)
   > F-5에서 `5ACEA309`와 `EAB3BB92`를 이름 끝으로 가려야 했던 것과 같은 종류다.

**그 뒤로 (재개 한 줄의 3번 다음)**
- **A(지난 마감 상한 N=7)** — 착수 전 `2026-08-07-macbook.md` §4-C의 「2026-08-08 추가」부터 읽을 것.
  clamp가 `resurfaceUpperBound`를 타므로 A가 clamp를 지난 마감에서도 깨운다 — **계획을 새로 짜야 한다.**

## 7. 사양서 반영

**불필요.** 오늘 코드 변경 0줄이고, 바뀐 것은 **검증 기록과 worklog**다. 동작·규칙이 달라진 것이 없다.
(F-3까지 닫혀 G-7이 끝나면 그때 §5 쪽에 「압축이 판정을 안 건드렸다」를 한 줄 남길지 물을 것.)
