# 작업 로그 — 2026-08-06 (목) 오후 · 회사 맥북 — F-1 ② 통과 + 규칙 1 조사 + Stage 5-C 체인

> **재개 한 줄:** 새벽 맥미니 세션(`80d80e8`, [`2026-08-05-macmini.md`](2026-08-05-macmini.md))을 **13:32 pull로 이어받았다.**
> **F-1 ②(구멍 1의 판정선)를 통과**시켜 기록했고, 그 과정에서 **규칙 1이 안 지켜진 경로**를 찾아냈으며(고치진 않음),
> **Stage 5-C 체인을 헤드리스로 끝냈다**(Core 244 통과 · iOS 빌드 OK · `c946a3d`).
> **오늘 남은 것:** **2026-08-06 19:00~21:00 — F-3(항목 `다`)**. 폰 알람을 따로 맞춰뒀다.
> **아무 때나:** F-5 셋(누르지 말 것). **2026-08-07:** F-1 ③ + ②의 잔여 둘 + `가`의 두 줄 확인.
> ⚠️ **폰엔 여전히 00:14 빌드.** Stage 5-C도 규칙 1 수정도 **안 올렸다** — F-1 ③④ 관찰 창 안이다. **설치는 F-1 ④ 이후.**

## 이 세션의 성격
새벽 맥미니(F절 착수) → 오후 회사 맥북. 08-03·08-04·08-05와 같은 **기기로 갈리는 짝 구조**다.
오늘 자 macmini 문서는 없다 — 새벽분은 `2026-08-05-macmini.md`에 들어가 있다.

## 1. 준비 (이어받기)

| | 결과 |
|---|---|
| `git pull` (13:33) | `4ce2491 → 80d80e8` fast-forward. HEAD = origin/main |
| `xcodegen generate` | OK |
| Core `swift test` | **231 통과 / 0 실패** |
| iOS 빌드 | **BUILD SUCCEEDED** (exit 0 직접 확인) |
| 기기 설치 | **안 함** — F-1 ③④ 관찰 창 안이다(아래 §5) |

## 2. F-1 ② 통과 — 구멍 1의 판정선 (`c946a3d`에 포함)

폰(iPhone 16 Pro, 00:14 빌드) **2026-08-06 13:28**, 항목 `가`. 마감 **13:00이 지난 뒤** 본 것 셋:
① 상세에 **"이번 회차 완료됨"이 그대로** — 회차 시각에 표시가 **안 뒤집혔다** ② 옆에 **[취소]도 그대로**
③ 회차 시각 **2026-08-07 13:00** — **정확히 1회차만 전진**.

**옛 코드였다면** 마감을 지나는 순간 `[이번 것 했어요]`가 돌아왔고, 한 번 더 누르면 마감이 **08-08로** 밀려
**08-07 회차가 목록·알림에서 조용히 사라졌을** 자리다. 값이 갈리므로(08-07 vs 08-08) 판정이 성립한다.

**⚠️ 확인된 것으로 안 적은 것 둘** — 사용자가 보고하지 않았다:
- 그 화면에 `[이번 것 했어요]`가 **없다**는 것(부재를 안 봤다. **존재**만 봤다)
- '살아있는 기억'에서 `가`에 **"완료" 칩**이 붙는지(목록을 안 봤다)
→ 2026-08-07(F-1 ③) 칸으로 옮겨 적었다. **둘 다 ②의 판정을 바꾸지 않는다.**

## 3. 계획이 두 군데서 어긋나 있었다 (실데이터 확인)

체크리스트를 그대로 믿지 않고 `inbox*.md`를 읽은 것이 값을 했다.

### (a) `나`가 매일이 아니라 **매주**였다 → 오늘 저녁 F-3 창이 없었다
`131B923C`는 만든 직후 편집에서 `"recur":"weekly"`로 들어갔다. 그래서 01:02의 정시 완료가 회차를
**일주일** 밀어 **마감 2026-08-12 23:55**가 됐다. 계획표의 "2026-08-06 20:00~23:55 — F-3"은 성립하지 않았다.
(같은 이름의 `70942905`는 되풀이로 바꾸기 **전에** 일반 완료가 눌려 `status=done` → 보관함. 대상 아님.)

**→ ②안 채택(사용자 결정): `다`를 새로 만든다. `나`는 지우지 않는다** — 매주 표본이 생긴 셈이고 08-12에 보면 덤이다.

### (b) `가`의 미리 알림 — 폰에서 08-07로 읽었으나 **저장값은 08-08**
손으로 병합하지 않고 **`MergeEngine`을 실데이터에 직접 돌려** 확인했다(`InboxStore.loadDirectory`):
`due = 2026-08-07T13:00` · **`resurface = 2026-08-08T12:00`** · `lastDoneDue = 2026-08-07T13:00`.
파일은 최신이고(14:38 이벤트까지 동기됨) 그 뒤 `가`에 대한 쓰기는 없다.

**→ 화면에서 두 줄을 섞어 읽은 것으로 본다**(회차 시각 바로 아래 줄). **2026-08-07에 두 줄을 같이 읽어 확정한다.**
④ 시각이 여기서 갈리지만(08-07 12:00 vs 08-08 12:00) **④는 판정선이 아니라 그물이라 급하지 않다.**

**②의 판정에는 영향이 없다** — 판정은 마감이 08-07이냐 08-08이냐에서 갈리고 그건 봤다. 다만 "lead 1시간 보존"은
이 관찰에서 확인된 것이 아니므로 그렇게 적지 않았다.

## 4. ★ 규칙 1 조사 — 구멍은 규칙 1에 없다 (고치지 않음, 미결로 기록)

새벽 00:57에 **미리 알림을 마감보다 뒤로**(08-07 12:00 > 08-06 13:00) 저장할 수 있었던 경위를 코드로 팠다.

**손편집 경로가 빠진 것도, 되풀이가 규칙 1을 안 타는 것도 아니다.** 손편집엔 두 겹이 걸려 있고
(`DetailView:374` DatePicker 상한 · `DetailView:682` 저장 최종 방어선), `violatesRule1`은 분류를 안 본다.

**실제로 일어난 것:** 완료 → 상세 닫고 다시 열기(F-1 ① 절차) → 화면이 **완료 후 값**을 스냅숏으로 듦 →
**[취소]** 로 모델은 08-06으로 돌아갔지만 **화면은 안 따라감** → 그 화면에서 미리 알림 시각만 손편집 → 저장.
**규칙 1은 제 일을 정확히 했다** — 검사한 쌍(마감 08-07 13:00 / 미리 알림 08-07 12:00)은 **적법**이다.
그런데 `EditDiff`가 **바뀐 필드만** 내보내 `set resurface=...` 한 줄이 나갔고, 그것이 **취소 뒤의 실제 마감**
위에 LWW로 얹혀 위반이 완성됐다.

> **진짜 결함 = "검사한 쌍 ≠ 저장되는 쌍".** 규칙 1은 **화면의 두 값**을 함께 보는 불변식인데 저장은
> **바뀐 한 값**만 낸다. 부분 저장 + 필드별 LWW에서 화면이 stale해지는 경로가 하나라도 있으면 위반이 저장된다.
> **되풀이 전용 문제가 아니다** — 규칙 1을 네 번째 경로에 더 넣는 걸로는 안 닫힌다.

**근거(코드):** `DetailView:27` `let item: ResolvedItem` = 스냅숏(주석도 "stale한 item"이라 부른다) ·
`DetailView:534` `Button("취소") { model.undoRecurComplete(item); cycleDoneLocal = false }` — **`cycleDoneLocal`만** 바꾼다.
⚠️ **행동 순서는 F-1 ① 절차에서 재구성한 것이고 재현 테스트는 안 만들었다.** 나머지는 코드에서 읽은 것.

**피해가 지금 `가`에서 일어나고 있다** — 되풀이 게이트는 미리 알림만 보므로 **회차가 자기 마감보다 늦게 열린다.**
규칙 1이 막으려던 목적("항목이 자기 마감을 지날 때까지 숨는 것")이 정확히 깨진 상태다.

**고칠 길 (승인, 지금 안 함):** **(a)** 저장 검사를 draft 쌍이 아니라 **병합될 최종 쌍**으로 — 저장을 지킨다.
**(b)** 완료·취소가 화면 draft도 같이 되돌린다 — 화면을 지킨다. **어느 하나만으로는 절반.**
**(c) 별건으로 따로 적었다** — `completionAdvance`가 규칙 1을 안 지킨다. 위반을 **새로 만들진 않지만 고쳐주지도 않고**,
되풀이는 매 회차 lead를 보존하므로 **한 번 뒤집힌 lead는 영구히 따라간다.**

**왜 지금 안 하나:** 폰에서 봐야 확인되는 변경인데 F절 관찰 창 안이다. **Stage 5-C와 같은 배(설치 미룸)에 태웠다.**

## 5. Stage 5-C — 체인 (헤드리스 완료, `c946a3d`)

**승인된 것 넷 중 셋 + 몫 재배분은 뺐다** (사용자 결정 — 지금 효과가 0인데 두 변경이 섞이면 원인이 안 갈린다).

| 넣은 것 | 어디 |
|---|---|
| 체인 = **시간 창 D(7) × 예산** | `NotificationPlanner.chainBundles` |
| 현재 회차·체인이 같은 몸통 | `makeBundle`로 추출(접힘·알려주기 규칙이 회차마다 똑같이) |
| 회차별 식별자 | `PlannedNotification.cycleKey` → `sb:<id>:<회차날짜>:<kind>` |
| **진짜 손실만 경고** | `droppedFirstRoundCycles` + `summary` 문구 분리 |
| **복귀 시 재등록** | `RootView`의 `scenePhase == .active`(탭 튐 원복 로직은 안 건드림) |

**왜 회차 수가 아니라 시간 창인가:** "예산 소진까지"로 두면 되풀이가 적을 때 체인이 **한 달 치**까지 뻗어
stale 알림 위험이 비례해 커지고, 라운드 수로 자르면 **매년 항목이 몇 년 치**를 건다. 시간 창이면 주기가
제 몫을 한다(매일 = D회차 / **매주·매년 = 1회차 = 옛 동작 그대로**). **첫 회차는 창 밖이어도 반드시 낸다** —
안 그러면 생일·기일이 0회차가 되어 후퇴한다.

**★ ③(계산만, 저장 안 함)을 회귀선으로 고정했다** — `testChainNeverAdvancesStoredCycle_missedSurvives`:
놓침 3이 쌓인 항목을 계획한 **뒤에** ① 저장된 마감 불변 ② `missed` 여전히 3 ③ `catchUpChanges` 여전히 nil.

**Core 231 → 244 통과 · iOS 빌드 OK.** 새 회귀선 = `NotificationChainTests`(10건).

### ★ 실측 — 구멍 1이 실데이터에서 닫혔다

```
알림 48건(반복 31/일반 17) · 대여 7 · 체인 축소 19회차(34건)
★ 라운드1 잘림(진짜 손실) = 0
```

| | 사용 | 몫 | |
|---|---|---|---|
| 반복 | **31** | 24 | + 대여 7 = 31, 다 씀 |
| 일반 | **17** | 24 | 여유 7 |
| 합 | **48** | 48 | 총량 정확히 소진 |

- **★ 「복약 : 놓치는 약 먹기!」(마감 2026-08-04 06:00·자동완성 없음)와 「약 먹기 시험 용 E-4」(마감·미리 알림
  둘 다 오늘 이미 지남)가 슬롯 0 = 영구 침묵이었다.** 완료를 누를 때까지 알림이 다시는 안 나갔다.
  **지금은 각각 3회차를 받는다.** 2026-08-04 실측의 *"되풀이 3개 중 2개가 슬롯 0"* 이 **실물로 재현됐고, 사라졌다.**
  **하필 "약을 3일 놓친 것"이 이 설계의 출발점인데, 앱이 정확히 그 자리에서 침묵하고 있었다.**
- **★ 잘림 경고 분리가 첫 실측에서 값을 했다.** 19회차(34건)가 잘렸지만 **전부 체인 뒷부분**이라 ⚠️가 안 켜졌다.
  **옛 회계였다면 "⚠️잘림 19회차"가 떠서** 상시 경고 = **신호가 죽었을 자리**다. 승인 때 정한 구분이 바로 값을 했다.
- **체인 길이 3~4회차** — D=7이 아니라 **예산이 먼저 걸렸다.** 승인 때 제시한 표의 예측과 같은 결.
- **매주(`나`)는 1회차(08-12) · 꺼둠 2개는 0건** — 설계대로. **E절이 지킨 약속이 체인 뒤에도 그대로다.**
- **일반 17/24(71%)** — 08-04의 79%보다 낮다(항목 정리됨). 몫 미결의 판단 근거는 아직 안 바뀌었다.

## 6. F-3 준비 — 항목 `다`

`가`·`나`와 이름이 겹치지 않게 **`다 - lead창 시험 F-3`**. 되풀이 · **매일** · 자동완성 **없음** ·
마감 **2026-08-05 21:00**(어제) · 미리 알림 **2026-08-05 19:00**(어제) → 그 자리에서 **정시(늦은) 완료**.

**실측(엔진으로 확인, 14:5x):** 마감 **2026-08-06 21:00** · 미리 알림 **2026-08-06 19:00** ·
`lastDone=2026-08-06T14:41` · `lastDoneDue=2026-08-06T21:00` · `doneThisCycle=true` · `isPublished=false`. **설계 그대로.**

**어제 날짜로 넣은 것이 핵심** — 오늘 날짜로 넣었으면 다음 창이 내일로 갔다.

**절차에 오늘의 조사를 반영했다:** 저장 → **상세 닫고 다시 열기** → 완료 → **또 닫고 다시 열어** 값 읽기.
저장 전에 누르면 완료가 **저장 전 스냅숏** 기준으로 전진한다(§4에서 본 그 문제).

## 7. 오늘·내일 볼 것

| 때 | 볼 것 |
|---|---|
| **2026-08-06 19:00~21:00** | **F-3** — 항목 `다`. ① '지금 챙길 것'에 있다 ② **"완료" 칩 없음**(판정선) ③ `[이번 것 했어요]` |
| 아무 때나 | **F-5 셋** — `복약 : 놓치는 약 먹기!`(2일 놓침) · `약 먹기 시험 용 E-4`("지남" amber) · `감기약 먹기 시험용…`(꺼둠 배너) |
| **2026-08-07** | **F-1 ③** + ②의 잔여 둘(버튼 부재·목록 칩) + **`가`의 두 줄**(회차 시각·미리 알림) |
| 2026-08-07 12:00 또는 **08-08 12:00** 이후 | **F-1 ④**(그물) — `가`의 미리 알림 값이 정한다 |

**F-3 판정선:** 옛 판정 `lastDone(14:41) ≥ 직전 회차(08-05 21:00)` = **참** → "완료".
새 판정은 3절(`!isPublished`)에서 거짓. **값이 갈린다.** 안 고쳤다면 **안 먹은 약을 먹었다고** 표시됐을 자리(구멍 2).

⚠️ **19:00 전에는 아무것도 판정되지 않는다** — 그때는 새 코드도 "이번 회차 완료됨"이 정상이다.
⚠️ **누르지 말 것 · 손편집 금지.** F-5 셋도 누르면 `lastDoneDue`가 생겨 새 판정으로 이주해 표본이 사라진다.
⚠️ **관찰 알림은 폰 알람으로.** Claude 세션 안에 건 알림(18:58)은 **세션이 닫히면 안 울린다.**
창이 두 시간뿐이고 놓치면 다음 창은 **2026-08-07 19:00**이라 하루가 밀린다. → 사용자가 폰 알람을 따로 맞춰뒀다.

## 8. 설치를 미루는 판단 (재확인)

**F-1 ③④는 "00:14 빌드 하나를 연속으로 관찰한 타임라인"이다.** 중간에 빌드를 갈면 ③④에서 무엇이 보이든
*"그 빌드가 아니라 새 빌드가 낸 값 아니냐"* 는 반론이 열린다. **E-6에서 정확히 이 이유로 08-05 밤 설치를
미뤘고, 그 판단이 08-06 판정을 살렸다.**

**Stage 5-C에 복귀 시 재등록이 들어갔으니 더 그렇다** — `reload()` 빈도를 늘리는 변경이다.
(가·나·다는 자동완성 **없음**이라 `catchUpChanges`가 no-op이므로 데이터가 움직이진 않는다.
데이터도 앱 샌드박스가 아니라 iCloud 폴더에 있어 덮어쓰기 설치로는 안 사라진다 — 그래도 미룬다.)

**결정적이었던 것: 미루는 비용이 0이다.** Stage 5-C를 설계·구현·테스트·빌드까지 전부 헤드리스로 갔다.
**설치는 F-1 ④ 이후.**

## 다음 할 일

1. **F-3**(2026-08-06 19:00~21:00) → **F-5**(아무 때나) → **F-1 ③ + `가` 두 줄**(08-07) → **F-1 ④**(그물)
2. **F절이 닫히면 설치** — Stage 5-C + 규칙 1 (a)(b) 수정을 함께 올린다
3. **규칙 1 (a)+(b)** 착수 (승인됨, 설치 시점에 맞춰) · **(c)** 는 별건으로 판단 대기
4. **Stage 5-D**(문구 두 톤 — 곧/지금/오늘) · **Stage 5-E**(알림 실기기 확인 몰아서)
5. **몫 재배분 미결** — 일반이 24에 닿으면 그때 정한다(지금 17/24)
6. **사양서 반영** — (b) `lastDoneDue` 반영이 아직 안 됐다(2026-08-05 결정: (b) 방향이 정해질 때 함께).
   F절이 닫히면 **"사양서에 반영할까요?"** 를 물을 것
