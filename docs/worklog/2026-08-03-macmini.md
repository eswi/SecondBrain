# 작업 로그 — 2026-08-03 (집 맥미니) — 반복(양력) Stage 0~4 + 게이트 시각화 + 앵커 전환

> **재개 한 줄:** 되풀이(반복, 양력)를 **Stage 0~4 + #3 게이트 시각화 + 규칙1 시각화 + 앵커 전환(마감=회차)** 까지
> **헤드리스로 다 만들고 실기기 설치까지 했다**(폰에 01:44 빌드). **실기기 확인은 안 했다** — 내일 회사 맥북에서
> `git pull` 후, **폰으로 `recurrence-verify-checklist.md` C-2·C-3(핵심)부터** 확인. 통과하면 **Stage 5(알림 전환)**.
> Core 167 테스트 0 실패. 정본 = `docs/native/recurrence-design.md`.

## 한 일 (커밋 `e6182e2`..`feb6a4f`, 전부 푸시됨)

되풀이 = 8번째 분류. 순서대로:
- **Stage 0** — 기존 완료 라우팅 고정(그물). "완료 → 살아있는 목록서 빠지고 보관함으로"를 테스트로.
- **Stage 1** — 분류 추가(비파괴, 수동 지정). ClassSpec + 앰버 시각메타.
- **Stage 2** — 반복 세부 필드(주기·자동완성·꺼두기) + 상세 "반복 설정" 카드.
- **Stage 3** — 완료 분기(★ 파괴적). 되풀이 완료 = lastDone만(안 사라짐)/그 외 = status=done. 오늘 완료·취소 토글.
- **#3 게이트 시각화** — `isPublished` `rd <= now`. 지난 시각 계속 보임, date-only 자정부터(불변).
- **Stage 4** — 회차 계산·완료 전진·catch-up·"N일 놓침" 표시.
- **규칙 1 시각화** — 시각 있으면 `미리 알림 ≤ 마감`(정각 허용), date-only는 하루 전(불변).
- **앵커 전환(★ 방향 정정)** — 처음엔 "미리 알림=앵커, 마감 안 씀"으로 잡았으나 **lead-time을 담을 칸이 없어**
  (8시 약·7시 알림) 뒤집음. **마감=회차 앵커, 미리 알림=게시 시작(lead).** 완료·catch-up이 둘 다 같은 간격
  전진(lead 보존). 두 칸 역할이 일반 항목과 동일. 정본 §3-A에 못박음.

## 지금 상태
- **헤드리스: Core 167 테스트 0 실패, iOS 빌드 OK.** 실기기(폰)에 01:44 빌드 설치됨.
- **실기기 미확인.** 확인 목록 = `docs/native/recurrence-verify-checklist.md` (A·B·C절). **C절이 최신 모델(앵커=마감).**
- **옛 되풀이 3개**(E036A094·90D927B2·01781308, 마감에 시각·미리 알림 없음)는 **새 모델과 그대로 맞음**
  (마감=앵커). 완료 시 마감만 전진, 미리 알림 안 들어감(테스트로 고정). `01781308`은 자정(00:10) 경계 시험용.

## 다음 할 일 (맥북)

> **정정(2026-08-05 추가):** *"내일 맥북"* 이 아니라 **같은 날 08-03 저녁 맥북**이었다
> (`ddd5e1d`·`8fec292`·`9dadbd5`·`58e3144` = 08-03 18:33~19:55) → [`2026-08-03-macbook.md`](2026-08-03-macbook.md).
> 이 세션이 **08-03 새벽(00:22~01:35)** 이라 "내일"로 적힌 것이다. **worklog의 상대 날짜는 절대 날짜로 적을 것.**

1. `git pull`. 필요시 `native`에서 `xcodegen generate` + iOS 빌드로 환경 확인.
2. **폰으로 실기기 확인** — `recurrence-verify-checklist.md` **C-2**(옛 3개 마감 시각부터 보이고 완료 시 다음날 전진)·
   **C-3**(8시 마감+7시 미리 알림 → 7시부터 보임, 완료 시 둘 다 다음날, lead 보존)부터. 통과분은 파일에 닫기.
   - 재빌드·재설치가 필요하면 맥북 + 폰 연결. 화면·회차 확인만이면 이미 설치된 앱으로 가능.
3. 통과하면 **Stage 5 — 알림 전환**(iOS 반복 트리거, 예산 분리, **+ lead-time 구멍**: 7시 미리 알림+8시 마감 약이
   지금 7시만 울림 → 마감·미리 알림 둘 다 울리게. §3-A·§9 기록).

## 미결 (정본에 기록됨)
- **Stage 5 lead-time 알림 구멍** — 위. 알림이 `publishDay` 한 지점만 발화.
- **음력** — 순서표의 다음 조각(§7).
- **하루 N회** — 다중 시각 입력 + 알림 다중 트리거와 함께(후속).
- **생일 lead-time** — 이제 앵커 전환으로 네이티브 지원 가능(마감=생일, 미리 알림=한 달 전).

## 안 한 것
- 실기기 확인 0건(설치만). 시험은 내일.
