# 작업 로그 — 2026-07-24 (mac mini · 07-23 저녁~심야 이어서)

> **⚠️ 재개 한 줄(내일 회사 맥북):** **Stage 4(주차위치 분류) 방식 재검토 필요 — 현재 구현이 의도와 다름, 회사에서 수정.**
> `git pull` → `cd native && xcodegen generate` → 빌드. 그다음 Stage 4 재설계부터.
>
> **Stage 4 왜 고치나 (내일 위대표가 더 설명):**
> - **"분류를 주차위치로 했는데 위치·메모를 또 2개나 입력"하는 UX가 별로.** 분류 지정 = 이미 의도 표현인데
>   그 위에 수동 입력을 겹으로 요구하는 흐름이 어색. (원문·사진·EXIF가 이미 있는데 왜 또?)
> - 방향 자체(공통 그릇 + 분류별 필드)는 맞지만 **주차 UX를 어떻게 태울지**가 틀림. 내일 다시 논의해 재설계.
> - **건드리지 말 것:** Stage 1~3(그릇·사진·EXIF)은 실기기 검증 통과·유지. 고칠 건 **Stage 4 주차 흐름/필드 UX**뿐.

## 오늘 한 일 — 사진 첨부 + 주차위치, 단계별 (커밋 순)

방향 전환: 분류 전체 재설계는 미루고 **공통 데이터 구조(그릇) 먼저 → 분류 하나(주차) 얹어 검증**. 근거·논의는
`docs/native/classification-redesign-open-questions.md`(결정 D1~D4 / 미결 O1~O3), 설계 정본 `docs/native/photo-capture-design.md`.

1. **분류 재설계 논의 반영** (`55793ad`) — 결정/미결 구분. D1 기본층(고정 6)/유연층(자유), D2 §2 용어 대조 일치,
   **D3 반복=분류 아닌 속성**, D4 그릇 먼저→주차 첫 검증. §2·§3 무변경.
2. **Stage 1 — 그릇(fields.v1 편집 블록)** (`7e5fbf1`) — `set k=v`가 공백 못 담던 문제(관통점 3) 해결.
   값에 공백 있으면 `@ hlc|id|edit` + `  fields.v1: <JSON>`, 없으면 기존 set(하위호환). 파서가 JSON을
   **개별 필드로 펼쳐** 엔진에 넘김 → **per-field LWW 그대로, MergeEngine·merge-design 무변경**(B2, 코드 추적 확인).
   테스트 5개 추가(왕복·question·하위호환·per-field·두 기기 동시편집). `swift test` 통과.
3. **question 배선** (`f80724c`) — 그릇 위에서 자동분류 question(공백 있음) 실제 저장 + DetailView "확인이 필요해요" 표시.
   → **실기기 검증 통과**(공백 질문 저장·재실행 유지). 그릇이 실데이터로 증명됨.
4. **Stage 2 — 사진 인프라** (`ae81a50`) — `PhotoStore`(음성 `AudioStore` 미러 + 리사이즈 2048/JPEG 0.7),
   카메라 촬영만(`CameraCapture`, 앨범 없음), create `photo:` 포인터(성역·불변), 상세 성역 카드에 사진.
   흐름: **본문 먼저 → 사진**(본문 없으면 카메라 disable + 안내). "원문 없는 기억" 3중 차단.
   `EventWriter` create 화이트리스트에 `photo` 1줄. `NSCameraUsageDescription`.
5. **Stage 2 수정** (`a067682`) — 다시 찍기 썸네일 갱신(촬영마다 고유 임시 경로) + 카메라 시스템 버튼 한글화
   (project.yml `developmentLanguage: ko` → CFBundleDevelopmentRegion=ko). → **실기기 검증 통과.**
6. **Stage 3 — EXIF GPS** (`8262265`) — 촬영 시 위치를 **JPEG EXIF에만** 기록(그릇엔 안 박음 → iCloud 누출 방지),
   볼 때 EXIF에서 읽어 상세에 작은 지도+핀. 권한 거부·실내면 GPS 없이(graceful). `LocationProvider`,
   `PhotoStore` jpegData→ImageIO 전환(GPS 태그+방향 정규화), `NSLocationWhenInUseUsageDescription`.
7. **Stage 4 — 주차위치 분류(유연층)** (`923d4a1`) — **⚠️ 방식 재검토 예정(위 재개 한 줄).**
   `FlexType.swift`: `FlexTypeCatalog`(parking 하드코딩) · `ClassSchema/FieldSpec` · `ClassRegistry`
   (기본층 `TypeCatalog` + 유연층 **통합 조회** — §2 무변경, 여기서 합침). DetailView: 분류 메뉴에 "주차 위치",
   parking일 때만 위치·메모 편집 카드(그릇 fields.v1). **개념(공통 그릇+분류별 필드)은 맞으나 주차 UX가 의도와 다름.**

## 상태
- main = `923d4a1`. **push 완료**(로컬=원격). working tree clean. 브랜치/worktree = main 단일(정리 불필요).
- 폰(iPhone 16 Pro): Stage 1~4 전부 포함한 최신 빌드 설치됨. Stage 1~3 실기기 검증 통과, Stage 4는 재검토 대상.
- 내일 이어받을 기기 = **회사 맥북**(→ `git pull` 먼저).

## 실기기 검증 결과(오늘)
- **Stage 1(그릇·question)**: ✅ 통과. **Stage 2(사진)**: ✅ 통과. **Stage 3(EXIF GPS)**: 확인 진행됨(다음 단계로 넘어감).
- **Stage 4(주차)**: 동작은 하나 **UX가 의도와 다름 → 재설계**(헤드리스 아님, 위대표 판단).

## 보호 자산 (이번 세션 무변경 확인)
- **§2 `TypeCatalog`(Theme.swift) / §3 프롬프트(`classify.py`·`ClassifyPrompt.swift`) / `MergeEngine`·`merge-design.md` 규칙 — 쓰기 0.**
- Core 변경은 **직렬화 층만**(`EventLog`/`EventWriter`: fields.v1 블록 파싱·출력 + photo 화이트리스트). 병합 알고리즘 불변.
- 유연층(주차)은 별도 레지스트리(`FlexType.swift`)로 §2와 완전 분리. 자동분류는 parking 안 찍음(`validTypes`에 없음).

## 사양서 반영 확인 (규칙 #2)
- **보류.** 사진·EXIF·주차는 **Stage 4 재설계가 끝난 뒤** 사양서/`memory-philosophy`에 반영하는 게 맞다(지금 반영하면 곧 바뀜).
  설계 근거는 `photo-capture-design.md`에 이미 정본으로 있음. 그릇(fields.v1)도 같은 문서 §3에 근거.
- 재설계 확정 후 "사양서에 반영할까요?" 다시 여쭐 것.

## 다음 할 일 (우선순위)
1. **Stage 4 주차 UX 재설계** (내일 회사, 최우선) — "분류=주차 지정만으로 필요한 게 채워지는" 흐름으로. 이중 입력 제거 방향.
2. 재설계 후 photo/parking 사양서 반영 여부 판단(규칙 #2).
3. (남은 것) 항목별 iCloud 옵트인(사진 동기화) · 여러 장 사진 · 반복 규칙 설계(O3) 등.
