# 2026-08-12 (맥미니) — 상세 화면 2차 압축 **1단계 완료** (성역 접기 + 분류 나란히)

> **재개 한 줄:** **① 오늘 20:15 F-3** — 「나 - 정시 완료 시험」(`131B923C`), **손대지 말 것**, 알람은 사용자가 건다.
> 통과하면 **G-7이 끝난다**(기한 8/13보다 하루 빠르다). **② F-3 뒤에 1단계를 폰에 설치**하고 아래 §5 목록을 본다.
> **③ 그다음 2단계 「수집 원본 자료」**(사진 · 펼치면 지도).
> ⚠️ **F-3 전에는 폰에 설치하지 말 것** — 지금 폰엔 08-10 빌드가 깔려 있고 F-3은 그것을 본다.
> 갈아버리면 「그 빌드가 아니라 새 빌드 아니냐」는 반론에 열린다(08-07에 겪은 그것).
> (08-11 밤 세션이 이어진 것 — 앞의 전말은 `2026-08-11-macmini.md`)

---

## 1. 1단계 = 네 커밋. 단계마다 멈추고 스크린샷으로 확인했다

| | 무엇 | 커밋 |
|---|---|---|
| **1-A** | 분류 카드를 **세로 배치**(아이콘 위 · 이름 아래) | `e01b041` |
| **1-B** | 성역 카드 **접힘/펼침**(접힘 = 세 줄) | `4de4f19` |
| **1-B 보정** | 표적 — 접힘은 카드 전체, 펼침은 표적만 44pt | `2d3dc67` |
| **1-C** | **성역 2/3 + 분류 1/3 나란히**(상단 정렬) | `7835bc5` |

**끊어서 한 것이 값을 했다** — 1-A에서 잘림이 없는 것이 세로 배치 때문임이 확실했고, 1-B에서 표적 결함이 **단독으로** 드러났다.
한 번에 했으면 "안 눌리는 게 표적 탓인지 폭 탓인지" 갈리지 않았다.

**회귀:** 매 단계 `swift test` **322 통과** · **iOS 빌드 OK**. Core는 한 줄도 안 건드렸다(App 층만).

## 2. ★ 계측이 계산을 세 번 고쳤다

| | 계산 | **실측** | 결과 |
|---|---|---|---|
| 카드 안쪽 폭 | 342pt | **342** (`metaHeader` = 342×14) | ✅ 폭 사슬 확인 |
| 분류 메뉴 높이 | 43pt | **43** | ✅ |
| 성역 겉폭 : 분류 겉폭 | 238.7 : 119.3 | **~235.7 : ~118** | ✅ |
| 사이 간격 | 12 | **~13** | ✅ |
| **분류 안쪽** | 91.3 | **~90** | ✅ |
| **최악값 「주차 위치 ⌄」** | 77.2 | **78.6** | ✅ **여유 11.4pt · 잘림 없음** |
| 「아이디어 ⌄」 | 72.9 | **74.7** | ✅ |
| 성역 접힘 카드 | 96.8 | **~92** | ⚠️ 아래 |
| 성역 펼침 카드 | 139.9 | **~139.7** | ✅ |
| 분류 접힘 카드 | 95.2 | **~97** | ✅ |

**★ 8-A에서 「342가 아니라 370에서 나눠야 한다」고 고친 것이 맞았다.** 처음 가정(분류 114pt)대로 갔으면 24pt를 잘못 잡았다.

**★ 높이 역전도 확인됐다** — 접힘에서 **분류(97) > 성역(92)**. 예측대로다. 상단 정렬이라 어느 쪽이 커도 안 깨진다.

### 틀린 계산 하나 — 폭 표를 높이에 썼다
성역 접힘을 **96.8**로 계산했는데 실제는 **~92**다. 원인: 머리 줄 높이를 **17pt**로 잡았는데
그 17은 계측표의 **`photo @12pt = 17`, 즉 폭**이다. 12pt 심볼의 실제 높이는 12pt대라 머리 줄 높이는
**sectionLabel의 줄 높이 14.2**가 정한다(접근성 좌표가 **14**로 확인). 96.8 − 17 + 14.2 = **94.0** ≈ 실측 92.

> **교훈: 폭 표의 숫자를 높이에 쓰지 말 것.** 08-10의 「간격을 줄마다 안 셌다」와 같은 종류다 —
> **재는 법이 아니라 가져다 쓰는 법**이 틀렸다. 계측값에 **폭/높이 딱지**를 붙여 적을 것.

## 3. 압축 효과 — 사진 없는 항목에서 **~129pt** 줄었다

| | 전 | 후 |
|---|---|---|
| 성역 | 139.9 | 접힘 **91.7** |
| 간격 | 14 | — (나란히) |
| 분류 | 72.3 (가로) | **97** (세로) |
| **두 자리 합** | **226.2pt** | **97pt** |

→ **129.2pt 절약.** G-8의 예산(원문 아래 ~590pt / 가용 694pt) 대비 큰 몫이다.
사진 있는 항목은 접기만으로 703 → 97로 보이지만, 그건 **감춘 것**이고 **2단계가 제대로 떼어낸다.**

## 4. ★ 표적을 두 번 재서 두 번 고쳤다 — 규칙 하나가 두 답을 줬다

접근성 좌표로 토글이 **342 × 14pt**였다. 애플 권장 최소 **44pt**에 한참 못 미치고 시각 줄과 붙어 있어
**스치면 옆이 눌린다**(탭 튐 버그 2026-07-21이 정확히 그 문제).

| 상태 | 표적 | 높이 대가 | 근거 |
|---|---|---|---|
| **접힘** | 카드 전체 **~92pt** | **0pt** | 접힘 카드 안에는 **누를 것이 하나도 없다**(음성 재생·지도는 펼침에만) |
| **펼침** | **342 × 44** | **0pt** | 여백 15 → 표적 잡기 → 음수 15로 되돌림. 덮는 위 15는 카드 여백, 아래 15는 **그냥 글자**(`metaRow`) |

> **★ 규칙: 표적 크기는 「일관성」이 아니라 「그 자리에 무엇이 들어 있나」가 정한다.**
> 처음엔 *"두 상태에서 표적이 같아야 예측 가능하다"* 는 일관성을 이유로 머리 줄만 잡았다. **재 보기 전엔 안 보였다.**
> 같은 규칙이 접힘·펼침 두 곳에서 **각각 0pt 대가의 답**을 줬다. 경위까지 코드 주석에 남겼다.

## 5. 📱 F-3 뒤 설치하고 폰에서 볼 것

**배치**
1. 접힘에서 성역이 **세 줄**인가 (제목+종류 아이콘+`⌄` / 시각 / 기기)
2. 시각·기기가 **@16 그대로**인가 — 접었다 펴도 **크기가 안 바뀌는지**
3. 종류 아이콘 개수 — **음성 있는 항목 / 사진 있는 항목**에서 각각 (시뮬레이터로 못 봄)
4. **「주차 위치」가 좁은 칸에서 안 잘리나** — 시뮬레이터 여유 11.4pt
5. 아이콘 접힘 **17** ↔ 펼침 **44**로 바뀌나
6. **사진 있는 항목**에서 분류 카드가 위에 붙어 있나(늘어나지 않나) — 시뮬레이터로 못 봄
7. **표적** — 접힘: 카드 아무 데나 눌러 펴지나 / 펼침: 머리 줄이 **쉽게** 눌리나 / 옆 줄이 오조작되지 않나
8. ⚠️ **Dynamic Type XXXL** — 아래 §6의 유일한 남은 위험

**★ 판정 입력 회귀 (계측이 짚은 셋 = 주석 ①②③)**
9. **성역:** 원문을 고쳐 저장해도 **수집 시각·기기가 안 바뀌나** · 「이 값은 어떤 편집으로도 바뀌지 않아요」 그대로
10. **분류:** 분류를 바꾸고 **저장 없이 닫으면 원래 분류로 돌아오나** (08-08 닫힘 시험)
11. **배너 다섯 무변경** — 되풀이 항목에서 꺼둠·놓침 배너 그대로. **`9F950B73`이 꺼둠이라 대조군이 된다**

**목적 지표**
12. **G-8 회귀** — 원문 아래가 스크롤 없이 한 화면에 들어오나

## 6. ⚠️ 남은 위험 하나 — Dynamic Type XXXL

「주차 위치 ⌄」 실측 **78.6pt / 칸 90pt**. XXXL(이름 17→20pt)로 올리면 **~92.5pt**로 **2.5pt 넘친다.**
**지렛대는 정해져 있다 — chevron을 뗀다**(63.2 → XXXL에서 74.4pt로 넉넉). 폰트를 줄이는 것보다 그쪽이 낫다(이름은 읽어야 하는 값).
코드 주석에 적어 뒀다. **폰에서 XXXL을 켜 보고 판단할 것.**

## 7. 곁가지 — 시뮬레이터 자동화

`idb`가 없어 **AppleScript로 시뮬레이터를 조작**했다. 접근성 요소가 읽혀서(요소 118개, 위치·크기까지)
**폭·표적을 실측**할 수 있었다 — 이번 계측의 절반이 거기서 나왔다.

⚠️ **불안정하다.** ⓐ **작업 중 시뮬레이터 창이 움직이면 좌표가 통째로 어긋난다**(그래서 한동안 헛클릭했다 —
기기 화면 원점은 루트 요소가 `402×874`로 알려주는 그 위치다). ⓑ 접근성 트리 읽기가 같은 화면에서
성공/실패를 반복한다. → **좌표를 쓰기 전에 매번 원점을 다시 읽을 것.** 그래도 막히면 사용자에게 탭 한 번 부탁.

**샘플 데이터:** 「주차 위치」를 넣었다(DEBUG·시뮬레이터 전용, 릴리스·실기기엔 안 들어간다) — 가장 긴 분류 이름이 없으면
정작 넘칠 후보를 못 본다. **여전히 못 보는 것:** 「되풀이」 · 음성/사진 **파일**이 있는 항목(포인터만으론 안내 줄만 그려진다).

## 8. 사양서 반영

**물어볼 것 하나 있다** — 상세 화면의 **성역·분류 배치가 바뀌었다**(위아래 → 좌우 2:1, 성역은 기본 접힘).
사양서가 상세 화면 구성을 적고 있다면 반영 대상이다. **1단계가 폰에서 확인된 뒤** 함께 묻는 편이 낫다고 본다.
(꺼두기 제목 줄 이동은 별건으로 남아 있다 — 그건 사양서 §5·`recurrence-design.md`가 배치를 **명시**하므로 착수 전 질문 필수.)
