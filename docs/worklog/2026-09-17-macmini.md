# 2026-09-17 (집 맥미니) — **「쓰다 만 기억」 사진이 안 살아나던 것을 `draft.log`로 갈라 고쳤다**

> ## 재개 한 줄 — **2026-09-17 02:1x (이 줄이 최신이다)**
>
> **폰 = `ebee442`(02:11 설치 · 맥미니) — 해시태그.** ⏸ **판정 대기 여섯**(`install-log.md` 맨 위 줄). 결정·구현 = §6 · 정본 `docs/native/hashtag-design.md`.
> 판정 뒤: **문구 둘**(설계 §4) → **「사양서에 반영할까요?」**(§6) → 태그 눌러 모아 보기는 **사용자가 별도 UX 제시.**
> ✅ 오늘 닫힌 것: 「쓰다 만 기억」 판정 전부(§5) · `ebc641c` 넷 통과(01:5x) — **폰 판정 대기는 해시태그 여섯만.**
>
> *(옛 재개 한 줄 · 01:4x)* 폰 = `bdc3d21` · ⓐⓑ 통과 · `discard` 줄 확인 · 남은 판정 = `ebc641c` 넷 → 그 뒤 해시태그 논의.
> ⚠️ 커밋 5(이 줄 포함) · **푸시 안 함** — 사용자 지시 뒤.

---

## 0. 세션 시작

`git pull` 8커밋(`2c90750..14b653d` · 맥북 09-14~15). `xcodegen generate` · 시험 **440** 통과 · CLAUDE.md의 434를 440으로(`61bec82`).
시뮬 둘 그대로(26.2 `734ABE20-…` · 26.5 `34A5033B-…` 부팅 중) · 폰 CoreDevice `95CF732E-…` 페어링됨 · 이 셸 `ls`는 `-Fsx` 별칭.

## 1. 무엇을 했나 — 한 줄씩

| 시각 | 무엇 | 커밋 |
|---|---|---|
| 01:2x | **사용자 폰 판정**(`ba4faa2`): ⓐⓑ 통과 · **ⓒ 사진이 안 살아난다** · 별개: 사진 찍으려다 **앱이 죽음**(한 번 · 재현 안 됨) | — |
| 01:29~01:32 | **`draft.log`를 폰에서 가져왔다**(`devicectl copy from` · 3분 걸렸다) → `install-log.md` `ba4faa2` 줄에 판정·읽은 것 | `bdc3d21`에 포함 |
| 01:3x | **고침** — `discardTemps`가 초안 폴더 밖만 지운다 · `folderURL`(만들지 않는다) · 시험 **441** · iOS·맥 `EXIT=0` | `bdc3d21` |
| **01:37:05 / 01:37:56** | **폰 빌드·설치**(낡은 `.app` 삭제 · `EXIT=0` · `error: 0` · `App installed`) → `install-log.md` 첫 줄 | `ddb781e` |
| 01:3x | **크래시 로그 321개**를 `systemCrashLogs`로 가져와 읽었다(§3) | — |
| 01:4x | 설계 §5 · HANDOFF · 이 worklog | `26c4d3e` |
| 01:5x | **사용자 판정 `bdc3d21` ⓐⓑ 통과** · `draft.log` 다시 가져옴(01:47) → **`discard … kept=1 closed=false` = 강제 종료 때 `onDisappear`** 확인 · 설계 §5-1 · 「추정」 표시 셋을 「확인됨」으로 | (이 커밋) |

## 2. 무엇이 갈랐나 — `draft.log` (원본은 설계 `capture-draft-design.md` §5 · 여기는 요약)

`move ok`(01:26:46) → `persist photos=[1]` → **`restore photos=1 found=0`**(01:26:59). **적기·옮기기는 됐고 파일만 사라졌다.**
JSON이 남았으니 `closeDraft`는 안 돌았고, **파일만 지우는 길은 `discardTemps` 하나** — 옮겨진 뒤의 URL을 임시 파일로 알고 지웠다.
**부른 자리(`onDisappear` · 강제 종료의 화면 해체)는 추정** → `discard` 로그 한 줄을 넣어 다음 재현이 가르게 했다.

★ **계측이 있어서 한 번에 갈렸다** — 09-15 맥북이 `draft.log`를 넣어 두지 않았으면 오늘은 「사진이 안 산다」에서 더 못 갔다
(`CLAUDE.md` 빌드 ⓒ: *"적게 만드는 것이 절반이다"*).

## 3. 새로 얻은 수단 — 폰 크래시 로그를 맥에서 읽는다

```sh
xcrun devicectl device copy from --device 95CF732E-D847-5594-A346-EC354A1D8B6C \
  --domain-type systemCrashLogs --source . --destination <로컬>      # 321개 · `Retired/` 포함 · 몇 분 걸린다
```
`.ips`는 **첫 줄이 헤더 JSON · 둘째 줄부터 본문 JSON**이다(둘로 갈라 읽는다 · `jetsam.py`를 스크래치에 두고 썼다).
- **09-17에 `SecondBrain-*.ips`는 없다.** 사진 찍으려다 죽은 것은 **못 가름**(설계 §5 끝 줄).
- ⚠️ **09-14 19:21:14 크래시가 있었다 — 어디에도 안 적혀 있다.** `SIGABRT` · `_swift_reportExclusivityConflict` ←
  `SwiftUI SheetBridge.preferencesDidChange` — **시트 상태를 두 곳이 동시에 만졌다**(배타 접근 위반). `ebc641c`(19:34 설치) **직전**이라
  회전 버그 재현 중의 죽음일 가능성이 있다(**추정**). 그 뒤 `SecondBrain-*.ips`는 없다.
- 09-13 12:44 `SIGKILL 0x8BADF00D`(*"Failed to terminate gracefully after 5.0s"*) — 「깨웠으면 끝낸다」 작업 중의 것으로 보인다(**추정**).

## 4. 다음

1. ~~폰 판정 둘 → `discard` 줄 확인~~ ✅ 01:5x 닫혔다(§5).
2. ~~`ebc641c`의 넷~~ ✅ 01:5x 넷 다 통과(사용자 · 탭바 = 09-13에 새로 그린 것 그대로).
3. ~~해시태그 논의~~ → ✅ 결정 여섯 받아 구현 `ebee442`(§6) · ⏸ 폰 판정 여섯.

## 5. 판정 · `discard` 줄 (원본 = 설계 `capture-draft-design.md` §5-1 · 여기는 요약)

사용자: *"ⓐ ⓑ 둘 다 통과했어."* `draft.log`를 다시 가져오니(01:47 · 이번엔 20초) 재현 다섯 번이 전부 같은 꼴:
`persist photos=[1]` → **`discard … temps=0 kept=1 closed=false`**(4초 뒤 · 강제 종료 순간) → **`restore … found=1`**(6초 뒤).
**`closed=false`가 「`closeDraft` 없이 `discardTemps`가 돌았다」 = `onDisappear`다.** 옛 코드였다면 `kept=1`의 그 사진이 지워졌을 자리.
★ **「추정」을 「확인됨」으로 바꿀 수 있었던 것은 01:3x에 `discard` 로그를 미리 넣어 둔 덕이다** — 판정과 함께 근거가 왔다.
⚠️ 관찰(해 없음): 줄을 열 때 다른 id의 `discard temps=0 kept=0` 한 줄이 앞선다 — 시트 정체성 잠깐 생성·해체로 보인다(**추정** · 설계 §5-1).

## 6. 해시태그 — 논의 → 결정 여섯 → 구현 → 폰 설치 (01:5x~02:11 · 원본 = `docs/native/hashtag-design.md`)

| 시각 | 무엇 | 커밋 |
|---|---|---|
| 01:5x | 정본에 정해진 것(§7-1 · v2 §1-2)과 코드 사실(필드별 LWW · 포인터 선례 · 실데이터 `#` 1건 · 검색은 원문·id)을 놓고 **물음 여섯** | — |
| 01:5x | **사용자 답 여섯**(설계 §1에 원문) — ㉡ 별도 필드 · 그릇 전부 · 사람만 · 공백 규칙 · 검색 제외 · 상세에서만 편집 · 캡션에 있을 때만 · 「해시태그」 | — |
| 02:0x | **Core `HashTag`** + 시험 6 → **447** · `InboxModel.addTag/removeTag/renameTag/usedTags` · 상세 `.tags` 절 · `WrapLayout` · 캡션 꼬리 · iOS·맥 `EXIT=0` | `ebee442` |
| **02:11:43 / 02:11:50** | **폰 빌드·설치**(낡은 `.app` 삭제 · `EXIT=0` · `error: 0` · `App installed`) → `install-log.md` 첫 줄 | (이 커밋) |

- ★ **저장 꼴에서 걸린 것 하나:** 「태그마다 필드」를 `tag.<태그글>`로 하면 **결정 4(안 공백·대소문자 구분)와 파서 제약이 충돌**한다
  (키는 `[A-Za-z0-9_.]+`·소문자 접기). → **키는 id, 글은 값**(`tag.<id>`=글) — 자료 포인터와 정확히 같은 꼴이라 새 규칙이 없다.
  공백 있는 값은 `EventWriter`가 스스로 블록 경로로 보낸다(시험 5가 못 박는다).
- ★ **Claude 판단 셋**(설계에 그렇게 표시 · 사용자가 뒤집을 수 있다): ① 미확정 상세에도 절이 보인다(식별 층으로 보았다)
  ② **그 자리에서 op**(`addURL`과 같은 길 · `commitEdits`는 미확정에서 `raw`·`type` 밖을 버리는 09-13의 덫) ③ 지우기에 되묻지 않는다.
- ⚠️ **문구 둘 임시**(항시 규칙 6): 「해시태그 추가」 · 「이 분류에서 쓴 해시태그」 — 설계 §4에 후보.
- 사용자가 되물은 「상세에서만 편집이 맞나」 — **맞다고 답했다**: 이 앱은 draft가 있는 화면이 상세 하나뿐이고(사양서), 수집 화면은 급히 만드는 자리라 방해가 된다(초안 결정의 뜻과 같다).
