# 작업 로그 — 2026-07-28 (mac mini · 07-27 심야 이어서)

> **⚠️ 재개 한 줄:** **§7 Stage A~D2 완료** — A(관리화면 제거)·B(ClassDef 틀)·C(주차 마감숨김)·
> D1(FlexTypeCatalog 흡수)·(a)씀여부·(b)라벨. **§7의 (a)쓸지·(b)제목이 화면에서 작동.** 실기기 통과·push·HANDOFF 갱신까지 끝.
> **다음 = §7 (c) 동작 / Stage D3** — 알림 규칙 + **안 쓰는 분류 값·알림 정리**(정보·아이디어·원칙 due/resurface).
> 이번 (a)는 **"표시만 숨김"**이라 저장값·예약 알림은 안 건드림 → **D3 안 하면 사양서에 "안 씀" 박기 위험.** **급하지 않음**(며칠 뒤).
> 그리고 **주차 resurface "기억마다 다름" = §7 (d) 세 상태(위임)** 도 대기.
> `git pull` → `cd native && xcodegen generate` → 스킴 **`SecondBrainApp-iOS`** 빌드.

## 오늘 한 일 (07-27 worklog `44271f8` 이후, 커밋 순)

세션 흐름: 재개 파악 → §7 Stage D 착수 → D1 → (계획-먼저 피드백) → D2 재논의·진단 → (a)/(b) 분리 구현.

### 1. §7 Stage D1 — FlexTypeCatalog 흡수 (`84f6d2f`)
- 기본층/유연층 2-레지스트리 구분을 코드에서 제거, 모든 분류를 **평등한 한 목록**(`ClassCatalog: [ClassDef]`)으로 통합.
  각 분류 = 시각메타 + `uses`(세부정보 집합). 주차위치도 6종과 나란히 한 항목.
- `FlexType.swift` → `ClassRegistry.swift` rename(히스토리 보존), `FlexTypeCatalog` enum 삭제.
  `ClassRegistry.assignable/meta/def` 전부 `ClassCatalog` 단일 정본에서 조회(concat·flex-우선조회 비대칭 제거).
- **경계 무변경**: §2 `TypeCatalog`(보호자산)·§3 프롬프트·`validTypes`·`MergeEngine`. 폴백으로만 §2 참조.
- **동작 0 증명**: 코어 71 불변 · iOS/macOS 빌드 · 전 call site byte-identical(주차 색·아이콘·마감숨김·순서 diff 확인).
  **실기기 통과**(주차 상세 마감 행 없음 = Stage C 유지).

### 2. §7 (a) uses 조정 — 정보·아이디어·원칙 마감·다시보기 안 씀 (`d00b8de`)
- D2 진단(7개 분류 × due/resurface 라벨·씀 여부)에서 나온 (a) 갈래. **참고 지식·발상·상시 원칙은 시점이 본질이 아님**
  (원칙은 ambient 상시라 날짜·마감이 근본적으로 안 맞음) → 상세에서 "시간 설정" 섹션 통째 숨김.
- `ClassCatalog`: 정보·아이디어·원칙 `uses = noTime([.photo,.location])`. `DetailView.timeSection`에 `usesResurface`
  gating + 둘 다 안 쓰면 섹션 숨김. 6종·주차·미분류 회귀 없음.
- **표시 전용·비파괴적**: 숨겨도 저장된 due/resurface 값·알림은 **안 건드림** → 값 무효화·알림 정리는 **§7 (c)/D3 몫(한계 인지)**.

### 3. §7 Stage D2 — 제목(=의미) 분류별 재정의 (`8bfb603`)
- §7 (b) "같은 데이터, 다른 의미"의 첫 실동작. **빈 틀이 아니라** 약속·일정이 실제 쓰는 재정의.
- `Detail.defaultTitle`(할일 기준) + `ClassDef.titles([Detail:String])` + `title(for:)`. `ClassRegistry.title(key,detail)` 통합 조회.
- 약속 `[due:"언제", resurface:"미리 알림"]`, 일정 `[due:"일시", resurface:"미리 알림"]`. 할일·미분류·주차=기본.
- **접미사 제거**: 개발용 영문 "(Due)"/"(Resurface)" 빼고 화면 라벨을 간결 한글("마감"·"다시 보기")로 통일.
- 회귀 없음(할일·미분류·주차 기본 라벨, 약속·일정만 새 라벨). 코어 71 불변 · iOS/macOS 빌드.

### 4. 작업 방식 (피드백 반영)
- **계획-먼저 규율 강화**: D1을 개요만 보이고 구체 diff 없이 착수 → 지적받음. 이후 D2는 **진단 표 → (a)/(b) 분리 →
  파일·심볼 단위 계획 → 승인 → 착수** 순으로 진행. 메모리 [[plan-first-and-resume]] 갱신(작동 코드 이동 시 구체 계획 명시).
- **커밋 위생**: D1 첫 커밋이 `git add` 경로 오류로 rename만 담김 → amend로 내용 포함 교정(`c231972`→`84f6d2f`).

## 상태
- main = `8bfb603`. **push 완료**(local=origin, `44271f8..8bfb603`). working tree clean. 단일 main(정리 불필요).
- 폰(iPhone 16 Pro): D2까지 최신 빌드 설치·실기기 검증 통과(6개 체크: 섹션 숨김·약속/일정 라벨·할일·주차·미분류 회귀).

## 사양서 반영 확인 (규칙 #2)
- **여전히 보류가 맞음 — D3까지 굳은 뒤.** §7 모델(A~D2)의 사양서(§2) 반영은 **Stage D3 완료 후**. 특히 (a)에서
  정보·아이디어·원칙의 due/resurface를 **"표시만 숨김"**으로 처리했을 뿐 **값 무효화·알림 정리는 §7 (c)/D3 몫으로 남음**.
  **이 정리를 안 한 채 사양서에 "이 분류는 안 씀"을 박으면, 실제로는 옛 값·알림이 살아 있어 사양-구현 불일치(위험).** D3 후 반영.
- 근거는 `memory-philosophy.md` §7 · `open-questions.md` · `docs/HANDOFF.md` §4"D3한계"·§6에 유지. (사진·EXIF 프라이버시는 이미 반영·변경 없음.)

## 다음 할 일 (우선순위)
1. ✅ **HANDOFF.md 갱신 — 이 세션에 완료.** §2 유연층(D1 흡수)·§3 구현표(§7 Stage A~D2 행+세부)·§4(D3한계)·
   §5(§7 구현→A~D2 완료·D3 남음·(d) 주차)·§6(사양 반영 D3 후) 전부 반영.
2. **(며칠 실사용 뒤) §7 (c) / Stage D3** — **알림 규칙 + 안 쓰는 분류 값·알림 정리**: 정보·아이디어·원칙 due/resurface
   **값 무효화·예약 알림 취소**(이번 "표시만 숨김" 한계 해소). 착수 전 계획 먼저(작동 코드 건드리니 구체 diff).
3. **주차 resurface "기억마다 다름" = §7 (d) 세 상태(위임)** — 새 메커니즘, D3 이후 또는 별도로 대기.
4. Stage **D3까지 굳은 뒤** §7 사양서(§2) 반영 판단(규칙 #2 — 그 전엔 "안 씀" 박기 위험).
5. (남은 것) question 답변 경로 · 여러 장 사진 · 반복 O3 · 검증 부채(자동분류·수집·EXIF, HANDOFF §4).

## 추가 (2차 세션 — 조사·확인만, 코드 변경 0)
- **§7 Stage D3 델타 보고서** — HANDOFF 최종본(`2ee78fb`) 기준 `8bfb603..HEAD` 확인 결과 **범위 내 코드 커밋 0**(docs 2건뿐: `e0d2e6e`·`2ee78fb`). **D3는 미착수** 재확인.
- **알림에 분류가 입력 안 됨을 코드로 확인** — `NotificationPlanner.plan(...)`·`ItemSchedule.effectiveDay(_:)` 모두 `resurface`/`due`만 보고 `type` 미참조. `NotificationScheduler.reschedule`는 `removeAllPendingNotificationRequests()` 후 재등록(전체 교체·멱등). → §4 "D3한계"의 실체 = 정보·아이디어·원칙 옛 값 남으면 알림 발화. `swift test` **71 통과**(불변).
- **"D3" 이름 두 개** 정리: **§7 Stage D3 = (c) 동작·값·알림 정리**(이번 대상). ↔ `open-questions.md`의 **결정 D1~D5 중 D3(반복=속성)**는 별개 번호(혼동 주의).
