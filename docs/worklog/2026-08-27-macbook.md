# 2026-08-27 (회사 맥북) — **시험용 되풀이 열을 그만뒀다**

> ## 재개 한 줄 — **2026-08-27 20:2x (이 줄이 최신이다 · 세션 끝)**
>
> ### ✅ 실데이터 조작 — **되풀이 12개 → 2개** (**09:23에 썼다** · 판정은 **20:2x**)
> 사용자: *"'지금 챙길 것'에 너무 많은 '되풀이' 기억이 빨간색으로 놓친 날짜가 많이 쌓인 채로 남아 있어 …
> 실제로 중요한 것들을 놓치게 하는 방해 요소야."*
> **남긴 둘:** `E036A094`「아침 간염 약 먹기!」(op 50 · 유일하게 정상 동작 중) ·
> `616D4DCE`「주차 위치」(시점 없음).
> ✅ **화면에서도 확인됐다** (사용자 2026-08-27 **20:2x**): *"12개에서 2개로 줄어든 것 확인했음!!!"*
> ⛔ **폰 설치는 없다** — 데이터만 고쳤다. 폰 = **`820e20e`**(08-26 20:25) 그대로다.

---

## 1. ⛔ 「시험용인가」는 데이터로 못 가른다 — **판별력 없는 표본이었다**

처음에 **저장소 문서와 대조**해 가르려 했다(계측 규칙 7: *"저장소에 있으면 문서를 열어 확인한다"*).
⛔ **12개 **전부**가 개발 문서에 나왔다.** 실제 기억도 개발 중에 표본으로 쓰였기 때문이다
(`01781308`「복약」은 어제 URL 표본이기도 했다).

★ **계측 규칙 7의 ★★가 말한 그 형태다** — *"비교가 통과했다는 것은 「표본이 그 차이를 드러낼 수
있었다」가 참일 때만 증거다."* **여기서는 아예 안 갈렸다.**
→ **가른 것은 원문이었다** — 「시험용」·「E-6」·「F-1」·「항목 가」는 **사용자가 그렇게 적어 둔 것**이다.
⚠️ **이름 유사로 고른 것이 아니다** — 사용자의 표시를 읽은 것이고, **최종 판정도 사용자가 했다.**

## 2. 사용자 결정 셋

| 물음 | 답 |
|---|---|
| **어느 것** | **열 개** — 시험용 아홉 + `01781308`「복약 : 놓치는 약 먹기!」 |
| **어떻게** | **그만두기(삭제)** — 꺼두기(잠시 멈춤)가 아니다 |
| **누가** | **맥에서 일회성 스크립트** (폰·맥 앱에서 열 번 반복하지 않는다) |

⚠️ **앱이 정한 뜻을 지켜 적는다**(기록 규칙 5): **그만두기 = 삭제** · 꺼두기 = 잠시 멈춤 · 완료 = 이번 것.

## 3. 어떻게 썼나 — **기존 파일을 한 글자도 안 건드렸다**

**도구:** `native/tools/2026-08-27-retire-test-recurrences.py`
(⛔ **쓰는 도구가 아니라 그날의 기록이다 · 재실행 금지** — `2026-08-18-strip-auto-fields.py`의 선례를 따랐다.)

| | 무엇 | 왜 |
|---|---|---|
| **새 파일에 썼다** | `inbox-cleanup-20260827.md` · `delete` op **열 줄만** | 앱은 `inbox`로 시작하는 `.md`를 **전부** 읽는다(`InboxStore`·`FragmentFolder`) |
| ⚠️ **맥 앱이 돌고 있었다** | 어제 띄운 것(pid 22753) | 그 앱의 dev 파일에 끼어들지 않으려고 **새 파일**을 골랐다 |
| **백업을 먼저 떴다** | `backups/2026-08-27-cleanup-20260827-091944/` (여섯 파일) | |
| **dry-run을 먼저 보였다** | 열 줄을 그대로 출력하고 **허락받고 썼다** | |
| **HLC를 검산했다** | 기존 최대 `…849602` < 쓴 값 `…188480` | ⛔ 기존 op보다 뒤여야 병합에서 이긴다 |

### ✅ 되돌리는 길이 둘이다 — **그래서 되돌릴 수 없는 조작이 아니다**
① 「보관된 기억」의 **왼쪽 스와이프 「되돌리기」**(`ArchiveView.restore`)
② **그 파일을 통째로 지우면** op이 사라져 원상복구된다

## 4. ⚠️ 못 잰 것 · 안 한 것

| 무엇 | 왜 |
|---|---|
| **폰 화면이 어떻게 보이나** | ✅ **확인됐다** — *"12개에서 2개로 줄어든 것 확인했음!!!"* |
| **「아침 간염 약 먹기!」가 지금 보이나** | **못 잼** — 다음 회차가 **2026-08-28 08:00(미래)**이라 게시 게이트에 안 걸릴 수 있다(`ItemSchedule.isPublished`: 되풀이는 마감 **시각부터** 게시). ⛔ **추정이다 — 화면에서 닫아야 한다** |
| **꺼둠 셋이 화면에 보였는지** | **못 잼** — 코드는 **안 보여야 한다**고 말하지만(`Recurrence.isDormant`) 확인 전에 지워졌다. ⚠️ `inbox-state.py`는 **분류 게이트를 안 본다**(그 도구 주석) |
| **앱 코드 변경** | **0줄** — 데이터만 고쳤다 |

## 5. ▶▶ **집 맥미니에서 할 것 둘** (2026-08-27 **20:2x** 사용자 지시 · 여기가 정본이다)

> **사용자 원문(그대로 옮긴다 — 요약하지 않는다):**
> *"1. 모든 '기억'의 상세 화면의 '원문' 영역 제목 '원문' 글자 바로 옆에 기억 ID를 표시하게 할 것임.
> 이렇게 '원문: E036A094' 그리고 기억을 검색할 때 기억 ID로도 검색 가능하게 할 것임."*
> *"2. 검색해서 나오는 결과 목록에, 각 기억 내용의 아랫줄에 있는 회색 날짜 정보가 표시되는 줄
> 가장 오른쪽에, 현재 그 기억이 들어가 있는 화면의 이름을 표시하게 할 것임! 지금은 검색이 되어도
> 어느 화면에 있는지 알 수가 없기 때문임."*

⚠️ **요약을 원본으로 읽지 말 것**(기록 규칙 10) — 위 인용이 **원본**이다. 아래는 내가 짚어 둔 자리다.

### ▶ 1. **상세의 「원문」 옆에 기억 ID · 검색에서 ID로도 찾기**

| | 자리 | 무엇 |
|---|---|---|
| 표시 | `DetailView.swift:462` — `sectionLabel("원문")` | 여기가 그 제목이다. ⚠️ `477`의 `TextField("원문", …)`은 **자리표시자**라 다른 것이다 |
| 검색 | `SearchView.swift` | 지금은 **원문만** 본다 |

**⏸ 집에서 물어야 할 것 셋** (⛔ 내가 정하지 않는다 — 항시 규칙 6):
1. **꼴** — 사용자가 *"'원문: E036A094'"*로 **직접 적어 줬다.** ✅ 이건 정해진 것이다.
   ⏸ 다만 **8자인가 전체 UUID인가**는 안 정해졌다(예시는 8자다).
2. **⛔ 「원문으로 찾기」가 틀린 말이 된다** — `SearchView.swift:100`의 안내 문구다.
   ID로도 찾게 되면 그 말이 사실과 어긋난다. **새 문구는 사용자가 정한다.**
3. **대소문자·부분 일치** — `E036A094` / `e036a094` / `E036` 어디까지 맞출 것인가.

⚠️ **항시 규칙 6의 「보고 대상이 아니다」에 걸리지 않는다** — 이것은 **화면에 보이는 문구**다
(소스코드 안의 이름이 아니다).

### ▶ 2. **검색 결과 줄에 「그 기억이 지금 있는 화면 이름」**

**자리:** `SearchView.swift:80~81` — 그 회색 줄이다.
```swift
HStack(spacing: 6) {
    Text(itemCaption(item)).font(.caption2).foregroundStyle(Palette.textTertiary).lineLimit(1)
```
**가장 오른쪽**에 붙이면 된다(`Spacer()` 뒤).

**★ 이름은 앱에 이미 있다** — 새로 짓지 않는다(항시 규칙 6의 거꾸로 쓰기). **탭 다섯**(`RootView.swift:23~39`):
**새로운 기억 · 검색 · 살아있는 기억 · 보관된 기억 · 설정**.
그리고 **「새로운 기억」 안의 섹션 둘**: **새 기억들 · 지금 챙길 것**. 보관 쪽엔 **완료된 기억**도 있다.

**⏸ 집에서 물어야 할 것 둘:**
1. **★ 어느 층의 이름인가** — **탭 이름**(「새로운 기억」)인가 **섹션 이름**(「지금 챙길 것」)인가.
   ⚠️ **사용자가 원한 것은 「어디 가면 찾을 수 있나」**이므로 **섹션 층이 더 쓸모 있어 보이지만
   그것은 내 짐작이다.** 물어서 정한다.
2. **어디에도 안 보이는 항목은 무엇으로 적나** — 예: **꺼둔 되풀이**는 「지금 챙길 것」에서 빠져
   「살아있는 기억」으로 간다(`ItemSchedule.isPublished` · `Recurrence.isDormant`).
   ⚠️ **미확정·시점 없는 것도 갈린다**(`InboxModel.partition`이 `recent`를 둘로 쪼갠다).
   ⛔ **그 계산을 새로 만들지 말 것** — **이미 있는 그 두 곳이 진실원**이다.

### ▶ 그 밖에 열려 있는 것 (08-26에서 넘어온 것 — 순위 낮다)
**⏸ 맥에서 URL 네모를 어떻게 여나**(사용자 결정 대기) · **⏸ 창·토스트 여백 판정** ·
**⏸ URL 네모 크기 62pt** · **⏸ 「사용자가 그림을 직접 붙이는 길」** ·
**⏸ 진단 파일(`capture-why.log`)을 걷어낼까** — 전말은 `docs/worklog/2026-08-26-macbook.md` §7.

---

## 6. 오늘 배운 형태

1. **★★ 판별력 없는 표본은 「아예 안 갈리는」 꼴로도 온다** — 문서 대조가 **12개 전부를 맞혔다.**
   계측 규칙 7의 ★★는 「같게 나왔다」를 경고하는데, **여기서는 「전부 걸렸다」였다.** 같은 뿌리다.
2. **★ 실데이터를 고칠 때 「기존 파일을 안 건드리는 길」이 있었다** — 앱이 `inbox*` 전부를 읽으므로
   **새 파일에 op만 넣으면 된다.** ⛔ 돌고 있는 앱과 겹칠 걱정도 사라진다.
3. **★ 되돌릴 길을 먼저 세었다** — 둘이었다(앱의 「되돌리기」 · 파일 삭제). **그래서 위험이 낮다고 말할 수 있었다.**
   ⛔ **「되돌릴 수 없다」와 「되돌릴 수 있다」를 짐작으로 말하지 않았다** — 코드에서 확인했다.

4. **⛔ 시각을 짐작으로 적었다 — 그 자리에서 `date`를 안 읽었다.** 데이터를 쓴 것은 **09:23**이고
   판정과 다음 지시는 **20:2x**인데, worklog에 **둘 다 09:3x**로 적었다(**열한 시간을 메웠다**).
   ★ **계측 규칙 7의 계기와 똑같은 형태다**(*"마지막으로 잰 시각이 13:51인데 그 뒤 「14:12」라고 말했다"*).
   ⚠️ **커밋 전에 `date`를 읽어서 잡았다** — 그래서 기록에는 안 남았지만, **잡힌 경로가 규칙이었다.**
