# 세컨드 브레인 — 네이티브 v1 설계 초안

> **이 문서의 위치:** 이것은 **Claude Code에 넘길 초안**이다. GitHub 정본(`second-brain-v0-spec.md`)이 아니다.
> Claude Code는 이 초안을 받아 GitHub 정본의 **아키텍처 층을 이 방향으로 갱신**한다.
> **주의:** 기존 사양서를 통째로 갈아엎지 말 것. 살아남는 것(분류 체계 §2, 분류 프롬프트 §3/classify.py, pull/push/ambient, "데이터=영속 평문" 원칙)은 그대로 두고, **아키텍처(기존 §0-A)만 교체**한다. 상세는 아래 §7·§8 참조.

---

## 1. 전환의 배경 — 왜 네이티브로 가는가

경로 1(웹 PWA로 먼저 검증)의 목적은 "네이티브가 진짜 왜 필요한지 알아내는 것"이었고, 며칠 실사용으로 그 목적이 달성됐다. 웹의 한계가 **구체적 통증 셋**으로 드러났다:

1. **동기화 취약** — 여러 수집 창구가 iCloud의 파일 하나에 각자 쓰고, iCloud는 "마지막 쓴 게 이긴다"라서 실제로 충돌·롤백이 발생했다(분류 2건 롤백, 재분류로 복구). 이게 가장 불안한 부분.
2. **쓰기 불가** — iOS Safari PWA는 파일 쓰기가 안 돼 확정·수정·삭제·미루기가 전혀 불가능했다. 사용자의 "적극적 실행" 욕구를 못 받아줌.
3. **기기 고유 기능 부족** — 진짜 알림(push notification), 위젯, 매끄러운 날짜 입력(웹은 v7까지 우회책) 등이 웹으론 약함.

→ 웹은 제 할 일을 다 했다. 이제 네이티브로 간다.

---

## 2. 범위 (확정)

- **v1 = Mac 앱 + iPhone 앱, 둘로 국한.**
- iPad·Windows PC는 **나중에 확장**. 단, 설계가 나중 확장을 막지 않아야 함(특히 Windows — §3·§4 참조).

---

## 3. 아키텍처 (기존 §0-A 대체)

### 분류의 중심 = **(가) iPhone 중심**

- iPhone이 **수집·분류·검토의 심장.** iPhone이 메인 디바이스이므로.
- 네이티브 앱이라 iPhone이 (a) 파일에 쓰기, (b) Claude API 직접 호출이 가능 → **수집한 것을 그 자리에서 즉시 분류**. Mac을 기다릴 필요 없음(웹 시절의 "Mac cron 15분 대기" 제거).
- **API 키는 iPhone의 iOS 키체인(안전 저장소)에** 둔다. (웹보다 안전. 단 키가 있는 곳이 하나 늘어남을 인지.)
- 백그라운드 자동 분류는 iOS 제약이 있으므로 **"앱 열 때 미분류를 모아 분류"**가 현실적.
- **Mac의 역할 = "대량 정리용"이 아니라 "펼쳐놓고 생각·수집하는 곳".** 긴 글 검토, 여러 항목 놓고 사고 정리(예: 영업 고민), 데스크톱 대량 수집(URL·파일). 검토·수집 창구로 살아있음.

### 기각한 대안과 이유

- **분류를 여러 기기가 제각각** → 여러 기기가 파일에 쓰면 충돌 재발. iPhone 중심으로 몰아 회피.
- **CloudKit 등 Apple 동기화 DB** → 충돌 병합은 깔끔하지만 **(1) Windows가 영영 못 낌(Apple 전용) (2) 데이터가 DB에 갇혀 평문·영속 원칙 훼손.** 게다가 "Windows만 평문으로 두고 CloudKit에 다리 놓기"는 *두 개의 진실원을 평생 잇는 부담* + Mac/서버 중개 필요라 기각. **CloudKit이 푸는 문제(충돌)는 아래 조각 파일 방식으로 더 싸게 풀린다.**

---

## 4. 데이터 저장·동기화 구조 (핵심 — 이 시스템의 척추)

### 만족해야 할 다섯 조건

1. 여러 창구가 **충돌 없이** 모인다
2. 여러 기기에서 **쓰기 가능**(iPhone에서도 확정·수정·삭제·미루기)
3. **평문·영속**(20년 뒤 에디터로 읽힘, 앱이 죽어도 데이터는 삶)
4. **Windows가 나중에 낄 수 있다**(Apple 전용 기술에 가두지 않음)
5. **분류를 유연하게**(한 곳 강제 아님)

### 채택: 기기별 조각 파일 + 앱이 읽을 때 합치기 (append 중심)

- 받은함을 파일 하나가 아니라 **기기별 파일 여러 개**로 나눈다. 예: `inbox-iphone.md`, `inbox-mac.md`, (나중)`inbox-windows.md`.
- **각 기기는 자기 파일에만 쓴다.** → 두 기기가 같은 파일을 건드릴 일이 없으니 **iCloud의 "마지막이 이긴다"가 애초에 발동 안 함 = 충돌 원천 제거.**
- 앱은 **읽을 때 이 파일들을 다 합쳐** 하나의 받은함처럼 보여준다. 사용자 눈엔 여전히 "받은함 하나".
- 다섯 조건 모두 통과: 충돌 없음(1) / iPhone 자기 파일에 자유 쓰기(2) / 평문 마크다운 유지(3) / iCloud 평문이라 Windows 참여 가능(4) / 각 기기가 자기 파일에 분류 기록(5).

### 대가와 v1 범위

- **난제: 기존 항목 "수정"을 어떻게 기록하나.** 조각 파일은 *새로 넣기(append)*엔 완벽하지만, iPhone이 다른 파일의 항목을 고치려면 그 파일을 못 건드리는 문제. 해법 방향은 **"수정도 새 줄(이벤트)로 자기 파일에 쌓기"**(append-only), 앱이 합칠 때 최신 이벤트 반영.
- **v1에서는 iPhone이 쓰기의 중심**이므로(§3) 대부분의 수정이 iPhone에서 일어나 이 난제가 상당히 완화됨. 착수 시 "수정 이벤트를 어느 수준까지 v1에 넣을지"(단순형: 새 수집 위주 / 완전형: 수정·삭제·미루기까지 이벤트)를 확정한다. → **§9 미결 참조.**
- 합치는 로직(정렬·중복제거·최신반영)이 앱의 핵심 엔진. 신중히 구현.

### 받은함 ↔ 아카이브 = "흐르는 강, 고이는 호수 아님"

- 처리·확정된 항목은 받은함에서 빠져야 함. **받은함엔 "안 본 것 + 곧 닥칠 것"만.** → 데이터가 수천 개로 쌓여도 받은함은 늘 작게 유지, 정리 오버헤드가 총량에 비례해 커지지 않음. **대량 정리가 필요하다면 그건 설계 실패 신호.**
- **v1 구현: (2) done 표시로 시작.** 항목은 제자리에 두고 `status: done` 같은 표시만 붙여 화면에서 숨김. 조각 파일에 "이벤트 한 줄 추가"로 깔끔, 충돌 없음, 되돌리기 쉬움.
- **나중: (1) 주기적 아카이브 청소.** 데이터가 실제로 커지면(수천 개~), done된 지 오래된 것을 별도 아카이브 파일로 일괄 이동. v1엔 안 만듦(지금은 수백 개라 파일 비대화 문제가 아직 안 옴). 구조는 이걸 염두에 두되 구현은 미룸.

---

## 5. 항목에 할 수 있는 행동 (일람)

네이티브가 주는 핵심 가치. 각 행동이 데이터에 어떻게 기록되는지(위 append 원칙)와 함께 설계:

- **확정** — 자동 분류/추출된 값(type, due 등)을 사용자가 승인.
- **수정** — 분류·날짜·내용 등을 고침.
- **삭제** — discard. 빠르고 쉽게(안 버려서 쌓이는 걸 막음).
- **미루기(defer/snooze)** — **분류가 아니라 시점 조작.** "지금 말고 나중에 다시 들이밀어줘" = resurface 날짜를 미래로 미는 것. push 시스템의 일부.
- (완료 = §4의 done 표시)

→ 이 행동들이 §4의 조각 파일 + append 이벤트 구조 위에서 어떻게 기록·병합되는지가 v1 설계의 뼈대.

---

## 6. 수집 입구 (기기별) — "이 부분 실패하면 프로젝트 실패"

세컨드 브레인의 생명은 마찰 없는 수집. 기기마다 제대로 구현돼야 함. v1은 Mac·iPhone 각각에서:

- **텍스트** — 가장 기본. iPhone 음성/타이핑, Mac 타이핑/클립보드.
- **URL** — 보는 페이지 URL+제목 자동, "왜" 한 줄만 손으로. 웹 시절 3단계 마찰을 네이티브로 원클릭化.
- **이미지 (4경로 모두)** — ①촬영(iPhone) ②복사·붙여넣기(스크린샷 포함) ③파일 지정 ④앱 공유 시트. 저장은 포인터 방식(`assets/`에 파일 + 받은함엔 경로+"왜" 한 줄). **OCR은 초기 제외(나중).**
- **파일** — 파일 지정/공유로 받아 포인터로 보존.
- **영상·녹음** — **뒤로 미룸.** 파일이 크고(iCloud 부담) 전사(STT)가 필요해 손이 많이 감. (사용자가 나중에 별도로 다룰 예정.)

---

## 7. 유지되는 것 (기존 사양서에서 — 버리지 말 것)

네이티브로 가도 그대로 쓰는 자산:

- **분류 체계(§2)** — event/promise/info-action/info/idea/principle/discard. "짧은 파편도 info로 보존" 규칙 포함.
- **분류 프롬프트(§3 / classify.py의 SYSTEM_PROMPT가 정본)** — 짧다고 버리지 말 것, 시점 적극 추출, question 필드 등. **분류 로직은 재사용**(iPhone이 부르든 Mac이 부르든 "어떻게 분류하나"는 동일).
- **소비 3방식** — pull(검색) / push(시점 알림) / ambient(상시 원칙 표시).
- **"데이터=영속 평문 / UI=소모품" 원칙** — 네이티브 앱도 평문 파일에 쓴다. 앱이 데이터를 독점하지 않음.
- **실사용 교훈** — iCloud 충돌이 실제로 난다는 것, +시점 우회의 한계, 이미지 4경로 필요성. 이게 네이티브 설계의 근거.

---

## 8. 버리는 것 (웹 PWA 특유 — 재활용하지 말 것)

네이티브는 깨끗이 새로. 웹 구현을 억지로 재활용하지 않는다:

- localStorage 임시 저장(pending 오버레이) — 네이티브는 파일에 직접 씀.
- `<input type=date>` + showPicker() 우회책(+시점 v3~v7) — 네이티브 날짜 피커 사용.
- 서비스워커 캐시 / PWA 배지 버전 체계 — 불필요.
- 데스크톱 File System Access API 쓰기 경로 — 네이티브 파일 접근으로 대체.

---

## 9. 미결 — 착수 직전 확정할 것

1. **기술 스택.** Mac 앱 + iPhone 앱을 만들며 코드를 최대한 공유하는 방법. Swift/SwiftUI 네이티브(두 플랫폼 공유) vs 크로스플랫폼 프레임워크. Claude Code와의 궁합, iOS 빌드 특성 고려. → 착수 직전 결정.
2. **수정 이벤트의 v1 범위(§4).** 단순형(조각 = 새 수집 위주, 수정·삭제·미루기는 최소) vs 완전형(수정도 이벤트로 쌓아 iPhone에서 다 됨). iPhone 중심이라 완화되지만, 어느 수준까지 v1에 넣을지 확정.
3. **합치기 엔진 세부.** 여러 조각 파일 → 하나의 받은함으로 합칠 때 정렬·중복제거·최신 이벤트 반영 규칙.

---

## 다음 단계

이 초안을 Claude Code에 넘겨 → GitHub 정본(`second-brain-v0-spec.md`)의 아키텍처 층을 이 방향으로 갱신 → 그다음 §9 미결(특히 기술 스택)을 정하고 네이티브 v1 착수. **전략·방향 결정은 Claude Code가 아니라 Claude 챗에서 하고, 정본 편집만 Claude Code가 한다.**
