# HANDOFF — 현재 스냅샷

> **이 문서는 스냅샷이다.** 「지금 어디까지 와 있나」만 적는다.
> 전말은 `docs/worklog/`, 정본은 `docs/native/`(뜻 = `memory-philosophy.md` · 동작 = `edit-policy.md`).
> **어긋나면 정본이 맞다.** 이 문서가 낡았으면 이 문서를 고친다.
>
> **갱신:** 2026-09-02 02:2x KST · **집 맥미니** (09-01 23:35에 시작해 자정을 넘겼다)
>
> *(옛 갱신 표시: `2026-08-31 05:3x` 집 맥미니 · `2026-08-30 21:3x` — 같은 세션의 앞부분이다.)*
>
> *(옛 갱신 표시: `2026-08-28 01:3x` 집 맥미니 — ⚠️ **표시 자체가 낡아 있었다.**
> 이 문서는 그 뒤 **08-29 00:21·00:51·00:52에 실제로 세 번 고쳐졌는데**(`1ca6013`·`658bd44`·`f33e219`)
> **갱신 표시만 안 따라왔다.** 그리고 **08-30 21:15 사양서 반영(`0582135`)은 아예 안 들어왔다** ·
> `2026-08-27 20:2x` 회사 맥북 · `2026-08-26 01:0x` 집 맥미니 · 그 앞은
> `2026-08-23 17:4x`로 **사흘 낡아 있었다** — 기록 규칙 8의 형태: 그때는 맞았던 값.)*

---

# ▶ 오늘(09-02 새벽) 한 것 — **`<` 글자 규칙이 뒤집혔다 · 「끝내기」는 미결**

> **폰 = `759bd32`** (2026-09-02 **02:13** 맥미니 설치) · 전말 = `docs/worklog/2026-09-02-macmini.md`
> 설치 이력은 **`docs/install-log.md` 맨 위**(오늘 셋: `c3c3b77` · `4956097` · `759bd32`).

### ⛔ **미결 하나 — 「액션 버튼으로 들어와 [취소하기] → 앱을 끝내기」**
사용자 판정: **"아직 해결안 됨. 미결된 것으로 하자."**
**증상(02:3x · 폰):** ✅ **목록은 안 스친다** · ⛔ **앱이 안 끝난다** — **앱 전환기에 카드가 남아 있다** (2026-09-02 02:3x · 폰 · 사용자).
★ **그래서 좁혀졌다 — 화면 쪽은 다 됐고 `exit(0)`이 여전히 안 돈다**(배경 시간을 열었는데도).
▶ **다음 후보 첫째: `didEnterBackgroundNotification`에서 끝낸다**(지연 블록을 안 쓴다).
**밟은 길 셋과 후보 전부**는 오늘 worklog **§3**에 있다.
- **뜻은 사양서에 있다**(§「수집 화면」 → 「진입 둘」 표) · 사용자의 한 줄:
  *"들어가기 직전 앱의 상태를 봐뒀다가 취소하고 나올 때 그 상태로."*
- ✅ **나가는 「모습」은 닫혔다** — 목록이 안 스친다(*"깨끗하게 나와지네. 이건 좋아."*).
  **남은 것은 「앱이 정말 끝나나」 하나다.**

### ✅ 오늘 닫힌 것 둘 (폰 판정 통과)
- **수집 화면 = 「‹ 새로운 기억」 + 「기억 수집」** — 두 번 읽히던 것이 사라졌다.
- **`<`에 붙는 글자 = 「갈 곳의 이름」** (사용자 결정) — 상세는 **온 화면의 이름**
  (「새로운 기억」·「살아있는 기억」·「검색」·**「원칙」**). **사양서에 올렸다.**
  ⛔ **줄 하나를 두 화면이 함께 쓰면 그 줄에 이름을 박을 수 없다** — 「살아있는 기억」에서 그렇게 깨졌다.

### ▶ **다음은 회사 맥북이다** (사용자: *"내일 맥북에 가서 개발 계속 할거야"*)
⚠️ **맥북에서 먼저:** `git pull` → `cd native && xcodegen generate`
(⛔ **`.xcodeproj`와 `Info-iOS.plist` 둘 다 생성물**) · **시험 434** · 시뮬 UDID는 그 기기에서 다시 읽는다.

---

# ▶ 그 앞(08-28) 한 것 — **기억 ID를 화면에 올렸다 · 검색이 「어디 있나」를 말한다**

> ### ✅✅ 폰 = **`12add9e`**(2026-08-29 **00:19** 맥미니 설치) — **00:19~00:51 사이 판정 통과**
> 사용자: *"폰에서 적당한 색깔로 설정된 것 확인했음! 완료!"* → **색 문제는 닫혔다.**
> **검색 결과의 화면 이름 색 — 왕복 넷 끝에 「날짜와 원문의 중간」으로 앉았다.**
> **한 줄 안에서 세 층:** 원문 #ECEBF1(15.72:1) > **이름 `textSecondary` #A7A4B3(7.63:1)** >
> 날짜 #746F82(3.85:1). **날짜도 원문도 한 번도 안 건드렸다** — 움직인 것은 **이름 하나**다.
> ⛔ **왕복 넷:** ①흐리다 ②덜 바꿨다 ③너무 밝다(**폰에서**) ④중간.
> **②만 읽고 「더 올려라」로 되돌리지 말 것** — ③에서 원문 밝기까지 올려 봤고 되돌아왔다.
> **전말은 `docs/worklog/2026-08-28-macmini.md` §7·§8** · 설치 이력은 `docs/install-log.md` 맨 위.
>
> *(그전 · **08-28 23:26** `a83e95d` = 이름을 **원문과 같은 밝기**로 → **폰 판정 통과**
> (*"의도대로 색깔이 밝아졌어"*) **+ 「너무 밝다」가 함께 왔다.**)*
>
> ### ✅ 그전에 — 08-27 맥북 지시 둘을 **다 닫았다** (폰 = `52557a1` · 2026-08-28 **01:31** 설치)
> 사용자 판정(01:34): *"1 2 3 4 모두 제대로 동작해! 소문자로, 앞네글자만 해도 잘 나오며 답답하지 않음."*
>
> | | 무엇 |
> |---|---|
> | **1** | 상세 원문 칸 제목이 **`원문: E036A094`**(8자 · 길게 눌러 복사) · **검색이 ID로도 찾는다**(대소문자 무시 + 부분일치) |
> | **2** | 검색 결과 회색 줄 **가장 오른쪽에 화면 이름**(섹션 층: 원칙 · 지금 챙길 것 · 새 기억들 · 살아있는 기억 · 완료된 기억) |
>
> **화면 문구 넷은 사용자가 골랐다**(항시 규칙 6) — 8자 · 「기억 검색」 · 「기억 찾기」 · 섹션 층.
> ⛔ **소속 계산을 새로 만들지 않았다** — `InboxModel.screenNames`는 **`partition` 결과에 이름표만** 붙인다.
> ★ **08-27이 「집에서 물을 것」으로 남긴 물음 하나가 코드에서 사라졌다** — *어디에도 안 보이는 항목*은
> **없다**(`InboxSections.swift`가 게시 안 된 것을 `recent`로 떨어뜨린다 → 살아있는 기억 / 새 기억들).

---

# ▶ 오늘(08-31 새벽) 한 것 — **수집 화면을 다시 지었다 · 「바로 수집 화면」이 닫혔다**

> ### ⏸ 폰 = **`eceb868`** (2026-08-31 **05:21** 맥미니 설치) — **판정 대기 셋**
> ① 수집·상세 둘 다 **`<` 글자가 보이나** ② ⚠️ **「‹ 새 기억」+제목 「새 기억」이 두 번 읽히는 것이 괜찮나**
> ③ 액션 버튼 → [취소하기] 때 **미끄러져 내려간 뒤 끝나나**.
>
> ### ★★ 「바로 수집 화면」이 닫혔다 — **왕복 넷 끝** (`f102879` · 05:15 · 폰 판정 통과)
> 사용자: *"오! 바로 뜬다. 앱을 완전히 죽이고 액션버튼 누르고 들어오니 새로운 기억 창이 **전혀 안 보임**."*
>
> | 시도 | 무엇 | 결과 |
> |---|---|---|
> | 1 | 시트 애니메이션 끄기 | ⛔ 부족(첫 프레임이 목록) |
> | 2 | **120ms 문**으로 덮기 | ⛔ 여전히 스쳤다 |
> | 3 | 문턱을 **재려고 계측 장치** | ⛔ **애초에 잴 문제가 아니었다** |
> | **4** | **URL 스킴 `secondbrain://capture`** + **뿌리로 그리기** | ✅ **닫혔다** |
>
> ★★ **사용자가 방향을 바로잡았다** — *"시간을 재는 방식을 써야 하는거야?"*
> **아니었다.** iOS에서 「런치 옵션」의 자리가 **URL 스킴**이고 `urlContexts`는 **첫 프레임보다 먼저** 온다.
> ⚠️ **그것만으로도 부족했다** — `fullScreenCover`는 **띄우는 것**이라 한 프레임 뒤에 올라온다.
> **`ZStack`의 형제로 그려야** 같은 프레임에 나온다.
> ★ **「언제 아는가」와 「언제 그리는가」가 다른 문제였다.**
> ⛔ **팝업은 사파리 정책이었다** — 액션 버튼에서는 안 묻는다. **사파리 결과로 접었으면 틀렸을 것이다.**
>
> ### ⚠️ 빌드 환경이 바뀌었다 — **맥북에서 주의**
> iOS만 **`GENERATE_INFOPLIST_FILE: NO`** + `project.yml`의 `info:`로 plist 명시(URL 스킴 때문).
> ⛔ **`native/Sources/App/Info-iOS.plist`는 생성물**이다(`.gitignore`) — `xcodegen generate`로 생긴다.
> ⚠️ **`UILaunchScreen`을 직접 넣었다**(빠지면 letterbox) · **버전 둘을 빌드 설정에 묶었다**.
>
> ### 만든 것 — Core 셋 · 시험 **410 → 434**
> **`RawLine`**(원문 줄바꿈을 한 줄에 접는다 · 8) · **`EdgeSlide`**(단추가 테두리에만 붙어 움직인다 · 10) ·
> *(전날)* `TranscriptJoin`(새 녹음 = 빈 줄 둘 · 7). ⛔ **기존 시험이 하나도 안 깨졌다.**
>
> **전말 = `docs/worklog/2026-08-31-macmini.md`** (재개 한 줄이 그 맨 위에 있다).

---

# ▶ 그 앞(08-30 저녁~08-31 00:57) 한 것 — **「보조 자료」 카드가 수집 화면으로 왔다**

> ### ✅ 폰 = **`8251d53`** (2026-08-31 **00:52** 맥미니 설치) — **00:57 판정 통과**
> 사용자: *"저장하니까 상세화면으로 잘 넘어가. < 아이콘도 하나야."*
>
> | | 무엇 | 판정 |
> |---|---|---|
> | **1** | 「사진 찍기」 줄 제거 → **「보조 자료」 카드**(껍데기는 **`MediaStrip`**으로 갈라 상세 카드와 함께 쓴다) | ✅ |
> | **2** | 텍스트 칸 **160 → 80pt** | ✅ ⚠️ **재검토 예정** |
> | **3** | 사진·URL **여러 개** · 길게 눌러 **「지우기」** | ✅ |
> | **4** | **[저장] → 그 기억의 상세 화면** · **`‹`는 하나** | ✅ |
> | **5** | 짧게 눌러 **크게 보기** | ⏸ **못 잼** |
>
> ### ★★ 성역 경계를 정했다 — **자료는 전부 op** (사용자 결정)
> 정본 §6 10번이 *"⛔ 여기서 정하지 않는다"*로 남겨 둔 자리다. **성역에 남는 자료는 녹음 원본 `audio` 하나.**
> ⚠️ **대가:** 「수집 당시 함께 들어온 사진」이라는 보장이 **데이터에서 사라진다**(알고 골랐다).
> ⛔ **정본 훑기가 밀려 있다** — 축이 바뀌었다(create → op). **고치기 전에 목록으로 보고한다.**
>
> ### ★★ [저장] → 상세를 **세 번 틀렸다** — 진짜 원인은 **`load()`가 비동기**였다
> `append`가 부르는 `load()`가 **`Task { await reload() }`**라 **저장 직후 `current(newId)`가 nil**이고
> 밀기 코드가 조용히 지나갔다. ★ **`06aff97`의 350ms가 실제로 기다린 것이 이 재로딩**이었는데
> 나는 주석에 **「시트 닫힘 대기」**로 적었고, `onDismiss`로 옮기며 **대기를 함께 잃었다.**
> ⚠️⚠️ **주석이 틀린 이유를 적고 있으면 다음 수정이 그것을 근거로 빗나간다.**
> ✅ **「찾을 때까지 기다린다」**(`InboxModel.resolveAfterWrite`) — ⛔ **`Task.sleep`으로 돌아가지 말 것.**
>
> **전말 = `docs/worklog/2026-08-30-macmini.md` §7** · 설치 이력 = `docs/install-log.md` 맨 위.

---

# ▶ 그 앞(08-30 저녁) 한 것 — **실사용 버그 넷 · 셋이 한 뿌리였다**

> ### ✅✅ 폰 = **`06aff97`** (2026-08-30 **21:42** 맥미니 설치) — **22:49 넷 다 판정 통과**
> 사용자: *"잘 됨!!! 1 2 3 4 모두통과!!"* → **수집 화면의 손실 셋이 닫혔다.**
> **사용자가 실사용에서 찾아 온 넷.** 뿌리 하나가 셋을 만들고 있었다:
> **이 시트의 `onAppear`·`onDisappear`가 「열림·닫힘」이 아니다** — 카메라(`UIImagePickerController`)가
> **`.fullScreen` 모달**이라 뜨는 순간 `onDisappear`, 닫힐 때 `onAppear`가 온다.
> ⛔⛔ **그래서 카메라를 열면 `cancelAndDiscard()`가 돌아 녹음한 원본 음성이 지워지고,
> 닫으면 `start()`가 돌아 받아쓰기가 `""`로 리셋됐다.**
> ★★ **신고보다 한 겹 더 나빴다** — 사용자는 「글이 사라진다」를 봤고, **음성 파일도 함께 사라지고 있었다.**
>
> | | 무엇 | 어떻게 |
> |---|---|---|
> | **1** | 사진을 찍어도 **글·녹음·먼저 붙인 사진이 안 사라진다** | 표시 둘로 갈랐다 — `didStart`(시작은 한 번만) · `cameraOpen`(덮는 동안은 종료가 아니다) · `SystemCamera.present`에 **`onFinish`** |
> | **2** | 새 녹음은 **빈 줄 둘**로 이어 붙는다(사용자: *"엔터키 두번"*) | Core **`TranscriptJoin`** — **이음새가 둘이다**(조각 회전 = 빈칸 하나 · 새 녹음 = 빈 줄 둘) · 결정 파수꾼 시험 7 |
> | **3** | **[저장] → 그 기억의 상세 화면** | `capture`가 id를 돌려주고 `model.openDetailId`에 올리고 **`InboxView`가 민다**(입구가 둘이라 배선을 한 자리로) |
> | **4** | **아래로 쓸어도 안 닫힌다** | `.interactiveDismissDisabled(true)` — ★ 이것도 **데이터를 잃는 길**이었다 |
>
> **시험 417 통과**(410 + 7) · iOS·맥 빌드 **`EXIT=0` `error: 0`** · 설치 **`App installed`**.
> ✅ **판정 통과 넷** (2026-08-30 22:49) — 판정 줄은 `docs/install-log.md` 맨 위에 있다.
> ⚠️ **①·②·④는 「없어지던 것이 안 없어진다」라서 직전 빌드와 나란히 볼 수 없었다** —
> **뚜렷한 확정 근거는 ③ 하나**였다(계측 규칙 8). **그래도 넷을 각각 물어 각각 통과를 받았다.**
> ★ **여기서 잰 것: 「안 없어진다」는 판정도 사용자가 밟아 주면 닫힌다** — 나란히 볼 수 없다는 것이
> 「판정할 수 없다」는 아니었다. **볼 것을 넷으로 갈라 적은 것이 그 값을 했다.**
>
> ### ⛔ **물어야 하는 것 하나 — 저장되면 단락이 접힌다**
> `raw`는 create 블록의 **한 줄**이라(`- <날짜> <시각> | <source> | <raw>`) **줄바꿈을 못 담는다.**
> `capture`가 **빈 줄 둘을 빈칸 하나로** 접는다. ★ **그러니 「엔터 두 번」은 편집칸에서만 살아 있다.**
> **정본 꼴을 바꿀지는 사용자 결정 사안이다**(성역·병합에 닿는다).
> ⚠️ **상세의 원문 편집(op)은 줄바꿈을 담을 수 있다** — 못 담는 것은 **create 블록 하나**다.
>
> ### ⏸ **[저장] 버튼 문구** — 사용자가 「[ 저장하기 ]」로 적었는데 **앱의 말은 「저장」**이다.
> 바꿀지는 **사용자가 정한다**(항시 규칙 6). ⛔ **안 바꿨다.**
>
> **전말 = `docs/worklog/2026-08-30-macmini.md`** (재개 한 줄이 그 맨 위에 있다).

---

# ▶▶ 다음에 할 것 — **문서 빚 둘을 닫았다 · 다음은 자료 확장 ②다** (2026-08-30 21:3x)

> ### ✅ 2026-08-30에 닫은 것 셋 (전부 **문서**다 — **앱 코드 0줄 · 폰 설치 없다**)
> | | 무엇 |
> |---|---|
> | **㉠** | `CLAUDE.md` 「세션 시작 절차」 3번에 **「자동 사본은 pull 이전 판이다」**를 올렸다 |
> | **㉢** | `CLAUDE.md` ⓑ의 **「못 잼」 → 「두 맥에서 같았다」**(옛 서술은 남겼다) |
> | **낡은 사실 둘** | 이 문서의 **「폰에 깔린 것」**(설치 셋을 안 따라왔다) · **§1 「코드가 마지막으로 바뀐 커밋」** |
>
> ### ▶ 이 기기에서 오늘 잰 것 (2026-08-30 21:2x · **집 맥미니** · 기기별로 갈리는 값이 섞여 있다)
> | 무엇 | 값 |
> |---|---|
> | `swift test --package-path SecondBrainCore` | **410 통과 · 0 실패 · `error: 0` · `EXIT=0`** (`CLAUDE.md` 값과 맞다) |
> | **코드가 마지막으로 바뀐 커밋** | **`12add9e`**(08-29 00:19) = **폰에 깔린 것과 같다.** 그 뒤 커밋 넷은 문서뿐 |
> | **미확정** | **6개** — `84BE980B` `06A88300` `F5433F49` `99E2B834` `516C17F0` `0E4B8C7F` · **전부 `due` 없음** → **A그룹 표본 조건 여전히 미충족**(§4 「표본이 없어 못 닫은 확인 넷」)<br>⚠️ **08-21의 8개와 대조했다**(그 명단은 `docs/worklog/2026-08-21-macbook.md` §216~217에서 되짚었다 — **8 − 3 + 1 = 6**):<br>　**나갔다 셋 — 전부 「확정됐다」다**(삭제가 아니다 · 2026-08-30 실측): `B3A9A907` `32B23159` `1D5E2FDE` 다 `confirmed=true`.<br>　**들어왔다 하나:** `06A88300`(op 0 · 새 기억들).<br>　**남았다 다섯:** `84BE980B` `F5433F49` `99E2B834` `516C17F0` `0E4B8C7F`.<br>⚠️ **`32B23159`가 A그룹 조건을 스쳤다** — 지금 **`due=2026-08-25` · `resurface=2026-08-28`**를 갖고 있는데 **확정된 뒤**라서 표본이 못 된다. **`4C0882A0`과 같은 순서다**(확정 먼저 → 날짜 나중) — §4의 「충족을 누가 알아채는가」가 여전히 열려 있는 이유다 |
> | **자료 검산**(`media-audit.py`) | audio **124** · photo **64** · url 포인터 7 · **누락·고아·겹침 셋 다 0** (08-23의 140에서 **188로 늘었다**) |
> | 시뮬 UDID | **26.2 = `734ABE20-…`** · **26.5 = `34A5033B-…`**(⛔ **이름 끝에 `(26.5)`가 붙은 쪽** · 지금 부팅돼 있다) |
> | 폰이 마지막으로 쓴 시각 | `inbox-iphone-4532.md` **2026-08-30 16:15** — **오늘 새 수집이 있었다** |
>
> ### ⏸ **㉣ 08-30 worklog가 없다** (2026-08-30 21:3x)
> **08-29의 판정과 08-30의 사양서 반영(`0582135`)이 worklog에 없다** — 08-29 것은
> `docs/worklog/2026-08-28-macmini.md`(세션이 자정을 넘었다)에 들어 있지만
> **08-30 사양서 반영은 커밋 메시지에만 있다.** 이 세션 종료 때 **`docs/worklog/2026-08-30-macmini.md`**로 남긴다.
>
> ### ⏸⏸ **㉤ 성역 빚 — create 블록 필드 목록을 열어야 한다** (2026-08-31 신설 · 사용자 결정 ⓑ)
> **오늘 수집 사진·URL이 op으로 갔다**(카드가 여럿을 받으므로). 그런데 사용자가
> **「원칙을 좁히지 말고 구현이 못 지킨 것으로 적으라」**(갈래 ⓑ)로 정했다.
> → **정본의 원칙은 그대로고**(`edit-policy.md` §6-② *"그때 있던 자료는 전부 성역·불변"*)
> **구현이 절반만 지킨다**(성역에 남는 자료는 **음성 하나**).
> ▶ **빚을 갚는 자리 = `media-expansion-design.md` §3-W-4 1번**
> (`EventWriter`의 create 필드 **고정 목록 → 접두어 허용**).
> ⛔ **그 항목이 「필요 없어졌다」가 아니라 「이제 필요해졌다」로 뒤집혔다** — 조건이 실제로 왔다.
> ⚠️ **그동안 「첫 자료 = 수집 당시의 원본」은 데이터가 아니라 「붙은 순서」로만 참이다**(사양서 §378).
>
> ### ✅ 정본 훑기를 돌렸다 (2026-08-31 · 축 둘)
> **축 ⑴ 수집 사진 create → op** · **축 ⑵ 원문 한 줄 → 줄바꿈 가능(`RawLine`).**
> **고친 자리 열하나 + 보강 셋:** 사양서 **§243·§294·§378** · `edit-policy.md` **§63·§6-②** ·
> `photo-capture-design.md` **§34·§161·§203** · `media-expansion-design.md` **§2071·§6-10·§3-W-4-1** ·
> **`automation/README.md` 신설 절**(되살릴 때 먼저 고쳐야 하는 것 — `classify.py`·웹 v0가 새 꼴을 못 읽는다).
> ⛔ **옛 서술을 하나도 지우지 않았다**(정본 훑기 규칙 5).
> ✅ **`merge-design.md`에는 걸리는 문장이 없었다**(grep 0건) — **병합 규칙은 안 건드렸다.**
>
> ### ▶ **그래서 다음 줄기는 §3의 「열려 있는 것 하나」 = 자료 확장 ②다**
> 남은 것: **대표 사진**(필드별 HLC) · **자료 삭제** · **자료마다 상태** · **추가 촬영의 EXIF 위치** ·
> **사양서 반영**(구현 뒤 · §5) · **동영상은 맨 뒤**(용량 · `CLAUDE.md` 「미뤄둔 것」).
> ⛔ **방향은 즉흥으로 정하지 않는다**(항시 규칙 1) — 어느 것부터 열지는 **사용자가 고른다.**


> ### ✅ **㉠ `CLAUDE.md` 「세션 시작 절차」에 올렸다** (2026-08-30 21:2x · 절차 **3의 하위 셋**)
> **어디에:** `CLAUDE.md` 「세션 시작 절차」 **3번**(*"그다음에 HANDOFF·worklog·정본을 읽는다"*) 아래.
> **거꾸로 쓰기를 함께 적었다**(규칙 쓰는 법): *"인용한 값이 실측과 어긋나면
> 「내 기억이 틀렸다」보다 **「내가 읽은 사본이 낡았다」를 먼저 의심한다.**"*
>
> *(옛 서술 · **2026-08-30에 닫혔다** — 아래가 그때 받은 지시 전문이다.)*
> ### ⏸ ㉠ (지시 전문 · 지우지 않는다) (2026-08-28 01:4x 사용자 지시)
> **무엇을:** *"**자동으로 실리는 `CLAUDE.md` 사본은 `git pull` 이전 판이다.**
> 절차대로 pull을 먼저 해도 **그 사본은 이미 읽힌 뒤**다 — 거기서 **값을 인용할 때는 파일을 다시 읽는다.**
> 특히 **그 pull이 `CLAUDE.md`를 건드렸을 때.**"*
> **왜:** 2026-08-28에 실제로 밟았다 — 시험 수를 **374**로 말했는데 파일은 **이미 410**이었다
> (그날 pull이 가져온 `496cde3`이 그 파일을 고친 것이었다).
> **아무것도 안 깨져서 안 걸릴 뻔했다** — 사양서 반영하러 실제 자리를 찾다가 드러났다.
> ⛔ **오늘 안 올렸다** — **항시 규칙을 고치는 일**이라 종료 중에 임의로 하지 않았고,
> **사용자가 「다음 세션에 올려라」고 정했다.** 전말은 `docs/worklog/2026-08-28-macmini.md` §4.
>
> ### ✅ **㉡ 검색 결과의 화면 이름 색 — 닫혔다** (2026-08-29 **00:19** · 커밋 `12add9e`)
> **무엇으로 닫혔나:** `textTertiary` → **`textSecondary` #A7A4B3(L\* 68.0 · 7.63:1)** =
> **날짜와 원문의 중간.** 사용자가 *"날짜 색깔과 원문 색깔의 중간 정도"*로 정했다.
> 폰 설치 **00:19** → **00:19~00:51 사이 폰 판정 통과**(*"적당한 색깔로 설정된 것 확인했음! 완료!"*).
> **물음의 답:** 「날짜와 갈라야 하나 같이 올려야 하나」 → **갈랐다**(날짜는 #746F82 그대로).
> ⛔ **왕복 넷을 코드 주석에 남겼다** — ①흐리다 ②덜 바꿨다 ③**너무 밝다** ④중간.
> **어느 한 쪽으로 다시 밀지 말 것:** ①에서 날짜와 같게 → 반려 · ③에서 원문과 같게 → 반려.
> ★ **새 색을 만들지 않았다** — 진짜 중간(#AEABB8 L\* 70.6)과 **L\*로 2.6밖에 안 벌어져서**
> 팔레트에 이미 있는 토큰을 썼다(사용자가 고름).
> 전말·내가 틀린 것 셋 = `docs/worklog/2026-08-28-macmini.md` **§7·§8**.
>
> *(옛 서술 · **08-29 00:19에 갱신** — *"✅ 닫혔다(08-28 23:2x · `a83e95d`) ·
> `textPrimary` #ECEBF1 = 원문과 같은 밝기 · ⛔ 중간값으로 내리지 말 것"*.
> **폰에서 「너무 밝다」가 나와 정확히 그 중간값으로 내려앉았다** — 그 ⛔가 반나절도 못 갔다.)*
>
> ### ⏸ **㉢ CoreDevice UUID — `CLAUDE.md` ⓑ의 「못 잼」을 고칠 것** (2026-08-28 23:2x)
> ⓑ가 *"CoreDevice UUID가 맥마다 같은지는 **못 잼**"*으로 적어 두었는데 **오늘 쟀다:**
> **맥미니도 `95CF732E-D847-5594-A346-EC354A1D8B6C`** — 맥북 08-26 값과 **같다.**
> ⚠️ **「그 기기에서 다시 읽는다」는 그대로 둔다**(공짜고 확실하다) — 고칠 것은 **「못 잼」 한 낱말**이다.
>
> ### ✅ **㉢도 닫혔다** (2026-08-30 21:2x)
> `CLAUDE.md` ⓑ의 「못 잼」을 **「두 맥에서 같았다」**로 고쳤고, **옛 서술을 그 자리에 남겼다.**
> ⚠️ **「그 기기에서 다시 읽는다」는 안 건드렸다** — 그리고 **「표본이 두 맥뿐이다」**를 더했다
> (둘이 같은 것이 「모든 맥에서 같다」는 아니다 · 계측 규칙 7).
>
> *(옛 서술 · **2026-08-28 23:2x에 닫혔다** — *"⏸ ㉡ 검색 결과의 화면 이름 색이 흐리다.
> ⛔ 오늘 안 고쳤다 — 빠뜨린 것이 아니라 사용자가 미룬 것이다. 지금은 `Palette.textTertiary`로
> 같은 줄의 날짜와 같은 색이다. 고칠 때 먼저 물을 것: 날짜와 갈라야 하나 같이 올려야 하나."*)*
>
> *(옛 서술 · **2026-08-28 01:4x에 정정** — *"`CLAUDE.md`의 시험 수 「374개」가 낡았다"*.
> **틀렸다. 이미 410으로 맞아 있었다**(08-26 맥북 실측). 내가 **세션 시작 때 자동으로 실린 사본**을
> 읽었는데 **그것은 `git pull` 이전 판**이었다 — 그 pull이 `CLAUDE.md`를 갱신했다.
> ⚠️ **세션 시작 절차의 사각이다:** 「pull 먼저, 그 다음 문서를 읽는다」를 지켜도
> **자동으로 실린 `CLAUDE.md`는 이미 낡은 판**이다. **거기서 값을 인용할 때는 파일을 다시 읽는다.**)*

---

# ▶ 오늘(08-27) 한 것 — **시험용 되풀이 열을 그만뒀다**

> ### ✅ **되풀이 12개 → 2개** (09:23에 썼다 · **20:2x 사용자 판정 통과**)
> *"12개에서 2개로 줄어든 것 확인했음!!!"*
> **왜:** 시험용 되풀이가 빨간 D+로 쌓여 *"실제로 중요한 것들을 놓치게 하는 방해 요소"*였다.
> **남긴 둘:** `E036A094`「아침 간염 약 먹기!」 · `616D4DCE`「주차 위치」.
> **어떻게:** 새 파일 `inbox-cleanup-20260827.md`에 `delete` op 열 줄 —
> ⛔ **기존 파일은 한 글자도 안 건드렸다**(앱이 `inbox*`를 전부 읽는다).
> **백업:** iCloud `backups/2026-08-27-cleanup-20260827-091944/`.
> ✅ **되돌릴 길 둘:** 「보관된 기억」의 「되돌리기」 · **그 파일을 지우면** 원상복구.
> ⛔ **앱 코드 0줄 · 폰 설치 없다** — 폰 = **`820e20e`**(08-26 20:25) 그대로다.
> **도구(재실행 금지):** `native/tools/2026-08-27-retire-test-recurrences.py`

---

# *(앞 세션)* 낡은 캡쳐를 갱신할 길을 만들었다 (2026-08-26 20:3x · 회사 맥북)

> ### ✅ 이 세션에 닫힌 것 넷 — **전부 사용자 판정 통과**
> | 무엇 | 판정 |
> |---|---|
> | **폰의 이름 띠** (나흘·세 세션 밀렸던 판정) | *"네모 아랫쪽에 도메인 이름 띠가 보임. inven이라 써있음."* |
> | **「미리보기 다시 받기」** — URL 네모 길게 누르기 | *"1 2 3 다 확인했어 … 모두 성공이야."* |
> | **낡은 캐시가 페이지 첫 화면으로 갱신된다** + 「내려받는 중」 | 같은 판정 (`wowanalytica` 171×171 → 캡쳐) |
> | **`wow.inven.com`은 여전히 ①꼴** | 같은 판정 — ⛔ **안 고쳐지는 것이 맞다**(TLS 거부 = 그 사이트 문제) |
>
> ### ⛔ 남이 적어 둔 다음 과제가 **틀린 진단**이었다
> 08-25가 *"늦게 그려지는 페이지를 더 기다리는 것"*으로 남겼는데, 재 보니 **TLS 거부**였다
> (인증서가 `CN=*.inven.co.kr`뿐 · `.com` 이름 없음). **기다려서 되는 일이 아니었다.**
> **그대로 했으면 헛일을 하고 「고쳤다」고 적었을 것이다.**
>
> ### ⛔⛔ 그리고 **`rule` 판이 한 번도 일을 안 하고 있었다**
> 08-25에 만든 캐시 판이 *"올리면 다시 뽑힌다"*고 주석에 적혀 있었는데 — **다시 뽑히지 않는다**
> (`fetchOnce`가 `addURL`에서만 불린다). **판을 올리면 캡쳐가 나아지는 게 아니라 전부 사라진다.**
> ✅ **그래서 판을 안 올리고 갱신 수단을 따로 만들었다**(사용자 결정 ㉟).
>
> ### ▶ **전말 = `docs/worklog/2026-08-26-macbook.md`** · **정본 = `media-expansion-design.md` §3-Z-16**
> **정본 훑기를 돌렸다** — 축이 바뀌어(「연결은 붙일 때 한 번만」 → 「+ 사용자가 누를 때」)
> **낡은 문장 아홉**이 나왔고 고쳤다(사양서 3 · 설계 6 · **옛 서술은 남겼다**).

---

# *(앞 세션)* 맥에서 자료가 여럿인 자리를 고쳤다 (2026-08-26 01:0x · 맥미니)

> ### ✅ 이 세션에 닫힌 것 넷 — **전부 사용자 판정 통과**
> | 무엇 | 판정 |
> |---|---|
> | **카드 테두리가 자료 전부 중 「가장 나쁜 것」을 말한다** | *"사진 네모 테두리가 앰버임!!"* |
> | **뷰어에서 넘어간 그 장을 받아온다** | *"뷰어를 띄우니까 정상적으로 내려왔어"* |
> | **「내려받는 중」 토스트** (앱에 이미 있던 말이다) | *"내려받을 때 토스트 뜬다"* |
> | **맥에서 썸네일 줄이 사진 종류에 따라 움직이던 것** | *"위치는 고정이 되었어"* |
>
> ⛔ **이 세션에 폰 설치가 없다** — 맥에서 빌드해 **맥에서** 돌렸다. **폰은 아래 배너 그대로다.**
> ⏸ **폰에서 이 넷이 어떻게 보이나는 안 봤다** — 폰은 로컬 사본이 남아 **늘 「여기 있다」**라서
> **앰버·토스트가 사실상 안 나온다**(그 상태를 만나는 것이 맥이다).
>
> ### ▶ **전말과 다음 할 일 = `docs/worklog/2026-08-25-macmini.md`** (재개 한 줄이 그 맨 위에 있다)
> **다음은 회사 맥북이다**(사용자: *"내일 맥북으로 가서 계속할테니"*).
> ⚠️ **dataless 수는 기기별이다** — 앰버 표본을 **맥북에서 다시 세야 한다.**

---

# ✅ 폰 상태 — 판정 끝났다

> ## 폰에 깔린 것 = **`eceb868`** (2026-08-31 **05:21** · **집 맥미니에서**) — ⏸ **판정 대기 셋**
>
> **확정 근거: 좌측 상단이 아이콘만이 아니라 「‹ 새 기억」(글자가 보인다).**
> 직전 빌드(`f102879`)는 **동그라미 안 `‹` 아이콘뿐**이었다.
> ⏸ **볼 것 셋은 `docs/install-log.md` 맨 위 줄에 있다.** ⛔ 「통과」로 적지 않는다(기록 규칙 7).
> ▶ **이력 전부는 `docs/install-log.md`** — **08-30 저녁부터 오늘 새벽까지 열두 줄이 쌓였다.**
>
> ---
>
> ### *(그 앞 빌드)* **`8251d53`** (2026-08-31 **00:52** 설치) — ✅ **판정 끝났다**
>
> **확정 근거: [저장]을 누르면 「그 기억의 상세 화면」이 열린다** — 직전 빌드(`78a4362`)는 목록에서 멈췄다.
> ✅ **판정 통과** (2026-08-31 00:57) — *"저장하니까 상세화면으로 잘 넘어가. < 아이콘도 하나야."*
> ▶ **이력 전부는 `docs/install-log.md`**(오늘만 다섯 줄이 쌓였다 — 왕복이 그만큼 있었다).
>
> ---
>
> ### *(그 앞 빌드)* **`06aff97`** (2026-08-30 **21:42** 설치) — ✅ **판정 끝났다**
>
> **확정 근거: 수집 화면에서 [저장]을 누르면 목록으로 나오지 않고 「그 기억의 상세 화면」이 열린다.**
> 직전 빌드(`12add9e`)는 **목록으로 나왔다.** 곁: **아래로 쓸어도 수집 화면이 안 닫힌다.**
> ✅ **넷 다 판정 통과** (2026-08-30 **22:49**) — *"잘 됨!!! 1 2 3 4 모두통과!!"*
> **미확인 없다.** 판정 줄은 `docs/install-log.md` 맨 위에 있다.
> *(옛 서술: "⏸ 판정 대기 — 다음 세션이 이것부터 확인받는다.")*
> ▶ **이력 전부는 `docs/install-log.md`** (여기엔 최신 한 줄만 — 기록 규칙 2).
> ✅ **폰 = 코드 최신이다** — `06aff97`이 **`native/`를 마지막으로 만진 커밋**이다.
>
> ---
>
> ### *(그 앞 빌드)* **`12add9e`** (2026-08-29 **00:19** 설치 · **집 맥미니에서**)
>
> **확정 근거: 검색 결과 한 줄 안에서 밝기가 세 층이다** — 원문 > **화면 이름** > 날짜.
> ✅ **판정 통과** (2026-08-29 00:19~00:51) — *"폰에서 적당한 색깔로 설정된 것 확인했음! 완료!"*
>
> ⛔ **옛 서술(낡아 있었다 · 2026-08-30 정정) — 설치 셋을 안 따라왔다:**
> *"폰에 깔린 것 = **2026-08-26 20:25 빌드**(회사 맥북에서)"* — 그 뒤 **`52557a1`(08-28 01:31) ·
> `a83e95d`(08-28 23:26) · `12add9e`(08-29 00:19)**가 깔렸고 **셋 다 판정을 통과했다.**
> ★ **이 배너가 낡는 것이 두 번째다** — 08-25에도 **나흘 낡아 있었다**(아래 블록).
> **그때 「최신 한 줄 + 링크만」으로 줄였는데도 또 밀렸다** — 줄이는 것으로는 안 막힌다.
>
> ---
>
> ### *(그 앞 빌드)* **`820e20e`** (2026-08-26 20:25 설치 · **회사 맥북에서**)
>
> **확정 근거: URL 네모를 길게 누르면 「미리보기 다시 받기」 메뉴가 뜬다** —
> 직전 빌드(`a8e99bc`)에는 **그 메뉴가 아예 없었다.**
> ✅ **판정 통과 셋** (2026-08-26 20:3x) — *"1 2 3 다 확인했어. 너의 안내대로 동작하고 있어. 모두 성공이야."*
>
> ---
>
> ### *(그 앞 빌드)* **`a8e99bc`** (2026-08-25 16:55 설치 · **회사 맥북에서**)
>
> **확정 근거: URL 네모의 그림 아래에 도메인 이름 띠가 있다**(흰 글자 + 검은 반투명).
> ✅ **판정 통과** (2026-08-26 19:5x · 맥북) — 사용자: *"네모 아랫쪽에 도메인 이름 띠가 보임. inven이라 써있음."*
> **표본 = `85346E05`「사냥감 추적자…」** — 첫째 URL `wow.inven.co.kr` · 캡쳐 **558×558**(08-25 16:51 = **그 빌드가 뽑은 것**).
> ★ **표본을 데이터에서 계산해 골랐다** — 폰의 캡쳐 캐시를 맥으로 가져와 **어느 URL에 그림이 있나**를 먼저 짝지었다(계측 규칙 8 — 띠는 **그림이 있을 때만** 얹히므로 그림 없는 항목은 표본이 못 된다).
> *(옛 서술: "⏸ **판정 대기** — 물었지만 사용자가 다른 물음으로 넘어가 답이 안 왔다. ⛔ 「통과」로 적지 않았다(기록 규칙 7) — 다음 세션이 먼저 이것부터 확인받는다.")*
> ✅ **그 앞 빌드(16:43)는 통과했다** — *"이제 제대로 가져오네"*(페이지 첫 화면 캡쳐).
>
> ⛔ **옛 서술(2026-08-25 17:1x 정정):** *"폰에 깔린 것 = `a5d0c3d`(01:33) · 확정 근거: URL 목록의 X가
> 동그라미 없는 빨간 X … ✅ 판정 통과 — 「1 2 확인됨」"* — **그날 오후에 다섯 번 더 깔렸다.**
> ### ▶ **설치 이력 전부는 `docs/install-log.md`** (여기엔 최신 한 줄만 둔다 — 기록 규칙 2)
> **전말 = `docs/worklog/2026-08-25-macbook.md`** (재개 한 줄이 그 맨 위에 있다)
>
> ### ⛔ 이 배너가 **나흘 낡아 있었다** (2026-08-25 정정)
> **옛 서술(지우지 않고 남긴다):** *"폰에 깔린 것 = `7cd8356`(2026-08-21 17:33 설치) ·
> 확정 근거: 원문 편집 칸이 6줄에서 멈추고 그 안에서 스크롤된다 · 편집 중에도 부호가 줄 앞에 안 온다.
> 그 뒤 소스 커밋은 `251e2dc` 하나이고 층 이동뿐이다 … ⛔ 일부러 안 깔았다."*
> ⚠️ **그 뒤로 08-23·08-24에 실제로 여러 번 깔렸다** — 그런데 **이 배너만 안 고쳐졌다.**
> ⛔ **같은 「폰에 깔린 것」이 이 문서 안에서 세 값이었다**(배너 `7cd8356` · §1 `1f1d2f8` · §3-A `60c12e1`).
> **한 곳만 고치면 통과로 보인다** — 08-24 종료 점검이 §3-A만 보고 「✅ 반영」으로 적었다.
> ★ **install-log로 이력을 모은 이유가 바로 이것이고**(자리가 나뉘면 어느 쪽도 전부를 못 갖는다),
> **그 교훈이 이 배너에는 안 닿아 있었다** — 「최신 한 줄 + 링크만」으로 줄여 둔다.
>
> ✅ **좌우 맞춤 판정 전부 통과**(2026-08-21 저녁) — *"4개 모두 정상 확인 됨!"*
> 부호 · 6줄 크기 · 스크롤 · 짧은 원문에서 바깥 스크롤 · 영어·숫자. **미확인 없음.**
>
> ### ✅ 원칙 목록 — 요구가 전부 착지했다 (2026-08-21 사용자 판정)
> *"이제 안 죽어. 세 경우 다 통과. 여백도 지금 이 상태가 좋아. 더 볼 것 없어."*
>
> | 무엇 | 어느 커밋에서 됐나 |
> |---|---|
> | 끌기가 살아 있고 순서가 남는다 | `11fd8cb` → **`50a8e48`**(UIKit 대화식 이동) |
> | **잔상 없음** — 살짝 끌어도 원래 자리가 빈다 | **`50a8e48`** |
> | **테두리** — 원칙 영역 짜임 차용(바탕 `0.14` · 테두리 `0.28` · 숫자) | **`c6ed58b`** |
> | 끌고 있는 도중 번호·바탕색이 따라온다 | `069f2e9` |
> | 놓아도 안 죽는다 | **`c756655`** |
> | 글자 = 원칙 영역 **+2pt** · 접근성 따라감 | `fa0f689` |
> | 「대기」 제거 · 항목 바탕색 · 카드 사이 6pt | `6ec812f` · `c6ed58b` |
> | 줄바꿈 고정 · ≡ 손잡이 제거 | `26c6560` · `30e2d91` |
>
> **미확인 없음.**
>
> ### ⛔ 2026-08-20에 「못 했다」로 닫아 둔 것이 있었다 — **뒤집혔다 (2026-08-21 정정)**
> **옛 서술 (지우지 않는다):**
> *"⛔ 「끄는 동안 그 줄만 테두리」는 못 했다 — 없앴다"* ·
> 사용자: *"테두리는 없애자. 너가 못 하는 것으로 생각해."* ·
> *"⛔ 다시 시도하지 말 것. 탈출구는 `List`를 버리고 `ScrollView`+직접 만든 순서 바꾸기뿐이고,
> 그것은 잔상 과제와 같은 탈출구다 — 둘을 묶어 값을 치를지 사용자가 정한다."*
>
> **무엇이 틀렸나 — 둘 다 같은 날 됐다:**
> ① **잔상:** 「값을 치를지 정한다」던 그 탈출구를 **실제로 걸었고 통했다**(`50a8e48`).
>    단 `ScrollView`가 아니라 **`UICollectionView` 대화식 이동**이었고, 랩 실측이 길을 골랐다 —
>    `List`는 흐린 사본이 남고(F), **편집모드도 똑같이 남고**(G), UIKit만 비었다(H).
> ② **테두리:** 「지금 끌리는 줄」을 **감지해서** 만들려다 끄는 시각이 흔들린 것이고,
>    **원칙 영역의 상시 테두리를 차용**하니 **감지 자체가 필요 없어졌다**(`c6ed58b`).
>    끌리는 줄의 구분은 **iOS 기본 들어올림 + 따라오는 번호·바탕색**이 한다.
>
> ★ **그러니 「못 한다」가 아니라 「그 길로는 못 한다」였다.**
>
> ### ★★ 무엇이 되게 했나 — **감지를 버린 것이 답이었다**
> ★ **되게 한 것은 「감지를 정교하게 만든 것」이 아니다 — 「감지가 필요 없어지게 한 것」이다.** 이미 있는 짜임(원칙 영역)을 빌려오니 **「지금 어느 줄이 끌리나」를 알 필요가 없어졌다.** **문제를 고친 것이 아니라 문제가 생길 자리를 없앴다** — 다음에 같은 벽을 만나면 쓸 형태다: **막힌 자리를 정교하게 만들기 전에, 그 자리가 필요 없어지는 짜임이 이미 앱 안에 있나 본다.** 판단이 틀린 채로 「하지 말 것」이 남으면
> 다음 세션은 **성공한 길을 금지된 길로 읽는다** — §4 머리의 **일곱 번째 형태**가 이것이다.
>
> ✅ **유지되는 것 (되밟지 말 것 — 이건 그대로다):**
> `LongPressGesture`로 돌아가지 말 것(`d1749e2`·`7e643f4`에서 드래그를 **두 번** 죽였다) ·
> **원본 숨기기**(`4dc8b9c`·`e2e4dfb` — 사본까지 죽는다) · **≡ 손잡이 되살리기**(`30e2d91`,
> 사용자: *"이거 뭐야?"*) · 머리글 둘째 줄 「눌러 끌어서 순서를 바꾸세요」를 **지우지 말 것**
> (지우면 「끌 수 있다」 신호가 하나도 안 남는다) · **맥은 `List` 그대로다**
> (`UIViewRepresentable`은 iOS 전용 — 줄 모양은 `PrincipleCard` 하나로 합쳐져 있다).

---

## ✅ 해소됨 — classify.py launchd (2026-08-19)

**두 기기 모두 확인 끝. 더 볼 것 없다.**

| | |
|---|---|
| 회사 맥북 | ✅ **차단 완료** (2026-08-18) |
| 집 맥미니 | ✅ **애초에 설치돼 있지 않았다** (2026-08-19 확인) |

확인한 셋: `launchctl print` **없음** · `~/Library/LaunchAgents`에 plist **없음** · `crontab` **없음**.
**결정적 근거는 파일 쪽이다** — `inbox.md`의 마지막 수정이 **2026-08-18 15:01**(맥북의 데이터 정리 시각) 그대로다.
launchd가 살아 있었다면 16:00·17:00…에 덮어썼을 것이다. **08-18 데이터 정리는 온전하다.**

⚠️ `python3 classify.py`를 **손으로 돌리지 않는다** — 자동 분류는 재개발 예정
(`docs/native/memory-philosophy.md` §2-1-B).

---

## ✅ 자료를 iCloud로 — **이관 완료** (2026-08-20 00:14)

**폰의 자료가 전부 iCloud에 있다. 「폰을 잃으면 자료를 전부 잃는다」가 닫혔다.**

| | |
|---|---|
| 올라간 것 | **135개** — `audio/` **106** · `photo/` **29** |
| 검산 | 포인터 고유 수와 **1:1** · **고아 0 · 누락 0** |
| 실패 로그 | **0줄** (`.sb-media.log`는 자리 두 줄뿐) |
| 찌꺼기 | **0개** |
| **㉠ 하위 폴더 생성** | ✅ **된다** — `place=subdir` 둘 · **폴백 안 씀** |

⚠️ **「131」은 낡은 값이다** — 08-19 01:0x의 수. 그 뒤 폰 실사용으로 4개가 늘어 **이관 시점엔 135**였다.
업로더가 **차집합으로 계산**하므로 늘어난 것도 그냥 따라왔다(자세히: 설계 §0-A · worklog §6-D).

⚠️ **맥의 사본 135개는 전부 `NotDownloaded`다** — 이름은 있고 바이트가 없다(설계 §0-B).
**정상이고**, 단계 5 검증 재료가 그것이다.

---

## 1. 마지막 커밋 단계

| | |
|---|---|
| 브랜치 | `main` 하나 · worktree 하나 · **clean** |
| 저장소 | **`main` 하나 · worktree 하나 · clean · 원격과 동기**(2026-08-25 맥북에서 푸시) |
| ⚠️ **기기가 하나 늘었다** | **맥이 쓰는 기기로 들어왔다**(2026-08-25 19:34 — 맥 버전을 처음 돌려 폴더를 고른 순간 **`inbox-dev-55e9c56d.md`**가 생겼다). ⛔ **맥에서 확정·삭제한 것도 iCloud로 나간다.**<br>⛔ **맥에서는 URL 네모가 막다른 길이다** — 앱 안 보기가 iOS 전용이라 뷰어로 가고 회색 아이콘만 보인다(⏸ 어떻게 열지는 사용자 결정 대기 · 설계 **§3-Z-14**) |
| ⚠️ **HEAD 해시를 여기 안 적는다** | **커밋마다 낡는다** — 정본 훑기 규칙 7의 「지금 무엇이 있나」에 해당한다. **보고 싶으면 `git log`를 읽는다.**<br>⛔ **옛 서술(2026-08-25 정정):** *"HEAD `1af11f3` (01:3x 맥북 · clean)"* — **적자마자 다음 커밋에서 낡았다** |
| **코드가 마지막으로 바뀐 커밋** | **`eceb868`** (2026-08-31 05:2x) — ✅ **폰과 같다**<br>**세는 법:** `git log -- native/` (2026-08-30 21:2x 맥미니 실측)<br>⛔ **옛 서술(낡았다 · 2026-08-30 정정):** *"**`a5d0c3d`**(2026-08-25 · 「지우기」 문구 + 빨간 X) — ✅ 폰과 같다"* — **그 뒤 코드가 세 번 바뀌었다**(`52557a1`·`a83e95d`·`12add9e`)<br>설치는 **전부 `docs/install-log.md`** 에 있다<br>⛔ **옛 서술(낡았다 · 2026-08-25 정정):** *"HEAD `3341a88`+ (08-23 17:4x 맥미니) · 폰 = `1f1d2f8` · 코드가 마지막으로 바뀐 커밋 `251e2dc`(08-21)"* — **08-23·08-24·08-25에 코드가 여러 번 바뀌었는데 이 줄이 안 따라왔다** |

**2026-08-20 맥미니 세션 (단계 5 — 스코프 C안 · 폴링 · Mac 사진 · 지도 핀):**

| 커밋 | 무엇 |
|---|---|
| **`0727499`** | **★ 코드 — 지도 핀 `q=`** (**27일** 묵은 결함 · **아이폰도 같았다**) · `MapsLink`(Core) |
| `9e52895` | 맥 앱 실행 기록 · 예측 대조 셋 · 표본 계산 오류를 어제 값으로 잡았다 |
| **`1dba4f8`** | **★ 코드 — 단계 5: 스코프 C안(받으면 로컬로) · 폴링 0.2초 · Mac 사진 보기** |
| `df962cd` | 다운로드 실측(0.53~0.92초) · **「15초가 아니라 1초 폴링이 문제」** · CLAUDE.md 규칙 둘 |

**2026-08-19~20 맥북 세션 (자료를 iCloud로 — 단계 1~4):**

| 커밋 | 무엇 |
|---|---|
| `5a6ccd4` | 이관 완료 135/135 · 「131은 낡은 값」 · §0-B 함정 전수 재현 · §10 evict 삭제 |
| `9b505f0` | 사용자 판정 셋 통과 · **맥 사본이 dataless로 실측** |
| `b7866e6` | **㉠의 답 — 하위 폴더 생성은 된다** · 폴백 기록 채움 |
| `942fe8e` | worklog — 설치 18:52 · 앱 열기 전 기준선 0 |
| **`f7018d6`** | **★ 코드 — 단계 3+4: 업로더(이관) + 받는 중·한도 배너** |
| **`7d3d987`** | **★ 코드 — 단계 2: 찾기를 세 갈래로** |
| **`ce16b55`** | **★ 코드 — 단계 1: iCloud 자리 계산 + `.sb-media.log`** · 결정 넷 기록 |

**`f43a84d` 이후 (그 전):**

| 커밋 | 무엇 |
|---|---|
| *(08-19 맥미니)* | **`media-icloud-design.md` 신설** · HANDOFF 로드맵 · worklog. **코드 0줄** |
| `4df0b02` | 규칙(푸시는 지시 후) · 맥미니 경고를 맨 위로 · 미룬 것 셋 |
| `c6ed9a2` | 실기기 확인 다섯 통과 — 「봐야 하는 것」 0 |
| `975767b` | 정본 ① — §1의 「알림이 울릴 수 있다」를 대조표로 뒤집었다 |
| `f04e9f1` | **정본 훑기 규칙**(CLAUDE.md + lessons) · **설치 기록** |
| `45d4f62` | 정본 ②③⑥ · 사양서 ④⑤ · `sample-pending-checks.md` 신설 |
| **`dbbafc8`** | **★ 코드 — `catchUpRecurrence`도 확정만 전진(게이트 셋 완성)** |
| `a07b06b` | 정본 — §2-1-B 신설 · §3 좁힘 · 축 구분 셋 |
| **`ce0142e`** | **★ 코드 — 목록에서 완료 제거 · 알림·파티션을 확정 축으로 · 자동 분류 일시 중지** |
| `2b2edcd` | HANDOFF 갱신(미확정 4→6 · 상세 완료 게이트) |
| `2b83359` | HANDOFF.md 신설 |
| `a53038b` | 맥북 인계 — `inbox-state.py` 신설 · 삭제 둘 정정 |
| `947f686` | 세션 인계 문서(`docs/handoff/2026-08-18-session-for-claude-chat.md`) |
| `de1d936` | 되풀이 시험 항목 조사 worklog (**아무것도 안 지웠다**) |

**직전 코드 커밋 둘 (2026-08-18 맥미니, 「임시」 표시 재배치):**

| 커밋 | 무엇 |
|---|---|
| `f43a84d` | 나가기 경고 문구를 **상태별로 가른다** (임시/확정) |
| `c045f31` | **「임시」 표시는 결정 버튼이 없는 곳에만** — 상세에서 빼고 목록 셋에 **캡션 줄**로 |

**마지막으로 확인한 값** — ⚠️ **행마다 어느 기기·언제인지 갈라 적는다**(시뮬 UDID가 기기마다 다르다):

| 검증 | 결과 |
|---|---|
| `swift test --package-path SecondBrainCore` | ✅ **386개 · 실패 0** — **2026-08-21 저녁 맥북 실측**(374 → **+12**: 줄바꿈 원칙 셋)<br>⛔ **옛 값 「372」·「374」는 낡았다** — 372는 08-20 맥미니, 374는 08-21 오전(Core 이동 전) |
| `xcodegen generate` | ✅ Created project — ⚠️ **새 파일을 넣으면 반드시 다시 돌린다**(안 하면 `cannot find … in scope`) |
| iOS 빌드 (스킴 `SecondBrainApp-iOS`) — **맥북 `ED17E1A9-8F2A-4994-AAD2-C9AC46177EB5`**(2026-08-20 · `c756655`) · 맥미니 `34A5033B-BCFC-4FF9-AD1B-7756397C09DE`(08-20 · `0727499`) | ✅ EXIT=0 · `error:` **0** · `warning:` **0** |
| macOS 빌드 (스킴 `SecondBrainApp-macOS`) | ✅ EXIT=0 · `error:` **0** · 코드 경고 **0** (AppIntents 툴체인 알림 1건은 코드가 아니다) |
| 실기기 빌드·설치 (`ter.iPhone 16p` · `95CF732E-D847-5594-A346-EC354A1D8B6C`) | ✅ EXIT=0 → `devicectl device install app` 성공 |

> ⚠️ **시뮬 UDID는 기기마다 다르다.** 맥북은 `ED17E1A9-…`, 맥미니는 `34A5033B-…`.
> ⛔ **이름으로 쓰면 안 된다** — 맥미니에는 **iOS 26.5에 `iPhone 16 Pro (26.5)`**와
> **iOS 26.2에 `iPhone 16 Pro`**가 있어 `name=iPhone 16 Pro`는 **26.2로 간다.**
> **그리고 빌드는 `EXIT=0`으로 성공한다 — 로그로는 알 수 없다**(2026-08-20 실측 · `CLAUDE.md` 빌드 ③).
> 매번 `xcrun simctl list devices available`로 확인할 것.

---

## 2. 실기기 검증 상태

| | |
|---|---|
| 폰 | iPhone 16 Pro · **`c756655`** 설치 (**2026-08-21 00:23** · 회사 맥북에서) |
| **저장소 코드와 같은가** | ✅ **같다** — 그 뒤 커밋은 문서뿐 |
| **미확인으로 남은 빌드** | **없다** |
| **이 빌드의 확정 근거** | **끌어서 놓아도 앱이 안 죽는다** (직전 `069f2e9`은 놓는 순간 죽었다) |
| **미확인** | **없다** — 원칙 목록 판정 **전부 통과**(2026-08-21) |
| **맥 앱 (`0727499`)** | ✅ **판정 다섯 전부 통과** (2026-08-20 01:3x~01:4x) — §2-B. **맥 앱이 처음으로 실기기 검증됐다** |

> ⚠️ **폰은 두 맥 어디서나 붙는다** — 회사 맥북에서도 `devicectl`에 `available (paired)`로
> 잡혔고 설치가 됐다(2026-08-20 16:36 실측). **기기를 옮겼다고 설치를 미룰 이유가 없다.**
> ⛔ 설치 성공은 **`App installed` 줄**로 본다 — `exit code`는 `grep`이 낸 값일 수 있다.

**옛 이관 판정 여섯 (`f7018d6` · 2026-08-19~20)은 그대로 닫혀 있다:**

| 확인 | 누가 | 판정 |
|---|---|---|
| ㉠ 하위 폴더 | Claude Code | ✅ `place=subdir` 둘 · 폴백 0 |
| 파일 수 · 종류 섞임 | Claude Code | ✅ 첫 실행 **5개(3+2)** → 최종 **135/135** |
| 성역 불변 | Claude Code | ✅ 포인터와 1:1 · 고아 0 · 조각 파일에 앱이 안 씀 |
| 로그 「정상이면 0줄」 | Claude Code | ✅ 2줄뿐 · 실패 0 |
| **배너 문구** | **사용자** | ✅ 「자료 5개를 옮겼어요. 나머지는 다음에」 **그대로 나왔다** |
| **음성 · 사진** | **사용자** | ✅ 재생됨 · 사진 + **지도까지** |

**옛 확인 목록 17개(2026-08-18까지)는 그대로 닫혀 있다** — 아래 §2-A.

## 2-A. 옛 확인 목록 (2026-08-18까지)

**확인 목록 17개 — 닫힘 15 · 「봐야 하는 것」 0개.**

| 상태 | 번호 | 수 |
|---|---|---|
| ✅ 닫힘 | 1·2·3·4·5·6·7·9·10·12·13·14·15·16·17 | **15** |
| 보면 좋은 것 | **8**(이름 칸 90pt, XXL까지) · **11 잔여**(배너 둘 — 실사용 중에 자연히 생기면) | **2** |

**2026-08-18 새벽에 닫힌 다섯:**

| 확인 | 판정 |
|---|---|
| 10번 — 분류 바꾸고 저장 없이 `<` → 되돌아오나 | ✅ 통과 |
| 9번 — 원문 고치고 저장 없이 → 성역(수집 시각·기기) 불변인가 | ✅ 통과 |
| ③ — 임시 항목 원문 고치고 `<` → 경고 뜨나 | ✅ 뜬다 (여기서 「문구가 양쪽 같다」가 잡혀 `f43a84d`로 이어졌다) |
| 14번 — chevron 없이도 메뉴인 줄 알겠나 | ✅ **닫힘** (사용자 최종: *"그냥 아이콘 누르면 변경 가능한 것으로 알게 될 것이라고 확신해"*) |
| ①②④ (08-14분) | ✅ op 로그로 확정 |

> ⚠️ **표본이 없어 못 닫은 확인은 따로 모아 뒀다 → `docs/native/sample-pending-checks.md`** (넷).
> A그룹 셋(「지금 챙길 것」에서 갈리나 · 「새 기억들」에 남나 · 알림이 안 울리나)은 **여는 조건이 같아
> 한꺼번에 열린다** — *살아있는 미확정 중 유효한 날짜를 가진 것이 하나라도 생기면.*
> A-4(미확정 되풀이의 회차 자동 전진)는 조건이 하나 더 붙고 **셋 중 가장 조용히 틀리는 자리**다.
> B그룹은 배너 둘(상태가 자연히 생기면).

**살아있는 미확정 = 8개 · 전부 미분류** (`inbox-state.py --unconfirmed` · **2026-08-18 15:01:33**)

| id | 원문 | 분류 | 섹션 |
|---|---|---|---|
| `B3A9A907` | 어서 말을 해 - 해바라기 | — | 새 기억들 |
| `991728DB` | 차에 비닐봉지 하나 갖다놓기 | — | 새 기억들 |
| `32B23159` | 류마 엄완식원장 컨택 | — | 새 기억들 |
| `4C0882A0` | 나올 거라면 나올 거라고 뚜렷하게… | — | 새 기억들 |
| `0E4B8C7F` | 회사 조직 관련 고민… | — | 새 기억들 |
| `F5433F49` | 여기 제품이 업데이트 된… | — | 새 기억들 |
| `99E2B834` | 영업이 밖으로 나가는… | — | 새 기억들 |
| `516C17F0` | 회사 일인데 메디게이트… | — | 새 기억들 |

- **8개 전부 due·되풀이 없음** → 「지금 챙길 것」의 임시는 **여전히 0**. **ⓔ·실기기 5·6이 다 같은 이유로 못 닫힌다.**

> ### ⚠️ 위 명단은 **2026-08-18 15:01:33의 값**이다 — **수는 같고 대상이 둘 바뀌었다**
> **2026-08-21 11:1x 실측도 8개인데 명단이 다르다:**
> **나갔다** — `991728DB`(삭제됐다) · `4C0882A0`(**08-19 20:21에 「주차」로 분류 + 기억하기** →
> 30초 뒤 미리 알림이 붙어 「지금 챙길 것」으로 갔다).
> **들어왔다** — `1D5E2FDE`(08-18 23:07 음성) · `84BE980B`(**08-20 09:46 음성 — 확인 목록 ③-1·③-2를
> 판정할 때 만든 시험 수집**이다).
> ⛔ **「8개 그대로」로 읽으면 이 넷을 다 놓친다.** → `CLAUDE.md` 기록 규칙 8에 **「명단은 수가 아니라
> id로 적는다」**를 올렸다. **8은 세 번 다르게 만들어진 8이다**(08-18 원본 · 08-19 −1+1 · 08-20~21 −1+1).
- **뒤의 넷은 2026-08-18 15:01에 손으로 고쳤다** — 자동이 쓴 `type`/`due`/`resurface`/`status`를 걷어냈다
  (`native/tools/2026-08-18-strip-auto-fields.py` · 백업 `backups/*.bak-20260818-150125`).

> ⚠️ **★ 이 숫자는 iCloud 동기화 시점에 따라 달라진다 — 한 세션 안에서 4→6→8로 늘었다.**
> 10:0x에 4개였을 때 `inbox-iphone-4532.md`의 마지막 쓰기가 **08-15 16:43**으로 보였고,
> 10:4x에는 **08-18 09:43**이었다 — **두 실행 사이에 파일이 내려왔다.**
> `inbox-state.py` 머리주석이 경고한 그 지연이다. **폰 화면과 어긋나면 파일이 뒤처진 것으로 본다.**
> **표본 수를 적을 때는 실행 시각을 함께 적는다**(사용자 지시, 2026-08-18).

---

## 2-B. 맥 앱 판정 다섯 — 전부 통과 (2026-08-20 · 단계 5)

**⚠️ 맥 앱은 이때 처음 실기기 검증됐다**(그전엔 빌드만). 설계 §7이 받아들인 대가였고, **값이 났다** — 아래 다섯째.

| 확인 | 누가 | 판정 |
|---|---|---|
| A(dataless)에서 「원본 사진 받는 중…」 | **사용자** | ✅ 보였다 |
| A·B 둘 다 사진이 그려지나 | **사용자** | ✅ **둘 다** → **§7 닫힘.** A·B가 갈렸으므로 **§6(세 갈래)도 닫힘** |
| 음성 재생 | **사용자** | ✅ 소리가 났다 |
| **지도 핀** | **사용자** | ⛔ 안 찍혔다 → **고쳤다**(`0727499`) → ✅ **「촬영 위치」로 찍힌다 · 한글 안 깨짐 · 위치가 EXIF 좌표 자리**(`37.4961, 127.0406`) |
| 들여오기(C안) | **Claude Code** | ✅ 맥 로컬에 **A·B의 것 4개만** · **md5가 iCloud와 넷 다 같다** · `.part` 0 · 고아 0 · 로그 2줄 · iCloud 135 그대로 · dataless 119→116(**검산 맞음**) |

> ### ★ macOS를 범위에 넣은 값이 여기서 났다
> 지도 핀 결함은 **2026-07-24 `8262265`(Stage 3)부터 27일간 있었고 URL 문자열이 한 글자도 안 바뀌었다** —
> ⛔ **아이폰에서도 그동안 핀이 안 찍혔다.** **이 버튼을 처음 눌러본 것이 이번**이다.
> 안 잡힌 이유: **지도 앱이 열리고 그 위치로 가기까지 한다.** 틀린 건 핀이 없다는 것뿐이고 **로그도 안 남는다.**

## 3. 다음 작업

> # ▶ **열려 있는 것 하나** (**2026-08-21 저녁** 기준 — **이 수가 정본이다**)
>
> | | 무엇 | 지금 |
> |---|---|---|
> | **1** | **자료 순서표** — **②자료 확장 → ③새 분류 셋 → ④해시태그** | ✅ **②의 「카드 구성」이 닫혔다**(2026-08-23 · 폰 판정 통과 — *"카드가 닫혔다"*). **08-13에 걸린 「성역을 펼쳐서 사진을 보는 방식은 매우 불편하다」가 오늘 닫혔다.**<br>✅ **「A. 포인터 꼴」이 닫혔다**(2026-08-23 19:4x) — **자료마다 별도 필드**(`<종류>.<자료id>`) · **Core에 못 박았다**(`MediaPointer` + 결정 파수꾼 시험 8개 · **394 통과**) · ⚠️ **앱은 아직 안 쓴다 = 화면 변화 0.** ⛔ **자료 id는 소문자 16진 32자(하이픈 없음)** — 하이픈이면 필드 줄이 **조용히 스킵된다**(§3-W-2). ✅ **B도 닫혔다**(20:1x) — **파일명 = `<항목id>-<자료id>.<확장자>`**(§3-W-6 · **§3-V-2의 두 안 어디에도 없던 셋째 꼴**). **시험 397 통과.** ✅ **C도 했다**(21:4x · **§3-X**) — 축이 **`name:`(파일명)**이 됐다(`forPointer:`가 아니다). **제약 9-b 닫힘 · 9-a 절반**(수집은 옛 꼴). **시험 399 · 빌드 `EXIT=0`.** ✅ **검산식도 다시 세웠다** — **누락·고아·겹침** 셋 · 도구 **`native/tools/media-audit.py`** · 실측 **2026-08-23 21:35 맥미니 140개 · 셋 다 0**. ⛔ **폰엔 안 깔았다**(1·2단계는 확정 근거 없음) — **「사진·음성이 여전히 보이나」를 3단계 설치에서 함께 판정한다.** ✅ **3단계도 했다**(2026-08-23 밤~24 새벽 · **§3-Y** · 설치 `f91a9bb` 22:49 → 판정 **여섯 통과**) — **카드의 `+` → 종류 시트 → 사진 찍기/앨범에서 고르기** · 추가는 **op**(성역 아니다 · 제약 10 닫힘). ★★ **실데이터가 A·B·C를 한꺼번에 증명했다**: 새 파일명 꼴이 생기고 · `set photo.<자료id>=…`로 붙고 · **한 항목에 둘·셋·다섯** · 업로더가 새 이름을 올리고 · **검산 photo 31 → 43 · 셋 다 0**. ⛔ **결함 하나: 가로로 돌려 찍으면 카메라 화면이 깨진다**(찍힌 사진은 정상 — 쟀다 4032×3024) → **카메라만 세로 고정**(설치 `439f708` 00:31 · ⏸ **먹는지 판정 대기**). ✅ **뷰어 `‹` `›`도 했다**(2026-08-24 새벽) — 같은 종류 안에서 넘김 · **「2 / 3」** · **끝까지 당기면 넘어가는 스와이프** · **하단 썸네일 줄**(초록 테두리 · 가운데 정렬 · 따라 스크롤 · **5초 뒤 사라짐**). ⛔ **실기기 판정이 결함 넷을 잡았다** — 카메라 임베드(가로 촬영 불가) · 회전 배율 · **뷰어가 낡은 항목**(C에서 카드만 고쳤다) · **`try?`가 취소를 삼켜 썸네일이 1초에 사라짐**. 넷 다 고쳤고 마지막 판정 *"다 잘 돼"*. **폰 = `60c12e1`(01:49) · 설치 아홉 번 · 시험 399.**<br>▶ **다음은 동영상인데 ⛔ 먼저 「용량 정책」 하나를 정해야 한다**(1분에 수십~수백MB · 지금은 첫 실행만 5개 제한 뒤 무제한). 입구는 몇 줄이고 썸네일 규칙도 이미 있다 — 조사 결과는 **`docs/worklog/2026-08-24-macmini.md` §7**. **함께 남은 것:** 대표 사진(필드별 HLC 필요) · 삭제 · 자료마다 상태 · 추가 촬영의 EXIF 위치 · **사양서 반영**(§8). 남은 것 = **§3-W-4** · 세션 전말 = **`docs/worklog/2026-08-23-macmini.md`**.<br>*(옛 서술: "▶ 다음은 9-a(파일명 규칙)인데 ⛔ 그 앞에 「포인터 꼴」이 있다 — 설계 §3-V")*<br>*(옛 서술: "▶ ②의 카드 구성이 끝났다 — 다음은 구현이다")*<br>★ **먼저 볼 것 하나: `docs/native/media-expansion-design.md` §0** — 「정해진 것 전부 · 구현할 때 볼 한 장」(35줄: 카드 10 · 네모 안 11 · 뷰어 6 · 움직임 5 · 순서 3). **결정이 아홉 절에 퍼져 있어 만든 표다.**<br>**할 일 = 커밋 둘:** ① **본문을 순서 배열로만**(화면 그대로 · `DetailView` 안쪽 `VStack` 하나 · 모델 변경 0) → ② **카드 추가 + 애니메이션.** ⛔ **①만 따로 폰에 깔지 않는다** — 화면이 안 바뀌는 것이 목표라 **확정 근거를 못 만든다**(§3-I-4).<br>⛔ **구현 전에 걸리는 것 둘:** **개수 배지가 제약 9-a·9-b에 막힌다** · 나중에 붙인 자료의 이벤트(제약 10).<br>**남은 물음은 §4-A**(㉡ 일곱 · ㉢ 다섯 · 못 잰 것 넷) · **뷰어 상세는 별도 문서**(받을 것 일곱 = §3-F-7).<br>⛔ **이 세션은 앱 코드 0줄이다** — 문서와 랩(`ghostlab` 꼴 열하나)뿐.<br>*(옛 서술: "▶ ②의 방향을 받았다(2026-08-21 저녁)* — **자료를 성역에서 떼어 폭 전체 카드로 · 접기는 따로 · 지도가 아니라 「사진에 딸린 위치 정보」.** **정본 = `docs/native/media-expansion-design.md` §3** · 답할 것 셋은 §4 · **실험은 집 맥미니에서**(§5).<br>⛔ **앱 코드는 0줄** — 오늘은 문서까지다. **자료 종류 늘리기는 이번이 아니다**(카드를 나누는 것까지). **①자료 위치는 닫혔다**(2026-08-20). ★ ③의 **첫 표본은 `D6E4950B`** → §3-A |
>
> ⚠️ **「언제 기준」을 함께 읽을 것**(기록 규칙 8) — 이 수는 **하루 안에 넷 → 셋 → 둘 → 하나**로 바뀌었다.
> **오전 넷 · 오후 셋 · 오후 늦게 둘 · 저녁 하나.** 날짜만으로는 부족한 값이다.
>
> ### 수가 어떻게 셋이 됐나 (2026-08-21 · 이 줄을 지우지 말 것)
> | 무엇 | 어떻게 |
> |---|---|
> | 원칙 목록 테두리·잔상 | ✅ **닫혔다**(`50a8e48`·`c6ed58b`) — 오늘 오전 정정 |
> | **`automation/` 표시** | ✅ **닫혔다**(2026-08-21 오후) — 파일 넷에 경고 + `automation/README.md` 신설 + **웹 v0 세대 표시**. ⛔ **고치지 않았고 지우지 않았다** → §4 |
> | 07-24 `discard` | ✅ **닫혔다**(오늘 오후) — `D6E4950B` 되살림 · **나머지 열넷은 다시 열지 않는다** |
> | **있는 목록이 소진되게 한다** | ▶ **과제 목록에서 뺐다 — 「어떻게 일하는가」로 옮겼다.** 오늘 실제로 밟아 보니 **단계 셋**(① 도달할 수 있나 → ② 목록에 올라와 있나 → ③ 목록이 소진되나)으로 갈렸고, 그 셋은 **규칙·계열**로 들어갔다(기록 규칙 8 · 정본 훑기 규칙 7 · §4 머리의 형태 ①~⑧). **끝난 것이 아니라 상시가 된 것이다** → `docs/worklog/2026-08-21-macbook.md` §11 |
> | **자료 순서표** | ▶ **들어왔다** — 앞의 것들이 닫혀서 **이제 이것이 다음 줄기다**(전엔 「넷 밖」이라 적었다) |
> | **`automation/` 표시** | ✅ **닫혔다**(2026-08-21 오후) — 위 줄 참조 |
> | **좌우 맞춤** | ✅ **닫혔다**(2026-08-21 저녁) — **원문이 보이는 곳 전부**에 들어갔고 **줄바꿈 원칙 셋**까지 사용자 판정 통과. 설치 `7cd8356` → §4 |
>
> ⚠️ **수를 갈라 적지 말 것.** 2026-08-21에 **문서 안에서 둘·넷이 갈렸다**(재개 한 줄 ↔ §4).
> **한 프로젝트가 다른 수를 말하면 안 된다** — 수를 바꿀 때는 **이 표와 두 worklog의 재개 한 줄을 함께** 고친다.

### ✅ 2026-08-20에 끝난 것 — 자료 단계 6 (정본 뒤집기)

⛔ **옛 서술(뒤집혔다 · 2026-08-21 정정):** 이 자리에 *"▶ (1) 자료를 iCloud로 — 단계 6: 정본 뒤집기
(마지막 단계) … 남은 것은 문서뿐 — 코드 0줄 … 할 일은 하나다: `media-icloud-design.md` §12의
일곱 자리를 고친다"*가 **다음 작업으로 남아 있었다.**
**그 일은 같은 날(2026-08-20) `24e39c0`에서 끝났다** — 설계 §12가 스스로 *"정본 뒤집기 — 완료"*로
적고 있고 자리도 **일곱이 아니라 여덟**이었다(하나는 목록 밖에서 나왔다).
★ **이것이 「이미 한 일이 안 한 것으로 적혀 있다」(여섯 번째 형태)다** — 안 고치면 **다음 세션이
여덟 자리를 다시 훑는다.**

**그때 가른 것은 그대로 유지된다** (다시 고칠 때 필요하다):
| | |
|---|---|
| ⛔ **뒤집혔다** | 「자료 **파일**이 기기에만 있다」 · 「항목별 옵트인」 · 「`searchDirs()`에 iCloud를 더한다」 |
| ✅ **유지된다** | 「**STT가 음성을 서버로 보내지 않는다**」(온디바이스 강제) · `absent` 상태 |

### ✅ 2026-08-20에 끝난 것 — 단계 5 (스코프 · 폴링 · Mac 사진 · 지도 핀)

| 구현 | 자리 |
|---|---|
| **스코프 C안 — 받으면 로컬로** | `MediaCloud.adopt` · `Audio/PhotoStore.adoptFromCloudIfNeeded`. **`readableURL` 삭제** |
| 폴링 **1초 → 0.2초** | `MediaFetch.pollSeconds`. `waitSeconds` 15는 **유지**(줄일 근거가 없다) |
| **Mac 사진 보기** | `PlatformMedia` · `DetailView.photoRow`에서 **`#if os(iOS)`가 사라졌다** |
| **지도 핀 `q=`** | `MapsLink`(Core) · 핀 이름 **「촬영 위치」**(사용자가 고름) |

**★ 스코프 밖에서 iCloud를 읽는 코드가 0개가 됐다.** 훑어서 확인했다 —
`withFolder` 호출 **일곱**이 전부 값(`Bool`·`Void`·`Plan?`·사실 튜플)을 돌려준다.
규칙을 `FragmentFolder.withFolder` 주석에 올렸다: *"`body`는 URL이 아니라 값을 돌려준다."*

**★ 잰 값 — iCloud 다운로드 (맥미니 · 유선 2500Base-T · 다운 850.7Mbps · 표본 16):**
**526~924ms**(중앙 604 · 평균 654). **크기와 무관하다** — 피어슨 r=−0.364 ·
`118218B=526ms` vs `118227B=924ms`(**9바이트 차이에 398ms**). **파일당 고정 왕복이 지배한다.**
⚠️ **자료 확장 ②(PDF·동영상)에서 다시 재야 한다.**
**못 잰 것 넷:** 부분 진행률(**관측 불가** → 「받는 중」에 퍼센트가 없는 것이 맞다) · iOS · 셀룰러/느린 Wi-Fi · 1MB 넘는 파일.

**★ 흔들린 전제를 쟀다 — 「Mac에서 걸린다」는 틀렸다.**
샌드박스 없는 프로세스에서 `.withSecurityScope` 북마크가 되고, `startAccessing…`이 **`true`**를 주고,
**스코프를 닫은 뒤에도 iCloud 파일이 읽혔다.** 즉 **「Mac에서 터진다」가 아니라 「폰에서 로컬 사본이
가리고 있을 뿐」**이다. iOS는 **못 쟀다**(탐침 빌드 필요) — **C가 그 경로를 없앴다.**

### ✅ 2026-08-18에 끝난 것 — 「임시는 기억하기 전까지 아무것도 하지 않는다」

**결정 넷과 코드 넷이 함께 들어갔다.** 전말은 `docs/worklog/2026-08-18-macbook.md`.

| 구현 | 자리 |
|---|---|
| 파티션 축을 **시점 → 확정**으로 | `InboxModel.partition` — 미확정은 시점이 있어도 「새 기억들」 |
| **알림도 확정만** | `InboxModel.notifiable` — 파티션과 **같은 조건**(한쪽만 고치면 반쪽) |
| 임시에서 **[완료]·[했어요] 제거** | `InboxView` — `newItemActions`는 기억하기·삭제 둘뿐 |
| **자동 분류 일시 중지** | `ClassifyPause` — 당겨서 분류·설정 버튼. **기능은 안 지웠다**(재개발 예정) |

**정본에 올린 것:** `memory-philosophy.md` **§2-1-B**(미확정에 시점이 없는 이유 — 귀결 형태) ·
같은 절의 **자동 분류 재개발 원칙**(「자동은 준비까지, 결정은 사용자가」) · **§5 경계 규칙 축 변경** ·
**§7-1 (c)에 축 구분**(§7 폴백 = 분류 축 ≠ 알림 게이트 = 확정 축) ·
`edit-policy.md` **§3 좁힘**([삭제]만) · **§1-A 표 정정**(상세의 완료는 되풀이+확정에만).

**데이터 정리 (2026-08-18 15:01):** 자동이 쓴 필드 **14줄 삭제** — 미확정 8개가 **전부 미분류**가 됐다.
항목 72 불변 · 신원(`id`/`hlc`/`device`) 71 불변. 도구·근거는 `native/tools/2026-08-18-strip-auto-fields.py`.

### ⏸ 남은 것 — 우선순위 순

1. ✅ **폰 설치 + 실기기 확인 — 끝났다.** `f7018d6` 설치(2026-08-19 18:52) · **판정 여섯 전부 통과** · 이관 135/135.
   (그 앞: `dbbafc8` 설치 2026-08-18 15:55 · 확인 다섯 통과 — **당겨서 분류는 그때부터 막혀 있다.**)
2. ✅ **집 맥미니의 launchd — 해소됐다** (2026-08-19). 애초에 설치돼 있지 않았다. → 이 문서 맨 위.
2-A. ✅ **자료 단계 6 (정본 뒤집기) — 끝났다** (2026-08-20 `24e39c0` · 여덟 자리).
   ⛔ **옛 서술(뒤집혔다):** *"⏸ … 단계 1~5는 닫혔다. 코드 0줄 남았다 · 자리 목록 = §12(일곱)"*. **자료 ①은 전부 닫혔다** → 다음은 순서표 **②자료 확장**(§3-A).
3. **⏸ `catchUpRecurrence` 미결** — 앱을 열기만 해도 **미확정 되풀이의 시점이 전진**한다
   (`confirmed`를 안 본다). §2-1-B와 정면으로 어긋나는 자리인데 **이번에 안 고쳤다 — 별개 결정.**
   지금 표본 0(미확정 8개 중 되풀이 없음). 자동 분류 재개발 전에 정해야 한다. → `edit-policy.md` §미결
4. **자동 분류 재개발** — 원칙만 정해져 있다(제안을 필드에 쓰지 않는다). **형태·수명·병합은 미정.**
5. **⏸ 「수집 원본 자료」 2차 압축 (2단계)** — 아래로 미뤘다. **코드 0줄 · 설계 전.**

### ⏸ 2차 압축 2단계 — 미뤘다 (착수 조건은 그대로)

| 단계 | 상태 |
|---|---|
| 1단계(성역 접기 · 분류 세로 · 좌우 2:1) + 2차 묶음 | ✅ 완료 (2026-08-12) · 사진 없는 항목에서 **~129pt** 감소 |
| **2단계**(사진·지도를 성역에서 떼어 새 카드로) | ⏸ **코드 0줄 · 설계 전** |

**왜 멈춰 있나:** 2026-08-13 실기기에서 사용자가 걸렸다 — **「성역을 펼쳐서 사진을 보는 방식은 매우 불편하다」**.
사진·지도가 **성역 카드 안**에 있고 성역은 **기본 접힘**이며 접기가 **두 카드를 함께** 움직인다.
그래서 2단계가 「하면 좋은 것」에서 **「걸린 것」**이 됐고, **계측과의 의존이 뒤집혔다** —
2단계 방향이 무엇을 잴지 정한다. ⚠️ **방향 없이 계측부터 하면 곧 버릴 배치를 잰다.**

**착수하려면:** ① 방향을 사양서/Claude 챗에서 받는다(즉흥 설계 금지) ② **화면 이름은 사용자가 정한다**
(「수집 원본 자료」는 작업용 이름) ③ 그 뒤 계측(`native/tools/measure-ui.swift`·`measure-text.swift`,
폰 스크린샷 가능 · 재료 `82B1044B`) — **Dynamic Type 실측도 이때 함께 닫는다**(지금 값은 공표 표)
④ 배너 다섯은 건드리지 않는다(두 카드 밖).

---

## 3-A. 자료 쪽 로드맵 — 미룬 것과 **순서** (2026-08-19 사용자 결정)

> ★ **2026-08-21: 이 순서표가 「열려 있는 것」 3번으로 들어왔다** — 앞의 것들이 닫혀서
> **이제 이것이 다음 줄기다**(전엔 「그 밖의 큰 줄기」로 적혀 있었다). → §3
>
> ### ★ 순서: **자료 위치 → 자료 확장 → 분류 → 해시태그**
> 이 순서는 사용자가 정했다. **앞의 것이 뒤의 것의 토대다** — 자료가 어디 있는지가 정해져야
> 여러 개·여러 종류를 얹을 수 있고, 그 뒤에 분류와 해시태그가 온다.

| 순서 | 무엇 | 상태 |
|---|---|---|
| **①** | **자료 위치 — iCloud로** | ✅ **전부 닫혔다**(2026-08-20) — 이관 135/135 · 단계 1~5 + **단계 6 정본 뒤집기**(`24e39c0`)<br>⛔ 옛 서술 *"▶ 단계 6(문서)만 남았다"*는 **낡았다**(2026-08-21 정정) |
| **②** | **자료 확장 2단계** — 여러 개 · **종류 넷**(저장된 사진 · PDF·기타 파일 · 웹 링크 · 동영상) · **수집 화면 확장** · 카드 분리 · 추가 · 삭제 · **어디서든 가져오기**(iPhone·Mac·Windows) · **자료마다 개별 보기** | ▶ **방향 받음**(2026-08-21 저녁 · 그 문서 §3) · **답할 것 셋**은 §4 · **실험은 맥미니**(§5). 요구·제약 정본 = **`docs/native/media-expansion-design.md`**(2026-08-21 신설) · **뜻 정본 = `edit-policy.md` §6**<br>⛔ **옛 서술(좁았다 · 2026-08-21 정정):** *「여러 개 · 여러 종류 · 카드 분리 · 추가 · 삭제」* — 다섯 낱말이었고 **「수집 화면 확장」과 「종류 넷」이 빠져 있었다.** ★ **그 요약을 원본으로 읽어서 2026-08-21 보고가 「PDF·동영상은 예고에만 있다」로 틀렸다** — 사용자가 Claude 챗 결정을 기억하고 있어서 되찾았다. ⚠️ **요약이 원본이 되면 빠진 것은 영영 안 돌아온다** |
| **③** | **새 분류 셋** — **추억 · 기록 · 지식** | ⏸ 설계 전. ★ **첫 표본이 있다: `D6E4950B`**(2026-08-21 사용자 지정) — 2026-07-19 21:14 음성, *"드디어 단축 버튼으로 음성을 텍스트로 바꾸는 이 단계까지 들어왔습니다…"*. **지금은 원칙 목록 맨 아래에 임시로 있고**, 이 설계가 서면 **「기록」이나 「추억」으로 옮긴다.** ⛔ **그때까지 원칙에서 빼지 말 것** — 눈에 띄는 자리에 두려고 거기 둔 것이다. ★ **실데이터로 「기록」과 「추억」을 가르는 재료다** — 이름이 닮았는지가 아니라 **무엇이 이것을 그것이게 하는가**를 이 항목에서 시작한다(계측 규칙 7) |
| **④** | **해시태그** | ⏸ 설계 전 |

### ⏸ 자료 삭제 · 보관함 완전 삭제 — **구현을 미뤘다. 정의는 섰다** (2026-08-21 정정)

**순서표에 안 넣었다.** ②의 일부이기도 하고 그보다 무겁기도 해서, **병합 설계가 풀려야 시작된다.**

> ### ⛔ **미룬 것은 구현이고 정의가 아니다** — 2026-08-19에 이미 정해졌다
> **뜻 정본 = `edit-policy.md` §6**(자료의 뜻 넷). 그중 삭제 관련 둘:
> **③ 자료 삭제 =** 기억에서 떼는 것 **+** SecondBrain이 복사해 둔 사본을 버리는 것.
> **복사 전 원본은 안 건드린다.** 되돌리기 없음 · 확인 문구에 **「원본은 지워지지 않는다」**.
> **④ 항목 삭제와의 관계 =** 보관함에 있는 동안은 자료를 **남기고**, **완전 삭제할 때 지운다**
> (보관함에 있는 항목은 **아직 고아가 아니다**).
> ⚠️ **이 블록이 「병합 설계가 풀려야 시작된다」까지만 적고 있어서 2026-08-21에
> 「정의도 안 정해졌다」로 읽혔다** — 아래 다섯은 **구현이 걸린 자리**일 뿐이다.

**왜 미뤘나 (2026-08-19 조사):**
- op 로그가 **append-only**이고 「지운다」 verb가 **없다**(`EventWriter`가 내는 verb는 create/delete/undelete/set/edit 다섯).
- `delete`는 **tombstone**일 뿐이라 항목이 영원히 존재한다.
- **반쪽 삭제는 유령을 만든다** — `MergeEngine.merge`는 이벤트를 id로만 묶고 **create 블록이 있는지 안 본다.**
  create만 지우고 op을 남기면 **`raw` 없는 빈 줄**이 보관함에 남는다.
- **다른 기기가 옛 사본을 들고 있으면 되살아난다.**
- 그리고 **급하지 않다** — 안 지워도 지금 상태 그대로다.

> ⛔ **①이 이것의 빚을 늘린다.** 삭제 상태 **36개**(음성 28 · 사진 8)도 iCloud에 올라간다.
> **나중에 회수해야 한다 — 2026-08-19에 알고 받아들인 대가다.**
> 근거·전말: `docs/native/media-icloud-design.md` §9.

**지금 코드 상태:** `ArchiveView`에 스와이프 동작은 **「되돌리기」 하나뿐**이다. 완전 삭제 코드는 **0줄**.
확정된 자료 파일(`<id>.m4a`/`<id>.jpg`)을 지우는 코드도 **0줄**(`removeItem`은 전부 임시 파일용).

---

## 4. 그 밖에 열려 있는 것 (참고 — 한 줄씩)

> ### ★★ 일곱 번째 형태 — **「이미 된 일이 「하지 말 것」으로 적혀 있다」** (2026-08-21 신설)
> 여섯 번째(*「이미 한 일이 안 한 것으로 적혀 있다」*)는 **다시 하게** 만든다.
> 일곱 번째는 **다시 못 하게** 만든다 — **방향이 하나 더 나쁘다.**
> **사례 둘 (둘 다 오늘 정정했다):**
> **① 원칙 목록 잔상·테두리** — 어제 `50a8e48`·`c6ed58b`로 됐는데 이 표에 *"⛔ 다시 시도하지 말 것.
> 탈출구는 `List`를 버리는 것뿐"*으로 남아 있었다. **다음 세션은 성공한 길을 금지된 길로 읽는다.**
> **② 지도 핀·삭제 스와이프** — 고쳐서 폰에서 판정까지 통과했는데 `⏸`로 남아 있었다.
> ★ **여섯·일곱 번째의 뿌리는 하나다: 문서가 실제와 어긋난 것.** 계열 ①~⑤는
> `docs/worklog/2026-08-20-macbook.md` §20에.
>
> ### ★★ 여덟 번째 형태 — **「있는데 찾을 방법이 없다」** (2026-08-21 신설)
> **①~⑦은 문서 쪽이었다. 여덟 번째는 앱 쪽이다** — **데이터도 있고 파서도 제대로 읽는데
> 사용자가 거기 도달할 길이 없다.**
> **사례:** `D6E4950B`를 「삭제된 기억」에서 찾아야 했는데 **검색이 그것을 안 훑는다**
> (`SearchView`는 `liveNonDone + doneItems`만 본다). 83줄을 눈으로 훑는 수밖에 없었고,
> **사용자가 첫 번째 시도에서 못 찾았다.**
> ⛔ **①(지도 핀)과 갈린다:** 지도 핀은 **눌러야 알았다**(누를 자리는 있었다).
> 이건 **누를 자리 자체가 없다** — 아무리 눌러도 검색으로는 안 나온다.
> ★ **그래서 「목록을 소진한다」로 안 잡힌다.** 확인 목록에 올리려면 **먼저 도달할 수 있어야** 한다.
>
> ### ▶ 2026-08-21 전수 대조 — **이 표 17줄을 코드·데이터와 맞춰 봤다**
> **닫혀 있는데 `⏸`로 적혀 있던 것 셋:** 원칙 목록(①잔상 ②테두리) · 삭제 스와이프 빨강 · 지도 핀 설치.
> **여전히 열려 있는 것 나머지** — 그중 셋은 **오늘 데이터·코드로 확인**했다:
> 알림 delegate **없음**(grep 0건) · A그룹 조건 **미충족**(미확정 8개 전부 `due` 없음 · 11:1x 실측) ·
> 확정 쪽 「수정」은 **두 번이 아니라 세 번**이었다.
> ⚠️ **못 잰 것:** B그룹 배너 둘의 조건(실데이터 전수 재훑기가 필요하다 — 안 했다).
> ★ **이것이 「있는 목록이 소진되게 한다」의 첫 걸음이다** — 소진하기 전에 **목록이 맞는지부터** 센다.

| | 무엇 | 상태 |
|---|---|---|
| **★ 새로** | **있는 목록이 소진되게 한다** (옛 이름: 「손으로 눌러야만 드러나는 자리」) | ⏸ **다음 과제.** ★ **2026-08-20에 성격이 바뀌었다** — 앱 경계를 넘는 자리를 전수로 훑었는데(`openURL`·App Intent·`UNNotification`·`fileImporter`·`CLLocation`·`SFSpeech`·`AVCapture`) **「아무도 모르고 있던 자리」가 안 나왔다.** 나온 것은 **이미 기록돼 있고 일부러 미뤄 둔 것**이었다. ⛔ **즉 문제는 「모르는 것」이 아니라 「알고 있는데 안 하는 것」이다** — 지도 핀은 **확인 목록**에, 알림 탭은 **미결 목록**에 있었는데 **어느 목록도 소진되고 있지 않았다.** → **답은 「목록을 더 만드는 것」이 아니라 「있는 목록이 소진되게 하는 것」이다.**<br>⚠️ **단서 — 이것은 정적 훑기의 결과다.** **지도 핀도 정적으로는 안 나왔다**(코드는 완벽했고 URL 문자열도 27일간 안 바뀌었다). **눌러야 나오는 것은 여전히 눌러야 나온다** — 훑기가 「없다」를 증명하지 않는다.<br>**소진할 목록 = `docs/HANDOFF.md`(2026-07-29)의 「미확인」 넷:** ④사진 EXIF GPS **✅ 닫힘**(2026-08-20) · ②자동분류 **⛔ 대상 아님**(2026-08-18 `ClassifyPause`가 진입점 둘을 다 막았다 — 재개발 때 검증) · **남은 것 = ①원칙 · ③수집.** 확인 항목 **7개**로 풀었다 → §4-A.<br>계기·전말: `docs/worklog/2026-08-20-macmini.md` §13-① · §15 |
| **✅** | ~~**`automation/`에 「건드리면 안 되는 것」을 표시한다**~~ | **닫혔다 (2026-08-21).** ⛔ **고치지 않았고 지우지 않았다 — 표시만 했다**(코드 동작 변경 0줄 · 문법 검사 통과).<br>**⛔ 되살리는 것 셋에 경고:** `automation/setup-mac.sh`(매시간 launchd) · `automation/run-classify.sh`(**직접 돌려도 돈다 · 화면에 아무것도 안 나온다**) · **루트 `classify.py`**(`type`·`due`… 직접 쓰기 · ⛔⛔ **`SYSTEM_PROMPT`에 `discard`가 있고 앱의 거부 방어가 여기엔 없다**).<br>**✅ 안전한 것 하나에도 적었다:** `automation/uninstall-mac.sh` — **끄는 도구다. 오히려 있어야 한다** (폴더 전체를 위험으로 읽으면 정작 끌 때 안 쓴다).<br>**신설 `automation/README.md`** — 등급 표 + **진입점 전수 결과**(`automation/` 셋 · 자동 실행은 **launchd 하나** · `.github`·`Makefile`·`package.json` **없다**) + 두 기기 상태. **「어디를 봤나」를 남긴 것이다.**<br>★ **문구는 후보 B** — 핵심은 **「그 결정 「이전」에 만들어졌다」**(사용자: *그것이 있어야 다음 사람이 경고가 왜 코드보다 늦게 붙었는지 알고, **경고 없는 다른 파일도 의심한다***).<br>★★ **훑기의 소득: 웹 v0에 쓰기 경로가 있었다** — `app.js`가 *"inbox.md는 읽기만 한다"*고 적어 두고 `writeToFile()`로 **`due`·`resurface`를 덮어쓴다.** 루트 `README.md`도 같은 거짓을 적고 있었다. **세대 전체를 표시했다**(웹 파일 다섯 + `add-2-inbox.ps1` + README 맨 위 · `manifest.webmanifest`는 JSON이라 README가 그 표시다). ⛔ **그 경로로 만들어진 A그룹 표본은 쓸 수 없다**(축을 우회한 것 → `sample-pending-checks.md`).<br>⚠️ **안 붙인 것:** `native/tools/2026-08-18-strip-auto-fields.py` — 방향이 반대고 재실행 금지가 이미 두 곳에 있다. 전말 → `docs/worklog/2026-08-21-macbook.md` §14 |
| **⏸** | **알림 탭 착지 — 「소진 안 되는 목록」의 사례** | **지금 열지 않는다**(2026-08-20 사용자). **`UNUserNotificationCenterDelegate`가 아예 없다**(2026-08-21 재확인 — `native/Sources/App/` grep **0건**) — 탭하면 **마지막에 머물던 탭**으로 가고(식별자에 항목·회차가 다 있는데 아무도 안 읽는다), **앱이 켜져 있으면 배너가 아예 안 뜬다**(`willPresent` 없음). 알림 제목 「지금 챙길 것」은 *"탭하면 갈 곳의 이름과 맞춘 것"*이라 골랐는데 **그 절반이 없다.**<br>⛔ **★ 이것이 오늘 배운 것의 사례다: 2026-08-06에 원인까지 짚어 미결로 적어 뒀는데, 미결로 적으면 목록에 안 올라가서 영영 안 열린다.** 「승인된 미결」과 「잊힌 것」이 문서에서 같은 모양이다.<br>후보 셋은 `recurrence-design.md` §9 미결에 이미 있다(① 탭 시 '새로운 기억'으로 ② 그 항목 상세로 ③ `willPresent`로 포그라운드 배너). **나중에 열어도 손해가 없다** — 그래서 지금 안 연다 |
| **✅** | ~~**원칙 목록 — ① 끌 때 원래 자리에 남는 잔상 · ② 끄는 동안 그 줄만 테두리**~~ | **둘 다 됐다 (2026-08-20~21) · 폰 판정 통과.** ★ **①은 `50a8e48`** — 「값을 치를지 정한다」던 그 탈출구를 **실제로 걸었다**(iOS만 `UICollectionView` 대화식 이동 · `PrincipleReorderList.swift`). 랩 실측이 길을 골랐다: `List`는 흐린 사본이 남고 · **편집모드도 똑같이 남고** · UIKit만 비었다. ★ **②는 `c6ed58b`** — **「새로운 기억」의 원칙 영역 짜임을 차용**했다(바탕 `tint.opacity(0.14)` · 테두리 `tint.opacity(0.28)` · 숫자 · `PrincipleCard`). 그 뒤 `069f2e9`(끌면서 번호·색 따라옴) · `c756655`(놓을 때 죽던 것). 사용자 판정: *"이제 안 죽어. 세 경우 다 통과. 여백도 지금 이 상태가 좋아. 더 볼 것 없어."*<br>⛔ **뒤집힌 옛 서술 — 지우지 않는다:** *"추후 과제"* · *"⛔ 두 번 시도해 둘 다 실패했다"* · *"★ 사본은 원본이 보이는 동안만 산다"* · ②는 *"못 했다 — 없앴다"*, *"⛔ ①과 ②의 탈출구가 같다 — 남은 길: `List`를 버리고 `ScrollView` + 직접 만든 순서 바꾸기. ⚠️ 지금 잘 되는 것을 통째로 갈아엎는 값이다. 착수 전에 그 값을 치를지 사용자가 정한다."* — **그때는 못 한다고 판단했고 그 판단이 틀렸다.** ①은 그 탈출구가 **통했고**(단 `ScrollView`가 아니라 `UICollectionView`였다), ②는 **감지를 버리고 상시 테두리로** 답이 났다(끌리는 줄의 구분은 **iOS 기본 들어올림 + 따라오는 번호·바탕색**이 한다).<br>★ **되게 한 것은 「감지를 정교하게 만든 것」이 아니다 — 「감지가 필요 없어지게 한 것」이다.** 이미 있는 짜임(원칙 영역)을 빌려오니 **「지금 어느 줄이 끌리나」를 알 필요가 없어졌다.** **문제를 고친 것이 아니라 문제가 생길 자리를 없앴다** — 다음에 같은 벽을 만나면 쓸 형태다: **막힌 자리를 정교하게 만들기 전에, 그 자리가 필요 없어지는 짜임이 이미 앱 안에 있나 본다.** → **일곱 번째 형태**(이 표 머리)<br>✅ **유지된다:** `LongPressGesture` 금지(`d1749e2`·`7e643f4`) · **원본 숨기기** 금지(`4dc8b9c`·`e2e4dfb`) · **≡ 손잡이 되살리기** 금지(`30e2d91`) · 머리글 둘째 줄 유지 · **맥은 `List` 그대로**(iOS 전용 `UIViewRepresentable`) |
| **ⓓ** | 회색 글리프를 누르면 상세로 가는 **폴백**을 의도로 채택할지 | 미결. 막는 근거가 하나로 줄었다(**「같은 탭이 상태에 따라 다른 일을 한다」**만 남음) |
| **⏸** | 되풀이 시험 항목 정리 | 조사만 함. **아무것도 안 지웠다.** 사용자: *"나중에 다시 물어볼께"* → worklog §12(특히 **12-5**, 이제 12개) |
| **⏸** | 날짜 표시가 화면마다 다르다 (수집 시각은 연도까지 · 마감은 올해면 생략) | 사용자: *"나중에 따로 맞추자."* 맞추면 검색·보관도 함께 바뀐다 |
| **⏸** | 확정 쪽 경고 문구에 **「수정」이 두 번** — **실측은 세 번이다** | 자잘한 미결. **여전히 열려 있다**(2026-08-21 확인). 문구: *"**수정** 중이에요. 저장하지 않고 나가면 **수정**한 내용이 사라져요"* + 버튼 **[계속 수정하기]** = **세 번**(`DetailView.swift:1400-1403`). ⚠️ **화면에 나오는 말이므로 바꾸려면 사용자가 정한다**(항시 규칙 6 — 「고친」이 아니라 「수정한」인 것도 2026-08-18 사용자 결정이다) |
| **✅** | ~~**삭제 스와이프 버튼이 빨강이 아니다**~~ | **해소(2026-08-20 `b49f85f`) · 폰 판정 통과**(그 빌드의 확정 근거가 바로 이것이었다 — *"항목을 왼쪽으로 밀면 [삭제]가 빨강(coral)"*). 원인은 `RootView.swift:41`의 전역 `.tint(Palette.accent)`가 `role: .destructive`의 기본 빨강을 덮은 것. 고친 자리 **다섯**(`.tint(Palette.overdue)`): `InboxView.swift` **182 · 213 · 232** · `LivingView.swift` **47 · 66** (2026-08-21 grep 실측)<br>⛔ **옛 서술(낡았다):** *"세 곳 … **고칠 곳 두 줄**"* — **「두 줄」은 실측 다섯이었다.** `CLAUDE.md` 기록 규칙 8이 지적한 그 형태다(**셀 수 있는 것은 셀 때 센다**). 사용자: *"나중에 몰아서 한 번에"*로 미뤄 뒀던 것이 **그 사이에 들어갔다** |
| **✅** | ~~**07-24 자동 분류가 `discard`로 버린 기억 10개**~~ → **`discard`는 15개였다** | **정리 끝났다 (2026-08-21).** ⛔ **「10개」는 07-24 배치만 센 값이다** — 실측 **15개**. 목록에 없던 다섯(`2DF53C23`·`0EFE8E72`·`6FC79DA5`·`2896870F`·`2A22F3E3`)은 **`inbox.md`에 op 없이 create 블록에 `type: discard`가 박혀 있다**(레거시 웹 v0) — **계열 ③, 세니까 나왔다.**<br>✅ **`D6E4950B` 하나만 되살렸다** — 사용자가 폰에서 **2026-08-21 13:55:06 `set type=`**, 그리고 **13:55:46 `set type=principle`**(40초 뒤). **살아있는 원칙 12 → 13개**이고 `order`가 없어 **맨 뒤**에 놓인다(노출 기본 3이라 **「원칙 영역」에는 안 뜬다**). 실데이터로 확인 13:56.<br>⛔⛔ **★ 「원칙으로 분류됐다」로만 읽지 말 것 — 임시 자리다**(2026-08-21 사용자). *잊지 않으려고 눈에 띄는 자리에 둔 것*이고, **새 분류 셋(추억·기록·지식)이 생기면 「기록」이나 「추억」으로 옮긴다.** 그때까지 **원칙 목록 맨 아래**에 있다. **확정된 분류가 아니다** → §3-A ③<br>★ **두 경로를 갈라 적는다 — 같은 `discard` 이름으로 묶여 있었다:**<br>　**⑴ 07-24 자동이 버린 열** — op(`set type=discard`)로 덮었다. ⛔ **문제였던 것은 이쪽이다** — `D6E4950B`은 **사람이 확정한 뒤**(카운터 157 → 165) 덮였다. 지금 코드는 막는다(`classifyFields`가 discard 거부).<br>　**⑵ 레거시 웹 v0가 버린 다섯** — create 블록에 `type: discard`가 박혀 있고 **op이 없다**. **그때 방식대로 처리된 것일 수 있다** — 사람의 결정을 덮은 흔적이 없다. **⑴과 같은 잘못으로 읽지 말 것.**<br>⛔ **나머지 열넷은 그대로 둔다 — 다시 열지 말 것**(사용자: 나머지는 전부 지운 채로 둔다 · 다시 볼 일 없는 기록이다). ⚠️ **그 열넷도 두 종류다**(자동 아홉 · 레거시 다섯) — **결정은 같고 경위는 다르다.** 시험 수집·혼잣말·오인식·중복이고, `4F4A4397`은 살아 있는 `D7B14AFA`와 **같은 발화의 나쁜 받아쓰기**다(되살리면 중복).<br>★ **열다섯 전부 자료가 없다** — `voice`인데 `audio:` 포인터가 없다. **지워진 게 아니라 07-22부터 포인터가 찍힌다**(그 전 수집은 원본을 안 남겼다).<br>⚠️ **부수로 나온 사실: 삭제된 기억은 검색으로 못 찾는다** — `SearchView`는 `liveNonDone + doneItems`만 훑는다. 그래서 83줄을 눈으로 훑어야 했다. 전말 → `docs/worklog/2026-08-21-macbook.md` §10 |
| **✅** | ~~`MediaFetch.waitSeconds` 15초를 안 쟀다~~ | **해소(2026-08-20).** 쟀다 — **526~924ms.** 15초는 **유지**(문턱이라 넉넉함이 비용이 아니고, 줄일 근거가 없다: 셀룰러·느린 Wi-Fi·1MB 넘는 파일을 **안 쟀다**). **진짜 문제는 1초 폴링이었고 0.2초로 줄였다.** 설계 §6 |
| **✅** | ~~지도 앱에 핀이 안 찍힌다~~ | **해소(2026-08-20 `0727499`).** `ll=`만으로는 핀이 안 놓인다 — **이름(`q`)이 있어야 한다.** **2026-07-24 `8262265`부터 27일간 있던 결함이고 아이폰도 같았다.** ✅ **폰에 깔렸고 판정도 통과했다**(`b49f85f` 설치 2026-08-20 09:39 · 판정 `docs/worklog/2026-08-20-macmini-2.md` §3의 「덤」)<br>⛔ **옛 서술(낡았다 · 2026-08-21 정정):** *"⏸ 폰에는 아직 안 깔렸다 → §2"* — ★ **27일 묵은 결함을 찾아낸 자리가, 고친 뒤에도 「안 깔렸다」로 남아 있었다** |
| **⏸** | **「나머지는 다음에」의 「다음」을 화면이 말하지 않는다** | 실제로는 **콜드 런치·포그라운드 복귀**다. **지금 문제가 아닌 조건 = 한도가 「iCloud 쪽 0개인 실행만」이라 평생 한 번뿐.** ⛔ **조건을 바꾸면 살아난다**(설계 §5) |
| **✅** | ~~**좌우 맞춤**~~ | **닫혔다 (2026-08-21).** ★ **원문이 보이는 곳 전부**가 좌우 맞춤이다 — 원칙 목록·원칙 영역·지금 챙길 것·새 기억들·살아있는 기억·검색·보관함 **그리고 상세의 원문(편집 중에도)**. ⛔ **수집 창은 제외**(사용자 명시).<br>**사용자 줄바꿈 원칙 셋:** ① 부호가 줄 앞에 오려 하면 **앞 글자를 함께 내린다**(그 글자가 **한글일 때만**) ② **줄 앞 빈칸 금지**(첫 줄 예외) ③ **비한글은 단어 안에서 안 자른다.**<br>**구현:** 표시 = `JustifiedLineBreaker`(줄을 직접 나눈다) + `CTLineCreateJustifiedLine` · 편집 = `UITextView` **어절 끊기**(`byWordWrapping` + `lineBreakStrategy []`) — 원칙 셋이 저절로 지켜진다. **높이 6줄 상한 + 안쪽 스크롤**(넘치지 않으면 바깥이 스크롤 — 옛 성질 유지).<br>⚠️ **맥은 왼쪽 맞춤으로 남는다**(`UIView`가 iOS 전용). ⚠️ **`justifyMinFill = 0.85`** — 너무 짧은 줄은 안 맞춘다(내가 더한 안전장치 · 값은 사용자 사안).<br>⛔ **재는 법:** `CTFramesetter` 계산은 `UILabel`과 다르다 — **픽셀로 재야 한다**(`native/tools/ghostlab/measure-justify-px.swift`). 전말 → `docs/worklog/2026-08-21-macbook.md` §17~21 |
| **⏸ 새로** | **★ 이름 없이 반복되는 형태를 센다** | **지금 하지 않는다**(2026-08-21 · 과제로만). 계기: 이름을 준 형태 셋이 **하나같이 「세어 보니 다섯」**이었다(계열 ①~⑤ · 「덮는 값」 · 「결정 파수꾼」). ⚠️ **다섯은 저장소의 성질이 아니라 「내가 알아채는 임계」로 보인다**(셋 다 **알아챈 시점에** 센 값이다 — **추정**). **그렇다면 지금 2~4쯤 쌓인 형태가 이름 없이 더 있다.**<br>★ **어디를 봐야 하나 — 답이 있다.** 저장소가 **스스로 표시해 둔 반복 신호**를 세면 된다: `grep -rn "같은 형태다\|같은 뿌리다\|번째로 같은 형태" docs/worklog/*.md CLAUDE.md` → **2026-08-21 기준 15줄 · 8파일.** 거기에 worklog의 **★ 문장**과 커밋 메시지의 「또」·「n번째」를 더한다.<br>**눈에 보이는 후보 셋**(2026-08-23에 하나 더 들어왔다): ⓪ ★ **「예고가 한 축을 안 따진 것」**
(`photo-capture-design.md` §4-②의 쉼표 목록 — **파서만 따지고 병합을 안 따졌다. 따르면 자료를 잃는다** ·
`audio-capture-design.md` §7의 「항목별 옵트인」 예고 — **둘뿐이라 아직 이름을 안 준다**.
⚠️ **계열 ①~⑧과 축이 다르다** — 저쪽은 「문서가 실제와 어긋난 것」이고 이것은 **「처음부터 덜 따진 것」**이다) ·
① **「고치기가 아니라 자리를 없애기」**(잔상 → 그릇을 바꿨다 · 테두리 → 감지를 버렸다) ② **「최적화를 넣으면 다시 그려야 할 때를 놓친다」**(서명 비교 · `didReorder`의 되울림 차단).<br>⛔ **묶는 기준을 먼저 적는다** — 억지로 묶으면 이름이 뜻을 잃는다(계열 번호를 안 더한 판단과 같은 이유). **셋 이상 · 막힌 자리가 같을 때**만 이름을 준다. 전말 → `docs/worklog/2026-08-21-macbook.md` §31 |
| **⏸ 새로** | **★ 규칙보다 오래된 자리를 훑는다** | **지금 하지 않는다**(2026-08-21 · 과제로만 올린다). ★ **규칙이 막는 것은 앞으로 생길 것이고, 이미 갈린 자리는 따로 찾아야 한다.** **정본 훑기 규칙은 2026-08-18에 생겼는데**(축을 바꿨으면 문서를 훑는다) **웹 v0의 「읽기 전용」이 갈린 자리는 그보다 오래됐다** — `app.js`와 루트 `README.md`가 둘 다 *"inbox.md는 읽기만 한다"*고 적어 둔 채 `writeToFile()`이 덮어쓰고 있었다. ⛔ **규칙이 없던 시절에 갈린 자리는 규칙이 못 잡는다.**<br>**첫 사례(미확인으로 붙어 있다):** **웹 v0에 같은 형태가 더 있나 — 안 훑었다(못 잼).** 세대 전체를 「쓰지 않는다」로 표시한 것이 **그 미확인을 덮는 값**이다(위험은 낮췄고, **훑은 척은 하지 않았다**).<br>⚠️ **다른 후보:** `CLAUDE.md`의 규칙들이 각각 **언제 생겼나**를 보고 그 앞 시기의 코드·문서를 대상으로 삼는다 — 기록 규칙 2·8·9 · 계측 규칙 5·7 · 정본 훑기 규칙 · 항시 규칙 6·7·8. **오래된 것이 많은 쪽부터.**<br>⛔ **계열 ①~⑧에 번호를 더하지 않았다** — 그것들은 **「문서가 실제와 어긋난 모양」**의 목록이고, 이것은 **「규칙이 닿는 시간 범위」**라 축이 다르다. 섞으면 번호가 뜻을 잃는다. 전말 → `docs/worklog/2026-08-21-macbook.md` §15 |
| **⏸ 새로** | **검색이 「삭제된 기억」을 안 훑는다 — 「있는데 찾을 방법이 없다」** | **지금 고치지 않는다**(2026-08-21 사용자: *"보관함 화면으로 가면 보인다"*). `SearchView`가 훑는 것은 **`liveNonDone + doneItems`뿐**이고 **`trashed`(삭제 + `discard`)는 대상이 아니다.** 계기: `D6E4950B`를 찾을 때 **검색이 무용지물이라 83줄을 눈으로 훑어야 했고 첫 시도에서 못 찾았다.** ★ **여덟 번째 형태의 첫 사례**(이 표 머리) — 데이터·파서는 정상이고 **도달 경로만 없다.** ⚠️ **의도인지 결함인지 아직 안 정했다** — 「검색은 살아있는 것만」이 설계일 수 있다. 고치면 **삭제된 것이 검색에 섞이는 것을 사용자가 원하나**부터 정해야 한다(화면 동작이므로 사용자 결정) |
| **⏸** | **표본이 없어 못 닫은 확인 넷** | → `docs/native/sample-pending-checks.md` (여는 조건이 그 문서에 있다). **A그룹 조건 = 미확정인데 `due`/`resurface`에 날짜가 있는 항목이 하나라도 생기면.** ⛔ **2026-08-21 11:1x 실측: 미충족** — 미확정 **8개** 전부 `due` 없음(`inbox-state.py --unconfirmed`). ⚠️ **명단은 08-18과 둘이 바뀌었다**(`991728DB`·`4C0882A0` 나가고 `1D5E2FDE`·`84BE980B` 들어왔다 — 수만 같다). **B그룹(배너 둘)은 못 쟀다** — 실데이터 전수 재훑기가 필요하다<br>⛔ **그 문서 자체에 약점이 있다는 것을 2026-08-21에 적었다:** 「조건이 충족되면 열린다」는 **충족을 누가 알아채는가**에 달려 있는데 **그 장치가 없다.** `4C0882A0`의 op 순서(`confirmed` 먼저 → 30초 뒤 날짜)가 실측 근거다 — **순서가 반대였으면 표본이 30초 살았다 사라지고 아무도 못 봤다.** **어떻게 알아챌지는 안 정했다**(사용자 결정) |
| **⏸** | **07-24 배치가 확정 항목에 남긴 자동 값** | 그 배치는 op **116개**다. 2026-08-18에 되돌린 것은 **미확정 넷뿐** — 나머지는 확정·완료된 항목이라 **손대지 않았다**(확정 = 사람이 수락한 것이므로 결정 C와 어긋나지 않는다). ⚠️ **다만 셋은 다르다** — `C6D42F14`·`D536886F`·`D7B14AFA`는 **두 번 돌아 답이 달라졌고**(info→idea 등) **지금 값이 어느 쪽인지 사람이 본 적이 없다.** 전말 `docs/worklog/2026-08-18-macbook.md` §5-2 <br>✅ **2026-08-21 재확인: 셋 다 그대로다**(`C6D42F14`=`info-action` · `D536886F`=`info-action` · `D7B14AFA`=`idea`) — 아무도 안 봤다 |
| **⏸** | **Core 시험의 `confirmed: false` 보일러플레이트** | 헬퍼 기본값이라 **의도한 단정이 아니다**(Core는 `confirmed`를 안 본다 — 완료·확정이 직교). 지금은 무해하지만 **나중에 Core에 확정 게이트를 넣으면 무더기로 깨진다**(`NotificationPlannerTests`·`NotificationBudgetTests`·`RecurrenceCompletionTests` 등). 그때 「시험이 깨졌다」가 아니라 **「기본값이 뜻 없이 false였다」**로 읽을 것 |
| **⏸** | **설정 「지금 분류하기」의 옛 `.disabled` 조건 셋** | 키 없음 · 미분류 0 · 진행 중. 2026-08-18에 **뗐다** — 회색이면 눌러도 안내가 안 떠 「왜 안 되나」를 알 길이 없어진다. **재개발 때 되살릴지 정한다**(코드 주석에 옛 조건이 남아 있다 · `SettingsView`) <br>⛔ **2026-08-21: 이 줄은 `CLAUDE.md` 항시 규칙 8의 영역이다** — 자동 분류를 **zero base로 새로 설계**하기로 했으므로 **되살릴지 정하는 것 자체가 그 설계 안에서** 이뤄진다. 허락 없이 손대지 않는다 |

---

## 4-A. ✅ 확인 목록 7개 — **①원칙 3 + ③수집 4 · 전부 통과** (2026-08-20 신설 · 08-20 닫힘)

> ### ✅ 판정 — **일곱 다 통과했다** (2026-08-20 · 사용자 · `docs/worklog/2026-08-20-macmini-2.md` §3)
> ①-1 목록이 열린다 · ①-2 드래그 정렬이 남는다 · ①-3 **0개 밴드 숨김** ·
> ③-1 온디바이스 STT · ③-2 텍스트 입력 · **③-3 액션 버튼** · **③-4 Siri·단축어**.
> **덤으로 둘:** 삭제 스와이프 빨강 · **지도 핀(폰에서 처음)**.
> ⛔ **`③-3`·`③-4`는 앱 경계 밖이라 눌러야만 나오는 것이었다** — 통과했다.
>
> ### 설치: **2026-08-21 00:23 · `c756655`** ← **최신**
> **확정 근거: 끌어서 놓아도 앱이 안 죽는다** (직전 `069f2e9`은 놓는 순간 죽었다).
> **원칙 목록 판정은 이 빌드에서 전부 통과했고 미확인이 없다** → 이 문서 맨 위.
>
> ### ▶ 설치 이력 전부는 **`docs/install-log.md`** 에 있다 (2026-08-21에 옮겼다)
> **여기에는 최신 한 줄만 둔다.** `HANDOFF`는 스냅샷이라 **이력을 담으면 반드시 잘린다** —
> 실제로 잘려서 **08-20 저녁 여덟 번이 worklog에만** 남았고, 그 구멍 안에 **원칙 목록을 고친
> 두 커밋(`50a8e48`·`c6ed58b`)**이 있었다. 그래서 §4가 **이미 된 일을 「하지 말 것」**으로 적고 있었다.
> ⛔ **여기에 이력을 다시 쌓지 말 것.** 설치 직후 한 줄은 **`install-log.md`에** 쓴다(기록 규칙 2).
>
> ⛔ **옛 서술(낡았다 · 2026-08-21 정정):** 이 자리에 *"설치: 2026-08-20 16:36 · `6973775` ← 최신 …
> ⛔ 테두리를 없앤 빌드다 … ㉡을 새로 열려면 `List`를 버리는 값을 사용자가 먼저 정한다"*가 있었다.
> **그 뒤로 설치가 여덟 번 더 있었고**(17:59 · 18:20 · 23:05 · 23:23 · 23:45 · 23:53 · 00:10 · 00:23)
> **㉡은 값을 치르지 않고 `c6ed58b`의 차용으로 닫혔다.**
>
> ⛔ **아래에 있던 설치 이력 열한 블록은 `docs/install-log.md`로 옮겼다**(2026-08-21).
> 확정 근거·판정이 **한 줄씩 그대로 살아 있다** — 빠뜨린 것은 없고, **worklog에만 있던 저녁 여덟 번이
> 함께 들어갔다**(그래서 이제 한 곳에서 「이 커밋이 깔린 적 있나」에 답한다).

> **누가 무엇을 하나** (`CLAUDE.md` 항시 규칙 7): **Claude Code = 빌드·설치.** **사용자 = 화면을 눌러 여는 것과 무엇이 보이는지 판정하는 것.**

### ① 원칙 (`PrincipleListView.swift` · `memory-philosophy.md` §3)

| | 무엇을 | 어디를 눌러주세요 | 무엇이 보여야 하나 |
|---|---|---|---|
| **①-1** | 목록 화면이 열리나 | 아무 탭에서나 **화면 맨 위 「원칙 영역」을 탭** *(2026-08-21 정정: 옛 서술은 「원칙 띠」였는데 **「띠」는 앱에 없는 말이다** — 머리글이 스스로 「원칙 영역」이라 부른다)* | 원칙 목록이 뜬다 |
| **①-2** | **드래그 정렬**이 되고 **남나** | 그 목록에서 항목을 **길게 눌러 위아래로 끌기** → **나갔다 다시 들어오기** | 순서가 바뀌고 **다시 들어와도 그대로다** ⚠️ **나갔다 오는 것까지가 이 항목이다** — 바뀌기만 하고 안 남는 것이 이 종류의 전형이다 |
| **①-3** | **「0개 밴드 숨김」** | 같은 목록에서 **항목이 0개인 밴드가 있나** 본다 | **안 보인다** |

### ③ 수집 (`CaptureSheet`·`SpeechCapture`·`CaptureIntents` · `memory-philosophy.md` §5)

| | 무엇을 | 어디를 눌러주세요 | 무엇이 보여야 하나 |
|---|---|---|---|
| **③-1** | **온디바이스 한국어 STT** | 「새로운 기억」 **우상단 마이크** 탭 → 말하기 | 말한 것이 **글자로** 뜬다 |
| **③-2** | 텍스트 입력 | 같은 시트에서 **키보드로 입력** → 저장 | 「새 기억들」에 든다 |
| **③-3** | **★ 액션 버튼** | 폰 **왼쪽 옆면 액션 버튼** (먼저 iOS **설정 ▸ 액션 버튼 ▸ 단축어**에서 **「음성으로 새 기억」**을 고를 수 있나부터) | 앱이 열리고 **마이크 시트가 바로 뜬다** |
| **③-4** | **★ Siri · 단축어** | Siri에 **「SecondBrain으로 기억하기」** (또는 단축어 앱에서 그 이름을 찾는다) | 같은 동작 |

> ### ⛔ ③-3·③-4가 이 목록의 핵심이다 — **앱 경계 밖이다**
> `AppShortcutsProvider`가 *"Shortcuts 앱·액션 버튼 설정에 **자동 노출**(별도 등록 절차 불필요)"*이라고
> **코드 주석이 말하지만, 실제로 그 목록에 뜨는지는 눌러야 안다.**
> **지도 핀과 정확히 같은 종류다** — 시스템이 어떻게 해석하는지는 코드 밖이고, 안 떠도 **오류도 로그도 안 남는다.**
> ⚠️ **「설정에 안 보인다」도 판정이다** — 그것이 결함이다. **못 찾으면 못 찾았다고 적는다.**

**②(자동분류)는 이 목록에 없다** — 2026-08-18 `ClassifyPause`가 진입점 둘(당겨서 분류 · 설정 「지금 분류하기」)을
다 막아서 **지금은 누를 수 없다.** 재개발 때 검증한다.

---

## 5. 이 프로젝트에서 항상 걸리는 규칙 (요약)

1. **화면에 나오는 말은 사용자가 정한다.** 후보를 내고 사용자가 고른다.
2. **명령은 Claude, 화면은 사용자.** 빌드·설치·스크린샷은 자동화, **판정은 사용자.**
3. **잰 값과 짐작한 값을 같은 말투로 말하지 않는다.** 표본·목록은 **이름이 닮았는지가 아니라 데이터에서 계산한다** (`native/tools/inbox-state.py`).
4. **조작 하나 → 기록 한 줄.** 설치·조작은 그 자리에서 적는다.
5. **실기기 확인은 단정 금지.** 헤드리스로 못 여는 것은 사용자에게 확인을 요청한다.
