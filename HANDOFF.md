# HANDOFF — 현재 스냅샷

> **이 문서는 스냅샷이다.** 「지금 어디까지 와 있나」만 적는다.
> 전말은 `docs/worklog/`, 정본은 `docs/native/`(뜻 = `memory-philosophy.md` · 동작 = `edit-policy.md`).
> **어긋나면 정본이 맞다.** 이 문서가 낡았으면 이 문서를 고친다.
>
> **갱신:** 2026-08-19 01:4x KST · 집 맥미니(`teri.kr`, macOS 26.5)

---

## ✅ 해소됨 — classify.py launchd (2026-08-19)

**두 기기 모두 확인 끝. 더 볼 것 없다.**

| | |
|---|---|
| 회사 맥북 | ✅ **차단 완료** (2026-08-18) |
| 집 맥미니 | ✅ **애초에 설치돼 있지 않았다** (2026-08-19 확인) |

확인한 셋: `launchctl print` **없음** · `~/Library/LaunchAgents`에 plist **없음** · `crontab` **없음**.
**결정적 근거는 파일 쪽이다** — `inbox.md`의 마지막 수정이 **2026-08-18 15:01**(맥북의 데이터 정리 시각) 그대로다.
launchd가 살아 있었다면 16:00·17:00…에 덮어썼을 것이다. **08-18 데이터 정리는 온전하다.**

⚠️ `python3 classify.py`를 **손으로 돌리지 않는다** — 자동 분류는 재개발 예정
(`docs/native/memory-philosophy.md` §2-1-B).

---

## 1. 마지막 커밋 단계

| | |
|---|---|
| 브랜치 | `main` 하나 · worktree 하나 · **clean** |
| HEAD | **2026-08-19 맥미니 세션 커밋** (설계 신설 + HANDOFF/worklog) |
| **코드가 마지막으로 바뀐 커밋** | **`dbbafc8`** — **폰에 깔린 것과 같다.** 08-19에는 **코드 0줄** |

**`f43a84d` 이후:**

| 커밋 | 무엇 |
|---|---|
| *(08-19)* | **`media-icloud-design.md` 신설** · HANDOFF 로드맵 · worklog. **코드 0줄** |
| `4df0b02` | 규칙(푸시는 지시 후) · 맥미니 경고를 맨 위로 · 미룬 것 셋 |
| `c6ed9a2` | 실기기 확인 다섯 통과 — 「봐야 하는 것」 0 |
| `975767b` | 정본 ① — §1의 「알림이 울릴 수 있다」를 대조표로 뒤집었다 |
| `f04e9f1` | **정본 훑기 규칙**(CLAUDE.md + lessons) · **설치 기록** |
| `45d4f62` | 정본 ②③⑥ · 사양서 ④⑤ · `sample-pending-checks.md` 신설 |
| **`dbbafc8`** | **★ 코드 — `catchUpRecurrence`도 확정만 전진(게이트 셋 완성)** |
| `a07b06b` | 정본 — §2-1-B 신설 · §3 좁힘 · 축 구분 셋 |
| **`ce0142e`** | **★ 코드 — 목록에서 완료 제거 · 알림·파티션을 확정 축으로 · 자동 분류 일시 중지** |
| `2b2edcd` | HANDOFF 갱신(미확정 4→6 · 상세 완료 게이트) |
| `2b83359` | HANDOFF.md 신설 |
| `a53038b` | 맥북 인계 — `inbox-state.py` 신설 · 삭제 둘 정정 |
| `947f686` | 세션 인계 문서(`docs/handoff/2026-08-18-session-for-claude-chat.md`) |
| `de1d936` | 되풀이 시험 항목 조사 worklog (**아무것도 안 지웠다**) |

**직전 코드 커밋 둘 (2026-08-18 맥미니, 「임시」 표시 재배치):**

| 커밋 | 무엇 |
|---|---|
| `f43a84d` | 나가기 경고 문구를 **상태별로 가른다** (임시/확정) |
| `c045f31` | **「임시」 표시는 결정 버튼이 없는 곳에만** — 상세에서 빼고 목록 셋에 **캡션 줄**로 |

**이 기기에서 마지막으로 확인한 값 (2026-08-18 15:4x, 맥북 · `dbbafc8` 기준):**

| 검증 | 결과 |
|---|---|
| `swift test --package-path SecondBrainCore` | ✅ **322개 · 실패 0** |
| `xcodegen generate` | ✅ Created project |
| iOS 빌드 (스킴 `SecondBrainApp-iOS` · UDID `ED17E1A9-8F2A-4994-AAD2-C9AC46177EB5`) | ✅ **BUILD SUCCEEDED** · `error:` **0건** · `warning:` **0건** · EXIT=0 |

> ⚠️ **시뮬 UDID는 기기마다 다르다.** 맥미니는 `34A5033B-…`를 썼다.
> 매번 `xcrun simctl list devices available`로 확인할 것.

---

## 2. 실기기 검증 상태

| | |
|---|---|
| 폰 | iPhone 16 Pro · **`dbbafc8`** 설치 (**2026-08-18 15:55**) |
| **저장소 코드와 같은가** | ✅ **같다** — `dbbafc8` 이후 커밋은 문서뿐 |
| **재설치** | ❌ **필요 없다** |
| **이 빌드의 확정 근거** | 「새 기억들」을 길게 눌렀을 때 **완료가 없다**(기억하기·삭제 둘뿐) |
| **실기기 확인** | ✅ **7·1·2·3·4 전부 통과** (2026-08-18 16:0x~16:1x) → **「봐야 하는 것」 0개** |

**확인 목록 17개 — 닫힘 15 · 「봐야 하는 것」 0개.**

| 상태 | 번호 | 수 |
|---|---|---|
| ✅ 닫힘 | 1·2·3·4·5·6·7·9·10·12·13·14·15·16·17 | **15** |
| 보면 좋은 것 | **8**(이름 칸 90pt, XXL까지) · **11 잔여**(배너 둘 — 실사용 중에 자연히 생기면) | **2** |

**2026-08-18 새벽에 닫힌 다섯:**

| 확인 | 판정 |
|---|---|
| 10번 — 분류 바꾸고 저장 없이 `<` → 되돌아오나 | ✅ 통과 |
| 9번 — 원문 고치고 저장 없이 → 성역(수집 시각·기기) 불변인가 | ✅ 통과 |
| ③ — 임시 항목 원문 고치고 `<` → 경고 뜨나 | ✅ 뜬다 (여기서 「문구가 양쪽 같다」가 잡혀 `f43a84d`로 이어졌다) |
| 14번 — chevron 없이도 메뉴인 줄 알겠나 | ✅ **닫힘** (사용자 최종: *"그냥 아이콘 누르면 변경 가능한 것으로 알게 될 것이라고 확신해"*) |
| ①②④ (08-14분) | ✅ op 로그로 확정 |

> ⚠️ **표본이 없어 못 닫은 확인은 따로 모아 뒀다 → `docs/native/sample-pending-checks.md`** (넷).
> A그룹 셋(「지금 챙길 것」에서 갈리나 · 「새 기억들」에 남나 · 알림이 안 울리나)은 **여는 조건이 같아
> 한꺼번에 열린다** — *살아있는 미확정 중 유효한 날짜를 가진 것이 하나라도 생기면.*
> A-4(미확정 되풀이의 회차 자동 전진)는 조건이 하나 더 붙고 **셋 중 가장 조용히 틀리는 자리**다.
> B그룹은 배너 둘(상태가 자연히 생기면).

**살아있는 미확정 = 8개 · 전부 미분류** (`inbox-state.py --unconfirmed` · **2026-08-18 15:01:33**)

| id | 원문 | 분류 | 섹션 |
|---|---|---|---|
| `B3A9A907` | 어서 말을 해 - 해바라기 | — | 새 기억들 |
| `991728DB` | 차에 비닐봉지 하나 갖다놓기 | — | 새 기억들 |
| `32B23159` | 류마 엄완식원장 컨택 | — | 새 기억들 |
| `4C0882A0` | 나올 거라면 나올 거라고 뚜렷하게… | — | 새 기억들 |
| `0E4B8C7F` | 회사 조직 관련 고민… | — | 새 기억들 |
| `F5433F49` | 여기 제품이 업데이트 된… | — | 새 기억들 |
| `99E2B834` | 영업이 밖으로 나가는… | — | 새 기억들 |
| `516C17F0` | 회사 일인데 메디게이트… | — | 새 기억들 |

- **8개 전부 due·되풀이 없음** → 「지금 챙길 것」의 임시는 **여전히 0**. **ⓔ·실기기 5·6이 다 같은 이유로 못 닫힌다.**
- **뒤의 넷은 2026-08-18 15:01에 손으로 고쳤다** — 자동이 쓴 `type`/`due`/`resurface`/`status`를 걷어냈다
  (`native/tools/2026-08-18-strip-auto-fields.py` · 백업 `backups/*.bak-20260818-150125`).

> ⚠️ **★ 이 숫자는 iCloud 동기화 시점에 따라 달라진다 — 한 세션 안에서 4→6→8로 늘었다.**
> 10:0x에 4개였을 때 `inbox-iphone-4532.md`의 마지막 쓰기가 **08-15 16:43**으로 보였고,
> 10:4x에는 **08-18 09:43**이었다 — **두 실행 사이에 파일이 내려왔다.**
> `inbox-state.py` 머리주석이 경고한 그 지연이다. **폰 화면과 어긋나면 파일이 뒤처진 것으로 본다.**
> **표본 수를 적을 때는 실행 시각을 함께 적는다**(사용자 지시, 2026-08-18).

---

## 3. 다음 작업

> # ▶ **(1) 자료를 iCloud로 — 구현 첫 걸음: 하위 폴더 생성 확인**
>
> **설계 정본:** `docs/native/media-icloud-design.md` (2026-08-19 확정 · **코드 0줄**)
> **어디서:** 회사 맥북에서 이어간다.
>
> **첫 걸음이 왜 그것인가:** iCloud 폴더 안에 **디렉터리를 만들 수 있는지**를 안 재봤다.
> 파일을 만드는 것은 확실하지만(`inbox-iphone-4532.md`가 증거) 하위 폴더는 처음이다.
> **되면 `audio/`·`photo/`, 안 되면 루트에 `sb-<id>.m4a` 폴백 + ⛔ 기록을 남긴다**(설계 §2).
>
> **정해진 것:** 문구 넷(설계 §8) · Mac 사진 보기 **넣는다**(§7) · 로컬 사본 **남긴다**(§5) ·
> 구현 순서 여섯(§11). **정본 뒤집기는 구현·검증 뒤에**(§0 머리).

### ✅ 2026-08-18에 끝난 것 — 「임시는 기억하기 전까지 아무것도 하지 않는다」

**결정 넷과 코드 넷이 함께 들어갔다.** 전말은 `docs/worklog/2026-08-18-macbook.md`.

| 구현 | 자리 |
|---|---|
| 파티션 축을 **시점 → 확정**으로 | `InboxModel.partition` — 미확정은 시점이 있어도 「새 기억들」 |
| **알림도 확정만** | `InboxModel.notifiable` — 파티션과 **같은 조건**(한쪽만 고치면 반쪽) |
| 임시에서 **[완료]·[했어요] 제거** | `InboxView` — `newItemActions`는 기억하기·삭제 둘뿐 |
| **자동 분류 일시 중지** | `ClassifyPause` — 당겨서 분류·설정 버튼. **기능은 안 지웠다**(재개발 예정) |

**정본에 올린 것:** `memory-philosophy.md` **§2-1-B**(미확정에 시점이 없는 이유 — 귀결 형태) ·
같은 절의 **자동 분류 재개발 원칙**(「자동은 준비까지, 결정은 사용자가」) · **§5 경계 규칙 축 변경** ·
**§7-1 (c)에 축 구분**(§7 폴백 = 분류 축 ≠ 알림 게이트 = 확정 축) ·
`edit-policy.md` **§3 좁힘**([삭제]만) · **§1-A 표 정정**(상세의 완료는 되풀이+확정에만).

**데이터 정리 (2026-08-18 15:01):** 자동이 쓴 필드 **14줄 삭제** — 미확정 8개가 **전부 미분류**가 됐다.
항목 72 불변 · 신원(`id`/`hlc`/`device`) 71 불변. 도구·근거는 `native/tools/2026-08-18-strip-auto-fields.py`.

### ⏸ 남은 것 — 우선순위 순

1. ✅ **폰 설치 + 실기기 확인 — 끝났다.** `dbbafc8` 설치(2026-08-18 15:55) · 확인 다섯 전부 통과.
   **1-D가 폰에 깔렸으므로 당겨서 분류는 이제 막혀 있다.**
2. ✅ **집 맥미니의 launchd — 해소됐다** (2026-08-19). 애초에 설치돼 있지 않았다. → 이 문서 맨 위.
3. **⏸ `catchUpRecurrence` 미결** — 앱을 열기만 해도 **미확정 되풀이의 시점이 전진**한다
   (`confirmed`를 안 본다). §2-1-B와 정면으로 어긋나는 자리인데 **이번에 안 고쳤다 — 별개 결정.**
   지금 표본 0(미확정 8개 중 되풀이 없음). 자동 분류 재개발 전에 정해야 한다. → `edit-policy.md` §미결
4. **자동 분류 재개발** — 원칙만 정해져 있다(제안을 필드에 쓰지 않는다). **형태·수명·병합은 미정.**
5. **⏸ 「수집 원본 자료」 2차 압축 (2단계)** — 아래로 미뤘다. **코드 0줄 · 설계 전.**

### ⏸ 2차 압축 2단계 — 미뤘다 (착수 조건은 그대로)

| 단계 | 상태 |
|---|---|
| 1단계(성역 접기 · 분류 세로 · 좌우 2:1) + 2차 묶음 | ✅ 완료 (2026-08-12) · 사진 없는 항목에서 **~129pt** 감소 |
| **2단계**(사진·지도를 성역에서 떼어 새 카드로) | ⏸ **코드 0줄 · 설계 전** |

**왜 멈춰 있나:** 2026-08-13 실기기에서 사용자가 걸렸다 — **「성역을 펼쳐서 사진을 보는 방식은 매우 불편하다」**.
사진·지도가 **성역 카드 안**에 있고 성역은 **기본 접힘**이며 접기가 **두 카드를 함께** 움직인다.
그래서 2단계가 「하면 좋은 것」에서 **「걸린 것」**이 됐고, **계측과의 의존이 뒤집혔다** —
2단계 방향이 무엇을 잴지 정한다. ⚠️ **방향 없이 계측부터 하면 곧 버릴 배치를 잰다.**

**착수하려면:** ① 방향을 사양서/Claude 챗에서 받는다(즉흥 설계 금지) ② **화면 이름은 사용자가 정한다**
(「수집 원본 자료」는 작업용 이름) ③ 그 뒤 계측(`native/tools/measure-ui.swift`·`measure-text.swift`,
폰 스크린샷 가능 · 재료 `82B1044B`) — **Dynamic Type 실측도 이때 함께 닫는다**(지금 값은 공표 표)
④ 배너 다섯은 건드리지 않는다(두 카드 밖).

---

## 3-A. 자료 쪽 로드맵 — 미룬 것과 **순서** (2026-08-19 사용자 결정)

> ### ★ 순서: **자료 위치 → 자료 확장 → 분류 → 해시태그**
> 이 순서는 사용자가 정했다. **앞의 것이 뒤의 것의 토대다** — 자료가 어디 있는지가 정해져야
> 여러 개·여러 종류를 얹을 수 있고, 그 뒤에 분류와 해시태그가 온다.

| 순서 | 무엇 | 상태 |
|---|---|---|
| **①** | **자료 위치 — iCloud로** | ▶ **지금 이것.** 설계 확정 · `docs/native/media-icloud-design.md` |
| **②** | **자료 확장 2단계** — 여러 개 · 여러 종류 · 카드 분리 · 추가 · 삭제 | ⏸ 설계 전 |
| **③** | **새 분류 셋** — **추억 · 기록 · 지식** | ⏸ 설계 전 |
| **④** | **해시태그** | ⏸ 설계 전 |

### ⏸ 자료 삭제 · 보관함 완전 삭제 — **①과 나란히 미뤘다**

**순서표에 안 넣었다.** ②의 일부이기도 하고 그보다 무겁기도 해서, **병합 설계가 풀려야 시작된다.**

**왜 미뤘나 (2026-08-19 조사):**
- op 로그가 **append-only**이고 「지운다」 verb가 **없다**(`EventWriter`가 내는 verb는 create/delete/undelete/set/edit 다섯).
- `delete`는 **tombstone**일 뿐이라 항목이 영원히 존재한다.
- **반쪽 삭제는 유령을 만든다** — `MergeEngine.merge`는 이벤트를 id로만 묶고 **create 블록이 있는지 안 본다.**
  create만 지우고 op을 남기면 **`raw` 없는 빈 줄**이 보관함에 남는다.
- **다른 기기가 옛 사본을 들고 있으면 되살아난다.**
- 그리고 **급하지 않다** — 안 지워도 지금 상태 그대로다.

> ⛔ **①이 이것의 빚을 늘린다.** 삭제 상태 **36개**(음성 28 · 사진 8)도 iCloud에 올라간다.
> **나중에 회수해야 한다 — 2026-08-19에 알고 받아들인 대가다.**
> 근거·전말: `docs/native/media-icloud-design.md` §9.

**지금 코드 상태:** `ArchiveView`에 스와이프 동작은 **「되돌리기」 하나뿐**이다. 완전 삭제 코드는 **0줄**.
확정된 자료 파일(`<id>.m4a`/`<id>.jpg`)을 지우는 코드도 **0줄**(`removeItem`은 전부 임시 파일용).

---

## 4. 그 밖에 열려 있는 것 (참고 — 한 줄씩)

| | 무엇 | 상태 |
|---|---|---|
| **ⓓ** | 회색 글리프를 누르면 상세로 가는 **폴백**을 의도로 채택할지 | 미결. 막는 근거가 하나로 줄었다(**「같은 탭이 상태에 따라 다른 일을 한다」**만 남음) |
| **⏸** | 되풀이 시험 항목 정리 | 조사만 함. **아무것도 안 지웠다.** 사용자: *"나중에 다시 물어볼께"* → worklog §12(특히 **12-5**, 이제 12개) |
| **⏸** | 날짜 표시가 화면마다 다르다 (수집 시각은 연도까지 · 마감은 올해면 생략) | 사용자: *"나중에 따로 맞추자."* 맞추면 검색·보관도 함께 바뀐다 |
| **⏸** | 확정 쪽 경고 문구에 **「수정」이 두 번** | 자잘한 미결 |
| **⏸** | **삭제 스와이프 버튼이 빨강이 아니다** | 원인 확정 — `RootView.swift:41`의 **전역 `.tint(Palette.accent)`**(2026-07-18)가 `role: .destructive`의 기본 빨강을 덮는다. 세 곳(`InboxView:158` = 새기억들·지금챙길것 공용 · `LivingView:47`). **고칠 곳 두 줄**(`.tint(Palette.overdue)` — 상세의 [삭제하기]가 이미 coral이다). 사용자: *"나중에 몰아서 한 번에"* → **색 정리 과제 · 다음 설치에 함께** |
| **⏸** | **07-24 자동 분류가 `discard`로 버린 기억 10개** | **판단 보류 — 이번엔 안 건드렸다.** 전부 되살려진 적 없음. 그중 하나는 **사람이 확정한 뒤** 버려졌다. id·전말은 `docs/worklog/2026-08-18-macbook.md` §8 |
| **⏸** | **표본이 없어 못 닫은 확인 넷** | → `docs/native/sample-pending-checks.md` (여는 조건이 그 문서에 있다) |
| **⏸** | **07-24 배치가 확정 항목에 남긴 자동 값** | 그 배치는 op **116개**다. 2026-08-18에 되돌린 것은 **미확정 넷뿐** — 나머지는 확정·완료된 항목이라 **손대지 않았다**(확정 = 사람이 수락한 것이므로 결정 C와 어긋나지 않는다). ⚠️ **다만 셋은 다르다** — `C6D42F14`·`D536886F`·`D7B14AFA`는 **두 번 돌아 답이 달라졌고**(info→idea 등) **지금 값이 어느 쪽인지 사람이 본 적이 없다.** 전말 `docs/worklog/2026-08-18-macbook.md` §5-2 |
| **⏸** | **Core 시험의 `confirmed: false` 보일러플레이트** | 헬퍼 기본값이라 **의도한 단정이 아니다**(Core는 `confirmed`를 안 본다 — 완료·확정이 직교). 지금은 무해하지만 **나중에 Core에 확정 게이트를 넣으면 무더기로 깨진다**(`NotificationPlannerTests`·`NotificationBudgetTests`·`RecurrenceCompletionTests` 등). 그때 「시험이 깨졌다」가 아니라 **「기본값이 뜻 없이 false였다」**로 읽을 것 |
| **⏸** | **설정 「지금 분류하기」의 옛 `.disabled` 조건 셋** | 키 없음 · 미분류 0 · 진행 중. 2026-08-18에 **뗐다** — 회색이면 눌러도 안내가 안 떠 「왜 안 되나」를 알 길이 없어진다. **재개발 때 되살릴지 정한다**(코드 주석에 옛 조건이 남아 있다 · `SettingsView`) |

---

## 5. 이 프로젝트에서 항상 걸리는 규칙 (요약)

1. **화면에 나오는 말은 사용자가 정한다.** 후보를 내고 사용자가 고른다.
2. **명령은 Claude, 화면은 사용자.** 빌드·설치·스크린샷은 자동화, **판정은 사용자.**
3. **잰 값과 짐작한 값을 같은 말투로 말하지 않는다.** 표본·목록은 **이름이 닮았는지가 아니라 데이터에서 계산한다** (`native/tools/inbox-state.py`).
4. **조작 하나 → 기록 한 줄.** 설치·조작은 그 자리에서 적는다.
5. **실기기 확인은 단정 금지.** 헤드리스로 못 여는 것은 사용자에게 확인을 요청한다.
