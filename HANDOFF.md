# HANDOFF — 현재 스냅샷

> **이 문서는 스냅샷이다.** 「지금 어디까지 와 있나」만 적는다.
> 전말은 `docs/worklog/`, 정본은 `docs/native/`(뜻 = `memory-philosophy.md` · 동작 = `edit-policy.md`).
> **어긋나면 정본이 맞다.** 이 문서가 낡았으면 이 문서를 고친다.
>
> **갱신:** 2026-08-18 10:0x KST · 회사 맥북(`MacBook-Pro-2.local`, macOS 26.5.2)

---

## 1. 마지막 커밋 단계

| | |
|---|---|
| 브랜치 | `main` 하나 · worktree 하나 · **clean** |
| HEAD | **`a53038b`** = `origin/main` (동기) |
| **코드가 마지막으로 바뀐 커밋** | **`f43a84d`** — 그 뒤 셋은 **문서·도구뿐**(`native/Sources` 0줄) |

**`f43a84d` 이후 (코드 무변경):**

| 커밋 | 무엇 |
|---|---|
| `a53038b` | 맥북 인계 — `inbox-state.py` 신설 · 삭제 둘 정정 |
| `947f686` | 세션 인계 문서(`docs/handoff/2026-08-18-session-for-claude-chat.md`) |
| `de1d936` | 되풀이 시험 항목 조사 worklog (**아무것도 안 지웠다**) |

**직전 코드 커밋 둘 (2026-08-18 맥미니, 「임시」 표시 재배치):**

| 커밋 | 무엇 |
|---|---|
| `f43a84d` | 나가기 경고 문구를 **상태별로 가른다** (임시/확정) |
| `c045f31` | **「임시」 표시는 결정 버튼이 없는 곳에만** — 상세에서 빼고 목록 셋에 **캡션 줄**로 |

**이 기기에서 오늘 다시 확인한 값 (2026-08-18 10:0x, 맥북):**

| 검증 | 결과 |
|---|---|
| `swift test --package-path SecondBrainCore` | ✅ **322개 · 실패 0** |
| `xcodegen generate` | ✅ Created project |
| iOS 빌드 (스킴 `SecondBrainApp-iOS` · UDID `ED17E1A9-8F2A-4994-AAD2-C9AC46177EB5`) | ✅ **BUILD SUCCEEDED** · `error:` **0건** · EXIT=0 |

> ⚠️ **시뮬 UDID는 기기마다 다르다.** 맥미니는 `34A5033B-…`를 썼다.
> 매번 `xcrun simctl list devices available`로 확인할 것.

---

## 2. 실기기 검증 상태

| | |
|---|---|
| 폰 | iPhone 16 Pro · **`f43a84d`** 설치 (2026-08-18 01:52) |
| **저장소 코드와 같은가** | ✅ **같다** — `git diff f43a84d..HEAD -- native/Sources` **비어 있다** |
| **재설치** | ❌ **필요 없다** |

**확인 목록 17개 — 닫힘 15 · 「봐야 하는 것」 0개.**

| 상태 | 번호 | 수 |
|---|---|---|
| ✅ 닫힘 | 1·2·3·4·5·6·7·9·10·12·13·14·15·16·17 | **15** |
| 보면 좋은 것 | **8**(이름 칸 90pt, XXL까지) · **11 잔여**(배너 둘 — 실사용 중에 자연히 생기면) | **2** |

**2026-08-18 새벽에 닫힌 다섯:**

| 확인 | 판정 |
|---|---|
| 10번 — 분류 바꾸고 저장 없이 `<` → 되돌아오나 | ✅ 통과 |
| 9번 — 원문 고치고 저장 없이 → 성역(수집 시각·기기) 불변인가 | ✅ 통과 |
| ③ — 임시 항목 원문 고치고 `<` → 경고 뜨나 | ✅ 뜬다 (여기서 「문구가 양쪽 같다」가 잡혀 `f43a84d`로 이어졌다) |
| 14번 — chevron 없이도 메뉴인 줄 알겠나 | ✅ **닫힘** (사용자 최종: *"그냥 아이콘 누르면 변경 가능한 것으로 알게 될 것이라고 확신해"*) |
| ①②④ (08-14분) | ✅ op 로그로 확정 |

> ⚠️ **표본 0이라 「통과」가 아닌 것 하나 — ⓔ.** 「지금 챙길 것」에서 임시가 갈리는지는 **볼 항목이 없어서** 못 봤다.
> 실데이터 확인(`inbox-state.py --unconfirmed`): **살아있는 미확정 4개가 전부 「새 기억들」의 `idea`**, due·되풀이 없음.
> **자동 분류가 시점을 붙이기 시작하면 그때 표본이 생긴다.**

---

## 3. 다음 작업 — 「수집 원본 자료」 2차 압축 (2단계)

### 지금까지 온 곳

| 단계 | 상태 |
|---|---|
| **1단계** (성역 접기 + 분류 세로 배치 + 좌우 2:1 나란히) | ✅ **완료** (2026-08-12 · `e01b041`·`4de4f19`·`2d3dc67`·`7835bc5`) |
| **2차 묶음** (분류 카드 접기 표시 · 접힘은 아이콘만 · 메뉴 chevron 제거) | ✅ **완료** (`ae0421d`·`dbc45c5`) — chevron 제거는 **2026-08-18에 「되살리지 않는다」로 최종 확정** |
| 압축 효과 | 📐 사진 없는 항목에서 **~129pt** 줄었다 (실측) |
| **2단계 「수집 원본 자료」** (사진·지도를 성역에서 떼어 새 카드로) | ⏸ **설계 전 — 착수 안 했다.** 코드 0줄 |

### 왜 멈춰 있나 — **방향 대기 하나**

**2026-08-13 실기기에서 사용자가 걸렸다:**

> **「성역을 펼쳐서 사진을 보는 방식은 매우 불편하다」**

구조상 사실이다 — 사진·지도가 **성역 카드 안**에 있고(`DetailView.swift`), 성역은 **기본 접힘**이며,
접기는 **성역·분류 두 카드가 함께** 움직인다. **사진 하나 보려고 분류 카드까지 펼친다.**

**이것이 2단계의 성격을 바꿨다:**

| | 08-12 | 08-13 이후 |
|---|---|---|
| 2단계는 | 「하면 좋은 것」 | **「걸린 것」** |
| 근거 | **계산** (성역 최대 617pt · 빈칸 499pt) | **실사용** |
| 계측과의 의존 | 계측이 2단계의 전제 | **뒤집혔다 — 2단계 방향이 무엇을 잴지 정한다** |

⚠️ **그래서 지금 계측부터 하면 안 된다.** 방향이 안 정해진 채로 재면 **곧 버릴 배치를 재게 된다**(08-13 §9).

**의미 쪽 어긋남이 뿌리다:** **성역 = 「어떤 편집으로도 안 바뀌는 것」(보관)** vs **사진 = 「보려고 있는 것」(열람).**
성격이 다른 둘이 한 카드에 있다.

### 착수하려면 필요한 것

1. **⛔ 방향 — 여기서 설계하지 않는다**(항시 규칙 1: 사양서 우선·즉흥 금지). **사양서/Claude 챗에서 받는다.** ← **지금 대기 중인 것**
2. **⚠️ 화면 이름은 사용자가 정한다**(항시 규칙 6). **「수집 원본 자료」는 작업용 이름이지 화면 문구가 아니다** — 후보 2~3개를 내고 사용자가 고른다.
3. **방향이 정해진 뒤 계측.** 도구는 저장소에 있다: `native/tools/measure-ui.swift` · `measure-text.swift`.
   - 폰 스크린샷으로도 잰다 (시뮬엔 없는 **진짜 사진·지도**가 있다). 재료 = **`82B1044B`**(주차 위치, 08-11 20:45).
   - 조건 넷: 같은 기종(iPhone 16 Pro) · **Dynamic Type 단계를 적을 것** · 다크/라이트 일치 · **AirDrop PNG 원본**(메신저는 JPEG 재압축).
   - **Dynamic Type 실측이 아직 없다** — 지금 쓰는 「L17/XL19/XXL21/XXXL23」은 **공표 표 값이다.** 이때 함께 닫는다.
4. **건드리지 말 것:** 배너 다섯은 2차 압축이 손댈 자리가 아니다(두 카드 **밖**에 있다). 압축은 **배치만** 바꾸고 판정 입력은 안 건드린다.

### ⚠️ 저장소의 우선순위와 다르다 — 확인 필요

`docs/worklog/2026-08-18-macmini.md` §13-4는 **1순위를 「목록에서 임시의 [기억하기]·[완료] 빼기」**로 적었고,
2차 압축은 그 목록에 없다. **Claude 챗이 2차 압축을 다음 작업으로 지목한 것**이 최신 방향이면 저 목록보다 이쪽이 앞선다.

**그 「목록에서 빼기」 과제는 착수 전 결정 하나가 걸려 있다 (미해결):**
[기억하기]와 함께 [완료]까지 빼면 **임시 항목을 완료할 길이 아예 없어진다**(상세에서도 반복 설정과 함께 사라졌다).
`edit-policy.md` §3의 *"기억하기로 하지 않은 항목도 완료·삭제할 수 있다"*를 좁혀야 한다. **사용자 결정 대기.**

---

## 4. 그 밖에 열려 있는 것 (참고 — 한 줄씩)

| | 무엇 | 상태 |
|---|---|---|
| **ⓓ** | 회색 글리프를 누르면 상세로 가는 **폴백**을 의도로 채택할지 | 미결. 막는 근거가 하나로 줄었다(**「같은 탭이 상태에 따라 다른 일을 한다」**만 남음) |
| **⏸** | 되풀이 시험 항목 정리 | 조사만 함. **아무것도 안 지웠다.** 사용자: *"나중에 다시 물어볼께"* → worklog §12(특히 **12-5**, 이제 12개) |
| **⏸** | 날짜 표시가 화면마다 다르다 (수집 시각은 연도까지 · 마감은 올해면 생략) | 사용자: *"나중에 따로 맞추자."* 맞추면 검색·보관도 함께 바뀐다 |
| **⏸** | 확정 쪽 경고 문구에 **「수정」이 두 번** | 자잘한 미결 |

---

## 5. 이 프로젝트에서 항상 걸리는 규칙 (요약)

1. **화면에 나오는 말은 사용자가 정한다.** 후보를 내고 사용자가 고른다.
2. **명령은 Claude, 화면은 사용자.** 빌드·설치·스크린샷은 자동화, **판정은 사용자.**
3. **잰 값과 짐작한 값을 같은 말투로 말하지 않는다.** 표본·목록은 **이름이 닮았는지가 아니라 데이터에서 계산한다** (`native/tools/inbox-state.py`).
4. **조작 하나 → 기록 한 줄.** 설치·조작은 그 자리에서 적는다.
5. **실기기 확인은 단정 금지.** 헤드리스로 못 여는 것은 사용자에게 확인을 요청한다.
