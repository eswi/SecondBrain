# CLAUDE.md — SecondBrain 항시 규칙

> 이 파일은 매 세션 시작 시 자동 로드된다. 아래 규칙을 항상 지킬 것.

## 프로젝트 한 줄
- 웹 v0(PWA)와 **네이티브 v1**(Swift, `native/`) 두 층. 데이터 = iCloud의 평문 `inbox*.md`.
- 정본 사양 = `second-brain-v0-spec.md` · 병합 규칙 = `docs/native/merge-design.md` · 분류 = `classify.py`.

## 작업 기기
- 집에서 사용하는 컴퓨터는 **맥미니(mac mini)**, 회사에서 사용하는 컴퓨터는 **맥북(맥북프로, macbook pro)**. 항상 인지하고 내 지시에 따라야 함.
- **두 기기가 `main`을 번갈아 pull한다.** 작업 시작 전 `git pull` 필수. **재개점 정본은 worklog다** — 메모리와 어긋나면 문서가 맞다.
- ⚠️ **`~/.claude` 메모리는 기기별로 갈린다.** 한쪽에서 배운 것을 메모리에만 적으면 **다른 기기는 모른다**(2026-08-14에 실제로 그래서 아래 `--package-path` 함정을 다시 밟았다). **기기를 넘어야 하는 것은 메모리가 아니라 여기(또는 `docs/`)에 적는다.**

## 빌드·테스트 (함정이 있다 — 매번 여기를 보고 시작한다)
```sh
git pull
cd native && xcodegen generate          # .xcodeproj는 gitignore 생성물 — 매번 재생성
swift test --package-path SecondBrainCore   # ← 374개(2026-08-21 맥북 실측). 경로 빼면 실패한다(아래 ①)
xcodebuild -project SecondBrain.xcodeproj -scheme SecondBrainApp-iOS \
  -destination 'id=<UDID>' build          # ← 이름 말고 UDID(아래 ③)
```
1. **`--package-path SecondBrainCore`를 빼면 안 된다.** `Package.swift`는 `native/`가 아니라 **`native/SecondBrainCore/`**에 있어서, `native/`에서 그냥 `swift test`를 돌리면 `Could not find Package.swift`가 난다. **2026-08-13(맥미니)·08-14(맥북) 이틀 연속 밟았다.**
2. **프로젝트 이름 ≠ 스킴 이름.** 프로젝트는 `SecondBrain.xcodeproj`, 스킴은 **`SecondBrainApp-iOS`**. 자주 헛짚는다.
3. **시뮬 대상은 `name=`이 아니라 `id=<UDID>`로 지정한다.** 같은 이름의 시뮬이 둘 이상 있을 수 있고(맥미니에 「iPhone 16 Pro」가 둘이었다), `name=`은 **조용히 엉뚱한 기기로** 간다. UDID가 틀리면 `EXIT=64`로 죽어 바로 안다. `xcrun simctl list devices available`로 확인.
   - ⚠️ **겹침은 「기기가 둘」만이 아니라 「런타임이 둘」로도 온다.** 2026-08-20 맥미니 실측:
     **iOS 26.2에 `iPhone 16 Pro`**(`734ABE20-…`) · **iOS 26.5에 `iPhone 16 Pro (26.5)`**(`34A5033B-…`).
     **26.5 쪽만 이름 끝에 `(26.5)`가 붙어 있어서** `name=iPhone 16 Pro`로 쓰면 **26.2로 간다.**
   - ⛔ **이 오작동은 `EXIT=0`으로 끝난다** — 빌드가 성공하므로 **로그로는 알 수 없다.**
     UDID 오타(`EXIT=64`)와 **반대 성질**이다: 틀린 UDID는 시끄럽게 죽고, `name=`은 **조용히 다른 데서 돈다.**
     그래서 「빌드 성공」을 「의도한 런타임에서 성공」으로 읽으면 안 된다.
4. **빌드 결과는 `|tail`로 판정하지 말 것** — 파이프가 exit code를 가린다. `> log 2>&1; echo "EXIT=$?"; grep -c 'error:' log`로 본다.
5. **★ 「빌드 실패인데 설치 성공」이 된다 — 설치 판정만으로는 새 코드가 깔렸는지 알 수 없다.**
   `DerivedData/…/Debug-iphoneos/SecondBrain.app`이 **지워지지 않으므로**, 빌드가 실패하면
   `devicectl … install`이 **직전 빌드의 앱을 그대로 얹고 `App installed`를 찍는다.**
   ⛔ **2026-08-21에 실제로 밟았다** — 크래시 수정을 커밋하고 설치까지 「성공」했는데
   폰에는 **죽는 그 빌드가 다시 깔렸다**(빌드는 `EXIT=66`, 원인은 경로 오타).
   - **판정은 둘을 함께 본다:** ① 빌드 `EXIT=0` + `error: 0` ② `App installed` 줄.
   - **의심되면 `.app`의 실행파일 시각을 `stat -f '%Sm'`로 보고 `date`와 비교한다.**
   - 확실히 하려면 **설치 전에 `.app`을 지운다** — 그러면 낡은 것이 얹힐 길이 없다.
6. **⚠️ `xcodebuild`·`git add`는 「어느 디렉터리에서 도는가」에 걸린다.** 프로젝트는 `native/`에,
   저장소 루트는 그 위다. 2026-08-21에 **셋을 연달아 밟았다**(`native/`에서 `git add native/` 두 번,
   저장소 루트에서 `-project SecondBrain.xcodeproj` 한 번). **경로는 절대경로로 쓴다.**

## 항시 규칙
1. **사양서 우선 · 즉흥 금지** — 전략·방향 결정을 즉흥으로 하지 않는다. Claude 챗에서 정해진 방향을
   사양서(`second-brain-v0-spec.md`)를 통해 받아서 진행한다.
2. **PR 머지 후 사양서 반영 확인** — PR 머지 후, 이번 변경이 사양서에 반영될 필요가 있는지 확인하고,
   필요하면 **"사양서에 반영할까요?"** 라고 사용자에게 물을 것.
3. **실기기 검증은 단정 금지** — 실기기 확인이 필요한 것은 "됐다/완료"라고 단정하지 말고,
   **사용자가 직접 확인하도록 요청**할 것. (헤드리스로 검증 불가한 GUI·실기기 동작)
4. **세션 끝에 worklog** — `docs/worklog/YYYY-MM-DD-*.md` 로 그날 작업을 남길 것.
   **단, 실기기 조작·설치는 세션 끝까지 미루지 말고 그 자리에서 적는다** → 아래 **「기록 규칙」**.
5. **살아있는 자산 보호** — §3 분류 프롬프트(`classify.py` / 사양서 §3), `docs/native/merge-design.md`,
   분류 체계(사양서 §2)는 큰 편집 때도 **함부로 건드리지 말 것**.
   - ⛔⛔ **`automation/setup-mac.sh`를 돌리지 말 것 — 그것이 차단해 둔 자동 분류를 되살린다.**
     그 스크립트는 **매시간 `classify.py`를 도는 launchd LaunchAgent를 설치**한다.
     자동 분류는 **2026-08-18에 앱에서 진입점 둘을 막았고**(`ClassifyPause`) **재개발 예정**이며,
     맥의 launchd도 **차단·확인 완료**다(맥북 08-18 차단 · 맥미니 08-19 애초에 없음).
     **`automation/`은 그 결정 이전에 만들어진 것이고 경고가 붙어 있지 않았다**
     (2026-08-21 전수 대조에서 발견). 되살리는 것은 **사용자 결정 사안**이다.
     ⚠️ `uninstall-mac.sh`는 반대로 **끄는 쪽**이라 안전하다.
   - ★ **이 경고는 아래 항시 규칙 8의 한 사례다** — 규칙 8이 그 영역 전체를 덮는다.
6. **★ 화면에 나오는 말은 사용자가 정한다.** 새로 만드는 **사용자 눈에 보이는 문구**(화면 라벨·버튼·
   배너·토스트·분류 이름 등)는 **임의로 짓지 말고 반드시 물어볼 것.** 후보 2~3개 + 뜻 한 줄을
   제시하고 **사용자가 고른다.**
   - **왜:** 이 앱의 말은 사용자가 정해왔다 — 세부정보·되풀이·미리 알림·회차·기억하기 전부.
   - **⛔ 보고 대상이 아니다 (묻지도 말고 알리지도 말 것):**
     **소스코드 안의 이름**(변수·함수·타입·**필드**·테스트) · **문서 절 번호·절 이름.**
     사용자가 볼 일이 없으므로 내가 정한다. **한글로 번역해서 부르지도 말 것** — 그냥 안 꺼낸다.
   - 문서 절 이름은 사용자 판단 사안이 아니다(목차일 뿐). 단 **이름이 서로 겹치지 않게** 유지할 것.
7. **★ 누가 무엇을 하는가 — 명령은 Claude, 화면은 사용자.**
   - **Claude Code가 한다:** 시뮬레이터 부팅·앱 설치·설정 변경(`simctl ui … content_size` 등)·
     스크린샷·계측. **명령이므로 자동화한다.** 사용자에게 시키지 않는다.
   - **사용자가 한다:** **화면을 눌러 여는 것**(탭·스크롤·이동)과 **무엇이 보이는지 판정하는 것.**
   - ⚠️ **주어를 뒤바꿔 쓰지 말 것.** 대화에서 화자가 뒤바뀌어 「내가 열겠다」로 쓰인 적이 **세 번** 있었다.
     그러면 **아무도 안 한 일이 확인된 것으로 기록될 수 있다.** 화면을 여는 문장은 반드시
     「**…을 열어주세요**」로 쓰고, 무엇을 어디서 눌러야 하는지 **화면 위치로** 짚는다.
   - #3(실기기 검증 단정 금지) · 「기록 규칙」 #7(제안 vs 실행)과 **같은 뿌리다** —
     말이 오간 것과 실제로 된 것을 갈라 적는 것.
8. **★★ 자동 분류는 zero base로 새로 설계한다 — 그때까지 그 영역에 손대지 않는다** (2026-08-21 사용자 결정).
   기존 자동 분류는 **고쳐 쓰는 것이 아니라 처음부터 새로 고민하고 설계할 예정**이다.
   그러므로 **관련된 코드·스크립트·설정을 건드리지도 되살리지도 않는다.**
   연관된 것이 나오면 **사용자에게 허락받고 움직인다.**
   - ⛔ **건드리지 않는다 (허락 없이는 한 줄도):** 앱 코드 — `classifyUnclassified` · `ClassifyPause` ·
     **진입점 둘**(당겨서 분류 · 설정 「지금 분류하기」) · `ClaudeClassifier` · `ClassifyPrompt` ·
     그리고 `classify.py` · **`automation/` 넷** · **키체인·키 파일.**
   - ✅ **해도 되는 것 둘:** ① **문서에서 사실을 바로잡는 것** ② **자동이 남긴 데이터를 사람이 읽고
     판단하는 것**(읽기·목록·근거 만들기 — **되살릴지는 사용자가 고른다**).
   - ⚠️ **설계·동작에 관한 것은 무엇이든 허락받는다.** 「고치는 편이 낫겠다」가 이 규칙을 넘지 못한다.
   - **왜:** 07-24 배치는 사람이 [기억하기]로 확정한 기억까지 `discard`로 버렸다(`D6E4950B`).
     **결정 구조를 그대로 두고 조각을 고치면 같은 형태가 다시 난다.** 설계가 먼저다.
   - 규칙 5의 ⛔ `automation/setup-mac.sh` 경고가 이 규칙의 한 사례다.

## 미뤄둔 것 — 우선순위 낮춤 (다시 올리는 것은 사용자가 정한다)
- **★ Dynamic Type XXXL 이상 — 뒤로 미룬다 (2026-08-14 사용자 결정).**
  XXXL에서 깨지는 것은 **깨진 사실만 기록하고, 그것을 근거로 결정을 바꾸지 않는다:**
  설계 비용 계산에서 빼고, 확인 목록에서 빼고, **「XXXL에서 넘친다」를 되돌림·반대 사유로 쓰지 않는다.**
  - ⚠️ **XL·XXL은 미룬 것이 아니다.** 확인 목록 **17번은 XL에서** 깨졌고, **8번의 위험은 XXL(21pt)에서** 시작한다
    (「주차 위치」+메뉴 chevron = **91.6 / 90**). **XXXL을 미뤄도 XXL 제약은 그대로 남는다.**
  - 근거·전말: `docs/worklog/2026-08-14-macbook.md`

## 성능 측정 규칙 (실사용 조건에서 잰다)
성능(시작 시간·STT "듣는 중" 지연 등)을 측정할 때는 **반드시 실사용 조건**에서 잰다:
1. **디버거 없이** — Xcode ⌘R(▶ Run)로 재지 말 것. ⌘R은 lldb를 attach해서 **pre-main에 큰 비용**이 붙는다
   (이 기기/iOS 26/현 Xcode 조합에서 ~10초). 이 비용은 앱/코드와 무관하다.
2. **이미 설치된 앱을 홈 화면에서 터치로 실행**해 잰다. (command line/devicectl로 설치 후 홈 탭, 또는
   Edit Scheme ▸ Run ▸ Info ▸ **"Debug executable" 체크 해제** 후 실행.)
3. **새 설치 직후 ⌘R 자동 실행의 느림을 "코드/앱이 느리다"로 착각하지 말 것** — 그건 디버거 attach 비용이다.
4. 결론은 **추측 말고 계측**으로: A/B를 **같은 조건에서 숫자로** 비교한다.
- 근거·전말: `docs/lessons/2026-07-21-perf-cmdr-debugger-premain.md`

## 계측 규칙 (숫자는 그 자리에서 확인한다)
**사흘 연속 같은 실수가 났다. 셋 다 「재는 법」은 맞았고 「가져다 쓰는 법」이 틀렸다:**

| 날 | 무엇이 틀렸나 | 대가 |
|---|---|---|
| 08-10 | `menuRow`의 **간격 20pt를 줄마다 세지 않고 한 번만** 셌다 | 폭 셋을 낙관적으로 적고 「제목 줄이면 들어간다」는 **틀린 결론** |
| 08-11 | 계측표의 **폭 값(`photo @12pt = 17`)을 높이에 갖다 썼다** | 카드 높이를 96.8로 계산(실제 92) |
| 08-12 | **Dynamic Type 크기를 기억으로** 썼다(XXXL을 20pt로 — 실제 **23pt**) | 위험 임계를 XXXL로 적었다. 실제로는 **XXL부터 넘친다** |

1. **상수는 기억으로 쓰지 말고 그 자리에서 확인한다** — 폰트 크기, Dynamic Type 단계, 권장 표적 크기(44pt),
   심볼 치수. 기억이 맞을 것 같아도 **한 줄 재는 값이면 잰다.**
2. **계측값에 「폭/높이」 딱지를 붙여 적는다.** 심볼은 폭과 높이가 다르고(@40pt `mappin` 45×**52**),
   **심볼마다도 다르다** — 높이를 고정 안 하면 항목마다 카드 높이가 달라진다.
3. **조립을 검산한다** — 요소 폭 + **요소마다의 간격**. 한 줄 공식을 여러 줄에 재사용할 때 간격을 한 번만 세기 쉽다.
4. **계산은 실측으로 닫는다.** 계산은 후보를 좁히는 데 쓰고, **결론은 화면/접근성 좌표에서 확인**한 숫자로 낸다
   (계산이 실측보다 2~3pt 낙관적인 편향이 실제로 있었다).
5. **도구가 있다 — 새로 만들지 말고 이걸 쓴다:**
   - `swift native/tools/measure-text.swift text "주차 위치" 17 19 21 23` — 글자 폭·줄높이
   - `swift native/tools/measure-text.swift symbol mappin.and.ellipse 30 40` — 심볼 **폭 x 높이**
   - `swift native/tools/measure-ui.swift <스크린샷.png> <x열…>` — **카드·블록 경계**(결론을 닫는 자리)
   - `swift native/tools/measure-icloud-download.swift <표본목록>` — **iCloud 다운로드 시간**
     (2026-08-20 실측 **526~924ms** · 유선 850Mbps). ⚠️ **상태를 바꾼다** — 받은 파일은 dataless가 아니게 된다.
     **자료 확장 ②에서 다시 잴 때 새로 쓰지 말 것** — 새로 쓰면 오늘 값과 비교가 안 된다.
   - `swift native/tools/probe-security-scope.swift` — **샌드박스 없는 프로세스의 보안 스코프**
     (설계 `media-icloud-design.md` §2-A의 「쟀다」 표가 이 출력이다). 상태를 안 바꾼다.
   - **`native/tools/sim-input.swift`** — **시뮬레이터를 CLI로 탭·드래그**(CGEvent). 좌표는 접근성
     트리에서 읽는다(툴바 44pt 함정이 주석에 있다). ⚠️ **항시 규칙 7을 바꾸지 않는다** —
     사용자가 명시적으로 지시했을 때만 쓴다.
     ⛔ **2026-08-20에 이 도구가 있는 줄 모르고 같은 것을 새로 만들었다** — 이 목록에 **없었기 때문이다.**
     그래서 올렸다. **목록에 없는 도구는 다시 만들어진다.**
   - **`native/tools/inbox-state.py`** — **실데이터(`inbox*.md`)를 접어 「항목의 지금 상태」를 찍는다.**
     `--unconfirmed` · `--type recurrence` · `--id <8자>`. **계측 규칙 7의 「표본·목록」을 데이터에서
     계산하려고 만든 도구다.** ⚠️ **iCloud 동기화 지연이 있다** — 폰 화면과 어긋나면 파일이 뒤처진 것이다.
     ⛔ **이것은 여기(CLAUDE.md)에 없었고 `HANDOFF.md`에만 적혀 있었다** — HANDOFF는 갱신되며 지워지는
     스냅샷이다. **오래 남아야 하는 것은 여기 적는다**(2026-08-21 전수 대조에서 발견).
   - **`native/tools/sb-migrate/`** — **레거시 → UUID 마이그레이션 CLI**(별도 패키지 · 앱 빌드 영향 0).
     `--dry-run <folder>`(파일 무변경) · `--apply <folder> --out <outdir>`(원본 무변경).
     해시·병합·검증을 **앱과 같은 `SecondBrainCore`로** 한다(재구현 divergence 방지).
     설계 `docs/native/legacy-uuid-migration.md`. **일회성 성격이지만 다시 쓸 수 있다.**
   - ⛔ **`native/tools/2026-08-18-strip-auto-fields.py`는 「쓰는 도구」가 아니다** —
     **그날 그 데이터를 고친 기록**이다(실행 완료). **재실행 금지.** 파일명에 날짜가 있는 이유가 그것이고,
     다시 필요하면 **날짜를 새로 붙여 복제**한다(그 파일 머리주석의 지시).
   - **`native/tools/ghostlab/`** — **「눌러야만 드러나는 것」을 재는 실험용 앱 + UI 시험 + 픽셀 판정기.**
     같은 목록을 여러 꼴로 그려 놓고 **끌다가 멈추게** 한 뒤 스크린샷을 잰다.
     `measure-frames.swift marks|bands <png>` = **색 띠 자리·줄 간격(pitch)**.
     ★ **2026-08-20에 이것으로 갈린 것:** 원칙 목록 **잔상**(「안 된다」던 결론을 뒤집었다) ·
     **편집모드도 잔상이 남는다**(문서에 있던 안이 죽었다) · `UIHostingConfiguration`은
     **Dynamic Type를 안 끊는다** · `List`가 **스스로 주는 여백 한쪽 15pt** · 좌우 맞춤 가능성.
     ⚠️ **앱 코드가 아니다** — `native/project.yml`은 `Sources/App`만 컴파일한다. 앱에서 참조 금지.
     ⛔ **판정기의 문턱을 고치지 말 것** — 이번 값(104.7 / 60.0 / 52.3pt)과 비교가 깨진다.
     ⛔ **`Test Case … passed`를 「안 죽었다」로 읽지 말 것** — `unexpected termination`과
     `~/Library/Logs/DiagnosticReports`를 함께 본다(2026-08-21에 크래시를 이렇게 놓쳤다).
     자세히 → `native/tools/ghostlab/README.md`
   ⚠️ 시뮬레이터 **접근성 좌표**(AppleScript)도 쓸 수 있지만 **창이 움직이면 통째로 어긋나고 읽기가 자주 실패**한다.
   **픽셀 측정이 안정적이다.** 좌표를 쓸 때는 **기기 화면 원점을 매번 다시 읽을 것.**
6. **Dynamic Type는 단계를 바꿔 실제로 잰다** — `xcrun simctl ui booted content_size <단계>` 뒤 스크린샷을 측정.
   ✅ **L 17 / XXL 21 / XXXL 23은 2026-08-12에 화면에서 확인됐다** — 「주차 위치」 글자 폭을 픽셀로 재니
   60.0 / 74.0 / 81.0pt였고 **비율이 17:21:23과 맞았다**(코드가 `.font(.body)`를 명시한다).
   ⚠️ **`XL 19`는 아직 안 쟀다** — 그 단계는 재지 않았으므로 여전히 공표 표 값이다.
   ⚠️ **편향의 방향은 대상에 따라 반대다** — 카드·블록 **높이**는 계산이 2~3pt **작게**(낙관적),
   **글자 폭**은 계산이 3.2~3.8pt **크게**(보수적) 나온다. 「깎아 읽어라」를 글자 폭에 그대로 쓰지 말 것.
7. **★ 「잰 값」과 「짐작한 값」을 같은 말투로 말하지 않는다. 화면 치수만이 아니라
   ★ 확인할 수 있는데 확인 안 하고 말하는 것 **전부**에 적용된다:**
   - **수** — 시간·개수·소요시간.
   - **목록·소속·이름** — *어느 항목이 어느 확인표의 표본인가* · *무엇이 그 목록에 들어 있나*.
     **수가 아니어도 같은 규칙이다.** 저장소에 있으면 **문서를 열어 확인한다.**
   - **시간을 말할 때는 그때마다 `date`를 읽는다.** 앞서 읽은 시각에 "이만큼 걸렸겠지"를 **더하지 않는다.**
   - **짐작이면 「추정」이라고 붙인다.** 못 잰 것은 **「못 잼」으로 적는다. 값을 만들지 않는다.**
   - **★ 표본·목록은 「이름이 닮았나」로 고르지 말고 「무엇이 이것을 그것이게 하는가」를 데이터에서 계산한다.**
     (F-5 폴백 표본 = **`lastDoneDue`가 없다** — 한 줄이면 갈렸다.)
   - 계기(2026-08-12): 마지막으로 잰 시각이 13:51인데 그 뒤 「14:12」·「14:47」이라고 말했다.
     **실제로는 18:04였다** — 네 시간을 짐작으로 메웠다. 원인은 열 단위 `sweep` 반복문이
     **`swift`를 열마다 새로 컴파일**해 한 번에 수 분씩 걸린 것인데, 그걸 「5분」으로 잡았다.
     같은 날 AX5 카드 높이를 **「≈393pt」**로 적은 것과 **똑같은 형태**다(대상만 pt → 시간).
   - **계기(2026-08-13) — 대상이 수가 아니어서 이 조항이 안 걸렸다.** F-5 폴백 표본 셋을 **기억으로**
     답하면서 `E-6`를 넣었는데 실제로는 **`복약`·`E-4`·`감기약`**이었다(`E-6`는 별개 대상).
     **사용자가 「확인표를 다시 읽어 보라」고 해서** 읽고 정정했다 — **발견은 내가 한 것이 아니다.**
     ⚠️ **앞의 계기들은 숫자가 틀렸지만, 이건 되돌릴 수 없는 조작을 시킬 뻔했다** —
     표본을 누르면 `lastDoneDue`가 생겨 **폴백 상태가 영구히 사라진다(재현 불가).**
     그래서 「값」의 범위를 수 바깥으로 넓혔다.
- 근거·전말: **`docs/lessons/2026-08-12-measure-then-use.md`** — 네 번의 어긋남과 위 조항의 1:1 대응
  (worklog: `2026-08-10-macmini.md` 정정 블록 · `2026-08-11-macmini.md` §8 · `2026-08-12-macmini.md` §2·§6·§9)
  · **#7의 「목록·소속·이름」 확장은 `2026-08-13-macmini.md` §10**(표본 오판의 경위와 1:1 대응)

## ⚠️ 기기별로 갈리는 값 — 다른 기기 값을 그대로 믿지 말 것
**iCloud 파일의 「받아졌나/dataless냐」는 기기마다 따로다.** 한 기기에서 받아도 **다른 기기는 그대로 dataless다**
(evict 상태가 기기별이다). 그래서 **「dataless 116개」 같은 수는 그것을 잰 기기의 값**이다.
- 계기(2026-08-20 맥미니): 표본 16개를 받아 **135 → dataless 116**이 됐다. **맥북에서 세면 다르다** —
  맥북은 08-19에 **135개 전부 `NotDownloaded`**로 봤고 그 뒤 맥북에서 받은 것이 없다.
- ⛔ **그래서 「줄어 있어야 하는데 안 줄었다」를 회귀로 읽으면 안 된다.** 수를 적을 때 **어느 기기에서 쟀는지** 함께 적는다.
- 같은 이유로 **로컬 사본**(`Application Support/SecondBrain/`)도 기기별이다 — 동기화되지 않는다.

## 기록 규칙 (조작은 그 자리에서 적는다)
**조작은 폰에 남고 기록은 문서에 남는데, 다음 세션이 먼저 읽는 것은 문서다.**
기록이 부정확하면 다음 세션은 틀린 전제로 출발하고, 잘못된 행동까지 한다:
1. **조작 하나 → 기록 한 줄.** 세션 끝에 몰아 쓰지 않는다. 커밋은 나중이어도 **문장은 그때** 쓴다.
2. **설치 직후 한 줄 — 적는 자리는 `docs/install-log.md`다** (2026-08-21 사용자 결정 · A안).
   `| 시각 | 커밋 | 어디서 | 확정 근거 | 판정 |` 를 그 파일 **표의 첫 줄**에 넣는다(append-only).
   (확정 근거 = 그 빌드에서 처음 생긴 화면 — 아이콘 신설, 「연결된 폴더·파일 5개」 등.)
   ⚠️ **앱에 버전 문자열이 없으므로 「눈에 보이는 새것」이 빌드를 가리는 유일한 수단이다** —
   커밋 해시·빌드 시각은 폰 화면에서 볼 수 없다. 확정 근거에 그 둘을 적어선 안 된다.
   - ⛔ **`HANDOFF.md`에 이력을 쌓지 말 것.** HANDOFF는 **갱신되며 잘리는 스냅샷**이라
     **최신 한 줄 + 링크만** 둔다. 전말(왜 그 빌드를 만들었나)은 **그날 worklog**에.
   - **왜 옮겼나:** 이력이 HANDOFF와 worklog에 나뉘어 있었고 **HANDOFF 쪽이 잘려서**
     08-20 저녁 여덟 번이 worklog에만 남았다. 그 구멍 안에 **원칙 목록을 실제로 고친 두 커밋**이
     있었고, 그래서 HANDOFF가 **이미 된 일을 「⛔ 다시 시도하지 말 것」**으로 적고 있었다.
     ⛔ **자리가 나뉘면 어느 쪽도 전부를 못 갖는다.**
3. **중간 단계도 기록·확인 대상이다** — 원상복구·되돌리기처럼 **상태를 만드는** 단계에 통과 조건이
   없으면 안 눌린 채로 넘어간다. 확인 절차를 쓸 때 그 단계에도 「무엇이 보여야 하나」를 적는다.
4. **worklog 커밋 전에 한 번 본다:** *"마지막 조작 시각이 이 문서에 들어 있나."*
5. **사용자 문구를 작업 기록에서 뜻대로 쓰지 말 것.** 「그만두기」(=삭제)·「꺼두기」(=잠시 멈춤)·
   「완료」는 앱이 정한 뜻이 있다(사양서 §5). 느슨하게 쓰려면 **앱 문구가 아닌 말**을 쓴다.
6. **실기기 상태를 전제로 판단할 때는 문서만 믿지 말고 실데이터를 읽는다** — `inbox-*.md`의 op 로그.
   문서가 뒤처져 있을 수 있다.
7. **제안이 「사용자가 기기에서 하는 조작」이고, 그 결과가 뒤 판정의 전제가 되면 — 그 판정을 하기 전에
   실데이터에서 실행 여부를 확인한다. 「제안했다」와 「됐다」를 같은 칸에 적지 않는다.**
   *#1~#6이 **내가 적을 때**를 다룬다면, 이 조항은 **사용자가 하기로 한 것**을 다룬다.*
   *#6과 짝 — #6은 「문서가 뒤처졌을 때」, #7은 「제안이 아직 행동이 아닐 때」.*
   계기: 2026-08-12, `9F950B73` 「꺼두기」가 worklog엔 **「→ 제안」까지만** 적혀 있었다. 실데이터에서
   `recurPaused=true recurPausedAt=2026-08-11T22:43`을 보고서야 실행된 것을 알았다. **안 봤다면 그날 밤
   20:00에 알림이 하나 올지 둘 올지 모른 채 F-3에 들어갔을 것이다.**
8. **★ 문서 자리를 목록으로 남길 때는 줄 번호가 아니라 절 이름과 인용문으로 적는다 — 줄은 밀린다.**
   계기: 2026-08-20. `media-icloud-design.md` §12의 「뒤집을 자리 일곱」이 줄 번호를 달고 있었는데
   **같은 날 경고 블록을 넣으면서 이미 밀려 있었다.** 그 번호로 찾아갔으면
   ⛔ **경고를 지우고 정작 근거 줄은 남기는 정반대**가 될 뻔했다. **인용문은 안 밀린다.**
   - **★ 다른 문서를 근거로 인용할 때도 절 번호만 적지 말고 인용문을 함께 적는다.
     절 번호는 기억으로 적으면 틀린다.**
     계기: 같은 날. `media-icloud-design.md` §2가 하위 폴더 이름의 근거로
     *"`photo-capture-design.md` §7이 이미 「iCloud 폴더의 `audio/`를 여기 더하면」이라 예고했다"*
     고 적고 있었다. **예고한 쪽은 `audio-capture-design.md` §7이었고 문구도 달랐다.**
     ⚠️ **결과는 맞았고 출처가 틀렸다** — 그래서 **아무것도 안 깨졌고, 그래서 안 걸렸다.**
     ⛔ **고치고 지우면 「원래 맞았던 것」처럼 보인다** — 잘못 짚었던 사실을 그 자리에 남긴다.
   - **★ 개수도 마찬가지다. 「두 줄」 같은 숫자를 목록에 박으면 코드가 자라면서 낡는다.
     셀 수 있는 것은 셀 때 센다.**
     계기: 같은 날. HANDOFF가 색 정리 과제를 *"세 곳 · **고칠 곳 두 줄**"*로 적어 뒀는데
     **실측은 다섯 자리**였다(`InboxView` 셋 · `LivingView` 둘). 그 숫자를 근거로 규모를
     잡았으면 절반만 고치고 **「했다」고 적었을 것이다** — 왼쪽 스와이프 셋이 남은 채로.
     ⚠️ **줄 번호·절 번호·개수 셋 다 같은 형태다** — **「그때는 맞았던 값」이 코드가 자라며 밀린다.**
   - **★★ 그리고 명단을 적을 때는 「수」가 아니라 「id」를 적는다. 수는 같은데 대상이 바뀐다.**
     계기: 2026-08-21. 「살아있는 미확정 **8개**」가 **세 번 다르게 만들어진 8**이었다 —
     08-18 원본 8 → 08-19 `991728DB` 삭제 −1 · `1D5E2FDE` 신규 +1 →
     08-20~21 `4C0882A0` 확정 −1 · `84BE980B` 신규 +1. **수만 보면 아무 일도 없어 보인다.**
     ⛔ **「8개 그대로」로 읽는 순간 대상이 둘 바뀐 것을 놓친다** — 그중 하나(`4C0882A0`)는
     **표본 조건과 맞닿은 항목**이었다. **개수 조항과 같은 뿌리이고, 대상이 수가 아니라 명단이다**
     (계측 규칙 7의 「목록·소속·이름」과 짝).
     → **id를 적고, 그 옆에 실행 시각을 적는다.** 수는 id를 센 결과로만 쓴다.
   - **★★ 수를 적을 때는 「언제 기준」인지 함께 적는다 — 데이터만이 아니라 과제 수·확인 수도 같다.**
     **하루 안에 바뀌는 값이면 날짜만으로는 부족하다.**
     계기: 2026-08-21. 「열려 있는 것」이 **오전에 넷 → 오후에 셋**이 됐다(둘이 닫히고 하나가 옮겨지고
     하나가 들어왔다). 같은 날 worklog에 *"다시 넷이다"*라고 적어 둔 것이 **반나절 만에 낡았다.**
     ⚠️ **「실행 시각을 함께」는 원래 데이터 표본에 붙은 조항이었는데**(위 명단 조항) —
     **과제 수·확인 수도 같은 성질이다.** 셀 수 있는 것은 다 그렇다.
     → 옛 수는 **지우지 않고 「이것은 오전의 값이다」**로 남긴다(그러면 무엇이 언제 바뀌었는지가 보인다).
9. **★★ 목록에서 항목이 빠지는 데는 둘이 있다 — 「끝나서」와 「성격이 바뀌어 옮겨져서」.
   어느 쪽인지 안 적으면 뒤엣것이 누락으로 읽힌다.**
   계기: 2026-08-21. 「있는 목록이 소진되게 한다」를 과제 목록에서 뺐는데 **끝난 것이 아니었다** —
   단계 셋(① 도달할 수 있나 → ② 목록에 올라와 있나 → ③ 소진되나)으로 갈려 **규칙·계열로 들어갔다.**
   그래서 그 자리에 **「끝난 것이 아니라 상시가 된 것이다」**와 **옮겨간 자리**를 함께 적었다.
   - **옮긴 것이면 어디로 갔는지를 적는다.** 없으면 다음 세션이 **빠뜨린 줄로 읽고 다시 만든다**
     (계열 ③ 「목록에 아예 없다」와 같은 결과가 된다).
   - ⚠️ **닫은 것도 마찬가지다** — 「✅ 닫혔다」만 적지 말고 **무엇으로 닫혔나**(커밋·판정)를 함께.

- **관행은 `docs/`+여기에 둔다** — `~/.claude` 메모리는 git 밖이라 **맥미니 ↔ 맥북을 안 넘어간다.**
- 근거·전말: `docs/lessons/2026-08-11-record-at-the-moment.md`

## 정본 훑기 규칙 (축을 바꿨으면 문서를 훑는다)
**문서는 컴파일되지 않는다.** 코드의 **가르는 기준**을 바꾸면 옛 기준으로 서술한 정본 문장이 여러 곳에 남고,
**지금 아무것도 깨뜨리지 않는다** — 그래서 안 찾으면 안 보이고, **다음 세션이 변경을 되돌릴 근거로 쓴다.**

1. **「축을 바꾸는 변경」이면 정본 전체를 훑는다.** 축 = **무엇을 기준으로 가르는지**(값이 아니라 기준).
   예: 파티션을 `시점` → `확정` 기준으로 · 알림 대상을 `전체` → `확정된 것`으로 · 「임시」 표시를
   「결정 버튼이 없는 곳」 기준으로. **정렬·문구·색 변경은 축 변경이 아니다.**
2. **신호 셋 — 하나라도 있으면 축 변경으로 본다:** ① `filter`/`where`/`if` 조건식에 **제어 필드**가
   들어간다(`confirmed`·`deleted`·`status`) ② 배타적 분기의 **소속**이 바뀐다(「어디로 가나」)
   ③ **「A 무관」이 「A에 따라 갈린다」가 된다** ← 가장 뚜렷하다.
3. **연쇄를 따라간 것으로 끝내지 않는다.** 연쇄에 걸리는 것은 **그 문장이 지금 하는 일이 있는 자리**뿐이다.
   읽히기만 하는 문장은 안 걸린다 — **그것이 정확히 위험한 종류다.**
4. **훑는 법:** `grep -rn "무관\|허용한다\|상관없\|여부와" docs/native/*.md second-brain-v0-spec.md CLAUDE.md`
   + 옛/새 축 이름으로. **코드가 아니라 문서를 찾는다.**
5. **찾은 것을 셋으로 갈라 처리한다:** **사실이 틀림** → 고치고 옛 서술을 날짜와 함께 남긴다 ·
   **시제가 틀림** → 「당시 …였다」 · **⛔ 권한을 주는 문장**(「허용한다」) → **지우지 말고 「뒤집혔다」로**
   남긴다(옛 입장·뒤집은 날짜·근거를 함께). 지우면 다음 세션이 **왜 바뀌었는지 못 찾는다.**
   ★ **「무엇이 뒤집혔나」만 적으면 부족하다 — 「무엇이 유지되나」를 같은 자리에 적는다.**
   그 줄이 없으면 다음 세션이 **과잉 적용**한다(2026-08-18: 「미확정은 아무것도 안 한다」를 과하게 읽으면
   알림뿐 아니라 **필터까지** 막아 기억이 사라진 것처럼 보인다 — 대조표의 「필터는 그대로 허용」 줄이 그것을 막는다).
6. **고치기 전에 목록으로 보고한다** — 화면 문구가 섞여 있으면 **사용자가 정한다**(항시 규칙 1).
7. **★★ 정본에는 「무엇인가」를 적고 「지금 무엇이 있나」는 적지 않는다.** (2026-08-21 사용자 판단)
   - **왜:** 적으면 **정본이 데이터를 추적하게 되고, 데이터가 바뀔 때마다 정본을 고쳐야 한다.**
     그리고 그런 상태는 대개 **곧 사라진다** — 사라질 것을 정본에 박으면 그 자리가 낡는다.
   - **어디에 적나:** *지금 무엇이 있나* = **데이터**(`inbox-state.py`로 센다) · 「지금 어디까지 왔나」는
     **`HANDOFF.md`**(스냅샷) · 「그때 무슨 일이 있었나」는 **worklog**. **정본은 뜻만.**
   - 계기: `D6E4950B`를 되살려 **원칙 목록 맨 아래에 임시로** 뒀는데, 그것을
     `memory-philosophy.md` §3(원칙 정본)에 적을지 물었다. **안 적는 것이 맞다** —
     §3은 「원칙이 무엇인가」를 적는 문서이고, 그 항목은 **새 분류 셋이 생기면 옮겨질 상태**다.
   - ⚠️ **뒤집어서도 쓴다:** 정본을 읽다가 **개별 항목 id·개수·현재 상태**가 박혀 있으면
     **그것은 낡을 자리다** — 발견하면 옮기거나 날짜를 붙인다(기록 규칙 8과 짝).
- 계기: 2026-08-18. 축 변경 뒤 여섯이 나왔고 **넷이 「지금은 안 터지지만 다음 세션이 되돌릴 근거가 되는」**
  종류였다. 그중 `edit-policy.md` §1은 **최상위 원칙 절에서 새 코드를 정면으로 부정**하고 있었다
  (*"기억하기 전에도 … 알림이 울릴 수 있다"* — 같은 날 만든 `notifiable`이 금지한 것).
  **별도 지시가 있어서 잡혔다** — 그래서 규칙으로 올린다.
- 근거·전말: `docs/lessons/2026-08-18-canon-sweep-after-axis-change.md`

## 세션 시작 절차 (읽기 **전에** 당긴다)

**`git pull`은 「작업 시작 전」이 아니라 「문서를 읽기 전」이다.** 순서를 뒤바꾸면 **낡은 문서를 정본으로 읽는다.**

1. **`git pull` — 제일 먼저. 파일 하나라도 열기 전에.**
2. **`git status -sb`로 확인** — clean인가 · 원격과 `0 0`인가.
3. 그다음에 HANDOFF·worklog·정본을 읽는다.
4. 기기가 바뀌었으면 `cd native && xcodegen generate` (`.xcodeproj`는 gitignore 생성물).
5. 시뮬 UDID를 `xcrun simctl list devices available`로 **그 기기에서 다시 읽는다** (위 ③ — 이름으로 쓰지 않는다).
6. 종료 명령 안내 (아래 종료 절차 7).

> ### ⚠️ 왜 절차로 올렸나 — 문장으로는 이미 있었는데 안 걸렸다 (2026-08-20)
> 「작업 시작 전 `git pull` 필수」는 **이 문서 위쪽에 이미 적혀 있었다.** 그런데 그날 맥미니 세션은
> **9커밋 뒤처진 채로 문서를 읽기 시작했다.** 「작업」을 **코드 만지는 것**으로 읽어서
> **읽기는 작업이 아니라고** 넘어간 것이다.
> **대가:** 사용자가 읽으라고 지정한 파일 둘이 그 HEAD에 **아예 없었다**
> (`docs/worklog/2026-08-19-macbook.md` · `media-icloud-design.md` **§12**).
> **없는 것을 「없다」로 답했으면 사용자의 기억이 틀린 것으로 만들었을 것이다** — 실제로는 저장소가 뒤처진 것이었다.
> ⛔ **뒤처진 HEAD는 조용하다.** 파일이 없으면 그나마 드러나지만, **낡은 내용이 들어 있으면 아무 신호도 없다.**

---

## 세션 종료 절차
사용자가 **"정리하자 / 종료하자 / 마무리하자 / 세션 종료"** 하면 아래 체크리스트를 밟는다:

1. **커밋** — `git status` 확인, 남은 변경 커밋.
2. **푸시·동기화** — **⛔ 푸시는 사용자 지시 후. 커밋 목록을 먼저 보인다.**
   커밋은 되돌릴 수 있지만 **푸시는 다른 기기로 나간다** — 이 프로젝트는 두 기기를 번갈아 쓰고
   **판정이 사용자에게 있다.** 푸시가 **마지막 관문**이다.
   순서: 커밋 → **`git log --oneline`으로 목록 보고** → 사용자 지시 → push → 로컬 = 원격 동기 확인.
   ⚠️ 「종료하자」는 **푸시 허가가 아니다.** 종료 절차를 밟으라는 것이고, 푸시는 그 안에서 따로 묻는다.
   (계기: 2026-08-18. 「푸시 및 종료 절차 진행」 지시를 받고 돌렸는데 **목록을 푸시 뒤에 보였다** —
   결과는 문제없었지만 순서가 뒤였다.)
3. **worklog** — `docs/worklog/YYYY-MM-DD-*.md`에 **한 일 + 다음 할 일** 기록.
4. **사양서 반영 확인** — 규칙 #2에 따라 이번 변경이 사양서에 반영될 필요가 있는지 확인·질문.
5. **브랜치/worktree 정리** — 머지된 브랜치·worktree 제거.
6. **재개 한 줄** — worklog 맨 위에 다음에 바로 이어갈 **"재개 한 줄"** 추가.
7. **종료 명령 안내 (세션 시작 시)** — 세션이 시작될 때마다 **항상 제일 먼저**, 사용자에게 이렇게 안내한다:
   > "세션을 종료할 때는 **'정리하자' / '종료하자' / '마무리하자' / '세션 종료'** 중 하나로 말씀해 주세요."
   (1~6은 종료 시 절차, 7은 매 세션 시작 시 안내.)

**단, 최종 종료 판단은 사용자가 한다.** 위 절차를 실행한 뒤 **결과만 보고하고 기다린다** (자동 종료·임의 판단 금지).
